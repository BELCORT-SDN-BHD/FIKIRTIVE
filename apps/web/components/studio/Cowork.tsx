"use client";
import type { EntityDTO, ChatThreadDTO } from "@/lib/types";
export function Cowork(_props: { projectId: string; entities: EntityDTO[]; threads: ChatThreadDTO[] }) {
  return null; // Plan-2 Task 8 implements the Cowork surface
}
