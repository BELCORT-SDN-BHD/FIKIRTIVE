"use client";
import type { EntityDTO } from "@/lib/types";

// Task 9 replaces this stub with the full editable proposal + Generate button.
export function GenerateCard(_props: {
  cardId: string;
  payload: unknown;
  entities: EntityDTO[];
  alreadyGenerated: boolean;
}) {
  return <div className="cw-card">Generate card (pending)</div>;
}
