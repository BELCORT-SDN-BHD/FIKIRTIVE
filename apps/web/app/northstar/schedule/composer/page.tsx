/* @nsPage district="排期区" page="composer" status="stub"
   sources="区划图·排期区;harmony-01 #4;X 定价判决(第四批)" approvedAt="" pr="" */
"use client";

// 占位 stub — zone builder 按 PAGE-INVENTORY 行整页替换,并同步 @nsPage 与 _registry.ts。
import { NsStub } from "@/components/northstar/_shared";

export default function Page() {
  return <NsStub path="/northstar/schedule/composer" />;
}
