/* @nsPage district="Onboarding + 登录" page="login" status="stub"
   sources="区划图·地下管网 / 住户服务(#74);design-rules §3" approvedAt="" pr="" */
"use client";

// 占位 stub — zone builder 按 PAGE-INVENTORY 行整页替换,并同步 @nsPage 与 _registry.ts。
import { NsStub } from "@/components/northstar/_shared";

export default function Page() {
  return <NsStub path="/northstar/onboarding/login" />;
}
