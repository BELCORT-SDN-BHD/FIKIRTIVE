import { Studio } from "@/components/studio/Studio";

export const metadata = { title: "Studio · Artlio" };

// Redesign-shell prototype route — mock-first, no auth/data wiring yet.
export default function StudioPage() {
  return <Studio />;
}
