"use client";

/**
 * 沉浸式 · Onboarding /checklist —— 首跑引导四步,每步 CTA 指向真实沉浸式目的地。
 * gallery 里此页为 stub,内容照 account-ops 先例现建(见 OnboardingChecklist 注释)。
 */

import { OnboardingChecklist } from "@/components/northstar/immersive/misc/onboarding-checklist";

export default function Page() {
  return <OnboardingChecklist />;
}
