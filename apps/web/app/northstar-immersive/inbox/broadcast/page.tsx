"use client";

/** 沉浸式 · 收件箱 /broadcast —— 分群群发 + 失败重发 + 群发后跟进(Z6 Wave B)。 */

import { InboxBroadcast } from "@/components/northstar/immersive/crm-inbox/inbox-broadcast";

export default function Page() {
  return <InboxBroadcast />;
}
