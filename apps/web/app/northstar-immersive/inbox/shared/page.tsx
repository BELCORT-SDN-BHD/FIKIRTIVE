"use client";

/** 沉浸式 · 收件箱 /shared —— 复用组内内容组件(gallery 里是 stub,内容现建于 crm-inbox 组)。 */

import { InboxShared } from "@/components/northstar/immersive/crm-inbox/inbox-shared";

export default function Page() {
  return <InboxShared />;
}
