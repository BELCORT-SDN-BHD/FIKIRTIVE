"use client";

/** 沉浸式 · 收件箱 /recipes —— 生命周期配方库 + 触发数据源 + 发送节流(Z6 Wave B)。 */

import { InboxRecipes } from "@/components/northstar/immersive/crm-inbox/inbox-recipes";

export default function Page() {
  return <InboxRecipes />;
}
