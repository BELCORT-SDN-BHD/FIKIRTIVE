"use client";

/** 沉浸式 · 收件箱 /templates —— WABA 模板库 + Flow 表单 + 话术库(Z6 Wave B)。 */

import { InboxTemplates } from "@/components/northstar/immersive/crm-inbox/inbox-templates";

export default function Page() {
  return <InboxTemplates />;
}
