"use client";

/** 沉浸式 · 资产区 /discover —— 原生重建(ENDGAME §五 资产区契约;不再套 GalleryFrame)。 */

import { AssetsDiscover } from "@/components/northstar/immersive/assets/discover";

export default function Page() {
  return <AssetsDiscover />;
}
