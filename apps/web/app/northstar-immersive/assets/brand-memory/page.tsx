"use client";

/** 沉浸式 · 资产区 /brand-memory —— 原生重建(ENDGAME §五 资产区契约;不再套 GalleryFrame)。 */

import { AssetsBrandMemory } from "@/components/northstar/immersive/assets/brand-memory";

export default function Page() {
  return <AssetsBrandMemory />;
}
