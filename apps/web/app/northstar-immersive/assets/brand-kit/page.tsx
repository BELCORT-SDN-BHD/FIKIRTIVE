"use client";

/** 沉浸式 · 资产区 /brand-kit —— 原生重建(ENDGAME §五 资产区契约;不再套 GalleryFrame)。 */

import { AssetsBrandKit } from "@/components/northstar/immersive/assets/brand-kit";

export default function Page() {
  return <AssetsBrandKit />;
}
