"use client";

/** 沉浸式 · 资产区 /library —— 原生重建(ENDGAME §五 资产区契约;不再套 GalleryFrame)。 */

import { AssetsLibrary } from "@/components/northstar/immersive/assets/library";

export default function Page() {
  return <AssetsLibrary />;
}
