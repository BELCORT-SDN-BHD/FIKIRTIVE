"use client";

/** 沉浸式 · 资产区 /templates —— 原生重建(ENDGAME §五 资产区契约;不再套 GalleryFrame)。 */

import { AssetsTemplates } from "@/components/northstar/immersive/assets/templates";

export default function Page() {
  return <AssetsTemplates />;
}
