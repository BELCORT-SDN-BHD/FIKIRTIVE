"use client";

/** 沉浸式 · Campaign 区 /calendar —— 日历批改面(Otto 铺日历→逐条批改),原生重建。 */

import { CampaignCalendar } from "@/components/northstar/immersive/campaign/campaign-calendar";

export default function Page() {
  return <CampaignCalendar />;
}
