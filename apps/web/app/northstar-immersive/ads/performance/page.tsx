"use client";

/** 沉浸式 · 广告区 /performance —— 原生重建(逐条诊断 + O-10 创作链;组件在 analytics-ads 组)。 */

import AdsPerformance from "@/components/northstar/immersive/analytics-ads/ads-performance";

export default function Page() {
  return <AdsPerformance />;
}
