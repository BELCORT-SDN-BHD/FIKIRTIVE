"use client";

/** 沉浸式 · 广告区 /competitors —— 同行广告透视(Ad Library 公开数据 → 按角度聚类 → 借角度进创作)。 */

import AdsCompetitors from "@/components/northstar/immersive/analytics-ads/ads-competitors";

export default function Page() {
  return <AdsCompetitors />;
}
