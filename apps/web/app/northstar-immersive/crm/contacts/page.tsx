"use client";

/** 沉浸式 · CRM 区 /contacts —— 复用组内内容组件(gallery 里是 stub,内容现建于 crm-inbox 组)。 */

import { CrmContacts } from "@/components/northstar/immersive/crm-inbox/crm-contacts";

export default function Page() {
  return <CrmContacts />;
}
