"use client";

/** 沉浸式 · Studio 创作首页(front door)—— 原生重建(ENDGAME §五 一区;不再 GalleryFrame 套壳)。 */

import { StudioHome } from "@/components/northstar/immersive/studio-factory/studio-home";

export default function Page() {
  return <StudioHome />;
}
