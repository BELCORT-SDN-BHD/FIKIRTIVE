"use client";

/** 沉浸式 · 分析区 /reports —— 原生重建(报表引擎 + 品牌化报告;组件在 analytics-ads 组)。 */

import AnalyticsReports from "@/components/northstar/immersive/analytics-ads/analytics-reports";

export default function Page() {
  return <AnalyticsReports />;
}
