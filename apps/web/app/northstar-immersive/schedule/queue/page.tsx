"use client";

/** 沉浸式 · 排期区 /queue —— 原生重建(Z5)。内容组件在 immersive/schedule 组。 */

import { ScheduleQueue } from "@/components/northstar/immersive/schedule/schedule-queue";

export default function Page() {
  return <ScheduleQueue />;
}
