"use client";

/** 沉浸式 · 排期区 /plan —— 原生重建(Z5)。内容组件在 immersive/schedule 组。 */

import { SchedulePlan } from "@/components/northstar/immersive/schedule/schedule-plan";

export default function Page() {
  return <SchedulePlan />;
}
