{
  "name": "fikirtive",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=22"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/engines",
      "esbuild",
      "prisma",
      "sharp",
      "unrs-resolver"
    ]
  },
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true",
    "dev": "pnpm --parallel --filter ./apps/* dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "lint:tenancy": "bash scripts/check-no-raw-prisma.sh",
    "lint:skills": "bash scripts/check-skill-imports.sh",
    "lint:otto-cli": "node scripts/check-otto-cli-fence.mjs --self-test && node scripts/check-otto-cli-fence.mjs",
    "lint:parity": "corepack pnpm --filter @fikirtive/otto build && bash scripts/check-parity.sh",
    "lint:margin": "corepack pnpm --filter @fikirtive/core build && node scripts/check-margin-floor.mjs",
    "backup:plan": "corepack pnpm --filter \"./packages/*\" build && corepack pnpm --filter @fikirtive/worker build && node scripts/db-backup-plan.mjs",
    "test": "pnpm -r test",
    "db:generate": "pnpm --filter @fikirtive/db generate",
    "db:migrate": "pnpm --filter @fikirtive/db migrate:dev"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "playwright": "^1.60.0"
  }
}
