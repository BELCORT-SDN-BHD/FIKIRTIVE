# Reviewed principal-less authorities from issue #442's semantic fence rollout.
# Format: path<TAB>export<TAB>reason<TAB>one-line authority justification
# Matching is exact. A missing finding makes its entry stale and fails the fence.
apps/web/app/api/health/route.ts	GET	missing-principal-resolution	Public health probe is intentionally sessionless and exposes only worker liveness.
apps/web/app/api/meta/data-deletion/route.ts	POST	missing-principal-resolution	Meta's verified signed_request HMAC is the callback authority; there is no user session.
apps/web/app/api/otto/stream/route.ts	POST	unprovable	The route consumes requireOwner, but four nested Otto port factories exceed the checker’s one-module call-following limit; reviewed call paths retain the owner-bound port context.
apps/web/app/api/stripe/webhook/route.ts	POST	missing-principal-resolution	Stripe's verified webhook signature is the money-in authority; there is no user session.
apps/web/lib/actions.ts	softDeleteEntity	principal-result-unused	pre-existing, ticketed #458 append candidate — client-supplied entityId reaches shotEntityRef.count before the later owner-scoped entity update validates ownership
apps/web/lib/admin-v2.ts	getAdminV2Data	missing-principal-resolution	pre-existing, ticketed #458 — remove when fixed
apps/web/lib/brand-record-actions.ts	saveBrandRecord	unprovable	The bounded unique-conflict retry recursively re-enters the same owner-gated export; cycle detection intentionally stops rather than assuming recursive safety.
apps/web/lib/campaign-actions.ts	proposeCampaign	unprovable	stableJson is a local pure recursive canonicalizer; cycle detection cannot prove recursion even though the helper has no sensitive operation.
apps/web/lib/conversation-admin.ts	listConversations	missing-principal-resolution	pre-existing, ticketed #458 — remove when fixed
apps/web/lib/conversation-admin.ts	getConversation	missing-principal-resolution	pre-existing, ticketed #458 — remove when fixed
apps/web/lib/cowork-knowledge.ts	getEnhanceDirective	missing-principal-resolution	Reads founder-owned global model guidance, not tenant-owned data.
apps/web/lib/cowork-knowledge.ts	listDirectives	missing-principal-resolution	Reads founder-owned global model guidance for the admin grid, not tenant-owned data.
apps/web/lib/cowork-knowledge.ts	getCastRule	missing-principal-resolution	Reads founder-owned global model guidance, not tenant-owned data.
apps/web/lib/cowork-knowledge.ts	getRulesMap	missing-principal-resolution	Reads founder-owned global model guidance, not tenant-owned data.
apps/web/lib/model-registry.ts	resolveDisabledModels	missing-principal-resolution	Reads the founder-owned global model-disable overlay, not tenant-owned data.
apps/web/lib/meta-build-actions.ts	maybeAutoBuild	unprovable	The record helper gates its update on a prior owner-scoped fallback read, but nested nullish-result correlation is intentionally left unproven.
apps/web/lib/otto-actions.ts	finalizeOttoRun	principal-parameter-unused-by-sensitive-operation	pre-existing, ticketed #458 — remove when fixed
apps/web/lib/otto-client-actions.ts	ottoTurn	unprovable	Round 8 unprovable: this exact thin server-action wrapper delegates to the directly analyzed owner-resolving ottoTurn; the wrapper hop consumes the one-module budget before four reviewed nested port factories.
apps/web/lib/otto-client-actions.ts	ottoTurn	missing-principal-resolution	Round 8 unprovable: this exact thin server-action wrapper delegates to the directly analyzed owner-resolving ottoTurn; the wrapper hop prevents its later owner-scoped persistence from retaining caller-visible resolver state.
apps/web/lib/otto-client-actions.ts	ottoApprove	unprovable	The consumed owner gate reaches four nested Otto port factories beyond the checker’s one-module call-following limit; reviewed ports retain owner-bound context.
apps/web/lib/runtime-config.ts	resolveVisionConfig	missing-principal-resolution	Reads the singleton global vision configuration, not tenant-owned data.
apps/web/lib/schedule-actions.ts	suggestPostTimes	principal-result-unused	Reads authenticated users’ shared static PostingTimeSeed craft data; the table is intentionally global and contains no tenant-owned rows.
apps/web/lib/segment-actions.ts	buildSegment	unprovable	stableJson is a local pure recursive canonicalizer; cycle detection cannot prove recursion even though the helper has no sensitive operation.
apps/web/lib/share-preview.ts	verifySharePreview	missing-principal-resolution	The HMAC-attested share token supplies and binds ownerId plus postId without a session.
apps/web/lib/tenant-admin.ts	listTenants	missing-principal-resolution	pre-existing, ticketed #458 — remove when fixed
apps/web/lib/tenant-admin.ts	getTenantDetail	missing-principal-resolution	pre-existing, ticketed #458 — remove when fixed
apps/web/lib/tenant-actions.ts	stopImpersonatingTenant	missing-principal-resolution	Better Auth permits stopImpersonating only on the current impersonated session.
apps/web/lib/trend-actions.ts	saveTrendSnapshot	unprovable	stableJson is a local pure recursive canonicalizer; cycle detection cannot prove recursion even though the helper has no sensitive operation.
apps/web/lib/web-page-cache.ts	readPageCached	missing-principal-resolution	Global URL-content cache is tenant-neutral and keyed only by normalized URL hash.
# Round 7 default-deny convergence. Each entry names the exact conservative analyzer limit;
# none re-enables generic expression-subtree taint.
apps/web/lib/actions.ts	deleteProject	principal-result-unused	Round 7 unprovable: the reviewed owner-scoped project read precedes advisory-lock keys built by template interpolation, whose transformed value is intentionally not principal-derived.
apps/web/lib/actions.ts	saveShotPrompt	principal-result-unused	Round 7 unprovable: createMany rows are produced by Array.map from an owner-scoped shot; callback collection-return taint is intentionally not modeled.
apps/web/lib/actions.ts	getTranscript	principal-result-unused	Round 7 unprovable: the global content-addressed transcript read is dominated by ownedAssetFromSrc, but cross-call prior-read correlation is intentionally not modeled.
apps/web/lib/canvas-node-placement.ts	placeCanvasJobNode	principal-parameter-unused-by-sensitive-operation	Round 7 unprovable: ownerId and projectId scope every row read, while template-derived lock keys and branch-merged validated ids are intentionally not propagated.
apps/web/lib/canvas-node-placement.ts	tombstoneCanvasNode	principal-parameter-unused-by-sensitive-operation	Round 7 unprovable: the owner/project-scoped node read dominates the write, but the intervening template-derived advisory-lock key is intentionally opaque.
apps/web/lib/cowork-actions.ts	coworkDeleteThread	principal-result-unused	Round 7 unprovable: the owner-scoped thread is re-read inside the transaction, but the advisory-lock key is template-derived.
apps/web/lib/cowork-actions.ts	coworkVaryCard	principal-result-unused	Round 7 unprovable: createMany rows come from a bounded map over owner-scoped source messages; callback collection-return taint is intentionally not modeled.
apps/web/lib/customer-inbox-service.ts	createCustomerInboxService.listConversations	principal-parameter-unused-by-sensitive-operation	Round 7 unprovable: the filter starts with principal.ownerId, then safe view-specific member writes intentionally invalidate the whole mutable filter object.
apps/web/lib/customer-inbox-service.ts	createCustomerInboxService.writeNormalizedInbound	principal-parameter-unused-by-sensitive-operation	Round 7 unprovable: ownerId is validated and trimmed by requiredString, but opaque method-call return taint is intentionally not inferred.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.createRoutineDraft	principal-parameter-unused-by-sensitive-operation	Round 7 unprovable: the owner-scoped draft transaction uses an advisory-lock key assembled by template interpolation.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.createWorkflowRun	missing-principal-resolution	Round 7 unprovable: requireWorker validates an injected resolveWorkerContext result; opaque callback-return authority is intentionally not inferred.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.transitionWorkflowRun	missing-principal-resolution	Round 7 unprovable: requireWorker validates an injected resolveWorkerContext result; opaque callback-return authority is intentionally not inferred.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.enrollWorkflowJourney	missing-principal-resolution	Round 7 unprovable: requireWorker validates an injected resolveWorkerContext result; opaque callback-return authority is intentionally not inferred.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.advanceWorkflowJourney	missing-principal-resolution	Round 7 unprovable: requireWorker validates an injected resolveWorkerContext result; opaque callback-return authority is intentionally not inferred.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.enterWorkflowJourneyWait	missing-principal-resolution	Round 7 unprovable: requireWorker validates an injected resolveWorkerContext result; opaque callback-return authority is intentionally not inferred.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.createWorkflowJourneyDueRun	missing-principal-resolution	Round 7 unprovable: requireWorker validates an injected resolveWorkerContext result; opaque callback-return authority is intentionally not inferred.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.evaluateWorkflowBusinessHours	missing-principal-resolution	Round 7 unprovable: requireWorker validates an injected resolveWorkerContext result; opaque callback-return authority is intentionally not inferred.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.dispatchWorkflowStep	missing-principal-resolution	Round 7 unprovable: requireWorker validates an injected resolveWorkerContext result; opaque callback-return authority and downstream lock-key transforms are intentionally not inferred.
apps/web/lib/gen-actions.ts	startCanvasGen	principal-result-unused	Round 7 unprovable: the live-project check spreads an immutable OWNED filter and the transaction lock key is template-derived; both transforms intentionally drop provenance.
apps/web/lib/gen-actions.ts	startCoworkGen	principal-result-unused	Round 7 unprovable: the live-project check spreads an immutable OWNED filter and the transaction lock key is template-derived; both transforms intentionally drop provenance.
apps/web/lib/gen-actions.ts	startGen	principal-result-unused	Round 7 unprovable: the live-project check spreads an immutable OWNED filter and the transaction lock key is template-derived; both transforms intentionally drop provenance.
apps/web/lib/otto-actions.ts	deleteCoworkThread	principal-result-unused	Round 7 unprovable: the owner-scoped live thread read precedes an advisory-lock key built by template interpolation.
apps/web/lib/otto-client-actions.ts	ottoApprove	missing-principal-resolution	Round 7 unprovable: owner authority is retained by reviewed nested Otto ports, but default-deny does not infer it through the multi-factory carrier chain.
apps/web/lib/research-actions.ts	approveResearch	principal-result-unused	Round 7 unprovable: the transaction re-reads the owner-scoped card, but template lock-key flow and the later validated-id-only update are intentionally not correlated.
apps/web/lib/schedule-actions.ts	updateScheduledPost	principal-result-unused	Round 7 unprovable: child createMany rows are produced by mapping ids validated against the owner-scoped post; callback collection-return taint is intentionally not modeled.
apps/web/lib/schedule-actions.ts	listScheduledPosts	principal-result-unused	Round 7 unprovable: the filter starts with gate.ownerId, then a safe scheduledAt member write intentionally invalidates the whole mutable filter object.
apps/web/lib/storyboard-gate1-actions.ts	prepareStoryboardFirstFrames	missing-principal-resolution	Round 7 unprovable: the owner/project-scoped lock helper returns a template-derived advisory-lock key.
apps/web/lib/storyboard-gate1-actions.ts	prepareStoryboardFirstFrames	principal-result-unused	Round 7 unprovable: the chat card id is established by owner-scoped reads, but the later id-only update exceeds modeled prior-read correlation.
apps/web/lib/storyboard-gate1-actions.ts	regenShotFirstFrameCard	missing-principal-resolution	Round 7 unprovable: the owner/project-scoped lock helper returns a template-derived advisory-lock key.
apps/web/lib/storyboard-gate1-actions.ts	regenShotFirstFrameCard	principal-result-unused	Round 7 unprovable: the chat card id is established by owner-scoped reads, but the later id-only update exceeds modeled prior-read correlation.
apps/web/lib/storyboard-gate1-actions.ts	syncStoryboardMedia	missing-principal-resolution	Round 7 unprovable: the owner/project-scoped lock helper returns a template-derived advisory-lock key.
apps/web/lib/storyboard-gate1-actions.ts	syncStoryboardMedia	principal-result-unused	Round 7 unprovable: the chat card id is established by owner-scoped reads, but the later id-only update exceeds modeled prior-read correlation.
apps/web/lib/storyboard-gate1-actions.ts	prepareStoryboardVideos	missing-principal-resolution	Round 7 unprovable: the owner/project-scoped lock helper returns a template-derived advisory-lock key.
apps/web/lib/storyboard-gate1-actions.ts	prepareStoryboardVideos	principal-result-unused	Round 7 unprovable: the chat card id is established by owner-scoped reads, but the later id-only update exceeds modeled prior-read correlation.
apps/web/lib/storyboard-gate1-actions.ts	regenShotVideoCard	missing-principal-resolution	Round 7 unprovable: the owner/project-scoped lock helper returns a template-derived advisory-lock key.
apps/web/lib/storyboard-gate1-actions.ts	regenShotVideoCard	principal-result-unused	Round 7 unprovable: the chat card id is established by owner-scoped reads, but the later id-only update exceeds modeled prior-read correlation.
apps/web/lib/customer-inbox-service.ts	createCustomerInboxService.writeNormalizedInbound	missing-principal-resolution	Round 7 unprovable: the service factory surface loses its owner-bearing input through the same opaque requiredString/trim return already reviewed on the INTERNAL export.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.listRoutines	principal-parameter-unused-by-sensitive-operation	Round 7 unprovable: the owner-bearing list filter is rebuilt with cursor/filter spreads, and spread/rebuild intentionally drops provenance.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.listRoutineRuns	principal-parameter-unused-by-sensitive-operation	Round 7 unprovable: the owner-bearing list filter is rebuilt with cursor/filter spreads, and spread/rebuild intentionally drops provenance.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.getContactJourneyStates	principal-parameter-unused-by-sensitive-operation	Round 7 unprovable: the owner-bearing list filter is rebuilt with cursor/filter spreads, and spread/rebuild intentionally drops provenance.
apps/web/lib/customer-workflow-service.ts	workflowLifecycleService.listBusinessHoursPolicies	principal-parameter-unused-by-sensitive-operation	Round 7 unprovable: the owner-bearing list filter is rebuilt with cursor/filter spreads, and spread/rebuild intentionally drops provenance.
apps/web/lib/factory-batch.ts	orchestrateBatch	missing-principal-resolution	Round 7 unprovable: the owner-bearing args are rebuilt while deriving stable cell plans; later direct args.ownerId queries remain reviewed but statically invalidated.
apps/web/lib/segment-actions.ts	buildSegment	principal-result-unused	Round 7 unprovable: the owner-bearing where object is spread into conditional/retry filters, and spread/rebuild intentionally drops provenance.
# Founder decision 2026-07-27 (#470): the two request-level principal SEAMS from #463.
# Each resolvePrincipal() takes zero parameters, so no request-supplied value can enter the
# principal; the semantic fence cannot express that shape. Production code is unchanged.
# https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/issues/470#issuecomment-5087070897
apps/web/lib/customer-broadcast-gateway.ts	listBroadcastRuns	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	getBroadcastRun	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	getBroadcastRunLivePreflight	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	getBroadcastComposerOptions	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	getMemberDirectory	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	previewAudienceEligibility	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	createBroadcastRun	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	freezeAudience	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	confirmBroadcastRun	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	cancelBroadcastRun	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	submitBroadcastRun	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-broadcast-gateway.ts	executeBroadcastRun	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	listConversations	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	getConversation	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	searchConversations	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	getHistory	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	getConversationPreflight	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	listTemplates	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	saveConversationDraft	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	assignConversation	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	takeOverConversation	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	handOffConversation	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	setConversationStatus	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	requestAutomationResume	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	createMessageTemplate	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	createMessageTemplateVersion	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	submitConversationReply	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	submitTemplateReview	missing-principal-resolution	Human-verified safe (2026-07-27 audit): the zero-parameter resolvePrincipal() derives ownerId from requireOwner() alone, so no request-side value has an entry point into the principal it returns; the fence's prover cannot express this shape. Disposition per Founder decision 2026-07-27 on #470, not a production-code change.
apps/web/lib/customer-inbox-gateway.ts	listChannelScopes	missing-principal-resolution	与本文件其余条目同一形状(zero-parameter resolvePrincipal 仅由 requireOwner 派生 ownerId,无请求侧入口),且函数体本身零参数:customer-inbox-service.ts 的 listChannelScopes 仅做 where ownerId = principal.ownerId 的 findMany(select 限 id/channel/scopeKey),全部可直接从代码核验;编排者按 #470 处置模式裁定新增,记录于 PR #500 供 Founder 审阅。
apps/web/lib/signup-gate.ts	admitSelfSignup	missing-principal-resolution	Self-service registration runs BEFORE any session exists by construction; the write only inserts the registering address into the non-tenant AllowedEmail table, never resurrects a revoked row, and grants no data access on its own.
# #601 T2b r2 (judge P2②): both board readers stopped keeping a private copy of the
# settlement rules and now go through this ONE reconciler into the shared writer.
apps/web/lib/canvas-settlement-reconcile.ts	reconcileSettledCanvasJobs	missing-principal-resolution	#601 T2b r2: the board reader delegates every delivered job's cards to @fikirtive/db settleCanvasCardsForGenJob so the browser and the worker cannot disagree about a board; each query inside that cross-package prisma.$transaction callback is pinned to the ownerId this export authenticated (never the job row's), and the callback frame in another package is exactly the shape the fence's prover cannot carry a resolver into — the same limit already reviewed for the twin writer canvas-node-placement.ts.
