# 组件基座切换：shadcn/ui → Base UI（方向碑）

> 状态: 草稿（方向已裁、S1 未冻结——冻结前切换代码不上主干）
> 裁决: Founder 2026-08-28 口头裁决入档：组件基座方向切换为 Base UI（`@base-ui/react`）；范围＝纯换、商家可见视觉零变化。
> 裁决二: Founder 2026-08-29 拍板（AskUserQuestion 三选一）：**beta 出售的前端 = main 血统**（即本切换所在的工作树 4232 的归宿）。R22 树（`codex/frontend-r22-clean-rebuild`，领先 main 146 笔、23 笔未推）**降级为设计参照库**：不删、不推、不再作为出售候选；其设计品味经由 main 血统的 UIUX 工作持续搬运。此裁决关闭顾问诊断书（2026-08-27）遗留的「卖哪个前端」未决题。
> 本文件的作用: 给上述裁决立碑，并废止 2026-08-11「shadcn/ui 唯一基座」常令中「唯一」的表述。

## 现状事实（2026-08-28 实查）

- 切换工作只存在于一处未提交工作区（`~/.codex/worktrees/4232/FIKIRTIVE`，345 个未提交文件，含 `@base-ui/react ^1.7.0`）；主干 `main` 仍是全 shadcn/ui。
- 主干围栏 `apps/web/lib/__tests__/shadcn-ui-fence.test.ts` 的机械规则——商家面裸原语一律走 `@/components/ui` 包装层——与包装件内部用哪个库无关，原样生效；其法条头部已随本 PR 改写为指向本碑。

## 接下来

- 切换本身按《开发作业手册》走一页 S1：grill → 验收表（含「视觉零变化」的可判定写法）→ Founder 在功能 issue 评论「S1 批准」冻结，然后 4232 工作区的成果以该验收表收账。
- 围栏若需随切换修订（如 Dialog 对位组件的实现依据），在那页 S1 的「受影响围栏清单」里点名，同批改。

## 不做

- 本文件不批准任何切换代码合入；它只是方向记录。在它对应的 S1 冻结之前，M1 不接受以本文件作为产品代码 PR 的规格引用（状态非「已冻结」）。
