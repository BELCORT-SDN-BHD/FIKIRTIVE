# Brand 产品身份 规格书（S1）

> 状态: 已冻结 · v1
> 批准: https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/issues/1313 Founder 评论「S1 批准 brand-product-identity.md」(2026-09-09)
> 规格前缀: PRODID（验收编号 = PRODID-A1、A2…，全仓不得与其他规格撞前缀）

<!--
来源：E2E Round 1 FSE-007（creation-engine.md §5 :168，Founder 2026-09-09 裁「下一场开 S1 grill」）。
决策记录：整理地图 #1304 子票 #1305（Founder 2026-09-10 逐题拍板）。备料事实见 #1305 评论。
-->

## 0. 一句话

商家在 Brand 页、Library、Otto 对话或网站理解里建的产品，是同一件东西：一处建，Library Products、@Products、确认卡、Otto 记忆处处可用；改名换图一处改两边同步；删除一起消失、可一起恢复。

## 1. 九问（S1 grill 的答案，一问一答；答不出的那问就是还没想清楚的那块）

1. **商家做什么动作、看到什么结果？** 在 Brand 页「新增产品」填名字、主图、价格、卖点、分类，保存。Library Products 立刻出现同一张产品卡；画布 @ 输入产品名能选到它；确认卡与生成结果的谱系指向同一个产品；问 Otto「我店里卖什么」答的是同一份。
2. **入口在哪里？（列全，含深链）** 写入口四个，走同一条共享动作：`/brand` 产品分区「新增产品」与 `/brand/records`；`/library`「新建元素 → 产品」；画布 Otto 对话「记下这个产品」（saveProduct 技能）；网站／素材理解自动提取（先进草稿，商家确认后才建身份）。读入口：Library Products 栏、@ 菜单、确认卡、Otto 记忆「Your products」段。界面现况见 §5 变更登记 2026-09-16 R3-F20。
3. **四态：空、加载、错误、成功各长什么样？** 空：Library Products 空态写「还没有产品，去 Brand 页或这里新增」；加载：列表骨架；错误：建产品整笔回滚、不留半条，提示重试；成功：两边同时出现，带同一个 id。
4. **数据从哪来、写到哪去？** 身份 = `Entity(type=PRODUCT)`：名字、主图（`baseAssetId` + `ReferenceImage`）。价签 = `BrandRecord(kind=product)`：价格、卖点、分类、链接、描述，新增 `entityId` 以复合外键 `(entityId, ownerId) → Entity(id, ownerId)` 指向身份。一条共享动作 `createProduct` 在一个事务里建两行；改名换图改 Entity（两边入口都可改）；价格卖点只改 BrandRecord（只在 Brand 页）。读路：Library、@ 搜索、确认卡读 Entity；Brand 五分区与 Otto 读 BrandRecord join Entity。@ 菜单来源标签由「Product · Otto IQ」改为「Product」。迁移：一次性回填——每条活跃 `BrandRecord(product)` 建对应 Entity 并把 `data.imageAssetId` 挂为主图；迁移前预检，发现跨租户或同名冲突即失败不落库；带 rollback.sql。
5. **碰不碰钱路（credits / 计费）？碰则幂等键是什么？** 建、改、删产品不扣 credits，不动 reserve / settle / ledger。产品作为参考进入生成时按 Creation 引擎既有规则计价，本规格不改。建产品的幂等键沿用既有 `(ownerId, brandId, kind, nameKey)` 查重转 update。
6. **权限与租户边界是什么？** `ownerId` 只来自服务端 principal；`BrandRecord.entityId` 复合外键由数据库拒绝跨租户连线（ADR 0002）；@ 搜索与 Library 只查本租户；双租户测试。
7. **参考对照：抄哪家？** Shopify 后台「Add product」：一件产品一个身份，Media、Price、Publishing「All channels」在同一张卡上，各销售渠道读同一条记录——https://mobbin.com/screens/16bd3aef-c6d9-48c9-a716-193482c90b57 。我们的「渠道」= Library、@ 引用、确认卡、Otto。
8. **胃口：轻／中／重挡，为什么？** 重挡：碰 schema 与迁移（M1 路径地板）、回填、共享动作、读路改标签、双租户测试、一条自动化旅程。估 3–4 天。Founder 2026-09-10 裁「不设上限，做完为止」（效果为王常令）。
9. **Otto 怎么协助这个功能？** `saveProduct` 与 `lookupProducts` 走同一条共享动作与同一读路；理解 worker 提取的产品先落草稿（`contextStatus` Draft），商家在 Brand 页确认后才建身份，Otto 在确认前不把草稿当事实。

## 2. 验收表（S5 只认这张表；一行一个可当场演示的判定）

| 编号 | 商家做 X | 看到 Y |
|---|---|---|
| PRODID-A1 | 在 Brand 页新增产品（名字、主图、价格） | Library Products 出现同一产品卡；该 BrandRecord 的 `entityId` 等于卡片的 Entity id |
| PRODID-A2 | 在画布输入 @ 加产品名 | 菜单出现该产品，来源标签为「Product」；选入确认卡后，生成结果谱系的 `approvedEntities` 指向同一个 Entity id |
| PRODID-A3 | 在 Library「新建元素 → 产品」 | Brand 页产品分区出现同一产品，价格卖点为空待填 |
| PRODID-A4 | 在 Library 改名或换主图；再在 Brand 页改名或换主图 | 另一边同步显示（同一行 Entity），无第二份名字或图 |
| PRODID-A5 | 在 Library 元素页找价格、卖点、分类的编辑入口 | 没有；这三项只在 Brand 页可改 |
| PRODID-A6 | 在 Brand 页删除产品；再在 Library 恢复它 | Library 该元素随删随消失、随恢复回来；反向亦然；已生成的成片不动 |
| PRODID-A7 | 对 Otto 说「记下产品 X」；另让网站理解提取一个产品但不确认 | 前者两边出现；后者在确认前不出现在 Library 与 @ 菜单 |
| PRODID-A8 | Founder 当场跑迁移前后两条计数 | 活跃 `Entity(PRODUCT)` 数 ≥ 迁移前活跃 `BrandRecord(product)` 数，且每条 `BrandRecord(product).entityId` 非空；预检对一条人造跨租户行报错且不落库；fresh DB 迁移无错 |
| PRODID-A9 | 用租户 B 的账号在 @ 菜单与 Library 找租户 A 的产品；再直接构造指向 A 产品的 `entityId` 写入 | 找不到；写入被数据库拒绝 |
| PRODID-A10 | 建、改、删产品各一次后看余额与账本 | 余额不变，账本零新行 |

## 3. 不做（非目标；写明为什么和触发条件，防「遗漏」误会）

- 产品变体 / SKU / 库存——不是营销工具的活；触发 = 商家提出且与商务连接规格一起立项。
- 新的多图管理 UI——主图之外的图用 Library 元素页现有 `ReferenceImage`；触发 = Round 2 走查发现商家找不到。
- 价格进计费或报价——价格只是营销事实；触发 = 商务功能规格。
- CRM / 订单关联——Otto IQ 词条已写「按 id 链、不复制」；触发 = CRM 出 beta 门。
- 同名旧产品自动合并——回填预检报出，人工处理；触发 = 真实商家数据出现批量冲突。
- Otto IQ 其它记录（segment / offer）——本规格只动 product。

## 4. 异议栏（AI 必填：本规格最大的风险或异议，一条即可；真没有就写「无异议」——套话算违规）

- 最大风险是回填迁移在生产跑一次不可逆，而生产里是 Founder 自己的数据。对策：迁移前预检（跨租户、同名冲突即失败）、`rollback.sql`、既有每日库备份（`docs/runbooks/db-backup.md`）；执行前 Founder 另行确认。第二风险：四个写入口只要有一处绕过共享动作，两套真相就回潮——加围栏测试「除 `createProduct` 外无人能 `brandRecord.create(kind=product)`」。

## 5. 变更登记（冻结后的中途想法只进这里，下次 S5 批量裁决；不当场执行）

| 日期 | 想法 | 裁决（留空待 S5） |
|---|---|---|
| 2026-09-10 | PR #1337：回填只做「价签 → 身份」一个方向，存量 Library 里已有的 PRODUCT 元素（商家自己建的）没有价签，Brand 页看不到它们，待裁 | |
| 2026-09-10 | PR #1337：迁移预检②（同租户同名活跃 Entity）在开发库上 100% 命中 —— 同名的 Library 产品与 Brand 价签要不要一次性链接，待 Founder 裁 | **Founder 2026-09-10 裁（#1321 评论）：一次性链接**。同店（同 ownerId）且 nameKey 相同的活跃 Library 产品卡恰有一张时，价签直接指向它、不新建身份，价签主图挂到该卡（仅当它自己没有主图）；两张以上同名才拒绝并报出（整条迁移不落库）；没有同名的照旧新建。只限这次迁移，运行时新增同名仍不自动合并（§3 不变）。落地 PR #1337（`packages/db/prisma/migrations/20260910120000_brand_product_identity/migration.sql` 预检②；验收 PRODID-A8 三例） |
| 2026-09-10 | PR #1337 判官第 4 轮：产品的**两个删除方向都不再清扫封面字节**（`deleteBrandRecord` 原本就不清扫，本轮 `softDeleteEntity` 也跟上）——理由是 `restoreBrandRecord` 能把行接回来，而字节删了接不回来（fail open）。代价：商家删掉的产品卡仍占着存储字节。「两边都删了才清扫」要不要做、什么时候做，待裁 | |
| 2026-09-10 | PR #1337 第五轮：`BrandRecord.data` 不再承载 `name` / `imageAssetId`，身份（`Entity`）成为名字与主图的**唯一源**（写路入库前剥掉这两个键，读路一律 `withProductIdentity` 从身份取，`nameKey` 列保留作去重索引、值取自 `Entity.name` 归一化）；回填把这两格搬进身份，同名一次性链接照 Founder 2026-09-10 裁（#1321）；两个方向的删除均**不清扫**封面字节（fail open），「两边都删才清扫」待 S5 裁 | |
| 2026-09-10 | **PRODID-R1**（登记编号）票 #1322：`updateEntity` 的类型守卫 —— 底下挂着**活价签**的 `PRODUCT` 卡不许改成别的类型（改了就是「Brand 页有这件产品、Library 里它不是产品」）。数据库那条 CHECK 只管「product 行有没有 entityId」，管不到那一行是什么 `type`。落地在 PR #1337，本票只把借着 PRODID-A1 的那条测试改挂这个登记编号 | |
| 2026-09-10 | **PRODID-R2**（登记编号）票 #1322：删掉一件产品**唯一**那张照片时，字节真删（`softDeleteReferenceImage` 把封面一起清掉，两个面同时变成「这件产品没有封面」，照 Founder 2026-09-03 裁「商家删掉一张参考照，存储桶里的字节也必须真的没了」，与演员、场景一个口径）。分界线是**有没有恢复入口**：删整件产品有（`restoreBrandRecord`，所以 fail open、字节留着），删一张照片没有。行为已在 PR #1337 落地，本票补登记 | |
| 2026-09-10 | **PRODID-R3**（登记编号）票 #1322：PRODID-A8 对**草稿行**的例外 —— CHECK `BrandRecord_product_needs_entity` 放行 `contextStatus = 'Draft'` 且 `entityId IS NULL` 的 product 行（规格 §1.9 / A7：确认前根本没有身份，「不出现在 Library 与 @」由数据本身保证）。所以 A8 那句「每条 `BrandRecord(product).entityId` 非空」读作「每条**非草稿**的 product 行」；迁移每一步都带 `contextStatus <> 'Draft'` | |
| 2026-09-10 | **PRODID-R4**（登记编号）票 #1322：存量 Library 里商家自己建的 `Entity(PRODUCT)` **没有价签**时 Brand 页看不到它们（回填只做「价签 → 身份」一个方向）；此时商家在 Brand 页新增一个同名产品，`createProduct` 的查重只看价签的 `nameKey`，于是会**新建第二个身份**，同名的两张 Library 卡并存。运行时不自动合并是 §3 明写的（一次性链接只限那一次迁移），所以这不是 bug 而是一条待裁的边：要不要给这些无价签的卡补一条空价签（反方向回填）／要不要在新增时按 `Entity.name` 也查一次重。**本票不改**，待 Founder 裁 | |
| 2026-09-10 | **PRODID-R5**（登记编号）票 #1322 判官 P2-b：核心不变量「非草稿的 product 价签不承载 `name` / `imageAssetId`」加**机器闸** —— 迁移 `20260910140000_brand_product_data_identity_gate` 的 CHECK `BrandRecord_product_data_has_no_identity`（`kind <> 'product' OR contextStatus = 'Draft' OR NOT (data ? 'name' OR data ? 'imageAssetId')`）。两个选项里选数据库 CHECK 而不是源码围栏：`BrandRecord` 的写路散在四个包，静态判据分不清「这一句写的是产品还是受众」，要么误报要么漏报；约束不需要分辨调用者。**回滚按时间倒序**：先跑这份的 rollback（只丢约束），再跑 20260910120000 的 rollback（它的 ①.5 要把两格写回价签） | |
| 2026-09-10 | **PRODID-R6**（登记编号）票 #1322 判官 P2-a：`saveBrandRecord` 的产品更新分支原先无条件把客户端 `data.name`／`data.imageAssetId` 当改名换图意图递给共享动作。可读路 `withProductIdentity` 会把身份那两格**补进** `data` 交给界面，于是归档、换封面、撤销一次 Otto 改动这三条与名字无关的路，手里都攥着一份可能过期的快照 —— 一次归档就能把商家刚改的名字打回去。改成显式 `identity` 意图：哪一个界面真的在编辑这两格，就由它自己交上来（Brand 页整张产品表单交两格；换/清封面只交主图；归档、撤销、Otto、理解 worker 一格都不交） | |
| 2026-09-10 | **PRODID-R7**（登记编号）票 #1322 判官 P2-e：`rollback.sql` ③ 前置原先用 `e.baseAssetId = ri.assetId` 判「这一格是不是本迁移写的」，判不准 —— 卡在 up 之前就把这张图当封面、而那条硬引用已被软删时，回滚会把**商家自己挑的**封面擦成 NULL。迁移未在生产跑过，故本票直接修 `rollback.sql`（只改 rollback 与演练，`migration.sql` 一个字节没动）：再排掉「这张卡除本迁移那一条外从来没有过指向这张图的引用行（含软删）」。今天所有写 `baseAssetId` 的路径都要求先有一条 `ReferenceImage`，所以这个判据是精确的 | |
| 2026-09-10 | **PRODID-R8**（登记编号）票 #1322 判官 P2-c：名字槽位已被新的那件占住时，`restoreBrandRecord` 给的是一句按 `kind` 分的人话，而不是一次被 catch 吞成「请重试」的 P2002。落地在 PR #1337，本票只把那条借着 PRODID-A6 的测试改挂这个登记编号 | |
| 2026-09-11 | **PRODID-R9**（登记编号）票 #1322 判官第 2 轮 P1（PR #1343）：**更正 PRODID-R6 的一句前提** —— 那一行写「Brand 页整张产品表单交两格」，但那张表单（`apps/web/components/otto/memory/ProductShowcase.tsx` 的 `ProdForm`）只有 Name* / Price / Description / Selling angle / Link / Tags / Category **七格，没有主图那一格**。它手里那格 `imageAssetId` 是读路 `withProductIdentity` 补进去的**客户端快照**，把它当意图递下去，正是 R6 要关掉的那一类：① 另一处（另一个标签页、Otto 的 `linkProductImage`）刚换的封面，会被一次「只改价格」静默写回旧值；② 无 id 新增撞名递归成 update 时，那个 `null` 等于显式清掉一件已有产品的封面。改法：表单交什么由**一处**说了算 —— 新增 `apps/web/lib/brand-product-form-identity.ts` 的 `productFormIdentityIntent()`，只交名字这一格，`imageAssetId` 这个键根本不出现（`undefined` = 不碰，`null` = 清空，两个意思）。主图仍只从卡片上的「换封面 / Remove image」走（`prodSetImage`）。这一格快照本身留着不动 —— 界面要拿它画封面缩略图 | |
| 2026-09-11 | **落地（PR #1346）**票 #1323「编辑与删除同步 + 自动化旅程」：①PRODID-A6 补上「已生成的成片不动」那半句的真测试（此前只有 `softDeleteEntity` 尾巴上一行注释担保）：两个删除方向各走一遍，成片行还活着、还读得出来、字节还在存储桶里，成片的输出资产刻意就是这件产品的封面。②新增旅程 `e2e/journeys/23-brand-product-identity.spec.ts`（建 → Library 同一张卡 → @ 菜单标签「Product」→ 确认卡芯片 `data-id` 逐字等于价签的 `entityId` → 改名同步 → 删除两边一起消失）。③补 PRODID-R9 在**编辑已有产品**那条路上的界面测试（#1343 那条走的是 Add product 那条路）。**PRODID-A4 的「表单没有主图栏就不许交主图」不在本票**：判官第 2 轮查出本票与 #1343 两边在同一个函数上给了两套互斥修法，按 7.3 单一源收到 #1343 的 `lib/brand-product-form-identity.ts`，本票只做上面那条编辑路的界面测试 | |
| 2026-09-11 | 票 #1323 报出的**缺口，待裁**（判官第 2 轮更正过一次，下面每条都逐条核过当前代码）：A6 与 A4 各有半边今天在界面上演示不出来。①**删产品做得到**：`/brand`（`app/brand/BrandWorkspace.tsx:668` 的 `Remove this context`）对产品价签调 `deleteBrandRecord`，产品在这一面是知识库分区里的一条（`lib/brand-context-data.ts:147` 的 `brandSectionForRecordKind`）；`/brand/records` 的产品卡只有 Archive、没有 Remove（`ProductShowcase` 不接 `onDelete`）。②**恢复入口的方向与规格写的相反**：`restoreBrandRecord` 有两个人手入口 —— `/brand` 的 Removed 列表（`BrandWorkspace.tsx:582`）与 Otto 的撤销条；**Library 没有元素的恢复键**（`LibraryView.tsx` 只有 `softDeleteEntity` 与恢复成片的 `restoreGeneration`）。所以 A6 那句「在 Brand 页删除产品；再在 Library 恢复它」演示不出来，反方向（Library 删 → `/brand` 恢复）整条演示得出来。③**Library 元素详情没有改名栏**：`updateEntity` 今天的调用方是 `components/otto/OttoStuff.tsx:260`（聊天壳时代的旧件，已无路由渲染）与 `lib/otto-entities-port.ts:37`（Otto 改名技能），Library 那一面一个字都没接，所以 A4 那句「在 Library 改名」演示不出来。三条动作层的路都在、都有真库测试，缺的只是入口；补入口 = 新面（重挡），本票不做，照原样报出 | 2026-09-15 Founder（对谈）：第③条（Library 改名／换主图入口）已由 PR #1444 落地；①（Brand 页删除产品的 Library 侧 Remove 入口）与②（恢复入口方向）仍待 |
| 2026-09-11 | **PRODID-R10**（登记编号）· **FSE-206（P2）· 第二轮 staging 走查（`docs/audits/fullstack-staging-2026-09-11/`）**：产品「归档」页面自称 `Archived (1) — hidden from Otto`，但归档之后 `@` 菜单仍然弹出该产品（来源标签 `Product`）并可选入，Library `Elements → Products` 也仍然列着它（现场已 `Unarchive` 还原）。**根因已确认（现象层）**：归档标记没有传到 `@` 菜单与 Library Elements 的查询条件；假说是归档只改了 `BrandRecord` 的一个展示字段、Entity 侧可见性未联动，**未核实**。证据：`docs/audits/fullstack-staging-2026-09-11/findings-catalog.md` FSE-206 六格、同目录 `run-ledger.md` §R2-09①′。建议：要么让归档真的从 `@` 与 Library 隐藏，要么把「hidden from Otto」改成它实际做到的事；复测＝归档后两处都搜不到、取消归档后两处都回来。（本规格未单列归档的验收编号，本条按页面许诺 + PRODID-A6 的同步精神判。） | S5 批量裁决 2026-09-12（#1358，Founder 场内整批拍板）：**本版修——真隐藏**：归档后 `@` 菜单与 Library Elements 一并消失（不走改文案那条路）；复测照左列。 |
| 2026-09-11 | **PRODID-R11**（登记编号）· **FSE-210（P2）· 第二轮 staging 走查**：PRODID-A2 **后半句不成立** —— `@` 选入的产品只以提示词文字上路、没有进入生成谱系：那一轮 USER 消息的 `payload.entityIds` **有**产品 Entity id，但确认卡 `entityIds=[]`、`GenJob.entityIds={}`、`approvedEntities=NULL`；同一张画布上 `@Xinyi`（官方演员）那一轮三格齐全。前半句（菜单出现、来源标签 `Product`、可选入并进入生成）成立。**根因：数据层已确认**（产品 Entity id 在「消息 → 确认卡」这一步就掉了，不是生成时才丢）；**代码层两条都还是未核实的推断** —— 服务端没有按类型硬过滤（`propose.helpers.ts:942` 的 `=== "CHARACTER"` 只是在计数），更像是 `propose` 的入参里就没带上产品 id，但这一点未被完全证死；`canvas.create-handoff` 丢 typed ref 同样只有现象。证据：`docs/audits/fullstack-staging-2026-09-11/findings-catalog.md` FSE-210 六格、同目录 `backend-evidence.md` §4.5（USER 消息 payload、GEN_CARD payload、GenJob 三处逐格对照；对照组 §3.1）。建议：产品与演员走同一条 typed ref 搬运路，`canvas.create-handoff` 保住 `referenceRefs`；复测＝`@` 一件产品出一次片，`GenJob.entityIds` 与 `approvedEntities` 必须指到同一个 Entity id，首页 composer 与画布内两条提交路各验一次。**注：本条直接抵触 Founder「有迹可循」原则，严重度请 Founder 过目。** | S5 批量裁决 2026-09-12（#1358，Founder 场内整批拍板）：**本版修，升 P1**（抵触「有迹可循」原则）——产品与演员走同一条 typed ref 搬运路；复测照左列。 |
| 2026-09-14 | **R3-F03**：Library Elements 产品详情没有已批准的改名／换主图入口（`apps/web/components/library/LibraryView.tsx:499–544` 详情分支唯一业务动作是删除），PRODID-A4 的 Library 半边不可演示；本节 2026-09-11 那行已把这个缺口报为「补入口 = 新面（重挡），本票不做」（findings-catalog R3-F03） | Founder 2026-09-14 当前对谈交办补齐（按已批准 PRODID-A4，不扩大到价格／卖点／分类；实现 PR 另开，分支 claude/library-product-rename-cover） |
| 2026-09-15 | **未钉封面时的显示规则**：产品身份（`Entity`）今天没有规定「没有人手挑过封面时显示哪张图」的默认行为（`Entity.baseAssetId` 可空，`packages/db/prisma/schema.prisma:100`）。提案：挂上该产品的第一张参考图时自动设为封面并写回 `Entity.baseAssetId`，商家随时可在 Library 或 Brand 换掉；存量已有产品、尚未有 `baseAssetId` 但已挂参考图的行，一次性补齐迁移；Library 与 Brand 从此读同一张图，不再各显各的。 | 2026-09-15 Founder（对谈）：采用。**落地后果（PR #1462 登记）**：这条不变量是**全量**的 —— 一件还挂着基础层参考图的产品永远有一张封面，「没有封面」只剩「一张基础层参考图都没有」这一种成因。于是 Brand 页产品卡上那颗「Remove from product」（只清 `baseAssetId`、不摘图）永远会被同一个事务里的 `reconcileEntityCover` 立刻钉回同一张图，按了没有任何反应，本 PR 随之撤掉该菜单项；换封面走「Replace image」，真要把某张图从产品上摘掉走 Library 的移除参考图。**2026-09-16 Founder 追认（对谈）：删除该菜单项**。撤掉一颗已上线的控件属交互变更（`frontend-integration-handoff.md` §4），此处按裁决的全量语义先行登记。 |
| 2026-09-16 | **R3-F20**（第三轮走查）：**「去加个产品」的指路没有一条落在真有「Add product」那颗键的页面上**。根因两条，都在指路层、不在动作层：①单一源指错了楼层 —— `apps/web/lib/exits.ts` 的 `BRAND_MEMORY_HREF` 指 `/brand`，而 `/brand` 是设计的五节工作台（`apps/web/app/brand/BrandWorkspace.tsx`），上面一颗产品控件都没有；真有那颗键的是 `/brand/records` 的 Products 页签（`apps/web/components/otto/memory/ProductShowcase.tsx` 工具条与空态各一颗）。②通向那一页的唯一入口被收在两节里 —— `/brand/records` 不在 `MERCHANT_NAV`，产品里只有 Brand 页顶那一行「Open the record editor」指得到它，而那一行原本只在 Knowledge base 与 Audiences 两节画，商家从导航点进 Brand 落的却是默认的 Brand voice（`apps/web/app/brand/page.tsx`），整屏没有一条通向产品的路。§1.3 要求的 Library 空态那句指路则从来没写过。**本 PR 修**：`BRAND_MEMORY_HREF` 改指 `/brand/records?tab=products`（`?tab=` 是 `OttoMemory` 真读的深链参数）；Brand 页顶那一行改为五节都画；Library 的 Elements → Products 空态补上 §1.3 那句指路，链接取同一个常量。验收：导航 Brand →「Open the record editor」→「Add product」这条路在旅程 23 里逐下点得通（`e2e/journeys/23-brand-product-identity.spec.ts` 开场不再用深链）；五节各一条 jsdom 断言 + Library 空态一条（`apps/web/lib/__tests__/product-entrance-signposting.test.tsx`）。**本 PR 不修（两条都是新面／重挡，报给 Founder 选）**：①`/brand` 五节壳上做一颗原生的「Add product」——那是给已批准的五节设计加第六件东西，按 §7.4 属新面；今天用一行指路代替。②Library 的「新建元素 → 产品」控件（**PRODID-A3 在界面上仍然不成立**，动作层 `createProduct` 早就在）——所以空态只说「去 Brand 加」，不写规格 §1.3 原话里的「或这里新增」，写了就是一颗找不到的键。另记两处**没有路由挂载**的死件（不复活、不删，只登记）：`apps/web/components/otto/OttoStuff.tsx` 与 `apps/web/components/otto/stuff/StuffLibrary.tsx` 的 library 模式自 PR #1152 起无人渲染，`BRAND_MEMORY_HREF` 的两个旧消费点都在那里；`apps/web/components/home/home-data.ts` 的 `equipmentSteps`（「Add what you sell」那一格）全仓零调用方。 | 2026-09-16 已随实现 PR #1463 落地（依 2026-09-15 Founder「本轮走查新发现全部本版修」裁决）；两项未做（/brand 原生新增键、Library 新建控件）留 Founder 缺口选择 |
| 2026-09-16 | **PRODID-R11／FSE-210 已落地**（承本节 2026-09-11 R11 行；实现 PR #1420，commit `f912c44d`，2026-09-13 合并主干；证明测试 `apps/web/lib/__tests__/fse-210-product-lineage.test.ts`）：R11 行建议的「产品与演员走同一条 typed ref 搬运路」已按 S5 批量裁决落地。**真实根因**：模型看到的 `@` 候选名单由 `loadAvailableRefsForAgent` 生成，该函数过滤掉没有挂参考图的元素——商家 `@` 一件还没挂图的产品时，模型的候选名单里根本没有它，模型没有任何一条路能把它的 id 主动写进 `propose` 工具参数，因此产品 id 在「消息 → 确认卡」这一步就掉了（R11 原文「代码层两条都还是未核实的推断」现已证实为此，不是 `canvas.create-handoff` 丢 typed ref）。**修法**：铸卡时（`buildProposeCard`）把模型自带的 `entityIds` 与服务端解析器已核过归属的 `turnEntityIds`（`packages/otto/src/context.ts` 新增字段，由 `apps/web/app/api/otto/stream/route.ts` 传入）取并集，模型「不知道」也不会把商家明确 `@` 过的东西静默漏掉；同一路由让落库的 `payload.entityIds` 与喂进 `buildOttoContext` 的 `turnEntityIds` 共用同一个数组，不再各自算一遍。复测照 R11 原文。 | 已落地（PR #1420） |
| 2026-09-16 | **测试债登记（源于 #1444 复核）**：`kind=products` 且 `origin=OFFICIAL_CATALOG` 的组合（官方目录产品——`entity-policy.ts` 的 `OFFICIAL_CATALOG_CAPABILITIES` 不按 `type` 区分，PRODUCT 与 CHARACTER 同样适用只读能力，`packages/core/src/entity-policy.ts:72-124`）今天没有任何 UI 测试覆盖：既有 `apps/web/lib/__tests__/official-avatar-readonly-ui.test.ts` 的 `entityOf()` 夹具硬编码 `type: "CHARACTER"`（:53），四条只读断言（:116/:125/:135/:143）全部只喂 CHARACTER 一种类型；本规格与其余围栏均未单独补 PRODUCT + OFFICIAL_CATALOG 这一格。触发条件：官方目录真的上架产品类条目时先补这组 UI 测试，不能假定 CHARACTER 那组能代表 PRODUCT 的只读行为。 | 待排期（登记，非本轮阻断） |
| 2026-09-17 | **R3-F30（第三轮付费旅程第三组）·派生图（Create variations、Regenerate）`entitySnapshot` 为空，产品身份一跳后丢失**：一张已生成图片再做 Create variations 或 Regenerate 得到的新 `Generation`，其 `entitySnapshot` 是空数组 `{"entities": []}`，而它的源图 `Generation` 携带完整的产品与 `refHash`——**两条派生路径都是如此**（canvas 变体 `01M2PWZ30DE2PXJ8MZWM2TDZJR`，key 族 `canvas:`；Library Regenerate `01M2PX3MQTZJWY0N1085CR5DBT`，key 族 `asset:regen:`），journeys worker 原本只报告了 Create variations 一条，独立核证员复核确认 Regenerate 有一模一样的缺口，不是变体专属。与图生图路径本身一致（发给引擎的是「`<Image_1>` is the image being edited.」），不是账本或计价缺陷——两条派生单各自仍恰一组 RESERVE/SETTLE，PRODID-A10（零扣费）与本行验收不冲突。但后果是：一张首生图之后，谱系上「这张图用了哪个商品」这条链在每一条派生路径上都断了一跳——是审计／回执缺口，本规格 §1.4「读路：Library、@ 搜索、确认卡读 Entity」的精神在派生图这一层没有被满足。**待 Founder 裁语义**：派生图的 `entitySnapshot` 要不要继承源图的产品身份（继承会不会让「这张变体到底算不算用了这个产品」这句话变得不准确，例如 Regenerate 换了完全不同的提示词时）——本票不改代码，只登记事实与两种走向供裁决。出处：`docs/audits/fullstack-staging-2026-09-14/report-round3.md` 2026-09-17（三）节、`local-logs/staging-r3-paid/real-04-06-08.json` `findings[6]`；`findings-catalog.md` R3-F30。 | |
| 2026-09-18 | **PRODID-R12**（登记编号）· **R3-F30 落地**（承本节 2026-09-17 R3-F30 登记行）：**派生图继承源图的商品／人物记录**。做法只动一个地方 —— `Generation.entitySnapshot` 全产品线唯一的写入点（`apps/worker/src/jobs/gen.ts` 那一处快照构造）：这一单自己**没有挂任何引用**（`GenJob.entityIds` 为空）且有派生来源时，把源图那一行的快照**逐字**继承下来；继承的是**记录**，不是引用 —— `entityIds`／`approvedEntities` 一格不动，发给引擎的图、提示词与价钱一个字节都不变。来源读作 `sourceGenerationId ?? lineageGenerationId`：画布 Create variations 的源图真被送进引擎当底图，答案已经在 `sourceGenerationId`；Library Regenerate 那条路引擎手上没有那张照片（`gen-actions.ts` 的 `ASSET_REGEN_UPLOAD_REFUSAL` 注释写明它不送底图），所以本票给 `GenJob` 加一列可空的纯谱系列 `lineageGenerationId`（迁移 `20260918120000_genjob_lineage_generation`，只有一句 `ADD COLUMN IF NOT EXISTS`，零回填、零约束改动、带 `rollback.sql`），值 = `startAssetGen` **在算幂等键之前**已按 `ownerId` 查过库的那个锚点，经进程内可信通道进 `startGen`。**验收（逐条可当场演示）**：①画布 Create variations 出的新图，`entitySnapshot` 逐字等于源图那一行的，且 `GenJob.entityIds=[]`、`approvedEntities=NULL`；②Library Regenerate 同样继承到同一份记录（来源走 `lineageGenerationId`），送给引擎的输入图仍然是 0 张；③派生时自己换了引用（`entityIds` 非空）⇒ 快照跟着新引用走，一个字都不从源图借；④源图不属于本租户 ⇒ 什么都继承不到、落 `{"entities": []}`，浏览器自带 `lineageGenerationId` ⇒ 整单被拒（不是 `genRequest` 的字段），$0、零 GenJob、账本零新增行；⑤钱路不动：派生单仍恰一组 RESERVE/SETTLE（**PRODID-A10 不受影响**）。证据：`apps/worker/src/jobs/gen-derived-lineage-db.test.ts`（真库、真 `handleGen`，四条）＋ `apps/web/lib/__tests__/derived-image-lineage.test.ts`（真库、真 `startGen`，三条），两份都留了 red→green。**必须一起读的副作用**：`lineageCarriesOfficialActor`（`packages/core/src/generation-reference.ts`）从此在派生图上**读得到**演员血统 —— 参考图自动放大闸（`packages/otto/src/skills/reference-upscale-gate.ts`）与 i2v 的「他已经挑过演员」拒绝文案（`gen.ts`），今天在派生图那一跳是漏判的，本票之后按「血统里有演员」判。方向是更严、更诚实（少一次放大、多一句对的话），但它是一次**可观察的行为改变**，不只是补一条记录。 | **Founder 2026-09-18 裁（对谈）：本版修，派生图继承源图记录**（与编排者的对谈；承 2026-09-17 R3-F30「待 Founder 裁语义」那一行）。落地 PR：见 `docs/audits/fullstack-staging-2026-09-14/findings-catalog.md` R3-F30 状态行。 |

## 6. 改签记录

- 无
