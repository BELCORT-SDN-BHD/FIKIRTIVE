# 钱引擎 规格书(S1)

> 状态: 草稿
> 批准: (冻结时填)https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/issues/1109 Founder 评论「S1 批准 money-engine.md」(YYYY-MM-DD)
> 规格前缀: MONEY(验收编号 = MONEY-A1、A2…,全仓不得与其他规格撞前缀)

## 0. 一句话

钱引擎立起来了,商家因此能:充值买 credits、花钱前看到确定的报价、逐笔查消费与退款——而平台这边,每一个价都由**同一条定价法**(固定毛利 65% + 成本钉点)机械推导,供应商成本再怎么波动,毛利不会被悄悄吃掉,价格也不会自己乱跳。

## 1. 九问(S1 grill 的答案,一问一答;Founder 2026-08-31 逐题拍板)

1. **商家做什么动作、看到什么结果?**
   充值(三个马币包)→ 余额到账;发起任何花钱动作(生成、搜索、Otto 卡片)→ 花钱前看到确定报价,失败/被拦截则自动全退;消费历史逐笔可查,退款有对应行;余额不足一条默认视频时提前被提醒(沿用现状)。商家**看不到**的同样重要:价格平时稳定,不随供应商促销上下跳;credits 永不过期。
   本规格给商家的直接变化其实很少——钱面大部分已在生产跑。变化在**法律层**:①生成侧定价从「Founder 逐格手裁死数字」改为「固定毛利公式推导」(见九问 4);②两个待定 SKU(1080p 视频、pro 图)有了数字;③过期/退款/BYOK 等政策首次成文。

2. **入口在哪里?(列全,含深链)**
   不新增商家可见入口。现有入口维持:充值(Stripe Checkout,billing 面)、余额显示、消费历史、低余额提醒(#791-7)、生成前置报价(Creation 报价、Otto 卡片报价)。
   工程侧入口 = 价格真相文件族:`packages/core/src/spend.ts`(生成收费)、`pricing-config.ts`(汇率钉点/充值包/搜索)、`llm-prices.ts`(LLM 牌价)、`margin-truth.ts`(毛利闸)——本规格**新增一员**:成本钉点表(见九问 4),并让 margin-truth 的目标线从「只查 45% 地板」升级为「查 65% 目标线」。

3. **四态:空、加载、错误、成功各长什么样?**
   商家面四态全部沿用现状(充值成功/失败/处理中、余额加载、报价展示),本规格不动任何 UI。若施工中发现必须动 UI,走变更登记,不悄悄做。

4. **数据从哪来、写到哪去?**
   **定价法(本规格核心,Founder 2026-08-31 拍板「margin fix」)**:
   - **目标毛利 65%**:售价 = 供应商成本 × 1/(1−0.65) ≈ **2.857 倍**,再**向上取整**到收费格。45% 地板(`docs/BLUEPRINT.md` 宪法、`MARGIN_FLOOR`)不动——65% 是目标线,45% 是底线,两线之间 20 个点是缓冲带(它是承重的,见异议栏)。
   - **取整只朝上**:按张的 SKU 取整到整显示 credit;按秒的 SKU 取整到「每 10 秒整数显示 credits」格(现役 `SEEDANCE_DISPLAY_CREDITS_PER_10S` 的形状)。价目允许在公式价之上取**好记数**(如 1080p 公式价 10.8cr/秒,取 11cr/秒),进位余量归我们——方向永远是收得多一点点,绝不落到公式价之下。
   - **成本钉点制(Founder 拍板:人批才动,不实时联动)**:每个在售 SKU 的供应商牌价钉成一条带四要素的记录——数值、来源、观察日、复核到期日(照抄 `FX_PIN` 的成功样式)。供应商调价 → 改钉点表走 PR、Founder 批 → 价格按公式重算。商家价格平时稳定;变价永远是 Founder 知情的决定。首批钉点复核日与汇率钉点同日(2026-11-18),一次复核管两张表。
   - **促销不入定价(收编现行实践为法)**:定价永远按**无促销牌价**(`spend.ts` 现行做法);促销差价 = 意外之财,不进毛利模型。2026-09-07 mini 促销到期只影响我们付出去的账单,不动任何商家价格、不动毛利模型——本条已由现状证实,写成法防后人再犯(2026-06 资源包折扣价定价、折扣到期毛利被吃穿的事故不许重演,留档 #644)。
   - **首批钉点(数字全有回执,2026-08-29 arkcli 实查牌价)**:seedance-2-mini $0.0035/K tokens;seedance 2.0 $0.007/K(1080p 档 $0.0077/K);seedream lite $0.035/张、pro $0.045/张。大图($0.09)计价单位未实证,**不入钉点表 = 不可售**(fail closed)。
   - **现役价格全部无痛过渡**:480p 1.1cr/秒(毛利 69.1%)、720p 2.2cr/秒(65.4%)、图 1cr/张(65.0%)——本来就都在 65% 线上,新法上路一格不改。
   - **两 SKU 回填(Founder 拍板:数字定死,上架随施工)**:1080p 视频 = **11cr/秒**(5 秒 = 55cr,毛利 65.7%);pro 图 = **2cr/张**(毛利 77.5%)。数字回填 Creation 规格变更登记待 Founder 追认;真正可售还要等 Creation 施工把围栏改完,本规格不催工。回填同时必须修掉 1080p 护栏价倒挂:今天 `VIDEO_CREDITS_BY_RESOLUTION` 的 1080p=16cr($1.60)低于成本($1.89)——幸好该档不可售,但护栏「宁贵不贱」的语义已破,护栏价须抬到不低于公式价。
   - **变价不追溯**:调价只影响其后动作的报价;已购 credits 面值不变、已扣账不重算。
   Ledger、充值入账、幂等键、包核对:一律不动(见九问 5)。

5. **碰不碰钱路(credits / 计费)?碰则幂等键是什么?**
   本规格就是钱路的宪法执行细则,但**不动任何管道**:reserve→settle→refund 单一通道与全部既有幂等键(`refId`、四族请求键、`signup:<orgId>`)一格不动;充值包核对三态判词(`verifyCreditPackPurchase`)照旧。本规格动的只有「价格怎么来」(手裁死数字 → 公式推导)和「政策成文」。
   **失败退款总法(收编两台引擎已冻条款的共同语义)**:任何付费动作,凡失败、被拦截、被截断且无交付,ledger 上 reserve/refund 成对、商家余额净变 0,绝不静默扣钱。各引擎自己的验收行照跑,本规格立总法一条。

6. **权限与租户边界是什么?**
   余额与账本 org 隔离(现状,租户铁律);改价唯一路径 = 改成本钉点或价目文件的 PR + Founder 批——**不存在任何运行时改价面**,谁也不能在后台点两下就变价。

7. **参考对照:抄哪家?**
   - 定价机制抄 **OpenAI API**:纯按量 + 公告式调价,无实时联动——成本钉点制就是它的机械化版。
   - 演员定价**反着打 Arcads/HeyGen**:Arcads 约 $110/月只给 10 条演员视频,HeyGen 数字人溢价卖;我们的参考图输入零计费有实测回执(成片账单 prompt_tokens=0),「AI 演员出镜不加钱」成本上站得住,当卖点。
   - Mobbin 截图:本规格无新 UI 面,不适用;S2 若动充值页再补。

8. **胃口:轻/中/重挡,为什么?**
   重挡——钱路(手册 M1 路径地板),且立的是全站定价法。

9. **Otto 怎么协助这个功能?**
   共享动作层(仓库铁律):Otto 替商家下单走**同一价目、同一报价卡、同一幂等键**,无 Otto 专价。聊天(成本×1.05,已裁豁免在案)与搜索(成本×3)费率沿用既有裁决,不在本规格重裁。

## 2. 验收表(S5 只认这张表;一行一个可当场演示的判定)

| 编号 | 商家做 X | 看到 Y |
|---|---|---|
| MONEY-A1 | (工程演示)在演示环境改一个成本钉点数字,重跑推导 | 对应 SKU 的价格按公式与取整规则变化,其余 SKU 价格不动;全仓不存在第二份手抄价目(价格只有推导一个来源) |
| MONEY-A2 | (工程演示)让任一在售生成 SKU 的推导毛利跌破 65% 目标线 | CI 毛利闸转红并给出「等 Founder 重定价」判词(沿用现有待裁决名单机制);45% 地板与「收费≤成本恒红」既有闸继续生效 |
| MONEY-A3 | (工程演示)查价目:1080p 视频与 pro 图 | 1080p = 11cr/秒(5 秒报价 55cr)、pro 图 = 2cr/张,均由公式推导可复算;1080p 护栏价不低于公式价(16cr 倒挂已修);两数字已回填 Creation 规格变更登记 |
| MONEY-A4 | (工程演示)把一条成本钉点的复核期改成已过期;另删掉一条钉点的来源字段 | 前者闸黄(提醒复核,不拦发布);后者闸红(没出处的成本不是证据)——判词样式与汇率钉点闸一致 |
| MONEY-A5 | 商家任何时候查余额与条款文案 | credits 永不过期:仓库不存在过期/清零代码路径,商家可见文案与此一致 |
| MONEY-A6 | 商家用演员库角色出一条视频,再用同参数(同时长同分辨率)出一条不带演员的 | 两次报价逐字相等;消费历史不存在「演员费」行 |
| MONEY-A7 | (工程演示)在演示环境调一次价,前后各发起一次同参数动作 | 调价只改其后动作的报价;调价前已扣的账一分不重算,已购 credits 面值不变 |

(注:MONEY-A2 所指 45% 地板闸、A7 所涉退款成对语义,与 Otto、Creation 两规格的既有验收行同源不重复——交叉引用见九问 5,表内按 M2 闸纪律不写他家编号。)

## 3. 不做(非目标;写明为什么和触发条件,防「遗漏」误会)

- **不做订阅**(Founder 2026-08-31 拍板):beta 纯充值包,三个包已在 Stripe 跑通,零新工程。触发条件:beta 商家出现稳定月用量或主动要求订阅。
- **不做 BYOK**(商家自带供应商钥匙):「Seedance/Seedream 独家且保密」是 Founder 产品原则,BYOK 等于把供应商印在设置页上;且 BYOK 单毛利归零、支持成本全在我们。触发条件:大客户点名要 + Founder 本人重议保密原则。
- **不做低余额自动充值**:自动扣卡最易出「惊奇账单」投诉;现有低余额提醒已够 beta。触发条件:商家真实撞墙数据出现。
- **不做自助退款**:未使用 credits 可申请退,逐单人工审(Stripe 后台手动退,Founder 或其授权者执行);不建自助退款钱路。触发条件:退款单量让人工吃不消。
- **不设 credits 过期**:付了钱的东西不蒸发,零工程零条款麻烦。触发条件:财务负债挂账成为真实问题时再议。
- **不做演员出镜溢价**(Founder 2026-08-31 拍板「不加价,当卖点」):输入零计费有回执,成本站得住。触发条件:正式版定价复审。
- **不卖大图 SKU**:计价单位未实证,未钉点 = 不可售(fail closed)。触发条件:单位实测 + 钉点入表。
- **不碰税务/SST**:法律事项,红线归 Founder 本人。触发条件:营收接近 SST 注册门槛时由 Founder 处理。
- **不扩收单方式**(FPX/GrabPay 等):beta 维持 Stripe 现状。触发条件:beta 商家付款转化受阻的真实案例。
- **不重裁聊天/搜索费率**:成本×1.05 与×3 均有裁决与豁免档案(margin-truth 的 RULED 行),本规格只收编不重开。
- **不重建收单**:Stripe 管道在生产跑,不动。

## 4. 异议栏(AI 必填:本规格最大的风险或异议)

- **充值包折扣没进毛利闸的口径**:毛利闸按 credit **面值**($0.10)算,但 Pro 包(RM250→600cr)按汇率钉点实收只有面值的 92.6%——商家全走 Pro 包时,面值毛利 65% 的 SKU 真实毛利 ≈62.2%;推到极端,一个贴着 45% 地板的 SKU 走 Pro 包实收,真实毛利只剩 ≈40.6%,**已在宪法地板之下**。今天没事,靠的是 65% 目标线把所有 SKU 抬得离地板够远(20 个点缓冲 > 包折扣 7.4% + 取整波动),所以 65% 目标线是**承重结构**,不是好看的数字。留给 S2 的硬要求:目标线闸落地时,把「面值口径 + 包折扣上限 7.4%」写进闸的注释与测试;未来任何加深包折扣或加大包的提案,必须先过「最深折扣 × 最低毛利 SKU 仍 ≥45%」这道算术。

## 5. 变更登记(冻结后的中途想法只进这里,下次 S5 批量裁决;不当场执行)

| 日期 | 想法 | 裁决(留空待 S5) |
|---|---|---|

## 6. 改签记录

- 无
