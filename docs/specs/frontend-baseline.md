# 前端基线规格书（S1）

> 状态: 已冻结 · v1
> 批准: https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/issues/1128 Founder 评论「S1 批准 frontend-baseline.md」(2026-09-02)
> 规格前缀: FRONT（验收编号 = FRONT-A1、A2…，全仓不得与其他规格撞前缀）

<!--
本规格的对象是 PR #1117（分支 codex/uiux-frontend，head c8ae482c）交付的前端基线。
Founder 2026-09-02 裁决（本对话原话要旨）：「做 UIUX 时完全按我对 beta 的构想设计，
所以要让后端服务这个最新的前端版本」；Library、Brand、@ 引用选择器、登录旅程全部接真实后端，
不藏、不用 fixture 冒充；过闸方式＝写一页规格、Founder 签字。
同日两问拍板：Brand 深度＝存真记录＋人工维护＋Otto 读得到；登录测试环境＝本地＋一把 Resend 测试 key。
设计层的权威仍是分支上 apps/web/design-system/ 的已批准 pattern 与 acceptance 记录；本规格不重开设计，
只定「后端服务前端」的验收口径。取证：本规格引用的行号均对分支 head c8ae482c。
-->

## 0. 一句话

把 Founder 按 beta 构想设计的前端（PR #1117）合进主干，并让后端真实服务它：商家在新前端每个入口看到的都是自己店的真数据、真权限、真扣费；这套界面就是 beta 出售的界面。

## 1. 九问（S1 grill 的答案；Founder 2026-09-02 拍板）

1. **商家做什么动作、看到什么结果？** 登录 → Home 看到自己店的真实状态（连接五态之一）→ Create 画布与 Otto 对话（现有真能力，新壳）→ Library 找到生成结果与上传、收藏、建 collection、送进画布 → Brand 五个分区写品牌事实，Otto 对话读得到 → Settings / Billing 改名、连接、看余额充值（现有真能力，新壳）→ 在 Otto 输入框打 `@` 从真实对象里挑参考。每一步的空、加载、错误、成功都来自服务器；没有真实能力的按钮不出现。
2. **入口在哪里？（列全，含深链）** 正式路由：`/`、`/analysis`、`/create`、`/create/canvas`、`/library`、`/library/editor`、`/brand`、`/profile`、`/settings`、`/settings/connections`、`/billing`、`/login`（含 `?from=` 安全回跳）、`/signup`、`/verify-email`、`/forgot-password`、`/reset-password`。评审夹具路由 `/product-patterns/*`、`/design-system/*` 不是商家入口，生产构建不得可达。钱引擎新增的 `/admin/reconcile` 在分支上不存在（分支基于 a32790b8），合并时必须保留。Campaign / Schedule / CRM 路由维持 #850 裁决（beta 期藏门），不因本规格回到范围。
3. **四态：空、加载、错误、成功各长什么样？** 各面 essential states 已在设计文档定义并获 Founder 验收（Home 五态 read model：`information-architecture/frontend-convergence-phase-2-home-spec.md`；Library 六态：`patterns/library/backend-handoff-contract.md`；引用选择器六态：`information-architecture/frontend-convergence-phase-5-reference-picker-spec.md`；Auth 中性错误：`patterns/auth/access-journey-spec.md`）。本规格只加一条硬规则：四态全部由服务器真相驱动——不用假延时、静态 credits、浏览器临时状态或成功 toast 冒充持久化、权限、扣费与完成（`governance/frontend-integration-handoff.md:39`）。
4. **数据从哪来、写到哪去？**
   - **Library**：读 `Generation`（`favorite` 列已有）、`Asset`（上传）、`Entity`（产品等四类）；现有 `getGenerationHistory` / `finalizeCandidateUploads` / `setFavorite` / `createCanvasNode` 沿用。**新建** `Collection` 与 `CollectionItem` 两表（带 `orgId`、唯一约束、迁移）。Elements 用现有 `Entity` 四类；Clothes 与 Official avatar 两类是 Creation 引擎演员库的产物，本规格不建（见「不做」）。
   - **Brand**：五分区（Brand voice / Audiences / Knowledge base / Style guide / Visual guidelines，`patterns/brand/README.md` §5）落到现有 `BrandRecord` / `Memory` 表与 `brand-record-actions.ts`（save / delete / restore）、`memory-actions.ts`（add / update / delete）；每条记录带「谁改、何时改」（施工时核对列，缺则迁移加列）；Otto 经现有 `getBrandContextText` 读取，施工时核对五分区都进这段文本。
   - **Home**：连接状态五态 read model 已接真（`MarketingHealthReadModel`）；**新建** workspace 级 Home 布局持久化（org 级一行，迁移）与 `Manage home` capability 检查；多来源 aggregate 带 freshness 由真实数据函数提供（可复用 #1087 / #1088 的函数族方案）。Otto 页面上下文读取器归 Otto 引擎。
   - **@ 引用选择器**：**新建**统一 reference search（服务端动作：租户内、分页、类型化 ID + 来源），覆盖 `Generation` / `Asset` / `Entity`；发送的消息保存真实引用 ID（施工时核对 `ChatMessage` 的引用字段，缺则加列）。Official avatar 类别随 Creation 引擎点亮。
   - **Auth**：Better Auth 现有能力（邮箱密码、邮件验证码、Google 条件登录、注册、验证、忘记/重置）；本地环境接一把 Resend 测试 key 跑真实邮件。
   - **分支自带的四处 server 改动**（`governance/frontend-baseline-handoff.md:46-55`：会话分页读取、软删除/恢复、Library 去重、Otto 导航指令）逐项对照 main 复审并补行为测试；分支未新增 schema/migration。
5. **碰不碰钱路？** 不新增计费点、不改价格。风险是合并回退：9 处冲突全在钱引擎两天前改过的文件（账单页、素材面板、Otto 聊天流、模板弹层、加素材弹层、剪辑台）。规则：钱的行为以 main 为准，页面长相以 #1117 为准；钱引擎规格验收表 14 条在新前端上重跑（A1）。幂等键不适用（无新扣费）。
6. **权限与租户边界是什么？** 一切读写走 `requireOwner()` 服务端 principal；客户端不提交 `ownerId` / `orgId`（`patterns/library/backend-handoff-contract.md:36-40`）。新表带 `orgId` 约束与双租户测试；collection、reference search、Home 布局、Brand 记录跨租户不可见；`Manage home` 用具体 capability 判定，不看角色名。
7. **参考对照：抄哪家？** 设计文档已引用并经 Founder 逐面视觉验收：Brand 走 Jasper IQ 式五分区（`patterns/brand/README.md:3`「Founder approved and frozen 2026-08-30」）；Auth（`patterns/auth/access-journey-spec.md:102`）；引用选择器（phase-5 spec:3，2026-09-02「ok 这个版本可以很棒」）；Home / Create / Settings 的 acceptance 记录各有 Founder 句。本规格不重开设计。
8. **胃口：轻／中／重挡，为什么？** 重挡：三张新表迁移、登录租户旅程、837 文件合并。分段施工（S2 定切法与写集互斥表）：先纯合并过全套测试与 Founder 视觉验收，再接 Library / Brand / @ / Home。
9. **Otto 怎么协助这个功能？** Otto 面板是新壳的一部分（现有能力）；Brand 记录进 Otto 上下文（A9）；`@` 引用进 Otto 对话（A10）；Otto 知道商家正在看哪一页（page-context reader）归 Otto 引擎，不在本规格。

## 2. 验收表（S5 只认这张表；一行一个可当场演示的判定）

| 编号 | 商家做 X | 看到 Y |
|---|---|---|
| FRONT-A1 | （工程演示）新前端合入主干后，重跑钱引擎规格验收表全部 14 条 | 全部通过；`/admin/reconcile`、账单页「Credits don't expire」、上传入口价目小字、聊天搜索成本提示等钱引擎交付面在新壳上仍在 |
| FRONT-A2 | 商家在本地测试环境用邮箱注册、收验证码、登录；再走一次忘记密码 | 收到真实验证码邮件并完成验证；从 `/login?from=/create` 登录后回到 `/create`；重置邮件可用、新密码能登录；错误提示不泄露该邮箱是否存在 |
| FRONT-A3 | 两个租户各自登录看 Home | 各自看到自己店的真实连接状态（五态之一）与「Continue creating」真实画布；对方的数据永不出现 |
| FRONT-A4 | 商家调整 Home 布局后刷新、换浏览器再登录 | 布局仍在（服务器持久化，不是浏览器存储）；没有 `Manage home` capability 的成员看不到该入口 |
| FRONT-A5 | 商家在 Library 看生成历史与上传，按提示词搜索、按收藏筛选、点收藏 | 列表与筛选结果来自服务器；收藏后刷新仍收藏 |
| FRONT-A6 | 商家新建 collection，加入一个生成结果与一个上传，移除一项，删除 collection | 每步刷新后仍成立；另一租户看不到该 collection；删除后其成员对象仍在 Library |
| FRONT-A7 | 商家在 Library 对一个生成结果点「Use in canvas」 | 当前项目画布出现该节点，节点归属当前项目与租户 |
| FRONT-A8 | 商家在 Brand 五个分区各写一条记录，编辑、删除、恢复 | 刷新仍在；每条显示谁改的、何时改的；恢复后内容完整 |
| FRONT-A9 | 商家写完一条 Brand 记录后，在画布问 Otto 一个与之相关的问题 | Otto 的回答用到该记录内容；（工程侧）该轮上下文可查到该记录 |
| FRONT-A10 | 商家在 Otto 输入框打 `@`，选一个最近对象或按类型搜到的对象后发送 | 列表来自服务器（最近用过 + 生成结果 / 上传 / 产品分类搜索）；消息记录保存该对象的真实 ID，可回链；Official avatar 类别在演员库交付前不出现假条目 |
| FRONT-A11 | 商家在新壳的 Settings 改个人显示名与工作区名，在 Billing 看余额并进充值 | 改名刷新仍在；余额、冻结额、充值包与结账走现有真能力；plan / payment method / invoice 这类无契约的控件不出现 |
| FRONT-A12 | （工程演示）用生产构建访问 `/product-patterns/*` 与 `/design-system/*` | 不可达（404 或仅 dev）；商家面任何页面不出现夹具数据；任何写入失败都有错误反馈，不出现「假成功」 |
| FRONT-A13 | （工程演示）跑分支自带四处 server 改动的行为测试（会话分页、软删除恢复、Library 去重、Otto 导航指令） | 在非生产数据库上权限、恢复、分页各自通过；与 main 对照无回退 |
| FRONT-A14 | Founder 在登录态走 Home → Create / Canvas → Library → Brand → Settings → Auth 六面 | 视觉与交互与已批准的设计文档一致；差异逐条登记 §5，不当场改方向 |

## 3. 不做（非目标；写明为什么和触发条件，防「遗漏」误会）

- **不做 Otto IQ 自动摄取、审核状态、版本对比**：Founder 2026-09-02 裁决 Brand 深度＝存真记录＋人工维护＋Otto 读得到；自动化三件归 Otto 引擎。触发条件：Otto 引擎 S2 纳入。
- **不建 Official avatar 与 Clothes 目录**：它们是 Creation 引擎演员库（五人组＋九套造型 preset）的产物，本规格建了就是第二套来源。触发条件：Creation 引擎交付后在 `@` 与 Library 点亮，不改本规格验收。
- **不做 Otto 页面上下文读取器**（Home 的 Otto context chip）：需要 Otto loop 消费页面上下文，归 Otto 引擎；在此之前 chip 不显示「已附上」的假状态。触发条件：Otto 引擎施工。
- **不做 subscription plan / payment method / invoice**：无后端契约，按设计文档不显示。触发条件：钱引擎 §5 登记后另立规格。
- **不重开设计**：已批准 pattern 不改；任何 UX 变化先让 Founder 看过，登记 §5。
- **不让 Campaign / Schedule / CRM 回到 beta 范围**：维持 #850 裁决。
- **不动价格与计费**：钱引擎已归档。触发条件：钱引擎 §5。
- **不做第二套组件基座**：Base UI 切换随本规格上主干；base-ui-switch.md 碑文里「商家可见视觉零变化」口径由 Founder 2026-09-02 裁决取代为「按 beta 构想设计、逐面验收」（A14）。
- **不做基础设施变更**（Vercel 搬迁等）：维持 2026-08-29「维持丙」裁决。

## 4. 异议栏（AI 必填）

- 最大风险：**合并把钱引擎刚交付的行为悄悄回退**。这是 837 文件、+46,740 / −16,167 的合并，9 处冲突全在钱引擎 48 小时内改过的文件。对策：A1 把「钱引擎 14 条重跑」列为第一条验收；施工先纯合并（不加功能）过全套测试与 Founder 视觉验收，再接功能。第二风险：本规格的接线部分像三个小引擎（collection、reference search、Home 持久化），估算两周级，若与 Otto / Creation 施工并行，共用文件冲突会重演——S2 必须画写集互斥表，谁后合并谁解冲突。

## 5. 变更登记（冻结后的中途想法只进这里，下次 S5 批量裁决；不当场执行）

| 日期 | 想法 | 裁决（留空待 S5） |
|---|---|---|
| 2026-09-03 | Connections 页的 **WhatsApp 行随新壳消失**。旧壳 Messaging 分区里有一行 WhatsApp，写着 "Not available yet"、零按钮（`MessagingRow`，PR #1139 前的 `components/otto/OttoConnections.tsx`）；新壳按已批准的 Settings pattern 重画成「先管已连服务，Add connection 才进 discovery」，整个 Messaging 分区连同那一行退场。产品事实没变（今天仍然没有任何一条连接消息渠道的路），变的是商家还看不看得到「WhatsApp 暂时连不上」这句话。待 Founder FRONT-A14 过目：要不要在 Add connection 弹层里把它作为一条 Unavailable 服务列回来。 |  |
| 2026-09-03 | **前门不再承诺「做东西与发布都要你先点头」**。换壳前登录页写着 "nothing gets made or published until you approve"，围栏 `public-copy-honesty-791.test.ts` 正向钉着它；新的登录/注册页只剩身份表单，全仓这句承诺只剩审批卡自己（`components/otto/OttoApprovalCard.tsx`）与 `packages/core/src/schedule-draft.ts`。反向禁令（不许说成「凡花钱都先经你点头」）本 PR 已补回并覆盖登录/注册两面；**正向那一句要不要回到前门是产品决定**，等 Founder 拍板。 |  |
| 2026-09-03 | **Otto 仍然告诉商家 `/create#templates` 有一个 Templates 区段**（`packages/otto/src/skills/recommend-templates.ts:67` 的 `TEMPLATES_SELF_SERVE`）。新壳的 `/create` 只挂 Otto 入口与画布历史，那个区段已经不渲染 —— 这句话今天是假的。本 PR 只把重定向表里的死锚点撤了（零可见变化），改 Otto 的说法是一次商家可见的文案改动，留给 Founder 定：改成「让 Otto 直接帮你做」，还是把 Templates 区段接回 `/create`。 | 2026-09-03 Founder 裁决（裁决九）：**删掉那句话**——它是假话；Templates 区段不接回 `/create`，Otto 改说「让 Otto 直接帮你做」。**落地**：PR #1148 → main `93b72293` |
| 2026-09-03 | **Founder 令：生产界面严格按 UIUX 设计（design-system/patterns 各面 Reference 组件与夹具）走**；非逼不得已（例如设计里没有、必须新增的东西）不得偏离；任何新增或改动都须遵守前端规则（PR #1117 交接的 SSOT 与接线规范）。触发＝2026-09-03 Founder 在主干 d24079b5 走 `/create` 与 `/create/canvas`，发现生产路由仍渲染旧组件（`ImmersiveCanvasEntry` / Ask Otto 侧栏），与设计夹具（`CanvasReference` / `CreateWorkspaceReference`）不一致。处置＝先做逐面对照审计（设计夹具 vs 生产路由，并排截图＋差异分级＋工程量），再按面接线；Canvas/Create 接线不在 §7.1 现有八段内，作为新段「⑨ Canvas/Create 按设计接线」待审计后估算与 Founder 批准；接线顺序 Canvas 优先。 | 2026-09-03：审计已结案（见下一行），Founder 同日出九项裁决，§7 据此修订（新增第⑨段与各段施工细则） |
| 2026-09-03 | **逐面对照审计结案**（设计夹具 `apps/web/design-system/patterns/*` 的 Reference 组件对生产路由，逐面并排走查）。结论分三级：①**外壳一模一样**——应用外壳（导轨、顶栏、Otto 面板骨架）设计与生产一致；②**只差一处**——登录页品牌标破图（候选根因：`apps/web/next.config.ts` 没开 `images.dangerouslyAllowSVG`，而 `FikirtiveMark` 用 `next/image` 取 `/brand/f-app-icon-coral.svg`；施工时坐实，不当结论用）；③**结构不同**——Create 起步页与 Settings/Billing（页面在，但版面与组件不是设计那套）；④**没接线**——Home、Canvas、Library、Brand、@ 引用选择器（设计里的版面与控件在生产上不存在，或接的是旧组件） | 2026-09-03（审计结论采信，据此出九项裁决） |
| 2026-09-03 | **裁决一 · 顺序**：「对齐轮」（不依赖新后端：画布对齐、Create/设置换皮、素材库接现有资产、@ 小片、登录破图、面包屑）与「四大块」（首页仪表盘、素材库全套含收藏与合集、品牌五节＋草稿流、@ 引用统一）**并行**开工，不排队 | 2026-09-03 |
| 2026-09-03 | **裁决二 · 账单页**：按设计的排版与组件重画，但**保留主干已交付的三条花钱披露**（花费上限、自动理解、网页搜索——钱引擎验收 A5／A9／A10 的商家可见面）；设计里的「Plan & payment」（套餐、付款方式、发票）在后端有对应对象之前**不显示、也不放占位** | 2026-09-03 |
| 2026-09-03 | **裁决三 · Brand 分区**：按设计五节（Brand voice／Audiences／Knowledge base／Style guide／Visual guidelines）；主干旧壳的「你的产品」「你的优惠」两节**并入 Knowledge base**（迁移映射，数据不丢）；Otto 读取口径不变（仍经 `getBrandContextText`，五节全进） | 2026-09-03 |
| 2026-09-03 | **裁决四 · Brand 保存安全模型**：按设计的五步链「加来源 → 抽取 → 生成草稿 → 预览效果 → 确认保存」，商家确认之前不落正式记录；预览要调模型，那笔小额成本按钱引擎自动理解的计费口径（钱引擎验收 A9：披露先于扣费、按动作时刻价目、我方故障重试不重复计费）在预览前披露 | 2026-09-03 |
| 2026-09-03 | **裁决五 · Create 起步页**：去掉主干自加的可见标题行「Create with Otto」（含 Otto 头像与副标题）与「Nothing paid starts before you confirm the exact credits in Canvas.」一句，完全按设计（h1 `Create` → composer → Canvas history）。证据：`apps/web/components/canvas/NorthstarHome.tsx:36-53` 对 `apps/web/design-system/patterns/canvas/CreateWorkspaceReference.tsx:44-63`（设计里 "Create with Otto" 只是 `aria-label`，屏幕上没有这一行） | 2026-09-03 |
| 2026-09-03 | **裁决六 · 画布两个控件不显示**：设计夹具里的 Frame select（右侧工具条第二枚）与 撤销／重做（右下角）在生产上没有对应功能，本轮**不显示**——宁可少一个控件，也不做点了没反应的假按钮。证据：夹具 `CanvasReference.tsx:421-422` 两处都是 `toast.info` 占位 | 2026-09-03 |
| 2026-09-03 | **裁决七 · 剪辑拼接不属阶段一**：阶段一只要「Edit this clip」改片入口可用；多片剪辑与拼接留到后续，不进本轮范围 | 2026-09-03 |
| 2026-09-03 | **裁决八 · 上传照片 Regenerate**：维持拒收，但**先把拒绝提示改成人话**（如 "Uploads can't be regenerated yet. Try Animate or Edit instead."）；图生图（i2i）能力归 Creation 引擎增强层，排进 `creation-engine.md` §8.2 批 II，请求形状接上后再开放。同条已回写 creation-engine.md §5 的 2026-09-03 行 | 2026-09-03 |
| 2026-09-03 | **裁决九 · 通用原则**：生产界面严格按设计走；设计里没有、而生产必需的东西（空态、错误态、钱披露）按一条规矩处理——**无契约的控件不出现，有契约的内容按设计的样式呈现**。另：Otto 话术里「Create 页有 Templates 区段」这句删除（见本表同日 Templates 行） | 2026-09-03 |
| 2026-09-03 | **裁决十 · 收藏存法**（回应 §7.3② 的报告项）：**新建一张统一的收藏表**——不分素材类型，记「谁收藏了哪个素材」，带租户约束；收藏页一次查询、按时间排。备选「给 `Asset` 补一列、两类各存各的」**否决**（那样 Favorites 视图要两次查询＋应用层合并排序，游标还要各算一套） | 2026-09-03 |
| 2026-09-03 | **裁决十一 · Brand 六→五节映射全表**（回应 §7.3④ 的报告项）：Founder 同意四条建议对应——about→Brand voice、customers→Audiences、look→Visual guidelines、rules→Style guide；产品／优惠→Knowledge base 已由裁决三点名。六节映射至此**全表已裁**，施工照落，不再有待确认项 | 2026-09-03 |
| 2026-09-03 | **⑨ 段下一刀「起步页参考契约」**：PR #1151（Create 起步页对齐，第⑨段）核实——Create 起步页 composer 的「+ Add context」（Upload image / Choose from Library / Add URL）本轮**不显示**，按已冻结的裁决九「无契约的控件不出现」处置。根因：起步页没有把参考带进画布的通道——`createCanvasConversation` 的 handoff 只落 `{prompt, threadId}`（`apps/web/lib/canvas-entry-actions.ts:87`）；夹具 `design-system/patterns/canvas/CreationComposer.tsx` 的上传回调只是 `onReferenceChange(file.name)`（:101 附近），不真上传（PR #1151 报告「设计有、生产暂不显示」表；证据已核）。登记为第⑨段下一刀，施工内容与工程量见 §7.3「⑨ 下一刀」小节 | |
| 2026-09-03 | **Otto 前厅（`OttoFrontDoor`，画布 Otto 覆盖层入口，`apps/web/components/canvas/CanvasOttoOverlay.tsx`）没有上传能力**：PR #1150（Canvas 表面按 pattern 对齐，state OPEN，未合并）只给 `OttoChatStream` 的 composer 加了「Add context」——已核 `gh pr diff 1150 --name-only` 只改了 `apps/web/components/otto/OttoChatStream.tsx`；`apps/web/components/otto/OttoFrontDoor.tsx` 里没有任何上传 / Add context 相关代码（已核，main 现状）。登记为 Otto 引擎面的待接线项，触发＝`otto-engine.md` S2 排进去（本条不改 `otto-engine.md`） | |
| 2026-09-03 | **⑤ 段租户列名措辞订正**：PR #1156（Home 仪表盘，已合入 main `9f966663`）落成 `ownerId` 而非 §7.3⑤ 原文字面写的 `orgId`——运行时租户守卫（`packages/db/src/tenant-guard.ts:71-74`）的 `scopeWhere`/`scopeCreateData` 注入的是字面列名 `ownerId`；`OrgHomeLayout` 模型实际字段是 `ownerId String @unique`（`packages/db/prisma/schema.prisma:1371`，`tenant-guard.ts:54-59` 已把它登记为 `TENANT_MODELS`）。已按此把 §7.3⑤ 与 §7.3② 中描述唯一约束的「orgId」措辞改成「租户列（本仓库为 `ownerId`）上有唯一约束」等对应表述，与本仓库既有约定一致——只有 `CreditAccount`／`CreditLedgerEntry`／`RefundJournal` 三张钱表按 `tenant-guard.ts:62-96` 例外用字面 `orgId`，其余租户表一律 `ownerId` | 2026-09-03：措辞已按此改，本行即落地记录 |
| 2026-09-03 | **评审原型 per-goal 版面 vs 生产 org 级一行**（PR #1156 报告「与评审原型的一处已知分歧」，登记未裁）：评审原型 `FounderHomeReference` 给每个 business goal 各存一份版面顺序；规格 §1 九问 4 与 §7.3⑤ 把落库口径定成「org 级一行」，生产上一个工作区只有一份顺序、三个 goal 共用，goal 仍决定「没保存过时」的推荐模板。按规格施工，差异已由 PR #1156 登记；是否回到 per-goal，等 FRONT-A14 走查时 Founder 定夺 | |
| 2026-09-03 | **画布卡片媒体井比例**：PR #1150（Canvas 表面按 pattern 对齐，state OPEN，未合并）报告「已登记的形状差异」——夹具的卡是「媒体井 + 42px 页脚」的竖长卡；生产沿用每张卡自己**持久化**的尺寸（默认 320×320）；`apps/web/lib/canvas-node-size.ts` 的 `canvasMediaNodeSize` 只在尺寸仍是默认值（`isDefaultCanvasMediaNodeSize`）时才按媒体比例重排，商家一旦拖动过尺寸即持久化、不再跟媒体比例走（已核代码），卡看上去偏方、图会略小。若 Founder 要求媒体井严格等于图片比例，需把页脚高度作为 chrome 纳入 `canvas-node-size.ts` 的尺寸计算。登记待 FRONT-A14 走查定夺 | |
| 2026-09-03 晚 | **裁决十二 · 画布卡下方那条改写输入条退场**：被选中的卡片下方浮出的 `NodeRemakeComposer`（Evolve —— 改写提示词再出一张）**去掉**；改写走卡上操作条的 **Edit with Otto**（把这张卡交给 Otto 接着改），能力不丢。触发＝第⑨段（Canvas 对齐）施工者把它列为「保留待裁」，Founder 当晚裁定去掉。**连带**：那条输入条里的**每卡形状选择器**一并消失（已批准的设计里卡上没有形状选择器）——「Create variations」从此交付**这张卡自己记着的那一格形状**，商家要换形状在创作输入条里选；价钱与「先确认再收钱」的承诺改挂在按钮自己的 tooltip/title 上，一格没缩水。**落地**：PR #1150（`ImageNode.tsx` / `VideoNode.tsx` / `FlowCanvas.tsx` / `globals.css`，`NodeRemakeComposer.tsx` 删除；围栏 `front-a14-canvas-alignment.test.ts`「FRONT-A14: no second input bar under a picked card」） | 2026-09-03 晚 Founder 裁决 |
| 2026-09-03 晚 | **裁决十三 · Otto 输入框下两行计费说明维持常驻**：不改成「按需披露」。口径以钱引擎规格 MONEY-A10（Founder 2026-09-02 裁决）为准，第⑨段派工书里那句「按需披露」作废。**落地**：PR #1150 保持原样并加围栏（`money-a10-search-disclosure.test.ts`） | 2026-09-03 晚 Founder 裁决 |
| 2026-09-04 | **Otto 侧栏默认改为收起**：全局 Otto 面板默认收起；仅当①这一页有活动对话，或②商家上次留着开着时才展开。**取代** `docs/specs/wave2-shell.md` Q3-A「面板首开后记忆」（首开默认开，之后按存档；Founder 2026-08-18 裁决）——该规格已「已交付 · 归档」，原文原样保留、不改，新口径记在本行。触发＝Codex 只读走查 QA-CRE-006：清空存储的全新会话里，存档记着的旧「开」在别的商家面上自动弹开，吃掉了 Create 极简页半屏（`/create` 本身已由 #1165 从面板挂载表移除，但那不解决「面板带着旧状态到处弹开」这条根因）。**实现**：唯一改动是 `panel-state.ts` 的 `defaultOttoPanelState()`——`open` 字段从「按视窗宽度算，≥1024px 开」改成恒为 `false`。「商家上次留着开着」（`parseOttoPanelState` 读到持久化的 `open:true`）与「这一页有活动对话」（深链强开 `?otto=1`，`OttoPanelShell` 的 `forceOpenSignal` 盖过存档）两条覆盖路径是既有机制，原样不动；「这一页有活动对话」这一条以深链信号近似（见下方追认）。**假设**（状态层拿不到更干净的判据）：「活动对话」取的就是既有的深链强开信号——面板体的会话数据（`activeThreadId`/消息）本来就要等面板真的开了才取数（见 `OttoPanelHost.tsx` 顶部「取数按面板开合来」），开之前没有更早、更干净的信号可读；`?otto=1` 已经是这套代码里唯一「这次到访确实带着一个 Otto 会话」的预取信号，不是新引入的会话状态。**Founder 2026-09-04 追认**（判官 #1168 P2-2 指出这是近似不是原意：商家在 Library 有进行中的对话、离开再回来面板不展开；`?otto=1` 不带 thread 也强开）：追认现状为近似口径；真信号「本页有进行中的 Otto 对话即展开」登记待下一轮，不占本轮 4 小时。**落地**：PR #1168（分支 `claude/otto-panel-default-collapsed`，叠在 #1165 之上）；测试以 `FRONT-A14` 命名，覆盖四态（首次访问收起 / 活动对话展开 / 存档开着展开 / 存档关着＋活动对话仍展开）—— `otto-panel-state.test.ts`（纯函数默认值）、`otto-panel.test.ts`（Shell 首帧收起、损坏存档退回收起）、`otto-panel-mount.test.ts`（挂进真壳的四态一组，外加窄屏守卫改钉、r3 竞态因首帧不再默认开而随之消灭）。 | 2026-09-04 Founder 裁决（经 orchestrator 决策提示下达）；落地见本行 PR |
| 2026-09-04 | **第⑨段真机走查落修:画布选中与覆盖层四条**(Codex QA-CRE-002／QA-CRE-008,真 Chrome 走 main `e622bec6` 的生产构建,1440×900;单测当时全绿而浏览器里是坏的)。**找到什么**:①选中一张卡按 Delete／Backspace 屏幕上什么都不发生;②Shift 多选本身是通的(实测 React Flow 记得住两张、描边与真选中一致、「2 selected」条也出来),坏的是键盘对这一组一样使不上劲;③「Fit to screen」把画摆在固定覆盖层底下(实测 Fit 之后一张视频卡 45%、另一张 32%、文字卡 42% 被压住),点视频卡落在 Otto 输入框的披露句上 —— 视频没被选中、上一张图的操作条还留着(走查记的「视频卡选不中」就是这一幕);④一张失败卡 320×320 站在 320×180 的正常卡旁边,把工作区往覆盖层里顶。**改了什么**:①②键盘接到**已有的那两个确认框**上(单张 `pendingDeleteId`、多张批量框),不新开删除路,「还在生成、删了不退款」那句警告一个字都绕不过去;③`fitView` 从对称的标量 `padding: 0.22` 改成**量出来的四边安全区**(`apps/web/lib/canvas-fit-padding.ts`;覆盖层清单含 Otto 当前轮卡／输入框／对话条／画布工具条纵列／模式条／缩放簇,同 `lib/canvas-otto-dock.ts` 那条「量、不写死」的规矩);④停下来的卡收成正常卡外形(`lib/canvas-node-size.ts` 的 `canvasTerminalNodeSize`)。围栏:`front-a15-canvas-selection.test.ts`(真 FlowCanvas)与 e2e 旅程 `17-canvas-selection.spec.ts`(真 `click()`)。**假设两条**:㈠已批准夹具 `CanvasReference.tsx:470` 的删除键是**不问就删**,生产上保留现有确认框 —— 夹具没有钱路,生产有在飞的付费卡;㈡夹具没有给失败卡另一个尺寸,「正常卡外形」按走查那块板上出好的卡取 16:9(高度从既有的 `DEFAULT_CANVAS_MEDIA_NODE_SIDE` 算出,不是另写一个数)。**PR**:#1172 | |
| 2026-09-04 | **文字便签的键盘删除只在光标不在它文本框里时成立(待裁)**。文字卡的输入框铺满整张卡的内里(`TextNode.tsx` 的 `Textarea` 是 `h-full`,外圈只剩 `.cv-node-frame-text` 的 11px／12px 内距),所以点卡的中间＝光标进框,这时按 Backspace 就该是退格,不是删卡 —— 键盘删卡的护栏(不在输入框里才算)照此正确地不动作。今天可走的路有两条:点那圈 11px 的边把卡选中(光标不进框)、或用框选圈住它,再按 Delete;卡上本来也有自己的Delete 按钮。已批准夹具在这一点上和生产一致:`CanvasReference.tsx:470` 的删除键只作用在 artifacts 上,便签(sticky／reference)本来就不吃这个键。**建议**(需 Founder 拍板,因为它改的是已通过表面的编辑模型):照 Figma／Miro／tldraw 的老规矩 —— 单击选中、双击才进文字编辑。本票不动,登记待裁。 | |
| 2026-09-04 | **画布卡的「对话色环」看起来就是选中框(待裁)**。每张卡按它所属对话拿一个颜色,画成一圈 2px 的`boxShadow`(`FlowCanvas.tsx` 的 `boxShadow: 0 0 0 2px convoColor(threadId)`,调色板里 `#e2725b` 这一格是砖红／粉,和 Otto 珊瑚几乎同色)。一块板通常只有一条对话,于是**每张卡都戴着同一圈珊瑚色**,而真正被选中的那张画的是已批准夹具的「墨色细边＋淡墨光晕」(`CanvasReference.tsx`:`border-foreground ring-2 ring-foreground/15`)。两者在屏幕上的强弱正好反过来 —— 这就是 Codex 走查写的「好几张卡同时描着粉边,却没有真的多选」。取证:`scratchpad/qa-canvas/after-13-variations-price-tooltip.png`(四张卡全戴珊瑚环,只有中间那张是选中的)。已批准夹具里没有对话色环这件东西。**三条路请 Founder 择一**:㈠撤掉色环(照夹具);㈡把对话颜色挪到卡上那颗「Image／Video」小标签上,边框只留给选中;㈢保留现状。本票不动 —— 撤或挪都是已通过表面的可见改动。 | |
| 2026-09-04 | **Create 起步页 Canvas history 三小点**（Codex 只读 E2E QA-CRE-006 §4.1，`docs/audits/creation-e2e-2026-09-04.md`，三条原话：① history 是旧到新不是「最近活动优先」；② history 仍出现「New project」，与已批准词汇「Canvas，不叫 Project」冲突；③ 很长的 prompt 直接成为 history title，缺可扫描的名称策略）。**① 最新在前**：`getProjects`（`apps/web/lib/data.ts:58-63`）排序改 `pinnedAt desc nulls last` → `updatedAt desc`（原是 `createdAt asc`）；`Project.updatedAt` 是 Prisma `@updatedAt`，rename／pin-unpin／`editJson` 保存都会碰它，今天近似「最近活动」；**假设待下一轮**：单纯生成／对话（不改名不存 editJson）目前不触达它，`canvas-actions.ts`/`otto-canvas-turn.ts` 等画布写入路径不在本轮写集，touch `updatedAt` 排下一轮。**② 不叫 project**：单一源头新增 `apps/web/lib/canvas-title.ts`——`DEFAULT_CANVAS_NAME="New canvas"`／`LEGACY_DEFAULT_CANVAS_NAMES`（"New project"／"New campaign"／"Untitled Project"／"My First Project"）；`actions.ts` 的 `DEFAULT_PROJECT_NAMES` 改为从此文件组装（`actions.ts:111-118`），`getOrCreateDefaultProject` 的建号默认名改用 `DEFAULT_CANVAS_NAME`（`actions.ts:186`附近）；起步页 `CreateWorkspace.tsx` 用 `formatCanvasTitle`/`canvasDisplayName` 做**显示层**映射（不迁移数据）；`createProject` 自身的空名兜底与「New project」canonicalize 分支本轮**未改**（该路径无生产调用者传字面量，改动会牵动约 10 条既有 mock 测试且不在 Codex 指出的起步页范围内，风险/收益倒挂，登记留后）；存量行的数据库迁移登记为下一轮。**③ 长标题可扫描**：`canvas-title.ts` 的 `truncateCanvasTitle`——首行优先、在 56 字符上限内优先取第一句、否则按词边界截断加省略号、去首尾引号与空白；`CreateWorkspace.tsx` 历史行用 `formatCanvasTitle(project.name)` 显示、`title={canvasDisplayName(project.name)}` 放完整名做 tooltip；根因未动：`canvas-entry-actions.ts:22` 的 `canvasName()`（起步页发消息建 Canvas 时的存库名）与 `otto-canned-starters.ts:112` 的 `newThreadTitle()` 都只做 80 字符裸截断、无词边界，本轮按写集边界只做显示层截断，存库侧的截断策略统一是下一轮候选。**grep 结果**（`apps/web/app`、`apps/web/components` 商家可见文案里的 project/Project）：起步页/Canvas 历史范围内的已处理；范围外仍存在且**本轮未动**——`campaign-confirm-page.tsx`／`campaign-detail-page.tsx`（Campaign 分组「project」，独立域）、`TenantDetail.tsx`（admin 内部页 "Projects" 指标）、`QuickBrief.tsx`／`OttoMemory.tsx`／`BrandRecordRemovalDialog.tsx`（Otto Brief／Memory 文案）、`EditDesk.tsx`（剪辑桌内部变量名）、`otto-nav-model.ts`（导航模型 kind 值）。**测试**：新单测 `canvas-title.test.ts`（16 条，`FRONT-A15` 命名）、新真库测试 `get-projects-recent-first.test.ts`（2 条，`FRONT-A15` 命名，真 Postgres 验证排序）；`default-project-actions.test.ts` 随 ② 同步改了两条既有断言（建号默认名、reuse 查询的 `name: in` 列表）。**验收编号口径**：§2 冻结验收表没有专门对应「history 排序/命名/截断」这条；沿用 §7.1 表内已把「Create 起步页」全段钉在 `FRONT-A15`（工程演示，设计对照）的既有安排，测试名里带 `FRONT-A15`——不完全贴合 FRONT-A15 原文字面（原文是控件对照，不是排序/命名契约），本行即是这处贴合度的登记，供下次 S5 一并裁定要不要另开一条编号。**落地**：本行对应 PR（分支 `claude/create-history-recent-first`）。 | |
| 2026-09-04 | **Codex 只读走查 QA-CRE-FE9-005（P1，画布的 New conversation 造出找不回的对话）**：画布 dock header 上那颗 `New conversation` 按下去会清掉当前 thread 并把它从 URL 上摘掉，而**画布没有 thread list / switcher** —— 旧对话写得进、找不回，只能靠浏览器 Back（证据：`NorthstarCanvasWorkspace.tsx:202-206` 清 active thread／pending first／references 并 replace URL；`OttoChatStream.tsx:1003-1031` 画布里始终渲染这颗键；Codex 截图 `creation-e2e-fe9c70bd/10-new-conversation-no-history.png`，报告 `docs/audits/creation-e2e-fe9c70bd-2026-09-04.md` Stage 7）。本行属类型①中途想法，验收编号沿用现有 FRONT-A 表（FRONT-A15「画布控件与已批准设计一致」，不发明新编号） | **Founder 2026-09-04 07:05 裁决：beta 先收掉画布里的「New conversation」**，一张画布就是它那一条按时间的 Conversation；**多对话切换列表登记下一轮**（Codex 给的另一条出路——补齐 switcher／标题／排序／active state／deep link——不在本轮 4 小时内）。**落地（本 PR）**：只拿掉画布那一支的渲染与它的接线，不加任何开关（M4：用「不渲染」实现，不引入 `BETA_*`）——`OttoChatStream.tsx` 的 `canvasLayout` dock header 里那个动作块删除，`CanvasOttoOverlay.tsx` 与 `NorthstarCanvasWorkspace.tsx` 不再传 `onNewConversation`（不留传进去没人用的死参数）。**侧栏 Otto 面板照旧有这颗键**：那一面有自己的 `OttoThreadList`，旧对话找得回，所以收的是画布、不是把能力一刀切掉；服务端多 thread 能力与既有数据一格未动，已有的多对话仍可经深链打开。行为测试（验收编号逐字）：`apps/web/lib/__tests__/canvas-single-conversation.test.ts`（逐块读真源码：画布那一支没有这颗键／侧栏那一支必须还有／画布两处接线不再传这个 prop——把能力一刀切掉会让第二条当场变红）；真浏览器那一头由既有旅程守着（`e2e/journeys/14-canvas-toolbar-reachable.spec.ts` 直接操作这个 dock header，16／17 走画布）。**假设**：画布的 dock header 此刻只在有 active thread 时才渲染，所以没有现成旅程能种出「画布上没有这颗键」的浏览器断言；本轮用逐块源码判据＋既有旅程保绿代替，若下一轮补 switcher 时会连同种子一起补上 |
| 2026-09-04 | **Creation 冲刺收尾 · 前端基线侧登记两条**：① Otto 侧栏 `?otto=1` 近似读法（#1168）判官 P2-1：r3 `pendingSelectRef` 修法（开→关→再开，深链 select 不许被吞）现无测试守，待补；② 旅程 15 `FRONT-A4 — a customized Home survives a reload and a fresh browser`（`e2e/journeys/15-home-layout-persists.spec.ts:67`）在干净主干 5bdfdec6 本机连跑三次全红（Save 后空态句先出现、`page.reload()` 后消失），CI 上间歇绿——与 Creation 无关，值得单开一票查 Home 定制的跨刷新持久；③ FRONT-A15 住在 `frontend-baseline.md` §7 S2 施工稿（该节标「待 Founder 再次批准」），不在 §2 冻结验收表；#1105／#1168／#1178 都这样挂，待 S5 裁。 | 登记；待排期 |
| 2026-09-04 | **出片弹窗里打的字看不见（矩阵走查 P1-a）**。**现象**：画布底栏 → `Video` → 在提示词框打字，字不可见（DOM 里值确实存在）；矩阵 worker 在本机 3310 生产构建实测 `.mention-input .tiptap` 计算色 `rgb(246,247,249)`、弹窗底 `rgb(255,255,255)`，对比度约 1.05 比 1。**根因**：`MentionInput` 是**一个**组件挂在多个面上，字色却写死成 Vapor 深壳前景 `--fg-1`（`#f6f7f9` 近白，主干 `globals.css:364`；提示语同病，写死 `--fg-3`）。近白只在深色面上对，于是每个浅色面各自把字色补回来一次——画布底栏 `.gb .al-promptbar .tiptap`、素材面板 `.gb .cv-detail .tiptap`。t2v 弹窗把 `<MentionInput>` 裸挂在（Animate 弹窗用的是普通 `Input`，不受影响） `DialogContent` 上（`FlowCanvas.tsx` 的 `canvas-t2v-…`），既不是底栏也不是面板，没有人给它补——白底白字。**修法（单一源头）**：组件按所在面拿语义字色——`.mention-input .tiptap` 改 `var(--foreground)`、提示语改 `var(--muted-foreground)`；两处按面补丁随之删掉，原地留一句注释说明为什么不再抄第二份。深色主题免费跟上（`.dark .gb` 把同一个 token 重指到浅色字）。**未动并登记**：`.al-promptbar .tiptap`（`globals.css:340-356`）是既存死码——它和 `.mention-input .tiptap` 命中同一个元素、特异性相同又排在前面，连 `min-height` 与 `font-size` 早已被后者盖掉；本轮按最小改动不碰。**测试**：`apps/web/lib/__tests__/front-a14-canvas-alignment.test.ts` 新增三条 `FRONT-A14` 命名——① 字色与提示语必须是语义 token；② 按 `.gb` 两个主题的 token 字面量算真实对比度，弹窗面（`--popover`）上打的字与提示语都不低于 4.5 比 1（把 token 改回 `--fg-1` 当场算出 1.07 比 1 变红，与走查实测同一回事）；③ 禁止再出现按面覆盖的 `.tiptap` 字色补丁。**编号口径**：走 §2 冻结表 `FRONT-A14`（六面走查「视觉与交互与已批准的设计文档一致」，画布面）；Creation 引擎规格自己那一族验收编号不在本文件里出现——前缀全仓唯一（M2），本条是前端基线的面级视觉缺陷，不跨规格借编号。**落地**：本行对应 PR（分支 `claude/video-dialog-prompt-color`）。 | |
| 2026-09-04 | **Codex staging 只读审计两条 P2 小活**（`docs/audits/creation-staging-product-avatar-video-2026-09-04.md`）。① Version and evidence boundary：`/api/health` 不报 commit sha，审计结论没法绑定到具体部署——`build:{sha,ref}` 已加进响应，取自平台注入的 env（Railway 优先，Vercel 后备），本机/未注入即 `null`，绝不现取 git；worker 启动横幅同一对 helper 补 `sha=`。② LIB-STG-P2-005：Library 素材按钮的 accessible name 曾是整段生成提示词、有时重复两遍，读屏逐格朗读、语音控制失灵——单一源头 `lib/library-item-a11y.ts`（首句或 60 字符按词截断＋去重＋媒体类型），`CanvasLibraryPicker.tsx` 与主网格 `StuffLibrary.tsx` 共用；整段提示词仍经 `title` 属性保留，视觉 caption 未改。均属轻改（无商家可见价格/权限/数据变化）。**落地**：本行对应 PR（分支 `claude/health-sha-and-a11y-names`）。 | |
| 2026-09-04 | **旅程 15 `FRONT-A4` 的红是旅程自己的等待写错了，不是 Home 定制没落库**（本文件 §5 同日「Creation 冲刺收尾」行第②条登记的那一票，本行是它的落地）。**定性**：③ 测试时序，商家可见行为零变化。**证据**：本机干净主干复现一次红（`fikirtive_home15_test`，端口 3474，CI 同配方生产构建），红时数据库 `OrgHomeLayout` 里那一行**已经写进去了**（`componentIds={}`、`hiddenIds={marketing-health}`），所以写入路径是好的；Playwright trace 里 `/` 上有两个不同的 server action，Save 那一个（`next-action` 尾号 `…53ca5cbe3d`）状态是 **-1 = 被中断**——点 Save 在 9628ms、`page.reload()` 在 9667.8ms，中间只有 39ms，旅程自己把这次保存打断了。**根因**：`MarketingHomeView.tsx` 主区渲染的是 `customizing ? draft : components`，面板还开着时屏幕上是**草稿**；所以「Save 之后空态句可见」在取消勾选那一瞬间就已经为真，`saveHomeLayout` 就算什么都不做它也过——这两条断言既没有验证保存，也没有拦住旅程去刷新。行落库比刷新晚一步，刷新读到的是保存前的版面，旅程把这件事读成了持久化缺陷。**修法**：只改旅程的等待方式，不动产品代码——两处 Save 之后各加一句「等 Customize home 面板消失」（`saveDraft` 是先 `await saveHomeLayout(...)` 再 `setCustomizing(false)`，面板消失＝服务端已经返回 ok＝行已落库；不加 sleep、不加固定超时）。**变异验证**：临时让 `writeHomeLayout` 不写，重建生产构建后旅程照样红，而且红得更早更准（第 78 行，数据库 0 行）——改完的旅程仍然抓得住真的持久化坏掉。**为什么以前 CI 绿本机红**：这是一场竞速，谁快谁赢；CI 跑手空闲时保存赶在刷新前落库，本机四个 agent 共用、负载 10 以上时赶不上。CI 今天红在第 87 行（第二次 Save 之后那次刷新）是同一个根因的第二处，两处一起修。**落地**：本行对应 PR（分支 `claude/home-layout-persist`），写集只有旅程文件与本行 | |
| 2026-09-04 | **画布首屏摆板改读同一份覆盖层安全区（三处同源修补）**——旅程 17 `FRONT-A15` 间歇红的根因。**① 两份留白**：修前 `<ReactFlow fitView fitViewOptions={{ padding: 0.22 }}>` 在挂载时按对称百分比摆一次，「每个项目摆一次」的 effect 再按 #1172 的安全区（`lib/canvas-fit-padding.ts`）摆第二次，谁最后落地取决于时序；修后挂载那一份删除，摆位只剩 `fitPadding()` 一个来源。**② 安全区没算卡自己的操作条**：`canvasFitPadding` 顶边在让开覆盖层之外再空出 `CANVAS_NODE_TOOLBAR_REACH`（卡上操作条 offset 22 + 高 32 = 54px；三张卡的 `NodeToolbar offset` 改为从这一处读，高度来源 `icon-xs`=`size-8`=32px）。**③ 安全区看不见门厅那张 Otto 卡**：画布左上角那张 280px 的 Otto 卡有两副面孔——还没开对话是 `OttoFrontDoor`、开了对话是 `OttoTurnCard`，而覆盖层清单只认后者的 `aria-label="Otto current turn"`，于是**商家还没开口的那一次**左边一寸不让；修后两副面孔各挂同一个记号 `CANVAS_OTTO_CORNER_ATTR`（`data-canvas-otto-corner`，与底部输入框 `CANVAS_OTTO_DOCK_ATTR` 同一条规矩），清单改读记号。**④ 首屏那一次不再做动画**（`fitBoard(0)`；手动「Fit to screen」仍是 220ms）：开画布的第一眼没有「从哪里来」可言，而那 160ms 里板上的卡还在移动，手已经伸出去的那一下会落空——探针实测同一次拖动里卡从 x=316 挪到 x=368，框选一张都没圈到。**复现数据**（本机生产构建 1440×900，e2e 探针）：顶栏 y=0…48、画板 y=48…900、门厅卡 x=16…296 / y=64…172；修前顶边只留 24px，最上排卡摆在 y=72、x=256，卡上操作条落在 y=18…50 全在画板外，`elementFromPoint` 在 Download 键正中取到 `<header class="… h-12 …">`，而在卡外 24px 起手框选取到的是门厅卡的表头；修前 5 次留了记录的预跑全红（3 次第⑤步、2 次第①步）。修后顶边 78px、左边 320px，最上排卡 y=126 / x=316，操作条 y=72…104 全在画板内、命中的是按钮本身；旅程 17 在合过主干的生产构建上连跑 10 次全绿（修 ①②③ 之后仍有 1/5 红在第①步，加 ④ 才收干净）。**商家可见变化**：首屏与「Fit to screen」摆出来的画整体下移 54px、右移到门厅卡之外，画略小；左下右三边的既有让位口径不变。**本轮不做（登记）**：卡被商家自己拖到画板顶上时，它的操作条一样会伸出画板被顶栏盖住——那要操作条自己翻到卡下面，不在本票。**落地**：本行对应 PR（分支 `claude/canvas-first-fit-safe-area`）；围栏 `apps/web/lib/__tests__/front-a15-canvas-selection.test.ts` 新增四条（首屏与手动读同一份留白／挂载处不再自带第二份留白／操作条不伸出画板／两副面孔都挂着记号），变异实证：把 `padding: 0.22` 加回挂载处、或把操作条高度改成 0，对应条即红。 | |
| 2026-09-04 | **Codex 全 beta 审计 P1-012（发布身份）**：staging 没有不可变的发布标识，工程师没法证明一次修复到底在哪次部署上验的（本条来自派工书转述——本 worktree 的 `docs/audits/` 下没找到对应审计文件，取证止于此，不假造路径）。上一条（同日、上一行）的 `/api/health` `build:{sha,ref}` 只报了 web 这一侧；本行**新增只读端点 `/api/build-info`**——`web:{sha,ref,startedAt}`（复用 `lib/health.ts` 的 `buildInfo`，同一对 `commitShaFrom`/`shortSha`，不另起一套）、`worker:[{role,sha,at}]`（每一班还活着的 `WorkerHeartbeat` 各一行，**不带** `configFingerprint`——那一格的比对纪律留在鉴权后的 `lib/deploy-fingerprint.ts` admin 面）、`migrations:{latest,appliedAt}`（`_prisma_migrations` 最新一条成功记录的迁移**id**，只取时间戳前缀，人写的描述后缀不外泄，读不到就 `null`）；匿名可读，但**不是** `/api/health` 那份「只报状态词、不报时间戳」的零数据契约——本端点如实报三个时间戳（`web.startedAt`/`worker[].at`/`migrations.appliedAt`），因为这几个时间戳本身就是「哪次部署」这句话要核对的东西（`lib/auth-wall-ledger.ts` 新增 `api/build-info` 精确豁免，`proxy.ts` 的 matcher 字面量同步再生成）。账号菜单（`design-system/patterns/application-shell/navigation/MerchantAccountMenu.tsx`，`components/navigation` 是它的 symlink 别名）底部加一行紧凑版本号 `Build <8 位 sha>`（本机无 sha 显示 `Build local`），点击复制 `/api/build-info` 的完整链接，该项 `aria-label` 单独描述动作（复制链接），可见文案不变；sha 由服务端 `buildInfo(process.env)` 经 `getMyAccount()` 既有的顺风车下传，菜单自己不发请求，不为一个 env 读取白付两次 DB 查询；纯读、不改导航结构，属 FRONT-A14「六面走查」覆盖的外壳一部分。判官四轮已裁：P1-1（时间戳/契约措辞/迁移 id 脱敏）、P2-2（aria-label）、P2-3（去客户端 fetch）均已落修，此处登记为最终状态；PR 描述已去掉「轻改」勾选句（账号菜单多了一行可见文案+点击 toast，非零商家可见行为变化）。**落地**：本行对应 PR（分支 `claude/build-info-release-identity`）。 | |
| 2026-09-04 | **Library 换壳三条（PR #1152）**：① **失败生成任务卡随旧 OttoStuff 退场**——已批准 pattern 的 LibraryAsset 无 status 字段、Generation history 只收成品；失败与「你没被扣费」仍在出事的画布卡上持久可见（`packages/core/src/canvas-card-status.ts`），但跨画布的失败总表没有了，待 Founder 裁：保持现状 vs 给 Library 补「Needs attention」入口。② **Elements 新增设计未画的 Brand marks 栏**（BRANDMARK 可创建，不画就永远看不见也删不掉），待 FRONT-A14 裁长期归属。③ **官方演员「不可删」是本仓自认假设**（Founder 2026-08-30 只写不能修改 identity），`lib/actions.ts:687-693` fail closed，待追认。④ 旧 OttoStuff 面的八个围栏测试改为只护组件，tidy 下一循环。另：搜索同时匹配上传文件名（上传行没有提示词可搜），占位符随之改成如实的「Search prompts or file names」（Founder 2026-09-05 裁，轻改）。 |  |
| 2026-09-04 | **侧栏 Otto 串到无关画布对话（Codex 全 beta 审计 P1-010）**。**现象**：商家在 `/billing` 展开侧栏 Otto，面板自动摊开的是一条画布对话「Professional Male Model Image」——他不是在这里开的、与这一页无关，而且面板上一个字都没写这段对话属于别处。**根因**：面板「打开时接着聊哪一条」的判据是「这个 project 里最近更新的那一条」（`apps/web/lib/otto-panel-seed.ts` 的 `openThreadId`），而那个 project 来自 `getOrCreateDefaultProject()` —— 与商家从哪一页展开面板毫无关系；画布对话与侧栏对话从来没有被分开登记过，面板没有依据只续自己那一批。**规则（本轮落地）**：① 开始写 `ChatThread.surface`（#879 step 1 预埋的列，nullable，此前零写入方，**不加迁移**）——画布那一侧开的登记 `canvas`（Create 画布入口、剪辑入口、`/otto` 前门），侧栏面板自己开的登记 `panel`；来源一律过服务端一道闸（`coerceThreadSurface`），认不出来的落回 `canvas`，客户端自报不原样落库。② 面板无深链打开时**只在 `surface='panel'` 的对话里**选最近一条；一条都没有就不预选，面板画新对话态，商家发出第一句时才建线程并登记 `panel`。深链 `?thread=` 照旧（商家自己点名的到达，画布对话经它打开不受影响）；画布对话只在从会话列表显式点选时打开。③ 面板头部接回一行**范围**标签：`Workspace · <页面名>` / `Canvas · <画布名>`。措辞守判官 r1 [P2] 的裁定——只说这段对话归谁，不说 Otto 看得见这一页（服务端至今没有任何读者读 `surface`/`subjectRef`），所以不写「On this page:」、不摆「停止使用本页作为上下文」那颗叉、不设 `contextAttached`；会话列表里画布那几条带 `Canvas` 来源标签。④ **老线程诚实登记**：本轮之前的每一行 `surface` 都是 `null`，无法回溯它当初从哪个门开的，一律按画布读——面板不再自动续它们（代价：商家要去列表里点一下；换来的是面板不再摊开别处的上下文）。**测试**（逐字含 `FRONT-A14`）：`otto-panel-seed.test.ts`（选择规则四条＋既有深链契约全绿）、`otto-panel-scope.test.ts`（挂载：/billing 打开不载画布对话、范围标签两态、反向钉「不许出现 On this page」、列表来源标签）、`otto-thread-surface.test.ts`（真库 `fikirtive_panelctx_test`：`surface` 真写真读、老行为 `null`、两租户不互见、服务端校验闸）。变异：把选线程规则改回「project 最近一条」⇒ `otto-panel-seed.test.ts` 6 条红；把 `data.ts` 的 `surface: true` 拿掉 ⇒ 真库那条红。**未做**：`ChatMessage.surface` 的语义（#879 step 2 的上下文装配）不在本轮；没有 workspace 级线程这个新概念，线程仍按 project 分组。**判官复审落修（同一轮）**：⑤ **点开即被改标**（P1-1）：`getCoworkThreadPage`（点开一条对话走的读路）没取 `surface`，取回来顶掉列表里正确的行 ⇒ 商家点一下自己的工作区对话就被标成 Canvas。补 select，并把 `dto.ts` 的 `ChatThreadDTOInput.surface` 从可选改必填——漏 select 从此是 tsc 错误（改必填当场点名三条读路），不是线上现象。⑥ **老行不冒充画布**（P2-1）：新增 `isCanvasThread`，界面上说出口的话（列表徽章、头部）只标确知是画布的；`null` 老行来路无法回溯，什么都不标；自动续接仍用 `isPanelThread`，行为零变化。⑦ **turn 接口不再兼任线程来源**（P2-2）：`coworkTurnRequest.surface` 是 #879 step 1 的**页面位置**字段（自测值 `campaign`），与线程来源只是重名；两扇 turn 门改为写死 `canvas`（面板永远先经 `createEmptyCoworkThread` 建线程），`ChatMessage.surface` 的原始值写入不动。⑧ **镜像同修**（P2-3）：画布 `selectImmersiveThread` 也按 project 取最新一条、不看来源——商家在侧栏聊完再开 Create，画布续的是侧栏对话。加 `!isPanelThread` 过滤（排的是「确知是面板的」，老行照旧被续，零降级）。⑨ **头部横条改成只在带新信息时画**（P2-4）：只有打开的是确知的画布对话才写 `Canvas · <画布名>`；工作区对话与老行一行都不画（`panelScopeLabel` 零调用者，删除）。**待 Founder 裁**：常驻 `Workspace · <页面名>` 横条要不要，请在 FRONT-A14 走查时定；接回来时 `panelContextSubject` 与围栏都还在。⑩ 参照页 `ApplicationShellReference` 改用同一个 formatter、不再传 `contextAttached`；「 · 」分隔符归并到 `panel-page.ts` 的 `formatPanelScope` 一处。 | 登记；本轮已落地，待 S5 |
| 2026-09-05 | **⑨ Canvas／Create 按设计接线第二刀**（第一刀＝覆盖层与工具条让位 PR #1144，第三批＝节点五图标／composer 两项／280px 历史 PR #1150／#1151，均已在主干）。**本刀做了两件**：① **卡片页脚补上「版本」栏** —— 版本＝这张卡在它那次付费按下里的 1-based 位置（`batchIndex + 1`，经 `canvasRecordedFacts` 读，`components/canvas/nodes/CanvasNodeFooter.tsx` 的 `canvasCardVersion`）（夹具那一侧的编号规则不是这一套，见下方差异 ⑦）；**不新增任何字段**，排队中的卡不说版本、删掉同批另一张也不会让这张改号（#603 T4 关掉的自造身份类），没记录就不画。第一刀把这一栏记成「没有诚实来源」，本行取代那条登记。同时卡名改走单一命名源 `apps/web/lib/canvas-title.ts` 的 `truncateCanvasTitle`，与 Canvas history 行断在同一个词边界，完整名仍在 `title=`。② **设计对照测试收成集合比对** —— 新 `apps/web/lib/__tests__/front-a15-design-parity.test.ts`（22 条，测试名逐字带 FRONT-A15）把两边的控件**整套读出来比集合**：夹具那一侧从已批准的 `CanvasReference.tsx` 抽 `aria-label`，**生产那一侧挂真 `FlowCanvas` ＋ 真 `ImageNode`／`VideoNode` 渲染出来再读 DOM**（`.cv-node-toolbar` 里 `button, a, input, [role=button]` 的可及名）。判官 #1194 P1-2 证过先前那版不成立：它用源码正则扫裸 `label="…"`，往 `ImageNode.tsx:207` 无条件插一颗 `aria-label="Publish to Meta"` 的真按钮，19 条照样全绿。改成 DOM 集合比对后四种变异当场红（本刀逐条实跑）：插那颗真按钮红、把 `Download` 改名红、真删掉一颗红、把版本序号 +1 改成 +2 红。既有的 FRONT-A14 各条是逐颗点名、对「多长出一颗没人写过断言的键」不敏感。**这份文件的边界**（写明白，免得被当成比它实际更宽的保证）：节点操作条＝DOM 集合比对；composer 的「+ Add context」与裁决六那三颗**仍是源码判据**——前者要挂整个 `OttoChatStream`（连着两条常驻钱披露），不在本刀写集内，后者的 DOM 那一半由同一份文件里挂真板子的那一条与旅程 17 守着。起步页那半边加进既有 `create-design-parity.test.tsx`。**本刀未做、连同理由**：（甲）**起步页「+ Add context」仍不渲染** —— 把引用带进画布要改 `apps/web/lib/canvas-entry-actions.ts` 的 handoff payload，该文件不在 §7.4 第⑨段写集内，且正被线程来源那一刀改、类型化引用形状要等引用选择器段合入；按裁决九「无契约的控件不出现」维持不渲染，施工内容与工程量见 §7.3「⑨ 下一刀 · 起步页参考契约」，依赖解除后即为下一刀。（乙）**composer 的计费说明没有改成按需披露** —— 此点**裁决十三（2026-09-03，本节同日那一行）已裁：维持常驻**，§7.1 ⑨「按需披露」那一句作废；本刀据此**原样不动**，不是待裁项（先前本行把它当未裁重新呈上、还给了已被否决的那个选项，判官 #1194 P1-1）。§7.1 ⑨ 那一行仍留着作废原文，待下次 §7 修订时删；主干那条围栏测试（`front-a14-canvas-alignment.test.ts`「钱披露不因对齐设计而缩水」）本刀一字未动。**与夹具的差异（逐条）**：① 卡不是钉死的 4:5 方框，而是按真实媒体比例收（`canvasMediaNodeSize`）——一张 4:5 的出片正好落成夹具那个形状，一段 16:9 的视频不被硬塞成竖版，测试里两个方向都钉住了；② 上传那一项文案是「Upload image or video」而不是夹具的「Upload image」，因为这个选择器真的也收视频；③ 夹具 ⋯ 菜单的 Share selected output 与 Duplicate 不出现（rule ①，无服务端动作）；④ 夹具的 Add URL 不出现（同上，全仓唯一按网址导入是 Otto 自己一轮里调的工具）；⑤ 对话历史的宽度与最大高度已按夹具（280px／260px，实测截图 `qa-i1/03`），但行内仍是聊天气泡、不是夹具那种「一行一轮：状态点＋提示词＋状态词＋一行回话」的紧凑行——改这个要动 `components/otto/OttoChatStream.tsx` 的消息渲染，紧挨着两条常驻钱披露，且不在第⑨段写集的四个目录内，本刀不动、登记待裁；⑥ 夹具没有的空态与错误态用设计系统自己的原语呈现（生产必需）；⑦ **版本号的语义两边分叉**——夹具 `CanvasReference.tsx:513` 的 `version` 是**血统累加计数**（`source ? source.version + index + 1 : index + 1`：拿一张 v2 的卡再按一次，新出的四张是 v3／v4／v5），生产的 `batchIndex + 1` 是**这一次按下里的批次序号**（同一块板上第二次按下，出来的仍是 v1…v4）。取生产这一版，是因为它是服务器当场记下的、删掉同批另一张也不会让幸存者改号（#603 T4 关掉的自造身份类），而夹具那种累加要一条卡与卡之间的代数链，今天 `lineage` 记的是来源、不记代数。登记待裁：要不要改成血统代数。**落地**：本行对应 PR（分支 `claude/canvas-create-design-wiring-2`） | |
| 2026-09-04 | **Brand 的「预览效果」偏离裁决四，待 Founder 追认**。裁决四（本节 2026-09-03 行与 §7.3④「草稿流动作链」）要的 Preview effect 是**调模型出样例文案**，并在点之前把那笔小额成本按钱引擎的计费口径披露。本 PR 的 `previewBrandContextEffect`（`apps/web/lib/memory-actions.ts:384`）换成了**免费、不调模型**的另一件事：把这条草稿保存前后 Otto 真读到的两段品牌上下文原文并排摆出来。理由——调这一次模型要花的钱，今天在集中配置里没有单一权威价目；没有价目就做不到「披露先于扣费」，那就不该有这颗会扣钱的按钮。要回到设计原样，须先给这次模型调用定价（另立规格），本行是这处偏离的登记。**同批登记的一处已知不准**：`authorLabel` 先认 `updatedById` 再问 `source`，所以人工先建、Otto 后改的行会指名一个没动过它的人；根治要统一 `packages/otto` 的写路径，下一循环。 | |
| 2026-09-04 | **`/brand/records` 过渡页的去留，待 Founder 裁**。已批准的五节设计里没有这一页；本 PR 保留 `apps/web/app/brand/records/page.tsx` 并在 Knowledge base 与 Audiences 顶上留一行指路，因为换成五节之前它是产品／优惠／客群**唯一**的结构化编辑入口，而五节页面只列出这些记录、可删除与恢复，没有编辑器。两条路请 Founder 择一——㈠现在就删（商家在 Library 接手之前无处编辑结构化记录）；㈡留着，等第②段 Library 接过产品管理时连同那行链接一起删。组件本身一行未改，导航里没有它，只能从五节页面点进去。 | |
| 2026-09-04 | **Settings ／ Billing ／ Profile 换皮（第⑦段 FRONT-A11）两类登记**。**甲 · 本段三处有意偏离夹具**（都按已冻结 pattern 的规矩呈现，不自创长相，登记请 Founder 在 FRONT-A14 走查时过目）：① `/billing` 账单说明句按夹具裁短；② `Spend history` 夹具里没有这一节 —— 生产已有真的流水表，用同一套 section 词汇呈现，不再放第二个入口；③ `/profile` 的**删除账号**夹具里也没有 —— 生产必需，画成 plain row（标签＋一句影响＋右侧 destructive 按钮），行为与文案一字未动。**乙 · 既存响应式缺陷三条，登记为下一张 Otto 段票，本 PR 不修**（内容都住在本段禁改的 `components/otto/**`）：① `/settings/connections` 在 1440 档「Retry status」按钮出框约 85px、1100 档「Services」那一列整体出框约 69px；② `/billing` 的「Set a cap」只在 1024 档出框约 20px，1440 与 1100 两个评审档都干净；③ 同一根因下，Otto 面板打开时 1366 档也出框约 38px。**根因**：`components/otto/settings/SettingsPage.tsx:160` 那个 `max-w-md`（448px）配上 `Field orientation="responsive"`，容器查询在 @md=448px 处转行，而这个盒子的宽度由 Settings 内容列决定、不由视口决定 —— 所以窄档与面板打开时都会挤出去。本段只把左轨从 232px 收到 220px（内容列因此宽了 12px），未与主干起服务器对照，故不主张这三条是本段引入的回归。**触发条件**：Otto 段接手 Connections inspector 换皮时一并修。 |  |
| 2026-09-05 | **登记轮 · 两条只写在代码注释里的裁决，回写本表**（触发＝2026-09-05 接线总盘点第 10 项；`docs/specs/` 全文查无对应条目，批准记录只活在源码注释与围栏测试里）。① **卡片 `⋯` 菜单里的 Share selected output 与 Duplicate 不渲染**：已批准的画布 pattern 的 `⋯` 列的是 Share selected output ／ Duplicate ／ Remove from canvas 三项，生产只画得出 Remove from canvas（外加板子自己的 Show what this card came from ／ Open image details）；注释逐字写「Founder 2026-09-03 rule ①」，理由是那两项没有生产契约——全仓唯一的分享链接 `sharePostPreview` 铸的 token 绑在 ScheduledPost 上（`apps/web/lib/schedule-actions.ts`），画布卡是 Generation、没有 post，Schedule 本身还在 #850 beta 门后；也没有任何动作复制得了一个画布节点。出处 `apps/web/components/canvas/nodes/CanvasNodeMoreMenu.tsx:1-16`，围栏 `apps/web/lib/__tests__/front-a14-canvas-alignment.test.ts:175`（FRONT-A14 命名）。② **画布 composer 的 Add URL 不渲染**：同一句「Founder 2026-09-03 rule ①」；全仓唯一的按网址导入是 `ctx.mediaImport.fromUrl`（`apps/web/lib/otto-media-port.ts`），那是 Otto 自己一轮里调的工具，没有 composer 能调的 server action。出处 `apps/web/components/otto/OttoChatStream.tsx:1884-1896`，围栏同一测试文件 `:200`。**待裁点**：注释里的「rule ①」这个编号在 `docs/specs/` 查无出处（2026-09-05 全仓 grep 只命中这两处源码与那一份测试）；本表 :91 已冻结的裁决九正文「无契约的控件不出现」与它口径一致，但编号对不上——请 Founder 追认「rule ①」＝裁决九，或给它一个正式出处。**不重复登记**：画布不渲染 New conversation 那一条已在本表 2026-09-04 行（QA-CRE-FE9-005，Founder 07:05 裁决），本行不再写第二遍 | 登记；待 Founder 追认编号对应 |
| 2026-09-05 | **登记轮 · 评审夹具 `/product-patterns/create` 与 `/product-patterns/auth` 按 FRONT-A12 生产不可达、本轮不接线**（触发＝2026-09-05 接线总盘点把这两条判成 real-gap，汇总时却没有任何任务承接、静默消失；盘点的复核员点名要求补一句登记）。**事实**：两页的组件第一句都调 `assertReviewFixtureRoute()`（`apps/web/app/product-patterns/create/page.tsx:15`、`apps/web/app/product-patterns/auth/page.tsx:16`），守卫在 `process.env.NODE_ENV === "production"` 时 `notFound()`（`apps/web/lib/review-fixture-guard.ts:12-13`）——与 home ／ brand ／ shell 三面对同一段守卫的已核裁定同形，同一条裁决同一段代码，不另立任务。**处置**：与 `/product-patterns/*` 十页、`/design-system/*` 三页同一条口径停放，不单独接线；FRONT-A12 的「用生产构建实访返回 404」这一步至今仍是静态推断，未跑生产构建实测——留给 S5 的工程演示 | 登记；随 FRONT-A12 一起验 |
| 2026-09-05 | **登记轮 · 外壳导轨两条生产不可达的分支，只登记不删**（§3 最小改动；触发＝2026-09-05 接线总盘点 shell 面）。① **分组下拉 `RailGroup`**（`apps/web/components/navigation/rail/NavigationRail.tsx:206-259`）：导轨的权威数据 `MERCHANT_NAV`（`packages/core/src/navigation.ts:176` 起）今天一个分组节点都没有（2026-09-05 复核：全文只有类型定义 `:58` 那一处 `items:`），所以这一支永远不执行——它是裁决③④把 CRM 整段收起之后留下的形状，恢复触发＝Meta verification，已登记在延期台账 issue #359。② **Preview 徽章与折叠态那颗圆点**（`NavigationRail.tsx:179-194`）：`MerchantNavLink.preview` 在类型上存在（`packages/core/src/navigation.ts:51`），但权威数据里零个链接设它（2026-09-05 复核：`preview:` 在该文件出现 0 次），两条分支同样不可达。**同轮更正一条误报**：盘点 shell 面报「`design-system/patterns/application-shell/navigation/` 五个文件与生产文件逐字节全等＝同一份真相的两个抄本，改生产文件没有机器闸提醒同步」——复核为**假警报**：`apps/web/components/navigation` 是**符号链接**（`ls -la apps/web/components` 显示 `navigation -> ../design-system/patterns/application-shell/navigation`；`apps/web/components/otto/panel` 同理指向 `design-system/patterns/otto-panel`），只有一份文件、一个 inode，改一处就是改那一份。**本行不把它登记为漂移**，写在这里是为了防止下一轮有人照那份盘点再报一次 | 登记；不删不改 |
| 2026-09-05 | **登记轮 · Home 上一代组件疑似死码，只登记不删**（§3：不是本轮改出来的，报告不动手）。`apps/web/components/home/HomeView.tsx` 与 `apps/web/components/home/home-data.ts` 全仓零生产 importer——2026-09-05 grep 的全部命中是：`HomeView.tsx:34` 自己 import `home-data`；`apps/web/lib/my-date-format.ts:20` 与 `apps/web/lib/__tests__/project-campaign-vocabulary.test.ts:15` 的注释提到它；`apps/web/lib/__tests__/otto-pronoun-consistency.test.ts:261` 与 `:582-583` 把 `home-data.ts` 当**文件**读进来当文案正例。也就是说：生产渲染路径（`HomeEntry` → `MarketingHomeView`）一行都不碰它们，但直接删 `home-data.ts` 会让那条文案围栏红。**处置**：登记待 tidy——删之前先给那条围栏换正例；本轮不动手 | 登记；待 tidy |
| 2026-09-05 | **登记轮 · 仓库里已经有「这一页有没有进行中的对话」的真信号，但前端一个消费者都没有**（触发＝2026-09-05 接线总盘点的复核清单第 2 项）。**事实**：`apps/web/app/api/otto/thread-activity/route.ts` 是真路由、读真库（`apps/web/lib/thread-activity.ts` 查 GenJob 的 QUEUED ／ GENERATING 与 pending 的 CanvasNode，owner ＋ project 双闸），有自己的路由测试与 `apps/web/lib/__tests__/thread-activity.test.ts`，也在鉴权 matcher 的围栏里（`apps/web/lib/__tests__/proxy.test.ts:463`）。但 2026-09-05 全仓 grep `thread-activity`：除了这条路由自己、它的两份测试与那条 matcher 断言之外，**没有任何客户端代码调它**。**为什么要紧**：本表 2026-09-04 行「Otto 侧栏默认改为收起」里那句「这一页有活动对话就展开」，今天是用 `?otto=1` 深链**近似**掉的，Founder 2026-09-04 追认了近似口径——追认那一刻，这条已经存在的真信号没有上桌。**另有一处口径矛盾**：`apps/web/lib/balance-refresh.ts:14-15` 的注释仍写着「#544 already flagged the existing 4s thread-activity poll as one too many」，`apps/web/lib/__tests__/balance-refresh.test.ts:8-9` 复述同一句（两处都是登记当时的行号与原文；那两段注释已在本节同日 L10 落地行里整段改写，矛盾已消）——「现存的 4 秒轮询」与「无人调用」不可能同时为真，其中一句是过期事实。**处置**：本轮派工书把它列为待做 **L10**（把侧栏展开条件从深链近似换成这条真信号），同票顺手订正那两处注释；本轮只登记，不动代码 | 登记；派工书列为 L10 |
| 2026-09-05 | **登记轮 · 旧日历重定向的目标地址与权威表不一致**（触发＝2026-09-05 接线总盘点 shell 面；行号经本轮复核订正）。**事实**：权威重定向表写 `/campaign/calendar` → `SHELL_ROUTES.home`（`packages/core/src/navigation.ts:289-291`，理由栏逐字写「returns to the active Home surface」），而真正的路由文件跳的是 `SHELL_ROUTES.schedule`（`apps/web/app/campaign/calendar/page.tsx:19`，注释里的理由是 W2-11「排期才是那本真日历」）。**商家侧今天看不出差异**：`apps/web/app/campaign/layout.tsx` 的重定向抢在前面，这一页的 `redirect()` 实际跑不到；但那张表是围栏的枚举源（`packages/core/src/navigation.ts:285` 注释逐字：每一条 `from` 都必须有一个真的 route 文件把人送到 `to`），两处对不上就是围栏在替一句假话背书。**处置**：本轮派工书把它列为待做 **L6**（外壳「话与真相不符」收口，与下一行同票）；两处哪一边才是权威由 Founder 定，本 worker 不擅改任何一边 | 登记；派工书列为 L6 |
| 2026-09-05 | **登记轮 · Otto 自己读的界面地图说「每一页都够得着」，而有两面根本不挂面板**（触发＝2026-09-05 接线总盘点 shell 面与 otto-panel 面同时报到；行号经本轮复核订正）。**事实**：地图那一行逐字是 `Reachable on every page, or with Cmd/Ctrl+J.`（`packages/core/src/navigation.ts:509`；助手自述 `OTTO_ASSISTANT.does` 也写「Otto sits on the right of every page」，`:157`）。而 `/create` 与 `/create/canvas` 两面按 `apps/web/design-system/patterns/otto-panel/panel-surface.ts:22-36` 刻意不挂面板，理由是「这一面自己已经有一个 Otto」——`/create` 那一条还写明了 2026-09-04 走查 P1-8 的根据（清空浏览器存储的全新会话里，屏幕上并排两个 Otto 入口）。所以 Otto 会对商家复述一句它自己表面上做不到的话；顶栏的 Ask Otto 与 Cmd/Ctrl+J 在这两面同样缺席。**处置**：改地图那句话、还是改挂载范围，是产品口径题，本 worker 不判；本轮派工书把它列为待做 **L6**（与上一行同票收口）。本轮只登记 | 登记；派工书列为 L6 |
| 2026-09-05 | **登记轮 · 本场判官与走查剩下的四条 P2（前端基线侧，本轮不做）**。① **判官 #1180 P2-1**（评论 5534795997）：`apps/web/app/globals.css:367` 的注释写「what every surface under `.gb` declares」，措辞偏差——`--foreground` 只由 `.gb` 的两个主题根声明（`:502` 与 `:629`），不是「每个面各自声明」；未改。② **判官 #1180 P2-3**：那一票双方的真浏览器核验都是 CSS 级联镜像页，不是整 app 走查；下次矩阵在真 app 的画布出片弹窗上对文字对比度复量一次收口。③ **判官 #1185 P2**（评论 5537276609）：两面素材的可读名口径不完全一致——一面用 `conciseAssetTitle`（无类型后缀，靠 Choose ／ Open ／ Actions 前缀区分），另一面用 `libraryItemAccessibleName`（带类型后缀）；规则源头已经是同一份（`apps/web/lib/library-item-a11y.ts`），差的是要不要统一后缀策略。**本轮复核（在 PR #1152 合入之后）**：判官当时点的「素材库网格」那一面已经换成 `apps/web/components/library/LibraryView.tsx:337`，用的是带后缀的 `libraryItemAccessibleName`，与画布的 Choose from Library 半屏（`apps/web/components/canvas/CanvasLibraryPicker.tsx:145`）一致；仍读无后缀 `conciseAssetTitle` 的是 `apps/web/components/otto/stuff/StuffLibrary.tsx:112`，它今天的生产挂载点是 Brand memory 的产品配图选择弹窗（`apps/web/components/otto/memory/ProductImagePickerDialog.tsx:83`）。所以口径差没有消失，只是换了一面——登记这一版事实，别照判官当时的面名去改。④ **矩阵走查（2026-09-04 09:45，本机 3310 生产构建）画布一条**：生成刚落卡的那一瞬间图片卡短暂点不中（react-flow 节点残留 `visibility:hidden`），稍后自行恢复。**同轮订正一条结论**：矩阵门 4 记的 ❌ 是**假阴性**——多选证伪 worker（2026-09-04 10:15，主干 8b3318ef）实测 Claude Browser 的修饰键点击不发真 keydown，而 React Flow 12.11 只认 window 上真实的 Shift keydown；macOS 上 Ctrl ＋ 点击是右键；空白拖拽＝手型平移、Select 工具框选正常（裁决六）。同一次实测留下的窄条件缺陷：Meta ＋ 点击在上一次点击落在 Otto 输入框之后失效，Shift 不受影响 | 登记；待排期 |
| 2026-09-05 | **第八型 `brandmark` 的去留**（判官把它从 PR #1158 描述搬进规格,原本只活在 PR 里）。已批准夹具的类型表只有七型（`apps/web/design-system/patterns/reference-picker/model.ts`）,而生产的 `EntityType` 在这份契约之前就有 `BRANDMARK`,那些元素今天能被 `@` 到。第③段①②刀的取舍是**保留它可被搜到、但不给它分类入口**（`packages/core/src/reference-ref.ts` 的 `REFERENCE_TYPES`;分类表在 `apps/web/components/reference-picker/useReferencePicker.ts` 只有 Products／Characters／Official avatars／Locations 四格）。请裁:补进契约,还是退役这一型。 |  |
| 2026-09-05 | **裸 `@` 的「最近」是对象自己的新旧,不是使用日志**（同上,原本只活在 PR #1158 描述里）。契约 §2 写「最近使用的 reference」,生产没有按引用记的使用日志,所以排的是元素的 `updatedAt` 与生成的 `createdAt`（`apps/web/lib/reference-search.ts` 的 `recentReferences`）。菜单副标题因此写 `Recently updated in your workspace` 而不是夹具的 `Recently used…`——照抄夹具就是对商家说一句假话。真正的「最近用过」要等第③刀把类型化引用 ID 落进消息之后才有数据。请裁:接受当前口径,还是等第③刀后改。 |  |
| 2026-09-05 | **别名搜索两套口径**（同上）。服务端只按名字匹配（`apps/web/lib/reference-search.ts` 的 `entityRows`,`name: { contains }`）;`aliases` 是数组列,子串匹配落不进索引。画布的提示词编辑器本地那一套仍然匹配别名（`apps/web/components/MentionInput.tsx` 的 suggestion `items`）。同一个 `@` 在两处搜得到的东西因此不完全一样。请裁:服务端补别名匹配（要一条索引方案）,还是画布那侧收敛到只按名字。 |  |
| 2026-09-05 | **契约 §2 的「当前 Canvas 相关性」排序未实现**（同上）。生产没有这个信号,排序只做到「名称匹配 → 最近 → id」三级（`apps/web/lib/reference-search-model.ts` 的 `referenceMatchRank` 与 `reference-search.ts` 的 `searchReferences`）——宁可不排也不编。请裁:补一个真信号（例如本画布已引用过的对象优先）,还是从契约里撤掉这一项。 |  |
| 2026-09-05 | **判官 P2-1:`entityIds` 是客户端自报,写进消息时没有服务端的归属校验**。`useReferencePicker.entityIdsForSend` 算出的 id 随请求上行,`apps/web/app/api/otto/stream/route.ts` 直接把它写进 `ChatMessage.payload`。已核的两条读路径都带租户过滤（`packages/otto/src/skills/propose.ts` 的 `where: { id: { in: … }, ownerId: ctx.orgId }`、`apps/worker/src/jobs/gen.ts` 的 `where: { id: { in: job.entityIds }, ownerId: job.ownerId }`）,所以今天取不到别人的元素——但那是「读不到」,不是「写时校验」,且不是每条读路径都逐条核过。收口的正位在第③刀（消息保存类型化引用 ID）:落库前按 owner 核一遍。登记,随第③刀做。 |  |
| 2026-09-05 | **判官 P2-5:画布那一半的空态与「搜索在途」态**。核对结果:空态文案与唯一出口本来就是同一份——三处输入口同走 `apps/web/components/reference-picker/ReferencePickerMenu.tsx`,生产侧只有那一处 `No references found` 与 `Browse Library`(夹具 `apps/web/design-system/patterns/reference-picker/ReferencePickerReference.tsx` 里另有一份,那是设计源、不是第二份实现;判官 P2-J2 —— 原话「全仓只有那一处」措辞过头)。真正的差别只剩等待态:画布不传 `pending`（它过滤的是本地数组,答案是瞬时的）,所以没有 `Searching references…` 那一行。登记待裁:画布改走服务端搜索的那一刀要不要一并补等待态。 |  |
| 2026-09-05 | **判官 P2-6:菜单宽度固定 30rem,与夹具「跟着输入框」不一致**。生产写死上限 `w-[min(30rem,calc(100vw-1.5rem))]`（`ReferencePickerMenu.tsx`）;已批准夹具把菜单和 composer 放进同一列 `w-[min(680px,calc(100%-3rem))]`（`design-system/patterns/reference-picker/ReferencePickerReference.tsx`）,菜单因此永远与输入框同宽。窄输入框（画布的镜头提示词编辑器）下两者肉眼可辨。请裁:统一到「跟输入框」,还是接受固定宽度并写进契约。 |  |
| 2026-09-05 | **侧栏 Otto 展开信号接真信号，`?otto=1` 近似退场（接线盘点 L10）**。**触发**：2026-09-04 那一行（上一行）把「这一页有活动对话即展开」以深链 `?otto=1` 近似实现，Founder 当天追认为近似，并登记「真信号待下一轮」——本行就是那一轮。**病灶**：`?otto=1` 说的是「商家点名要开面板」（`/otto` 旧地址的重定向落点），不是「这里有一段正在跑的对话」；判官 #1168 P2-2 举的两面都成立：商家在 Library 留着一条在跑的对话、离开再回来面板不展开，而 `?otto=1` 不带 thread 也照样强开。仓库里早有 `/api/otto/thread-activity` 与真库读（`apps/web/lib/thread-activity.ts`），但**零消费者**。**本轮落地**：① 新增服务端判据 `hasPendingPanelThread()`（`apps/web/lib/thread-activity.ts`）——两句缺一不可：这个商家名下有一单 `QUEUED`/`GENERATING` 的 `GenJob` 挂在某条对话上，且那条对话 `surface='panel'`。第二句不是多余的谨慎：面板「打开时接着聊哪一条」今天只在 `surface='panel'` 里选（上一行的裁决），少了它，一单画布生成会把面板顶开而面板画的是**新对话空态**，比不展开更糟；老行（`surface=null`）按画布读，同样不算。② 路由多一种问法：`/api/otto/thread-activity` **不带参数**答 `{ pending: boolean }`（面板问的那一句），带 `?projectId=` 的既有契约一个字没动。没有 project 参数是有意的——面板挂在每一个商家表面上，不属于任何一个 project，而 `getOrCreateDefaultProject()` 那条路会**建** project，一个纯读的展开信号不该有副作用。③ 客户端 `apps/web/lib/otto-panel-activity.ts` 一次 `fetch`，`OttoPanelHost` 挂载时问一次；**不新增轮询**（#544 的纪律）：`OttoPanelHost` 整页加载挂一次、软导航不卸载，「每次挂载问一次」＝「每次到访问一次」；答「有」就走与深链同一条强开路（token 只 +1 一次，商家自己关掉不会被重新弹开），答「没有」什么都不做。读失败一律当「没有」——展开信号读不到只该让面板保持收起。④ **租户只信服务端 principal**：`ownerId` 只来自 `requireOwner()`，客户端连一个参数都不传。⑤ `?otto=1` 保留它本来的「显式打开面板」语义，只是不再兼任「本页有进行中的对话」；`panel-state.ts` / `OttoPanelShell.tsx` / `otto-panel-state.test.ts` 里把它写成近似的那几段注释同步改口。⑥ 顺手改一处已过期的注释：`apps/web/lib/balance-refresh.ts` 说 thread-activity 有一条「existing 4s poll」——全仓 grep 无此轮询（该路由本轮之前零消费者），改成如实描述，纪律（不新增计时器）原样保留。**测试**（逐字含 `FRONT-A14`）：`apps/web/lib/__tests__/otto-panel-expand-signal.test.ts`（真库 `fikirtive_l10_test`：面板对话有在途 `GenJob` → 展开 / 无在途 → 不展开 / 已结束的不算 / **别家 org 的在途不算，且 B 自己看得见**（双租户）/ 画布对话与老行在跑不算 / 删掉的面板对话不算 / 未登录是错误不是「没有」）、`apps/web/lib/__tests__/otto-panel-mount.test.ts`「默认开合」一组改用真信号（有在途 → 展开且只问一次、无 → 不展开、存档关着＋在途 → 展开、关掉后不被重新弹开）、`apps/web/app/api/otto/thread-activity/__tests__/route.test.ts`（无参数分支与它的 401）。**变异**：把 `OttoPanelHost` 那句 `forceOpen()` 摘掉 ⇒ 挂载那一组 3 条红（已实跑）。**未做**：面板展开后不再复查（面板真开起来之后会话那一侧自己有取数）；`?projectId=` 那条列表读路仍无消费者，本轮不动它。**落地**：本行对应 PR（分支 `claude/otto-panel-thread-activity`）。 | 登记；本轮已落地，待 S5 |
| 2026-09-04 | **外壳与登录三处对齐设计（本 PR 落地）**：① 登录页两个错误标题按已批准的 Auth 夹具对齐——邮箱步空值拒绝写「Email needed」（夹具 `design-system/patterns/auth/AuthAccessJourneyReference.tsx:136`（邮箱步）；同一句在 `:255` 的重置／注册步复用）、密码步拒绝写「Password not accepted」（同文件 `:219`）；主干原本这两处写的是 hub 那句「Sign-in failed」与「Email could not be continued」，hub 的社交登录失败态、以及邮箱步的服务端故障态仍保留原句（那时邮箱本身是好的，写「Email needed」会指错地方）。**这是商家可见的文案改动，而 §7.5 放行清单只点名了「登录页破图、面包屑、设置换皮」、没有点名文案**，故据 §2 冻结表 FRONT-A14「差异逐条登记 §5」补记于此，请 Founder 在 FRONT-A14 走查时一并过目。② `public/brand/` 六枚产品标识图进认证墙豁免簿，`exact` 语义逐个文件登记——刻意不用 `brand` 子树，因为 `/brand` 是商家自己的 Brand 页面，一条子树豁免会把它整个搬到墙外；`proxy.ts` 的 matcher 字面量按清单重新生成（不手拼），`proxy.test.ts` 有目录对账与逐字节断言。③ 顶栏面包屑：`/analysis` 写出「Home / Analysis」，字进导航权威源 `NAVIGATION_OWNED_SURFACES[].breadcrumbLabel`，壳里一个地名都不手打；没有这个字段的 child surface（`/billing`、`/profile`、`/settings/connections`）行为零变化。 |  |
| 2026-09-05 | **画布五处「按了没反应」的失败语义收口（接线盘点 L1，`FRONT-A12` 后半句「任何写入失败都有错误反馈，不出现『假成功』」）**。五处共同病根是同一个：动作真的发出去、服务端或浏览器真的拒绝了，而拒绝落在 `console.warn`、落在 `void`、或者落在一个恒不渲染的分支里——屏幕上一个字都没有，商家读到的不是「错了」而是「什么都没发生」。**① Add text 建卡被拒零反馈**（`FlowCanvas.tsx` `addTextNode`）：`createCanvasNode` 返回 `{error}` 时只 `console.warn`，商家按了工具栏没有卡出现，只能再按一次进同一个拒绝；改成走隔壁掉落图片一直在走的那条 `toast.error`，服务端有原话就照读原话。**② 文字卡保存不读错误**（`FlowCanvas.tsx` `onTextChange` + `nodes/TextNode.tsx`）：修前是 `void updateTextNode(...)`，服务端的拒绝（会话过期、卡在另一个标签页被删）掉在地上，商家继续往一张已经不存钱的便签里打字，直到下一次板子读回来悄悄把字换掉；改成板子把结果交回卡片，卡内出错误行、商家打的字一个不动、`savedRef` 回卷到服务端最后接受的那一版（否则重试会被当成「已经存过了」空转），并给一颗真的再发一次的 Try again。**③ 图片卡收形不落盘**（`FlowCanvas.tsx` `fitMediaNodeToSize`）：卡片按真实像素收形只改 `style`，数据库还留着 320×320，刷新后商家看着归位的板子又变回一排方块；改成走拖拽与手动改尺寸一直在走的同一个 `moveCanvasNode`（x/y/w/h，不花钱、服务端带租户约束），测量移出 `setNodes` updater（updater 必须纯、可能跑两次，而服务端写只能发生一次），同一次会话里同一个拒绝只报一次。**④ 失败卡叫商家按一颗不存在的键**（`lib/canvas-terminal-copy.ts`）：`failed` 这张脸 `offersRefresh: false`，永远不渲染重试键，文案却以「Try again.」收尾；按默认裁决只去掉那四个字，`You weren't charged.` 照留，别的脸（`timeout` 的 Check again）一个不动。**⑤ Copy prompt 被拒无提示**（`nodes/NodeLineagePanel.tsx`）：剪贴板被拒（非安全上下文、手势窗口已过、权限被拒）只把 `copied` 放回 `false`——跟「还没按过」长得一模一样，商家把上一次的剪贴板贴进了广告；改成面板上说出来，出路本来就在屏幕上（提示词自己印在下面，可手动选中复制）。**人话文案零新造**：请求根本没拿到回答时用 `memory-actions.ts` / `schedule-actions.ts` / `brand-record-actions.ts` 三处同一句 `Couldn't save that — please try again.`；复制失败用 `components/otto/OttoResult.tsx` 同一句 `Couldn't copy automatically.`；服务端有原话时一律照读原话。**跨模块**：`packages/core/src/gen-failure.ts` 只改一段注释——它原本写着通用兜底文案「keeps 『Try again』 verbatim, because for THAT case pressing retry is the whole fix」，这句话在 ④ 之后为假（而且 `GENERATION_DID_NOT_GO_THROUGH` 本来就不含这四个字），改成如实记述，零行为变化。**证据**：新围栏 `apps/web/lib/__tests__/front-a12-canvas-failure-semantics.test.tsx`（17 条，每条测试名逐字带 `FRONT-A12`，驱动真 `FlowCanvas` / `TextNode` / `NodeLineagePanel` / `FailedBody`，付费路径 `useCanvasGen` 全换假件、一个积分也花不掉）。 | 登记；本轮已落地，待 S5 |
| 2026-09-04 | **裁决 · Home 做成「Meta 单源版面」**（Founder 2026-09-04 20:45；落地 §7.3⑤ 第③刀，取代原文「ready 版面按真实生产者逐个点亮」）。**组件集合**＝有真实生产者的那一块（`marketing-health`，生产者 `getAnalytics()` → `marketingHealthFromAnalytics()`）；其余七块在 `apps/web/lib/home-layout.ts` 的 `HOME_COMPONENT_PRODUCER` 里逐块写明没有生产者的理由，既不渲染、Customize 面板里也不出现勾选格（裁决九）。**五态口径**：未连接（去 Connections 连 Instagram 或 Facebook，同一条 Meta 连接）／需重连（token 解不开或 Meta 退回授权错误）／读不出来（原样重试，与「真的没有数据」分开说）／连上了但这个 Meta 登录名下没有广告账号（判据取 `me/adaccounts` 的**原始账号清单长度**，不是「这段期间有 insights 行的账号」——后者已被零投放滤空，真有账号只是停投的商家会被误告「你还没有广告账号」；＝只为发帖连了 Instagram／Facebook 的商家，说的是「接一个投广告的账号」，不引去换期间；判官 2026-09-05 P1-1 追加与落修，仍归 `not-configured`，读模型状态仍是冻结的五个）／已连接且有广告账号但这段期间零投放（说得出为什么零，并给更宽的期间）／partial（真数据＋「数到哪一天」＋「只包含 Meta 广告」一句；freshness 取 Meta 日序列最后一天，不是我们读它的时刻；判别名 `known`／`unknown`，故意中性——没有任何地方拿这个日期和今天比过，不叫 `current`，判官 2026-09-05 P2-3）。**未做**：多来源 aggregate 与 `ready` 态（契约保留、代码里不可达，等第二个来源）；Otto 页面上下文 chip（归 Otto 引擎，§3「不做」）；Connections 页按渠道深链（该页不接受选中参数，本轮不改 settings）。 | 2026-09-04 |
| 2026-09-04 | **素材详情面板四处「点了没反应」（接线盘点 L2）**。**现象**（main `2da07be4` 逐处已核）：① 收藏失败只把心形悄悄弹回去（`DetailPanel.tsx` 的 `handleFavorite`：`if ("error" in result) applyLocal(!next)` 一句带过），观感是「点了又弹回来」，商家不知道被拒了；② `Copy link` 的 `catch` 里原文写着 silently ignore clipboard errors，失败一个字不说——而且复制出去的本来就是 `storage.url()` 的站内相对路径 `/files/u/<owner>/<hash>.png`（`packages/storage/src/index.ts` 的 `url()`），登录墙后面、又没有域名，贴给别人一定打不开；③ 删除把 `deleteGeneration` 的返回值整个丢掉、无条件 `onClose()`，服务端 `{error}` 时屏幕上跟删成功一模一样；④ 变体缩略图选中哪一张只写 `localStorage` 的 `otto:pick:<id>`（`lib/result-pick.ts`），界面上没说这是本机偏好。**修法**：①②③ 一律读返回值、给可读错误——服务端有话就原样显示（不编新句），删除失败时确认框不关、就地显示、可重试，成功才关；`Copy link` 改成复用既有的签名公共门 `/api/media/pub/<token>`（新的最小 server action `lib/media-link-actions.ts` 的 `getPublicMediaLink`：`requireOwner` + 资产命名空间双核 + `MEDIA_PROXY_SECRET` 缺席即 fail closed），并把链子的有效时长说在屏幕上；④ listbox 下方一句 `Variant choice is saved on this browser only.`，文案常量与存法同源（`result-pick.ts`）。**单一源头**：签名地址的 TTL 与路径模板原本抄在 `share-preview-view.ts` 里，第二个消费者出现即收进 `lib/media-public-link.ts`，share-preview 同 PR 改读它；公共分享页的可达围栏 `share-preview-page.test.ts` 据此加一行并写明理由（该模块零 import、零数据库）。**商家可见变化两条**：`Copy link` 复制的东西从打不开的站内路径变成会过期的公开链接；「Copied!」窗口 2 秒改 6 秒（要放得下那句时长）。**本轮不做（登记）**：㈠ 变体选择升级成账号级要新的持久化列，另立；㈡ `apps/worker/src/jobs/publish.ts:297` 有它自己的 `MEDIA_TTL_MS`（发布给 Meta 抓取，时长本就该不同），跨 app 边界未收进单一源头。**测试**：`apps/web/lib/__tests__/asset-detail-write-failures.test.ts`（12 条，名字逐字含 `FRONT-A12`）；新 server action `getPublicMediaLink` 的租户闸在 `apps/web/lib/__tests__/isolation.test.ts` 补三条（名字逐字含 `FRONT-A12`）：跨租户铸链的回话与「根本不存在的 id」一字不差（不构成存在性探针）／自家能铸出令牌且令牌里签的 owner 与 storage key 都是自家的（控制组）／`MEDIA_PROXY_SECRET` 缺席即 fail closed。变异实证：拆掉 `where` 里的 `ownerId`、或把签名换成别处的 ownerId，这三条都会红（修前把两道闸全拆掉，apps/web 全量仍全绿）。资产命名空间的二次闸不另立测试 —— 数据库复合外键 `Generation.[assetId, ownerId] → Asset[id, ownerId]`（`packages/db/prisma/schema.prisma:161`）不让摆出触发它的行（本轮实测被 `Generation_assetId_ownerId_fkey` 拒），它是纯冗余防线。FRONT-A5「收藏后刷新仍收藏」的真验证已在 `asset-actions.test.ts` 的 `setFavorite` 一族，不重复。**落修（判官 P2-b）**：「Copied!」那 6 秒的计时器改成带 ref、设新的先撤旧的、卸载一并清（`DetailPanel.tsx:187`／`:510-511`／`:231-232`）—— 连按两次复制时，第二次的提示会被上一轮计时器提前抹掉，而 6 秒本来就是为了放得下那句时长。**本轮不做（登记，判官 P2-c）**：`DetailPanel.tsx` 里 server action 的 `await` 都在 `try` 之外（本轮新增三处 `:311`／`:491`／`:528`，既有付费三路 `:374`／`:443`／`:577` 同病），传输层失败（掉线、换机、500）会静默 reject、屏幕上一个字都没有 —— 正是本行要拦的那种沉默；按 §7.3 收成一个共用小包装、连付费三路一起改，另开 tidy 一票，本轮不改邻近既有代码。**落地**：本行对应 PR #1193（分支 `claude/asset-panel-failure-feedback`）。 |  |
| 2026-09-05 | **Connections 的 X 行维持诚实 Unavailable；真连 X 需开发者凭据，另立规格（接线盘点 L7）**。**今天的事实**：X 没有授权路由 —— `apps/web/lib/channels/x.ts:26` 的 `connectUrl` 指向 `/api/x/authorize`，而 `apps/web/app/api/x/` 整个目录不存在（`app/api/**` 下唯一的 authorize 路由是 `meta/authorize/route.ts`）。所以 `UNAVAILABLE_PUBLISHING_CHANNEL_IDS`（`apps/web/lib/channels/channel-meta.ts:64`）把 `"x"` 留在名单里，商家看到一枚 `Unavailable` 徽章、三处零可点控件（`components/otto/OttoConnections.tsx:151`／`:399`／`:594`）。**本轮只加围栏，商家可见行为零变化**：`apps/web/lib/__tests__/otto-connections-page.test.ts` 新增两条逐字带 `FRONT-A11` 的断言 —— ① 把徽章绑在「仓库里那条授权路由存不存在」上，**双向都红**（名单先删 ⇒ 商家被送进 404；路由先落地却忘删名单 ⇒ 屏幕继续说一句已经不成立的 Unavailable）；② X 那一行在清单、详情、Add connection 弹层三处都读作 Unavailable，且整页没有一条通往它授权地址的 `<a>`。授权地址从 `x.ts` 的 `connectUrl` 读回来，不写死字面量，适配器改了围栏跟着走。变异实证：把 `"x"` 从名单里删 ⇒ 两条全红；临时建出 `app/api/x/authorize/route.ts` ⇒ 第①条红。**真连 X 不在本轮**：要走通得先有 X 开发者账号与一对 OAuth 凭据（app key／secret 与回调白名单，属外部采购与凭据保管，Founder 决定），再写 `/api/x/authorize` 与回调、把令牌落进既有的 `ChannelConnection(kind="x")`，最后同一次动作里删掉名单那个 id。发布侧的闸已经在（`lib/channels/x-publish-adapter.ts` 的 `authorizeX` fail closed）。本行只作登记，等 Founder 定是否另立规格。 |  |
| 2026-09-05 | **Otto 两张 Meta 卡的 Deny 从「只改浏览器」接到真 `ottoReject`；「Looks great」按钮删除**（接线盘点 L8，按 FRONT-A12「不出现假成功」）。**改前事实**：`OttoActionPlanCard` 与 `OttoAdBuildCard` 的 Deny 只跑 `setDenied(true)`，商家看到「Plan declined — nothing was changed」，刷新后卡片回到待批准、仍可被批准；`OttoResult` 的「Looks great」只把自己那一行藏起来，不发任何请求、不存任何偏好、Otto 永远读不到。**改后**：两颗 Deny 都走商家 Deny 一直在用的那一个动作 `ottoReject`（`apps/web/lib/otto-actions.ts`），它按卡片种类分派到唯一一份实现 `apps/web/lib/meta-card-decline.ts`——不另立第二套业务动作；落库内容＝卡片 payload 记 `declinedAt` 并把 `approval.consumedAt` 盖上（`verifyApproval` 从此答 consumed，`approveMetaActionPlan` / `approveAdBuild` 结构性拒绝）＋对话里一条确定性拒绝消息＋`ActionEvent(approval.declined)` 审计行；对外零写入（此刻 Meta 上什么都还没建）。「Looks great」删除，「Tweak it」保留（它真的把光标交给输入框）。**取证**：真库行为测试 `apps/web/lib/__tests__/otto-meta-card-deny-front-a12.test.ts`（拒绝后重读为已拒、二次点击只写一次、别家 org 拿着本家 threadId/cardId 拒不动、已自动跑过的计划答 approved 而不是 declined）与点击路径测试 `apps/web/lib/__tests__/otto-card-deny-ui-front-a12.test.tsx`。 | 登记；随 FRONT-A12 一起验 |
| 2026-09-05 | **接线盘点 L6 落地 · 外壳两句「话与真相不符」收口**（承接本节同日两条登记行：「旧日历重定向的目标地址与权威表不一致」与「Otto 自己读的界面地图说『每一页都够得着』」）。**① 界面地图那一句改成诚实措辞**：`packages/core/src/navigation.ts` 的 `merchantNavMap()` 原写 `Reachable on every page, or with Cmd/Ctrl+J.`，改成 `Reachable with Cmd/Ctrl+J on every page except ${CREATE_NAV_HREF} and ${CANVAS_HREF}, where the page itself already carries an Otto input instead.`——**只改这一句措辞，挂载范围一个字没动**（`panel-surface.ts` 未改，`ottoPanelMountsOn()` 行为不变，商家侧零可见变化）；连带只有 Otto 提示词 golden 快照那一行（`packages/otto/src/__snapshots__/otto-instructions.golden.txt`，diff 恰好 1 行）。**② `/campaign/calendar` 的重定向对齐权威表**：`apps/web/app/campaign/calendar/page.tsx` 由 `redirect(SHELL_ROUTES.schedule)` 改为 `redirect(SHELL_ROUTES.home)`，与 `MERCHANT_NAV_REDIRECTS` 那一行逐字一致；商家侧同样看不出差别（`app/campaign/layout.tsx` 抢在前面，这一页的 `redirect()` 实际跑不到），改掉的是「表说的」与「文件做的」两句话。**围栏两条（均已做变异验证：改回旧写法即红）**：`apps/web/lib/__tests__/otto-panel-mount.test.ts`「FRONT-A14: 界面地图那句话点名了每一面不挂面板的地方,一面都不漏」（不挂面板的名单从 `ottoPanelMountsOn()` 现算，不手抄）与 `apps/web/lib/__tests__/route-redirects.test.ts`「FRONT-A14: /campaign/calendar 这一页跳的地方,与权威重定向表那一行逐字同一个去处」（期望值从权威表读；此处必须逐字比 message —— `rejects.toThrow(string)` 是子串匹配，去处是 `/` 时任何目标都绿，本轮先写错过一次）。**同类残留两处，本刀不碰、登记待裁**：`OTTO_ASSISTANT.does`（`packages/core/src/navigation.ts:157`）仍写 `Otto sits on the right of every page`，`packages/otto/src/instructions.ts:117` 仍写 `you are beside the merchant on every page` —— 两处与本次改的那一句同源同病，派工书本轮只点名了地图那一句，改它们属于同一族措辞题，请 Founder 一并裁（改文案，还是把 `/create` 与 `/create/canvas` 也挂上面板） | 落地；两处同类残留待裁 |
| 2026-09-05 | **登录码「Send again」失败照样报成功，登出点下去屏幕不动（接线盘点 L5，本 PR 落地）**。**病灶一（假成功）**：code 步的「Send again」原本是 `await sendSignInCode(); setCodeSentAgain(true)`，重发失败时商家同时读到一条红色错误和一句绿色的「A new login code was sent.」——后者是假的。**落地**：`sendSignInCode()` 改成返回「码到底送出去了没有」，按钮按返回值决定报不报「已重发」（`apps/web/app/login/LoginForm.tsx`）。**病灶二（标题写死）**：同一步的错误标题恒为「Code not accepted」，可是「Send again」失败时码根本没送出去，商家再检查手上那六位数也没用。**落地**：标题读真分支——输入被拒仍是夹具那句「Code not accepted」（`design-system/patterns/auth/AuthAccessJourneyReference.tsx:172`），重发失败沿用主干在邮箱步已经在用的那一句，两处共用一个常量 `SIGN_IN_CODE_FAILED_TITLE`，不在 code 步另写第二份措辞（夹具没有「重发失败」这一态，按 Founder 2026-09-04 裁决②沿用主干原句）。**病灶三（登出静默）**：`MerchantAccountMenu` 的 Sign out 原本是 `void signOutAction()`——无进行中态、失败无声。**落地**：点下后菜单不关、该项变「Signing out…」＋ Spinner＋`aria-busy`、进行中不重复提交；失败弹 toast「Couldn't sign you out. Try again.」并复位可重试；成功那一路不复位，因为服务端 `redirect("/login")` 会把整个壳换掉（`apps/web/lib/account-actions.ts:235`）。**测试**（逐字带验收编号）：`apps/web/app/login/__tests__/login-code-resend.test.tsx` 四条（FRONT-A12 失败不报成功／成功照报／FRONT-A2 重发失败不写「Code not accepted」／输入被拒仍写它）、`apps/web/lib/__tests__/merchant-topbar.test.tsx` 新增 FRONT-A12 登出一条。**变异实跑**：把三处改回原样 ⇒ 对应 3 条红、控制组绿。**这三处里有两句商家可见的新文案**（code 步重发失败的标题复用、登出失败的 toast），按 §2 FRONT-A14「差异逐条登记 §5」请 Founder 走查时过目。 | 登记；本轮已落地，待 S5 |
| 2026-09-05 | **`/login?error=` 的三个分支在全仓没有生产者（接线盘点 L5，只登记不动代码）**。`apps/web/app/login/page.tsx:14-19` 的 `ERRORS` 表有四个键：`AccessDenied`、`Verification`、`Configuration`、`Default`。全仓 grep 的结果是**没有任何代码往 `/login` 带 `?error=`**：站内所有跳登录的地方只带 `?from=`（如 `app/admin/queue/page.tsx:35`）或裸 `/login`（如 `app/library/page.tsx:39`、`app/files/[...key]/route.ts:79`），Better Auth 侧也没有配 `errorCallbackURL`／错误重定向（`apps/web/lib/better-auth/` 内零命中）。这张表是 NextAuth 时代的遗留——那三个键是 NextAuth 的错误码。**未删**：`Default` 仍是任何外部带进来的 `?error=` 的兜底，删表要连兜底一起想；而三个死键留着零成本、删掉是一次没人要求的清理。登记待裁：清成只剩 `Default`（一句兜底），还是整张表连同 `initialError` 一起退役。 |  |
| 2026-09-05 | **更正上一行的登出机制，并落修判官 P1-1／P2-3（接线盘点 L5 落修轮）**。**更正**：上一行写「成功那一路不复位，因为服务端 `redirect("/login")` 会把整个壳换掉」——机制说反了。Next 16 的 server-action reducer 只要服务端答了 redirect 就一律 `reject` 一个 redirect 错误（`next/dist/client/components/router-reducer/reducers/server-action-reducer.js`，原注释「the action promise will be rejected with a redirect」），只有**没有** redirect 的那一路才 `resolve`；而 `signOutAction()` 每一次成功都以 `redirect("/login")` 收尾（`apps/web/lib/account-actions.ts:235`）。于是那个不分辨的 `catch` 把**每一次成功登出**都当成失败：商家落到 `/login`（其实已登出）同时读到红色「Couldn't sign you out. Try again.」。上一轮的测试只用 `new Error("offline")` 假失败，从未跑过真实成功路径，所以证明不了它声称的事。**落地**：`MerchantAccountMenu.tsx` 加 `isRedirectRejection()`（按 digest 前缀 `NEXT_REDIRECT` 认框架控制流，仓库已有先例 `apps/web/lib/__tests__/brand-route.test.ts:312`），catch 里识别出 redirect 就直接返回（保持进行中、不弹 toast），其余出口一律复位；顺带修判官 P2-3：正常 `resolve`（评审夹具传的 `async () => {}`，不跳转）也复位，否则那颗菜单项永久卡在「Signing out…」。不用 `next/navigation` 的 `unstable_rethrow`：在事件处理器里 rethrow 只会变成一次 unhandled rejection，导航本来就由 reducer 自己完成。**测试**（逐字带 `FRONT-A12`）：`apps/web/lib/__tests__/merchant-topbar.test.tsx` 新增两条——「成功那一路（action 以 Next 自己的 `getRedirectError("/login", "push")` 拒绝）保持进行中且不出声」与「不跳转的 action 正常 resolve 后复位」。**变异实跑**：摘掉 `isRedirectRejection` 判断 ⇒ 前一条红；摘掉 resolve 后的复位 ⇒ 后一条红；控制组 11 条绿。 | 登记；本轮已落地，待 S5 |
| 2026-09-05 | **判官 P2-2 不改代码，理由是该状态不可抵达（接线盘点 L5 落修轮，只登记）**。判官指 code 步的标题只分 `error.source`、不分 `reason`，`invalid_email` 会落到「Email could not be continued」而不是邮箱步的「Email needed」，并举「深链 `/login?step=code` 时 `email` state 为空 → 按 Send again」为可达路径。**实核不成立**：`apps/web/app/login/LoginForm.tsx:218` 早有守卫 `if (step === "email" || ((step === "code" || step === "password") && !email))`——email 为空时深链根本渲染不出 code 步，渲染的是邮箱步（该守卫自 2026-09-02 `c8ae482c` 起在主干，判定当时就在）；而 code 步只能由一次**成功**送码进入（那一步已经 `normalizeSignInEmail()` 过），进去之后没有任何输入能再改 `email`。故 code 步的 `invalid_email` 不可抵达。**另一层理由**：把判断抽成共用函数会打掉一道有意的围栏——`apps/web/app/login/__tests__/auth-design-system.test.ts:95/125` 是**逐个枚举夹具标题、在生产的 `<AlertTitle>` 块里逐句比对**，标题一旦移出 JSX 成为函数调用，这道「夹具新增一句而生产没跟上就当场红」的围栏就失效（实跑：抽函数后该文件 2 条红）。**结论**：维持现状，两步各自的标题字面量留在 `<AlertTitle>` 里；若日后 code 步真的能带着无效 email 抵达，届时连同这道围栏的形状一起改。 |  |
| 2026-09-05 | **Home／Analysis 四处「话比事实说得满」收口（接线盘点 L3，`FRONT-A3` 五态与 `FRONT-A12` 「商家面不出现夹具数据」）**。**四件与各自的根因**：① **Retry 现在是一次真重取** —— Home 与 Analysis 的 Retry 此前是一条指回**同一个地址**的链接（`MarketingHomeView.tsx` 的 `href: homeHref(filters)`、`HomeAnalysisView.tsx` 的 `href: homeAnalysisHref(context)`），「同一个地址也会重取」靠的是 Next 对 same-page 导航的特判加 `staleTimes.dynamic` 默认 0 —— 两条都是框架行为、不是我们的保证，升级就可能静默失效，商家按下去看到的还是同一份读不出来的旧结果。判官 2026-09-05 P2-1 点过这一条，#1192 只在源码里留了一段注释（含「或者把这个链接改成一颗调 `router.refresh()` 的按钮」），**没有落修**；本刀落修：改成按钮，按下去 `router.refresh()` 让服务器重跑这一页的 RSC，`HomeEntry` 因此重新 `getAnalytics()`，等待期间按钮自己说「Retrying…」。② **Comparison 下拉在 partial 版面不出现**（裁决九「无契约的控件不出现」）—— partial 单源之下没有任何东西消费它：`HomeEntry` 只按 `range` 去读 Meta（`analyticsRangeForHomeRange`），`marketingHealthFromAnalytics` 全程不看 `comparison`，换个对比口径页面上一个数字都不会变；全仓唯一读它的是 `ReadyMarketingHealth`／`ReadyHomeAnalysis` 的 `dashboard.comparison`，而 `ready` 态今天不可达（同表 2026-09-04「Meta 单源版面」行）。**URL 那一段照旧解析与保留**，深链与 Analysis 的 `originComparison` 回程不受影响；`ready` 接上时控件自己回来（围栏里钉了这一条）。③ **新鲜度只在有真时间戳时显示** —— 拿不到 Meta 日序列时 `freshnessFromSeries` 回 `unknown`，界面过去照样摆一栏「Freshness unavailable」；那一栏占着 provenance 的位置却什么都没告诉商家，现在整栏不摆，分析页那句 limited coverage 也不再往句尾贴它（`marketing-home-copy.ts` 的 `limitedCoverageDescription` 第二参数改收 `string | null`）。④ **Analysis 顶栏不再写死「Live source data」** —— 那句话不管数据从哪来、有多旧都照说，「Live」是一句没人验过的新鲜度断言（与读模型刻意不叫 `current` 是判官 2026-09-05 P2-3 同一条道理）；现在逐字来自读模型（新 `sourceProvenance()`）：说得出是谁报的，有真时间戳才补一句数到哪一天，`unavailable` 等态下这一栏整个不出现。**围栏**：新 `apps/web/lib/__tests__/home-honesty-l3.test.tsx`（14 条，测试名逐字带 FRONT-A3／FRONT-A12），用 jsdom **真挂载、真点击** —— 断言的是 `router.refresh` 被调用，不是一句文案；纯字符串渲染看不出「按钮被换回链接」这种回退。**变异验证**（五种回退逐条实跑，各自当场红）：把 Home 的 Retry 换回链接红、把 Analysis 的 Retry 换回链接红、让 partial 重新画出 Comparison 红、把顶栏写死回「Live source data」红、把新鲜度改回无条件显示红。**未做**：`ready` 多来源 aggregate 仍不可达（等第二个来源，同表 2026-09-04 行的「未做」栏未变）；`comparison` 没有从 `HomeSearchState` 与 URL 里拿掉 —— 它是 `ready` 的保留契约，拿掉要动 `home-analysis-context.ts` 的回程形状，超出本刀写集。 |  |
| 2026-09-05 | **④ Brand 段的真实写集比 §7.4 表里写的宽五个文件（尾巴组一，#1157 判官 P2-2；只登记，不改冻结正文）**。§7.4 的「④ Brand」一行只点了 `apps/web/app/brand/`、`apps/web/components/brand/`、`packages/core/src/memory-sections.ts`、`apps/web/lib/brand-record-actions.ts`、`apps/web/lib/memory-actions.ts` 与 prisma。实际交付时这一段还改了五个文件：**`apps/web/lib/brand-context-data.ts`**（Brand 五节的读模型）、**`apps/web/lib/brand-context-format.ts`**（记录正文的打包与回拆）、**`apps/web/lib/brand-revision.ts`**（「谁改的」与改动史的唯一写法）、**`apps/web/lib/brand-revision-actions.ts`**（改动史的客户端可调动作）、**`packages/db/src/tenant-guard.ts`**（租户闸，Brand 的每一次读写都要过它）。**为什么要登记**：写集互斥表是并行开工时「谁碰得到谁」的唯一依据，而漏掉的五个文件里有两个是跨段共用的——`tenant-guard.ts` 全仓共用，`brand-revision.ts` 被 Brand 记录与 Memory 两条写路径共用；下一次照旧表分工就会撞车，而且撞得无声。本行只更正事实，冻结正文的 §7.4 表一个字未动，由 S5 决定要不要把这五个文件正式并进那张表。 |  |
| 2026-09-05 | **尾巴组一落地汇总：Brand 改动史的重复行与 Settings／Profile 两处围栏（#1157、#1154 判官 P2）**。**① 连按 Remove／Restore 不再在改动史里多记**：`apps/web/lib/memory-actions.ts` 的 `deleteMemory`／`restoreMemory` 与 `apps/web/lib/brand-record-actions.ts` 的 `deleteBrandRecord`／`restoreBrandRecord`，原本 `where` 只带 `id` 与 `ownerId`，所以第二、第三次点击照样命中同一行、把 `deletedAt` 盖成新时间；改动史的幂等键含 `updatedAt`，于是一次删除被讲成三次。现在 delete 的 `where` 带 `deletedAt: null`、restore 带 `deletedAt: { not: null }`；命中 0 行时照 `confirmBrandDraft` 的写法回查真实状态，已经是目标状态就仍然回「成功」（不确定的请求可以放心重发），只有真的不在了才报错。两条真库断言钉住结果：连按三次 Remove 改动史里 `deleted` 只有一行，连按两次 Restore `restored` 只有一行。**② 写集更正**见上一行。**③ 保存回执的 live region 常驻**：`apps/web/lib/__tests__/profile-design-system.test.ts` 在按下 Save **之前**就断言 `[role="status"][aria-live="polite"]` 已经在表单里——读屏对「这一帧才出现的区域」往往什么都不报。**④ 账单页 Spend history 的 e2e 锚点**：`e2e/support/ui.ts` 的 `spendHistory()` 从「所有装着这个标题的 section」改成从标题往上取最近的那一个 `section`，外面再包一层 section 也不会因为 strict mode 命中两个而红成「产品坏了」。零商家可见行为变化：改的是重复点击留下的历史行数与两处测试锚点。 |  |
| 2026-09-05 | **素材面板 Copy link 可自设有效期（Founder 2026-09-05 裁决，紧接 PR #1193）**。**裁决原话**：「同意，但是加上可以自由设定时间」——追认 Copy link 复制免登录公开签名链接，并要求商家能自己定这条链子活多久。**本刀落地**：① **时长口径单源**在 `apps/web/lib/media-public-link.ts`：默认仍是 `PUBLIC_MEDIA_TTL_MS`＝10 分钟（share-preview 那一头不传时长，行为一字未改），预设四档 `PUBLIC_MEDIA_TTL_PRESETS_MS`＝10 分钟／1 小时／24 小时／7 天，自定义单位为分钟或小时，下限 `PUBLIC_MEDIA_MIN_TTL_MS`＝1 分钟，**服务端硬上限 `PUBLIC_MEDIA_MAX_TTL_MS`＝30 天**；越界一律**拒绝铸链、不静默夹到上限**（夹一下＝商家以为拿到 90 天的链子，同一族假成功）；判据 `publicMediaTtlProblem()` 与人话 `publicMediaTtlLabel()` 也在这一个文件，UI 与 server action 读同一份（该文件仍**零 import**，公共分享页那张边表一行没多）。② `getPublicMediaLink(generationId, ttlMs?)` 新增可选时长并在服务端自判（闸放在读库之前，答案与 id 存不存在无关，不构成存在性探针）；跨租户仍逐字回 `Not found.`，与不存在一致；返回改为 `{ path, expiresInMs }`＝**它真签进令牌的那个时长**，屏幕上那句话照它写。到期时刻本来就在 HMAC 载荷里（`signMediaToken` 的 `exp`），客户端改一个字节整条令牌失效，所以「时长由客户端自报」不构成可被拉长的路。③ **面板**（`components/asset/DetailPanel.tsx`）：Copy link 键旁一格原生 select（四档＋`Custom…`，自定义时出数字框与分钟／小时单位），默认 10 分钟，挑过的那一档记在这台浏览器（`otto:link-ttl`），并在键下方常驻一句 `Link duration is saved on this browser only.`（与变体那一格 `PICK_SCOPE_NOTE` 同口径、同源写法）；成功句改成写明实际时长（例：`Anyone with this link can open the asset for 24 hours.`）；**判官 #1193 P2-d 一并修**：换变体、换素材都把「Copied!」与那句时长当场作废并撤掉计时器（剪贴板里躺的是上一张的链子）。**人话取舍**：满两天才说「天」，24 小时那一档就叫 24 hours（预设怎么写、成功句就怎么写）。**测试**：`apps/web/lib/__tests__/isolation.test.ts` 补 5 条（真库、名字逐字含 `FRONT-A12`）：预设 24 小时与自定义 90 分钟**令牌里签的到期时刻**正确／超 30 天拒绝（不夹）／0 分钟撞下限、半分钟撞「整分钟」那句／B 挑任何时长拿 A 的 id 仍与不存在逐字相同；`asset-detail-write-failures.test.ts` 补 10 条（真组件渲染，名字逐字含 `FRONT-A12`）：挑 24 小时后下发 24 小时而成功句照服务端签的那个时长写／默认档与四档＋Custom… 的菜单／自定义 2 小时／挑过记在本机且重开面板仍在／自定义值正好等于某预设时重开仍停在 Custom…／只开面板不碰那一格就一个字不写存储／`getItem` 直接抛时面板照常打开按默认 10 分钟渲染／自定义 90 天当场拒绝不发请求／自定义框空着说该填什么／换变体后成功句消失而挑好的那一档不变。变异实证（本轮亲跑）：服务端改成静默夹到 30 天 ⇒ 2 条红；签名忽略 `ttlMs` 只签默认档 ⇒ 2 条红；删掉换变体那一处作废 ⇒ 1 条红。**落修（本 PR 判官六条 P2）**：㈠ 验收编号从 `FRONT-A5` 改回 `FRONT-A12` —— 冻结表的 FRONT-A5 说的是 Library 搜索／筛选／收藏，与链接有效期无关（六条测试名与本行同改）；㈡ 面板测试的假件不再回显请求里的 `ttlMs`，改由每条测试指定「服务端真签了多久」，成功句读服务端回话而不是屏幕上那一格（成功句改读屏幕的数 ⇒ 红）；㈢ `readTtlPick`／`writeTtlPick` 与 `result-pick.ts` 的读写全部 try/catch —— 浏览器禁掉站点存储时 `getItem` 自己抛，这一格只是方便，绝不能连累素材面板打不开；㈣ 挂载不再写存储，只有商家真动过那一格才记（默认档不冒充成他挑过的偏好）；㈤ 存储里区分 `preset`／`custom` 来源，自定义填的数正好等于某预设时重开面板仍停在 Custom…；㈥ 毫秒↔「2／Hours」的逆向换算收进 `media-public-link.ts` 的 `publicMediaTtlToCustom()`，与正向 `publicMediaTtlFromCustom()` 同处，面板只调用。**待 Founder 追认（默认值）**：上限 30 天、下限 1 分钟、四个预设档与「自定义只到小时」这四个数字是本刀按最简可靠取的默认，未经点名批准。**本轮不做（登记）**：时长升级成账号级偏好要新的持久化列（与变体那一格同一张待办）；`apps/worker/src/jobs/publish.ts` 的 `MEDIA_TTL_MS` 仍在 app 边界外。 |  |
| 2026-09-05 | **段② Library 施工与已冻结 §7.3② 细则的两处偏离(只登记,不当场改规格正文)**。① **`Generation.favorite` 的读时回落整个去掉**:§7.3② 写的是「施工内一次性回灌进新表,之后只作读时回落」;实现里回灌照做(`packages/db/prisma/migrations/20260903130000_library_favorites_and_collections/migration.sql`),但回落**没有**留 —— `apps/web/lib/library-subjects.ts` 只读 `Favorite` 表,`apps/web/lib/asset-actions.ts` 把 `favorite: true` 从 select 里摘掉,全仓再无第二个读写者。理由:两个权威同时在,就是「哪一个才算数」的老病;判官 2026-09-05 认方向对(单源),但它改的是已冻结的 §7 细则,所以登记。待 S5 裁:是把细则改成「回灌之后旧列不再是任何读路径的真相」,还是要求把回落加回来。② **Otto 的 `manageLibrary` 说明书补一句互斥**:实现里 `favoriteOnly` 与 `search` 等筛选键同时传会当场回错误(`apps/web/lib/library-actions.ts` 的 `Favorites can't be filtered yet`),而 `packages/otto/src/skills/manage-library.ts` 与 `packages/otto/src/instructions.ts` 原本还写着两者可以同用、且那一路「newest first」;本轮已把两处文案改成「不可与 search 同用、按收藏时间排」,零商家可见行为变化,登记备查。 | |
| 2026-09-05 | **画布直出 composer 退役（Otto 引擎 ⑦段落地的连带登记）**。**做了什么**：画布右侧工具条的 `Generate image` 键与它掀开的那个直出 composer 一并撤下（`apps/web/components/canvas/FlowCanvas.tsx`）；画布上从此只有 Otto 那一个输入框，出图由 Otto 在对话里谈定、在确认卡上按下才扣钱。**为什么是这一份规格的事**：这是第⑨段（Canvas 对齐）之后画布界面的又一次实质变化，而**已批准的画布夹具本来就是这个样子** —— `apps/web/design-system/patterns/canvas/CanvasReference.tsx` 底部只有一个 composer，右侧工具条只有 select ／ frame select ／ hand 三枚，没有 Generate；确认长在 Otto 当前轮的卡片上。第⑨段当时保留那个 composer，理由写在 `e2e/journeys/14-canvas-toolbar-reachable.spec.ts` 的抬头（「三个直接创作工具在那份设计里没有家，所以留在创作带里」）——本次由 Otto 引擎规格的第⑦段把它退役，那句话随之作废。**连带**：工具条余下两枚都不是直出（Video 先开确认框「No charge until you confirm」，Add text 一分钱不花）；「Filter to this convo」那颗开关长在被撤的旧皮 composer 上，随它退场（板上一律显示这个 project 的全部卡片，那本来就是它唯一的默认态）。**裁决十三（Otto 输入框下的计费说明维持常驻）不但没缩水，还加了第三行**：这一轮对话本身按用量计费（Otto 引擎 §7.6 处置一），围栏在 `apps/web/lib/__tests__/front-a14-canvas-alignment.test.ts` 那条「both composer cost disclosures」上，三行一起钉。**落地**：Otto 引擎 ⑦段 PR（分支 `claude/otto-s2-7-canvas-conversation`）；规格登记与由此产生的一个待裁缺口见 `docs/specs/otto-engine.md` §5 同日那一行 | |
| 2026-09-05 | **尾巴组二落地汇总：侧栏 Otto 面板的展开信号与 Otto 两张 Meta 卡（#1200、#1202 判官 P2）**。**① 展开信号的口径逐字更正：从「本页有进行中的对话」放宽到「全店」**（#1200 P2-1）。服务端那一句（`apps/web/lib/thread-activity.ts` 的 `hasPendingPanelThread`）按 `ownerId` 查、不带 project，所以它答的是「这个商家有没有一条面板对话在跑」，不是「这一页有」；本行更正措辞，代码行为未变。**② 面板落座跟着放宽到全店，两处口径合一**（#1200 P2-2）。改前：信号是全店，落座只在当前 project 里选（`apps/web/lib/otto-panel-seed.ts`），于是一条在别的 project 里跑着的面板对话把面板顶开、面板打开却画新对话空态 ——商家看到凭空弹出来的一块空面板。改后：当前 project 没有面板对话时续全店最近那一条，并让「停在哪个 project」跟着它走；深链点名了 project 就不放宽（地址栏说的项目与会话仍是同一件事）。**③ 商家自己关掉的面板，这一程不再被同一个信号弹开**（#1200 P2-4）。/create 与画布往返都是整页加载，在跑的那单生成还在跑，于是每一次到访都把刚被关掉的面板顶回来；现在关闭那一下记进 `sessionStorage`（`fikirtive:otto-panel-dismissed:v1`，读写全 try/catch，记不住就退回「没关过」），这一程不再自动展开 ——深链 `?otto=1`（商家点名要开）不受影响。**④ `apps/web/lib/otto-panel-activity.ts` 补直接测试**（#1200 P2-3）：读失败一律当「没有」（非 2xx／网络断／答的不是 JSON）、请求 URL 逐字不带任何参数（尤其没有 `ownerId`）、取消信号原样转交。**⑤ `OttoPanelHost.tsx` 文件头那句纪律改口**（#1200 P2-5）：「取数不跟挂载走」说的是会话取数，展开信号必须跟挂载走（一次不带参数、不落库、不轮询的读）。**⑥ Deny 不再把每一种「已经settled」都说成拒绝**（#1202 P2-1）：服务端答 approved／expired 时，卡片分别显示 "This plan was already approved." / "This plan expired before you decided."（Ad build 卡同形状、自己的措辞），一份映射 `settlementTextFor()` 两张卡共用。**⑦ 过期也落终态**（#1202 P2-2）：改前过期分支只盖 `approval.consumedAt`、不盖任何卡自己的终态标记，刷新后卡片回到待批准、照旧摆出 Approve；现在盖独立的 `expiredAt`（不叫 `declinedAt`——商家没有拒绝过它），与批准闸的失败理由 expired 同一个词，二次点 Deny 也仍答 expired 而不是 approved。**⑧ 审计行断言改成有序**（#1202 P2-3）：`orderBy` 取最新那一行再比 cardId，不再靠用例执行顺序。**测试**（编号逐字进测试名）：新 `apps/web/lib/__tests__/otto-panel-activity.test.ts`（7 条 FRONT-A14）、`otto-panel-seed.test.ts` 新增 4 条 FRONT-A14、`otto-panel-mount.test.ts` 新增 1 条 FRONT-A14（真挂载：关掉后重挂不再展开）、`otto-card-deny-ui-front-a12.test.tsx` 新增 5 条 FRONT-A12、`otto-meta-card-deny-front-a12.test.ts` 新增 1 条 FRONT-A12。**变异实跑**：过期分支改回不盖 `expiredAt` ⇒ 红；落座去掉全店回退 ⇒ 红；`panelDismissedThisSession` 恒 false ⇒ 红；关闭抑制的闸摘掉 ⇒ 红。**商家可见的新文案四句**（两张卡各两句），按 §2 FRONT-A14「差异逐条登记 §5」请 Founder 走查时过目。 | 登记；本轮已落地，待 S5 |
| 2026-09-05 | **尾巴组四落地汇总：Home Retry 的失败反馈、Connections 路由探测、写侧存储失手（#1209／#1193／#1201／#1210 判官 P2）**。**① Home／Analysis 重试失败留一句实话（#1209 P2-3，商家可见）**：改前按下 Retry、服务器仍读不出来时，屏幕只闪一下「Retrying…」就回到逐字相同的一屏，商家读成「按了没反应」。现在按过一次、这一轮跑完、状态还是 unavailable 时，恢复卡里那个常驻的 `role="status" aria-live="polite"` 区填上 `Still unavailable. Try again in a few minutes.`；首屏不说（没试过就没有「仍然」），重试成功时服务器给的是 partial/ready、整块恢复卡不出现，那句自然也不出现。落点 `apps/web/components/home/MarketingHomeView.tsx`（`RecoveryState` 的 live region、`retryAttempted` 状态与 `retryFailed={retryAttempted && !retrying}`）与 `HomeAnalysisView.tsx`（`BlockedAnalysis` 同形）。**写集溢出一处**：那句话的单一产地放在 `components/home/marketing-home-copy.ts` 的 `retryStillUnavailable`（Home 与 Analysis 共用一句，抄两份就是第二个产地，§7.3）。**② #1209 P2-2 只登记不做**：`ready` 分支里顶栏 `sourceProvenance()` 与页内 `ReadyHomeAnalysis` 的 `freshness.label` 会各说一次同一个日期；`ready` 今天生产不可达，商家零影响，等第二个来源点亮那一票二选一。**③ Connections 的路由探测扩到路由组与 `.tsx`（#1201 P2-1）**：`apps/web/lib/__tests__/otto-connections-page.test.ts` 原来只 `existsSync(app/<各段>/route.ts)`，真路由若落成 `app/(group)/api/x/authorize/route.tsx` 会被判成「路由不存在」而继续绿，屏幕上那句 Unavailable 就成了没人管的假话；改成递归探测 `route.{ts,tsx,js,jsx,mjs}` 并穿过 `(...)` 段，另加一条「探测器本身是活的」用例（真路由找得到 + 临时树上造出「路由组 + .tsx」）。**自证**：临时在 `app/(social)/api/x/authorize/route.tsx` 造一条路由 ⇒ 那条围栏当场红（旧写法会继续绿）；把 `app/api/meta/authorize/route.ts` 挪走 ⇒ 探测器对照组当场红；两次都已挪回，工作树干净。**④ #1201 P2-2（PR 描述行号旧 4 行）已随 #1201 合入，不再复述。****⑤ 写侧存储失手各补一条钉子（#1210 P2-3／P2-2）**：`setItem` 抛 `QuotaExceededError` 时 `writeTtlPick`（`lib/media-public-link.ts`）与 `writePick`（`lib/result-pick.ts`）都必须吞掉、不炸到素材面板上；`readPick` 的 `getItem` 直接抛同样补一条。变异实证：拆掉这三处 try/catch ⇒ 三条各自当场红（改前拆掉全绿，正是判官说的「写侧无任何变异可证」）。**⑥ #1193 P2-d 现码已闭合，本组跳过**：`DetailPanel.tsx` 的 `handleVariantPick` 与换素材的重置块都已清 `copied`／`copiedMs`，围栏在 `asset-detail-write-failures.test.ts` 的「换一张变体」一条（main `e2092cbb` 之前已落）。**⑦ #1193 P2-e 指针订正**：复合外键 `Generation.[assetId, ownerId] → Asset[id, ownerId]` 在 `packages/db/prisma/schema.prisma:404`（`model Generation` 起于 :348）；此前三处写的 `:161` 是 `ReferenceImage` 的同形关系。本行更正本节 2026-09-04 那一行与 `isolation.test.ts` 里的注释（该注释本 PR 已改；§5 历史行按只追加不改的规矩留在原处）。**测试**：`apps/web/lib/__tests__/home-honesty-l3.test.tsx` 17 条（新增 3 条，名字逐字含 `FRONT-A3`）、`otto-connections-page.test.ts` 26 条（新增 1 条，含 `FRONT-A11`）、`asset-detail-write-failures.test.ts` 23 条（新增 1 条，含 `FRONT-A12`）、`result-pick.test.ts` 7 条（新增 2 条）。**落地**：本行对应 PR（分支 `claude/tails-home-asset-misc`）。 |  |
| 2026-09-05 | **尾巴组九 · 判官 #1159 的 Library 五条 P2 收口**。① **连按两次建出同名的第二个合集（商家可见，本轮修）**：`apps/web/components/library/CollectionDialogs.tsx` 的 `create()` 在两条路上刻意留着弹层不关——素材一件都没进去（`addToCollection` 回 `{error}`）、或有几件已经不在了（`unavailable > 0`）——留着是对的（那句话得说完），但名字还在框里、「Create collection」原样可按，商家再按一次拿到的是同名的第二个合集，而屏幕上没有任何东西说过第一个已经建好。**修法取「停用建键」这一支**（另一支是把键改成 Try again 语义，不取：合集已经建成，重来一次要么建重复的、要么得先说清楚它要重试的是加入那一半，两句话都比停用长）：合集一旦真的建出来就封口——名字框与建键一起停用，退出那颗键从 `Cancel` 改说 `Done`（合集是建出来了，不是取消掉了）。**测试**：`apps/web/lib/__tests__/library-collection-create-once.test.ts`（4 条，真组件真 React，名字逐字含 `FRONT-A6`）：不可用件数那条路连按两次只建一个／素材加不进去那条路连按两次只建一个／名字框里再按一次 Enter 也只建一个／退出键改读 `Done`。变异实证（本轮亲跑）：把组件退回修前，这 4 条全红。② **「目标不可用」如实反馈——#1159 落修轮已做，本轮零改动**：动作层 `apps/web/lib/library-collections.ts:160-202` 返回 `{added, skipped, unavailable}`（`:200` 算出），弹层 `CollectionDialogs.tsx:103-106`（挑一个）与 `:131-136`（新建）各自把 `unavailableNote()` 那行小字显示出来；真库测试 `apps/web/lib/__tests__/library-favorites-collections.test.ts:325`「`FRONT-A6`:目标里有一件已经不在了时,加入不会假装全成」已钉住（`:347`／`:357` 两个断言）。③ **Otto 的 `manageLibrary` 说明书口径——同上，已在 #1159 落修轮改口，本轮复核属实**：现码 `packages/otto/src/instructions.ts:328` 与 `packages/otto/src/skills/manage-library.ts:30`／`:112` 都写着 `favoriteOnly` 不可与 `search` 同用、同传即拒；服务端真相在 `apps/web/lib/library-actions.ts:76-83`（`FAVORITE_ONLY_FILTER_KEYS` 六键名单）与 `:121-131`（同传即回 `Favorites can't be filtered yet (…)`，不静默丢筛选）；金样围栏 `packages/otto/src/__snapshots__/otto-instructions.golden.txt:235`。商家侧 `LibraryView` 的收藏是独立一格视图（走 `listLibraryFavorites`，`apps/web/components/library/LibraryView.tsx:583-609`），不会把两者并用，故无商家可见面。④ **`Generation.favorite` 读时回落被整个去掉——已登记在本节 2026-09-05 上一行的 ①，本轮不重复登记**，仅补一条现码复核：`apps/web/lib/library-favorites.ts:23-25` 的文件抬头把「那一列自一次性回灌之后没有任何写入者、读路径也不再拿它当真相、留列只为迁移可干净回滚」写在源头上，`apps/web/lib/asset-actions.ts:143-149` 与 `:333-344` 是它今天唯一的读写形态（`favoriteGenerationIds()` 读新表、`setFavorite()` 转调 `setLibraryFavorite`）。待 S5 裁的仍是那一行提的两条。⑤ **段② 的写集溢出（P2-5，本轮补登记）**：§7.4 给 段② Library 的写集是 `apps/web/app/library/`、`apps/web/components/library/`、`apps/web/lib/data.ts` 的历史读取段、prisma 三张表的迁移；而 #1159 实际还改了 `apps/web/lib/asset-actions.ts`（收藏改路转调，见 ④）、新建的 `apps/web/lib/library-favorites.ts`／`library-collections.ts`／`library-subjects.ts`／`library-types.ts`／`library-collections-model.ts`、改了 `apps/web/lib/library-actions.ts`，以及 `packages/otto/src/instructions.ts`／`skills/manage-library.ts`（见 ③）——**六个文件家族全在写集外**。理由都成立（收藏与合集的业务动作层本来就该在 `lib/` 而不是组件里；Otto 说明书不改就会与现码说两样话），但表没跟着更新，并行开工的段只看表就会误判互斥。待 S5 裁：把 §7.4 段② 那一格补成 `apps/web/lib/library-*.ts`、`apps/web/lib/asset-actions.ts` 的收藏段与 `packages/otto/` 的 Library 说明书段，还是维持原表、把这几处一律当溢出逐次登记。**本轮自己零溢出**：只改 `apps/web/components/library/CollectionDialogs.tsx`（段② 写集内）、新增本节点名的那一个测试文件、与本行登记。 |  |
| 2026-09-05 | **尾巴组三落地汇总：登录围栏、Otto 地图那句话与围栏、四个登记锚点（#1153、#1207、#1195 判官 P2）**。**① 登录页两条判官 P2 复核为已在主干、本轮零改动**：`#1153` P2-1（枚举夹具标题的测试要只在 `<AlertTitle>` 块里找，别让中文注释里的同名字符串把它喂饱）与 P2-2（别钉带缩进的多行源码字面量）都已由主干 `07173f9e` 落修，`344b780a` 又把第三条断言跟着源码改成常量名——现码 `apps/web/app/login/__tests__/auth-design-system.test.ts:109-113` 逐块取 `<AlertTitle>` 正文再 `join`，`:137-145` 是三条不依赖格式的断言（`error.reason === "invalid_email"` ／ `? "Email needed"` ／ `: SIGN_IN_CODE_FAILED_TITLE`，后者的字面量在 `LoginForm.tsx:48` 只写一次并另有唯一性断言）。本轮只做核实，不重复落修。**② `FRONT-A14` 地图那句话不再自相矛盾**：`OTTO_ASSISTANT.does`（`packages/core/src/navigation.ts`）原写 `Otto sits on the right of every page`，而同一行拼上去的后半句自己数出两个例外（`/create` 与 `/create/canvas`）——商家读到的是前后打架的一句话。改成 `Otto sits on the right of the workspace pages`，例外仍由后半句逐条点名；**挂载范围一个字没动**（`panel-surface.ts` 未改，`ottoPanelMountsOn()` 行为不变）。同源同病的第二处一并改口：`packages/otto/src/instructions.ts` 的 `you are beside the merchant on every page` → `on the workspace pages`，连带 golden 快照恰好 2 行（`packages/otto/src/__snapshots__/otto-instructions.golden.txt`）。**合入主干 ⑥ 技能文件柜 #1203（`f18e3fb2`）之后的落点订正**：`instructions.ts` 退役成组合根，这句散文的单一出处改为 `packages/otto/knowledge/_core.md:16`，本 PR 的改口即落在那里（生成物 `packages/otto/src/knowledge-cabinet.generated.ts` 由 `pnpm --filter @fikirtive/otto run knowledge` 重生成）；旧 golden 已随 #1203 删除，现在冻结这句话的是 `packages/otto/src/__snapshots__/otto-core.golden.txt`，diff 仍恰好 2 行。这两处正是本节同日 L6 落地行末尾登记的「同类残留两处、待裁」；本轮按判官口径走**改文案**这条路，**另一条路（把 `/create` 与 `/create/canvas` 也挂上面板）仍是 Founder 的选项，未被本 PR 关闭**。**③ 那两条围栏改成不会空转的形状**（`apps/web/lib/__tests__/otto-panel-mount.test.ts`）：`FRONT-A14` 点名例外那一条原来用 `toContain(href)`，而 `/create` 是 `/create/canvas` 的前缀——地图只写长的那一条也能绿；改成整段比（新 `namesPathExactly()`，路径后面不许紧跟又一段路），并加一条自检钉住「不挂面板的两面互为前缀」。`FRONT-A14` 反向那一条原来 `slice(indexOf("except"))`，`except` 一旦消失 `indexOf` 回 -1、`slice(-1)` 把整句缩成一个字符，底下每条 `not.toContain` 都白过；现在先断言 `except` 真的在。**变异验证两条**：地图只写 `/create/canvas` → 第一条当场红；把 `except` 换成 `everywhere but` → 第二条当场红；两次改回即绿。**④ 四个 `file:line` 锚点按现码订正**（判官 #1195 P2-1，主干已走远，写的是今天的行号不是判官当时的）：`docs/specs/creation-engine.md` 引的 `OttoChatStream.tsx:566-568` → `:570-573`（`freshResultCount` 三行加那句 `void onBalanceRefresh?.()`）；`apps/web/design-system/information-architecture/frontend-convergence-phase-2-home-acceptance.md` 第 9 条引的 `MarketingHomeView.tsx:381` → `:442-443`；本节评审夹具行引的 `review-fixture-guard.ts:11-13` → `:12-13`（`:11` 是注释收尾）；本节 thread-activity 行引的 `balance-refresh.ts:15` → `:14-15`，并注明那两段注释已在同日 L6／L10 落地里整段改写、矛盾已消。**⑤ 判官 #1195 P2-2 的措辞收口**：`creation-engine.md` 那条转述从「没有测试点名」改成「`freshResultCount` 那一支没有行为覆盖（围栏只做源码正则点名）」 | 落地；②的另一条出路仍待 Founder |
| 2026-09-05 | **走查修复一：画布里 Otto 一轮失败时状态卡仍写着 Ready（2026-09-05 走查，截图 `preserved/creation-12h-2026-09-04/walkthrough-2026-09-05/13-15`）**。**病象**：在 /create/canvas 底部 Reply to Otto 发一句，输入框清空，48 秒里左上角状态卡始终「Otto / Ready / Tell Otto what you want to create or change.」，无 thinking／working／failed、无 toast；只有左下角**默认折起**的「Conversation」计数从 2 变 4，展开或整页刷新之后同一张卡才变「Otto / Failed / Otto hit a snag — please try again. Reference: OTTO-…」。商家读到的是「按了没反应」。**根因（修根不修表）**：那张卡的脸由 `apps/web/lib/otto-canvas-turn.ts` 的 `latestTurnTerminal` 从消息列表投影，而它**只认落了库的两种终局**（GEN_RESULT / TURN_ERROR）；落库那份要整页刷新才回到 `messages`（`appendMissingCards` 只补卡、`backfillMissingAssistantText` 只补一句 TEXT，两者都刻意不补 TURN_ERROR）。直播里的失败其实**已经在屏幕上的数据里**——路由把它写成一个 `data-error` 部件，AI SDK 把非 transient 的 data 部件写进那条 assistant 消息的 `parts`（抽屉里那张 `OttoStreamErrorNotice` 读的就是它）——投影从来没读过它。**落地**：不新增状态源，只让同一个 `TurnTerminal` 多两条到达路径——① 直播 `data-error` 部件（`dataErrorOf`，与抽屉同一个函数）；② 传输级失败（流没打开就断了，消息上无部件，由 `OttoChatStream` 把 `useChat` 的 error 作为 `liveError` 传进同一个纯函数）。终局带上有类型的 `error`（`error` 能重试／`insufficient_credits` 要充值／`spend_cap` 要抬上限，刷新后由 `persistedStreamErrorOf` 从持久 payload 认回同一个岔路口）。**失败不再藏在折起的抽屉里（二选一，选后者）**：状态卡自己承载失败原句与 Reference（`role="alert"`、失败色），并在能重试的那一种下摆一颗「Edit and retry」，按下把商家原来那句话放回输入框——措辞与抽屉里那张告示共用一个常量 `EDIT_AND_RETRY_LABEL`，两处不可能长出两种写法。另把 `failed` 排到 `isBusy` 前面：`data-error` 是终局部件（路由写完就 return），而 `useChat` 的 status 要等流关掉才落回 ready，那半拍写「Working」说的是一件已经不成立的事。**写集溢出两处**（本组清单点名的写集是 `components/canvas/**`、`components/otto/OttoChatStream.tsx`）：`apps/web/lib/otto-canvas-turn.ts` 是「这张卡该说什么」的单一产地（§7.3，判断只能落在这里，不能在组件里长第二份）、`apps/web/components/otto/OttoTurnCard.tsx` 是状态卡本体（失败原句与出路必须落在它身上）；另在 `OttoStreamErrorNotice.tsx` 导出上述那一个常量，措辞零变化。**测试**（编号逐字进测试名）：新 `apps/web/lib/__tests__/front-a12-canvas-turn-failure.test.tsx` 14 条 FRONT-A12——10 条投影（直播失败当场 Failed 且带 Reference／传输级同样上脸／刷新前后逐字同一张脸／出路按失败类型判／充值那种不给重试键／送出当场 Working／上一轮失败不挂这一轮／对照组），4 条真接线（画布形态的真 `OttoChatStream`，含按下 Edit and retry 之后输入框里真的是商家那句话）。**变异实跑**：摘掉直播 `data-error` 的读取 ⇒ 10 条中 6 条红；摘掉 `retryDraft` 接线与传输级 `liveError` ⇒ 真接线那组 2 条红。**商家可见的新出路一处**（状态卡上的 Edit and retry，措辞沿用抽屉里已有的那一句），按 §2 FRONT-A14「差异逐条登记 §5」请 Founder 走查时过目 | 登记；本轮已落地，待 S5 |
| 2026-09-05 | **Founder 2026-09-05 七裁决（AskUserQuestion，逐条原话）回写本表四条**（另两条③④回写 `docs/specs/otto-engine.md` §5 同日行；⑤草稿规格 #1212 见该 PR 评论，不改任何规格正文）。**①登录页邮件服务未配置时的提示口径**：「按环境提示」——邮件服务整个没配时对所有人显示同一句「这个环境暂时不能发送登录码」，不按邮箱判断、不泄露账号存在性；测试环境另给假投递通道。**②/create 起步页第一轮付费对话的价钱说明**：「输入框下加一行价钱」——松开 2026-09-03 裁决五（本表同日行）中删去价钱句的那一格，起步页输入框下面加一行小字价钱说明，与画布同一份文案；裁决五其余部分（无标题行、照设计顺序）不变。**⑥ #1210 Copy link 有效期四个默认值追认**（本表 2026-09-05「素材面板 Copy link 可自设有效期」那一行末尾「待 Founder 追认（默认值）」的答复）：四档预设、自定义到小时、上限 30 天、下限 1 分钟——「追认」。**⑦ /create 起步页与画布是否挂侧栏 Otto 面板**（回应 #1207 L6 判官提问，与本表同日「尾巴组三」行 `OTTO_ASSISTANT.does` 措辞收口那一条留的选项二）：「不挂」——两页自带 Otto 输入，维持现状。 | Founder 2026-09-05：①按环境提示；②输入框下加一行价钱；⑥追认；⑦不挂 |
| 2026-09-05 | **裁决② · Create 起步页输入框下加一行价钱（松开裁决五的一格）**。**Founder 2026-09-05 裁决原话**：「输入框下加一行价钱」。**为什么现在补**：起步页按一下发送键，`apps/web/lib/canvas-entry-actions.ts` 在同一笔事务里建好一条 `surface="canvas"` 的对话，画布拿到 `pendingFirst` 后**挂载即把第一轮送出去**（`apps/web/components/otto/OttoChatStream.tsx` 的预扣 `otto-stream:<userMessageId>`）——也就是说第一轮付费对话由这一页那一下发出，而这条路径此前从按下到扣钱**全程零披露**；画布门厅那一支挂着的披露被整条路径跳过。**松开的范围只有一格**：2026-09-03 裁决五那一行里「这一页不出现价钱」这一格；该裁决点名删掉的可见标题行「Create with Otto」（含 Otto 头像与副标题）与「Nothing paid starts before you confirm the exact credits in Canvas.」整句**不恢复**，围栏（`apps/web/lib/__tests__/create-design-parity.test.tsx` 与本轮新围栏）两侧都仍钉着它们不在。**落地**：`apps/web/components/start-something/StartSomething.tsx` 在输入框正下方、Canvas history 之上挂上**画布与门厅用的同一个**披露组件（`apps/web/components/otto/ConversationCostHint.tsx`，`text-[0.75rem] text-muted-foreground` 的灰字层级），这一页一个钱数都不写——数值只有 `apps/web/lib/credit-format.ts` 一处作者；新围栏 `apps/web/lib/__tests__/front-a15-create-start-disclosure.test.tsx`（6 条，测试名逐字带 FRONT-A15）钉四件：渲染时披露在场、位置在输入框之下 Canvas history 之上（真 DOM 的 `compareDocumentPosition`，不是源码正则）、挂的是同一个组件只挂一次、起步页源码零手抄钱数（「界面不许写死价钱」那道围栏此前按文件点名，只罩着三条成本小字组件自己，起步页不在名单里，本轮加上）。**两次变异实证**（逐次实跑并还原）：摘掉 `<ConversationCostHint />` ⇒ 3 条红；在同一处手抄一句「4 credits」⇒ 围栏那条当场红。**写集零溢出**：只改起步页组件、新增该围栏文件、本行登记 | 2026-09-05（Founder 裁决②，本行即执行记录） |
| 2026-09-05 | **尾巴组 F2 · 登录页在邮件寄不出去时仍宣称已寄出（3310 走查 03-04）——查清了，修法有代价，请 Founder 二选一**。**现象**：`/login` 填邮箱按 Continue with email，页面翻到「Check your email / We sent a temporary login code to …」，Send again 后写「A new login code was sent.」；同一时刻服务端日志写着 `[better-auth] auth email delivery failed: EmailSendError`。**根因（已核实，非推测）**：投递整个不在请求路径上——`apps/web/lib/better-auth/sender.ts` 是 #678 刻意搬走的后台队列（并发 4、抖动 0–2s、槽底 3s、故障只写运维日志），`apps/web/lib/better-auth/signin-code-request.ts` 的四步里没有任何一步会知道信寄没寄成，所以 `app/login/actions.ts` 永远回同一句成功。**这是设计，不是疏漏**：#678 的理由写在 `apps/web/lib/better-auth/signin-code-contract.ts` 的 `SignInCodeFailureReason` 注释里——只有**有权限**的地址才会被交给邮件商，所以「这一封寄不出去」等于「这个邮箱有权限」，正是 FRONT-A2「错误提示不泄露该邮箱是否存在」要挡的。**为什么走查看到的那次是可修的**：`RESEND_API_KEY` 在 env contract 里是 optional（`packages/core/src/env-contract.ts:338`），生产可以在**根本没有邮件通道**的情况下起来；「这个部署有没有邮件通道」是进程的一个 env 读，对每个地址答案都一样——要么对所有商家都寄不出、要么对谁都寄得出——因此把它如实说出来**不构成存在性探针**，与单封投递故障不是一回事。**修法已写出来并跑通，但本轮不落**：在请求路径格式检查之后、闸门之前加一步「这个部署能不能寄信」，不能就回既有的 reason `unknown` 与既有那句 `We couldn't send a sign-in code. Try again.`（契约零新增导出、`LoginForm.tsx` 一个字不改，页面自然留在邮箱步亮 Alert）。单元测试全绿、变异实证成立。**代价是它把 e2e 全套打红**：29 条旅程失败，报 `e2e: no sign-in code was minted within 15s`（本 PR 首推的 e2e，run 33956021083、job 101279408913，日志末行 `29 failed`、58 处 `no sign-in code was minted`）。原因不是 bug——`e2e/support/env.ts` 的 `OFF_MACHINE_CREDENTIAL_NAMES` **明令**测试机不许带 `RESEND_API_KEY`（带了就拒开跑），`e2e/support/auth.ts` 抬头也写着「send fails into an operator log by design」，然后从数据库里读那颗已铸出的码当收件箱。**也就是说：整套 e2e 一直在一个「真的寄不出信」的部署上登录**。所以这一步不是走查级文案修，而是一条产品裁决：**没有邮件通道的部署，到底该让商家登录不进去（诚实），还是照旧放行（今天）**——落它就要连带决定 e2e 与任何没配 key 的 staging 怎么办。**请 Founder 三选一**：① 落这一步，并给 e2e 与 staging 一条明说的替代投递路（否则登录不进去）；② 不落，改把 `RESEND_API_KEY` 升成生产必需（但 3310 跑的是 `FIKIRTIVE_ENV_CONTRACT=warn`，逃生阀会绕过它，等于没修）；③ 维持现状，接受「配错了 key 的部署会对每个商家说假话」。**另有一条独立的、更小的待裁**：code 步那句 `We sent a temporary login code to …` 是**完成时的断言**，而我们真正做到的只是「交给了队列」；它是已批准 Auth 夹具原文（`apps/web/design-system/patterns/auth/AuthAccessJourneyReference.tsx:167`）且被 FRONT-A14 逐字钉住（`app/login/__tests__/auth-design-system.test.ts:76`），改它是设计变更，按 §3「不重开设计」只登记。可选口径：沿用同一份夹具在重置步已经用的条件句（`:276` 的「If … has an account, a one-time reset link is on its way.」）。**本轮实际落地（零行为变化，只补围栏）**：`app/login/__tests__/login-code-resend.test.tsx` 新增 2 条 `FRONT-A12` 真挂载测试——服务端一旦如实回绝（resolve 的 error，不是 reject），页面绝不出现 `Check your email` 与 `We sent a temporary login code to`；`Send again` 被回绝时绝不出现「A new login code was sent.」。这两条钉的是 #1208 L5 建好、但服务端今天还从不触发的那条分支，任何一版服务端修法都必须让它们保持绿。**落修轮（判官 B1／B2／P2-1／P2-2）补充**：**⑴ 本条正式列为待裁项，建议取出路①**——那一步是**部署级、与地址无关的一个 env 读**（同一个部署对每个邮箱答案都一样），不构成存在性探针，与单封投递故障不是一回事；连带条件是给 e2e 与未配 key 的 staging 一条**明写的替代投递路**（例：只在非生产／测试镜像下生效的 file／stub transport，仍不带 `RESEND_API_KEY`，`e2e/support/env.ts` 的 `OFF_MACHINE_CREDENTIAL_NAMES`「绝不带可花钱凭据」围栏一个字不动）。出路② 已被 3310 跑的 `FIKIRTIVE_ENV_CONTRACT=warn` 逃生阀架空（等于没修）；出路③ 是继续对每个商家说假话。**裁决前本 PR 本身可合**：零行为变化，先把两条围栏钉住。**⑵ 诚实句待 Founder 二选一（P2-1，落修那一轮才改）**：本 PR 断言的是既有句 `We couldn't send a sign-in code. Try again.`（单一产地 `apps/web/lib/better-auth/signin-code-contract.ts:4-5`），而走查清单点名的示例句是 `We couldn't send the code. Try again in a moment, or use another email.`——两句并排请 Founder 挑一句；若取清单那句，改在该单一产地，**不得**在 `LoginForm.tsx` 另写一份（§7.3）。**⑶ B2 指针订正**：测试文件里两处「服务端现在如实回绝、见 `signin-code-request.ts` 的 ①′」是撤回版（`2efdc57f`）留下的假话——`①′` 那一步已被本 PR 第二笔提交撤掉，全仓再无第二处；本轮改写成现况（页面侧前置围栏 + 指回本行），纯注释改动。 | 待 Founder 三选一（出路① 需连带批 e2e／staging 替代投递路；另有诚实句二选一） |
| 2026-09-05 | **Founder 2026-09-05 裁决①「按环境提示」落地：邮件商没配置的部署，登录页在输入邮箱那一步就说环境本身；测试环境走一条明写的假投递通道**（承接同表上一行尾巴组 F2 的三选一，取出路①；F2 的 PR 由本轮取代）。**部署级判定，单一源**：`apps/web/lib/email/transport.ts` 的 `emailTransportChoice()` —— 显式 `AUTH_EMAIL_TRANSPORT=stub` 走 stub；否则配了 `RESEND_API_KEY` 走 resend、没配则非生产走 stub、在服务的生产答 `none`（`emailDeliveryAvailable()` 即 `!== "none"`）。「配没配」不另写第二套读法：`packages/core/src/env-contract.ts` 新导出 `envVarConfigured()`，与开机检查 `checkEnv` 共用同一个 `isSet`（空串与纯空白都算没配），且名字必须先在契约里登记否则抛错；只判有无、永不回显值。**为什么说出来不是存在性探针（FRONT-A2）**：它是一次 env 读，对每个地址同一个答案，在任何地址被看一眼之前就定了；单封信投递失败仍是运维信号，与它不是一回事（`apps/web/lib/better-auth/signin-code-contract.ts` 的 reason 注释未动）。**页面**：`app/login/page.tsx` 把答案当 prop 递给 `LoginForm`（与 `googleEnabled` 同一做法，#681「两半读同一份事实」）；寄不出去时邮箱步的说明由夹具那句 `We'll send a temporary login code.` 换成 `Sign-in codes aren't available in this environment yet.`（单一产地：新常量 `SIGN_IN_CODE_UNAVAILABLE_MESSAGE`），`Continue with email` 禁用、输入框里按 Enter 的隐式提交也拦住，密码步的 `Use a login code` 不画 —— 于是再也到不了 `Check your email / We sent a temporary login code to …` 与 `A new login code was sent.`。夹具原句一个字没删（条件渲染），FRONT-A14 的逐句对照与标题枚举仍全绿。**服务端兜底**：`app/login/actions.ts` 在解析地址**之前**先判一次（旧标签页、直接调用拿同一句），四步请求路径 `signin-code-request.ts` 一字未动。**假投递通道明写**：Resend 适配器里那段没钥匙回落整块搬成 `apps/web/lib/email/stub-adapter.ts`（内容、日志、落盘路径 `.data/last-magic-link.txt` 一字未改），由 `lib/email/index.ts` **每次发信**按名字挑；`resend-adapter.ts` 只剩真 Resend 一条路，没钥匙即抛既有的 `config_missing`。**开关为什么必须显式**：e2e 跑 `next start`，Next 自己把 `NODE_ENV` 设成 production，「非生产才用 stub」在那里永远不成立（正是 F2 那一轮 29 条旅程全红的机制）；所以 `e2e/support/env.ts` 的 `appEnv()` 显式加 `AUTH_EMAIL_TRANSPORT="stub"`，`OFF_MACHINE_CREDENTIAL_NAMES`（含 `RESEND_API_KEY`）一个字不动、`e2e/support/auth.ts` 从库读码的做法保留（只更新抬头注释）。生产围栏落在契约的 `productionValues: ["resend"]`：对外服务的生产进程设成 stub 即拒绝开机（e2e 是显式带 `FIKIRTIVE_ENV_CONTRACT=warn` 跑的，在那里表现为一条响亮的警告）。**测试**（名字逐字带验收编号）：新 `apps/web/lib/email/__tests__/transport.test.ts` 6 条（FRONT-A12 五条＋FRONT-A2 一条）、新 `stub-adapter.test.ts` 3 条（自 `resend-adapter.test.ts` 整块搬来，行为未改）、`app/login/__tests__/signin-code-action.test.ts` 新增 3 条（含控制组「能寄信的部署行为一字不变」）、`login-code-resend.test.tsx` 把 F2 那一组改成环境口径共 4 条（含控制组）。**变异实跑（本轮亲跑）**：㈠ 让 `emailTransportChoice()` 恒回 `stub` ⇒ 5 条红（transport 3 ＋ action 2），控制组绿；㈡ 邮箱步说明写死成夹具原句 ⇒ 1 条红；㈢ 摘掉按钮禁用与提交前那道拦 ⇒ 1 条红。**跨模块**：`packages/core`（新导出 `envVarConfigured` ＋新契约条目 `AUTH_EMAIL_TRANSPORT`）、`e2e/`（stub 开关与抬头注释）、`.env.example`（新变量说明）。 | 登记；本轮已落地，待 S5 |
| 2026-09-05 | **尾巴组十落地汇总：侧栏 Otto 面板的落座口径与 Home Retry 的复位（#1215、#1200、#1216 判官 P2）**。**① 深链带 `?project=` 的那条例外撤掉（商家可见，FRONT-A14）**：`apps/web/lib/otto-panel-seed.ts:154-157` 从前只在「没有深链点名 project」时才回落全店，而展开信号那一边（`apps/web/lib/thread-activity.ts` 的 `hasPendingPanelThread`，按 ownerId 查、不带 project）不认识 project——于是 `/?otto=1&project=P` 进来、P 里一条面板对话都没有时，信号照样把面板顶开、落座却选不到任何一条，同一块凭空弹出来的空面板换个入口原样复现。现在点名的 project 里有面板对话仍然优先用它，没有就退到全店最近那一条、并跟着它停在那个 project（`?thread=` 逐字点名的一条不受影响）。测试 `apps/web/lib/__tests__/otto-panel-seed.test.ts:263-282` 两条（撤掉回落 ⇒ 当场红）。**② 商家关掉的面板不再被同一信号弹开：现码已闭合，本组跳过**——记号在 `apps/web/lib/otto-panel-activity.ts:61-79`（sessionStorage）、抑制闸在 `apps/web/design-system/patterns/otto-panel/OttoPanelHost.tsx:234-241`、围栏在 `apps/web/lib/__tests__/otto-panel-mount.test.ts:845-870`，均随 PR #1215 入主干。**③ 两张 Meta 卡「已 settled 说哪一种」的期望值改成逐字钉点（FRONT-A12）**：`apps/web/lib/__tests__/otto-card-deny-ui-front-a12.test.tsx` 六处从前拿 `settlementTextFor()` 自证，判官把 approved 那一句改成假话时十条全绿；现在写死四句英文原文，同一变异当场红。**④ 文件头改口说实话（FRONT-A14）**：`apps/web/lib/thread-activity.ts:44-49` 与 `apps/web/app/api/otto/thread-activity/route.ts:6-9` 从前写着「本页有进行中的对话」，而那句判据按 ownerId 收口、一个 project 参数都没有——改成全店口径。**⑤ Home／Analysis 的「Still unavailable」不再无故出现（商家可见，FRONT-A3）**：`retryAttempted` 从前只置真不复位，同一挂载里重试读回来（partial/ready）、换个筛选再翻回读不出来时，商家没碰 Retry 屏幕就先说那一句；`apps/web/components/home/MarketingHomeView.tsx:398-403` 与 `apps/web/components/home/HomeAnalysisView.tsx:265-270` 在读回来那一次翻面时复位（写在渲染里而不是 effect 里——`react-hooks/set-state-in-effect` 判红），测试 `apps/web/lib/__tests__/home-honesty-l3.test.tsx:225-254` 两条（去掉复位 ⇒ 各自当场红）。**写集溢出**：`apps/web/lib/otto-panel-seed.ts` 与它的测试不在本组写集清单里，①落座那一条只能改在这里，已按最小一处改并在此登记。 |
| 2026-09-05 | **尾巴组十三落地汇总：价目围栏判据收单源、三处指针与措辞订正（#1227／#1219／#1220 判官 P2）**。**① 「界面不许写死价钱」那道闸的判据收成单一源**（判官 #1227 P2-3 ＝ #1219 P2-4）：`HAND_TYPED_CREDITS` 正则与 `copyLines()` 此前在四份围栏里**各手抄一份、逐字相同**（`apps/web/lib/__tests__/understanding-disclosure.test.ts`、`money-a10-search-disclosure.test.ts`、`engine-a3-front-door-disclosure.test.tsx`、`front-a15-create-start-disclosure.test.tsx`）；新增测试助手 `apps/web/lib/__tests__/helpers/price-literal-fence.ts` 作**唯一作者**，四份改为 import 同一份。手抄件本身不影响任何一道闸咬不咬，但它复制的是**判据**——哪天放宽或收紧那条正则、或者改「哪几行算文案」，四处漏掉一处就是一道悄悄失效的闸。**四次变异实证**（逐次实跑并还原）：往四个目标文件（`apps/web/components/otto/UnderstandingCostHint.tsx`、`components/otto/SearchCostHint.tsx`、`components/otto/ConversationCostHint.tsx`、`components/start-something/StartSomething.tsx`）各临时塞一句手打价钱，对应那一份围栏各自当场红。**② 预扣 refId 的归属订正**（判官 #1227 P2-1）：本节同日「裁决②」那一行与 `front-a15-create-start-disclosure.test.tsx` 抬头都把 `otto-stream:<userMessageId>` 写成 `components/otto/OttoChatStream.tsx` 的预扣——那是**服务端**写的，现码住在 `apps/web/app/api/otto/stream/route.ts:324`；客户端组件只负责挂载即 `sendMessage`。测试抬头已就地改正；「裁决②」那一行是历史记录，不改，以本行为准。**③ 起步页抬头注释由两条改三条**（判官 #1227 P2-2）：`apps/web/components/start-something/StartSomething.tsx` 抬头原写 `Two deliberate departures` 而现码已有三条，第三条正是裁决②那行披露（第三条逐字点名 Founder 2026-09-05 裁决②）；同段 `Every class string … verbatim` 加上「披露那一条除外」——夹具根本没有这一行，它的 class 无从抄起。**④ 面板挂载名单的抬头引文改成现码指针**（判官 #1220 P2-2）：`apps/web/design-system/patterns/otto-panel/panel-surface.ts` 抬头原引 Otto 自述「beside you on every page」，那句已在本节同日尾巴组三行里改成 `Otto sits on the right of the workspace pages`（`packages/core/src/navigation.ts:166`），例外由 `merchantNavMap()` 后半句 `:545` 逐条点名；抬头改成这一对现码指针。**零行为改动**：产品侧只动两处文件头注释，`ottoPanelMountsOn()` 与起步页渲染一个字节都没改。**连带的行号漂移**：本节同日「Otto 自己读的界面地图说『每一页都够得着』」行引的 `panel-surface.ts:22-36`，因本轮抬头多了三行，现码是 `:25-39` | 落地 |
| 2026-09-05 | **判官 #1219 三条登记订正（P2-1／P2-4／P2-5）**。**① 写集溢出是四处，不是三处**：`docs/specs/otto-engine.md` §5 同日行的 (b) 段数了三处「删掉传给 `OttoFrontDoor` 的死参数 `entities`」的溢出文件，实际第四处是 `apps/web/lib/__tests__/front-a15-canvas-selection.test.ts`（同一提交 `807d19ba` 改的，同样是一行删传参、零行为变化，不删 `tsc` 不过）。以本行为准。**② P2-4 已落地**，见上一行①。**③ `SearchCostHint` 还没挂门厅默认那一支，待下一刀**：`apps/web/components/otto/OttoFrontDoor.tsx:329` 画布那一支挂着，默认那一支（四颗目标格子那一屏）没挂；同文件 `:372` 的注释自称「这里登记待下一刀」，而规格里从来没有过那条登记——本行就是那条登记。判官口径：不算实现缺陷，因为当时的清单只要求「同一个披露组件」（单数）。**④ 与 `docs/specs/otto-engine.md` §5 同日行 (f) 段同源的陈旧代码注释一处，本轮只登记不改**（不在本组写集内）：`apps/web/components/start-something/CreateWorkspace.tsx:14-17` 仍写 `the credits disclosure lives on the Canvas confirmation card, which is where the money actually moves; this page starts no paid action, so it makes no money claim of its own.`——(f) 已经证明按一下就在同一笔事务里开一条按用量计费的对话，裁决②又给这一页加上了那行价钱，这句话今天两半都不成立 | 登记；③④待下一刀 |
| 2026-09-05 | **判官 #1220 三条登记（P2-1／P2-2／P2-3）**。**① `the workspace pages` 与 `/create` 的自称撞词，措辞裁量留 Founder**：Otto 自述现在写 `Otto sits on the right of the workspace pages`（`packages/core/src/navigation.ts:166`），而**不挂面板的两面之一** `/create` 在已批准设计里恰恰叫「the Create workspace」（`apps/web/design-system/patterns/canvas/stitch-image-video-parity-spec.md:70` 的 `B. First-class Create workspace`；生产抬头 `apps/web/components/canvas/NorthstarHome.tsx:3` 用同一个词）。商家读到的是「工作区页面上都有」，而那个字面上叫 workspace 的页面偏偏没有；例外仍由后半句逐条点名（`navigation.ts:545`），所以整句不假，只是选词打架。**本轮只登记、不改词**——换哪一个词是产品口径题。**② P2-2 已落地**，见上一行④。**③ 本节同日 thread-activity 行第三段括注自相矛盾，本行说明**：那一行给出 `apps/web/lib/balance-refresh.ts:14-15` 与 `apps/web/lib/__tests__/balance-refresh.test.ts:8-9` 两个行号并引了原文，括注又说「那两段注释已整段改写」——既然改写过，那两个行号就不再指向被引的那句话，只能当**登记当日的历史快照**读。**现码复核（2026-09-05）**：旧引文 `#544 already flagged the existing 4s thread-activity poll as one too many` 逐字已不存在；今天 `balance-refresh.ts:14-18` 写的是 `#544 flagged one timer too many … the 4s thread-activity poll this comment used to name is gone`，`balance-refresh.test.ts:8-9` 是同义的华语复述。**通则**：本节所有行内的 `file:line` 一律按「登记当日的现码」读，主干前进之后以最新一次复核的那一行为准 | 登记 |
| 2026-09-05 | **Founder 2026-09-05 追认：Brand 的「预览效果」保持现状＝免费对照、不调模型、无需披露**（对应本节 2026-09-04「Brand 的『预览效果』偏离裁决四，待 Founder 追认」那一行）。**裁决原话经编排者代记**：AskUserQuestion 里选的是「追认现状：免费对照，不调模型」。**含义**：`previewBrandContextEffect`（`apps/web/lib/memory-actions.ts:417`，抬头注释 `:414` 逐字写「免费、无模型」）继续把这条草稿保存前后 Otto 真读到的两段品牌上下文原文并排摆出来，**不调模型、不花钱**，因此不落「披露先于扣费」那一层；那一行提的另一条路（改成调模型出样例文案）不做。**触发条件**：哪天真要调模型出样例，先在集中配置里给它一个单一权威价目，再按钱路口径补披露 | 编排者代记 Founder 2026-09-05 |
| 2026-09-05 | **Founder 2026-09-05 裁决 · 接线盘点 L4：Brand 的视觉资产 beta 不做**——logo／颜色／字体这三样资产本身、Visual guidelines 一节的示例媒体、以及把这一节链到 Library asset 的那条链接（已批准的品牌 pattern 在 `apps/web/design-system/patterns/brand/README.md:113`「管理 visual direction、logo usage、colors、type、imagery principles 与 approved / avoid examples」、`:114`「示例媒体只链接 Library asset IDs，不复制文件」、`:138`「Visual guidelines 链接 Library assets，不复制 media」三处画了它们）。**做法**：**保留现有的诚实说明句，不加空控件**——这一节的说明现码逐字是 `The visual direction Otto should follow in Creation.`（`apps/web/app/brand/BrandWorkspace.tsx:74`，夹具同句在 `apps/web/design-system/patterns/brand/BrandReference.tsx:65`）。它说的是「Otto 该跟的视觉方向」，本来就没承诺这里能存图、存色值或链素材，所以现状不说谎，一个字都不用改。**触发条件**：有商家开口要，或者另立规格 | 编排者代记 Founder 2026-09-05 |
| 2026-09-05 | **尾巴组十五落地汇总：登录页围栏回补与措辞、Auth 旅程真测试、删账号诚实文案（#1229 判官 P2-3／P2-1／P2-2、#1223 P2-2、尾巴清单 F3／F4）**。**① 邮箱步「发送失败不许翻页」那条页面侧围栏回补（#1229 P2-3）**：#1223 原有的 `FRONT-A12: stays on the email step and says why, instead of claiming a code was sent` 在 #1229 改写成环境口径时被净减掉了——环境那一组说的是「这个**部署**寄不出信」（判定在 env 上、按钮按不下去），与「部署能寄信、而服务端对这一次如实回绝」是两件事。现回补为独立一组 `FRONT-A12 — a refused request never becomes 'We sent a temporary login code'`（`apps/web/app/login/__tests__/login-code-resend.test.tsx`，`signInCodesAvailable: true` + 服务端 resolve 一个 `{status:"error"}`，断言仍站在邮箱步、Alert 说了原因、`requestSignInCode` 真的被调过一次）。**变异实证（本轮亲跑并还原）**：摘掉 `LoginForm.tsx` 里失败分支的 `return false` ⇒ 该文件 4 条红（含新这一条），改回即绿。**② `transport.test.ts` 的 FRONT-A2 从同义反复改成变异型（#1229 P2-1）**：旧写法 `emailDeliveryAvailable(env) === emailDeliveryAvailable(env)` 对**任何**实现都成立，包括按邮箱分叉的实现；现改成把五个地址塞进谓词够得着的每个位置（多出来的实参、env 上的地址形字段），能寄信与寄不出信两种部署各自的答案集合必须只有一个元素。**变异实证**：给 `emailDeliveryAvailable` 加第二个 `address` 参数并按前缀分叉 ⇒ 当场红。同时删掉旧的 `length <= 1` 断言并写明理由：`Function.length` 只数第一个默认值之前的形参，加地址形参后仍是 0，那是一条永远不会红的装饰。**③ 「生产设成 stub 即拒绝开机」改成实话（#1229 P2-2）**：这句此前被当成无条件保证写在四处注释、契约条目的 `summary` 一格与本表同日 #1229 那一行里，而 `AUTH_EMAIL_TRANSPORT` 没有打 `moneyInvariant`，`bootEnvDecision` 的 `FIKIRTIVE_ENV_CONTRACT=warn` 逃生门够得着它——**任何**带该开关起的生产进程（不只是 e2e）都会把这条判定降成一条警告、照常起来、照常一封信也寄不出去。四处注释（`apps/web/lib/email/transport.ts`、`packages/core/src/env-contract.ts` 的 `AUTH_EMAIL_TRANSPORT` 条目、`apps/web/lib/email/stub-adapter.ts`、`.env.example`）与该条目的 `summary` 字段（这一格会同步进契约报告）全部改成「默认拒绝，不是免疫」——**后两处注释与 `summary` 是落修轮补上的**（判官 #1237 P1-1：首轮只改了前两处，`stub-adapter.ts` 仍写着 `A serving production process never gets here`，被本 PR 自己新加的那两条围栏当场证伪；本行原写「两处注释」，与 `packages/core/src/env-contract.test.ts` 抬头写的「四处」自相矛盾，一并订正），并新增两条会红的围栏 `packages/core/src/env-contract.test.ts`（默认 ⇒ `exit`；带 warn ⇒ `warn`，都点名该变量），日后有人给它打 `moneyInvariant` 或收紧逃生门时第二条当场红、注释与本行随之要改。**④ 测试文件抬头改成现状（#1223 P2-2）**：`login-code-resend.test.tsx` 抬头还写着「登录码这一步的两条」，而该文件已是三组十条且覆盖邮箱步；改写成现在的三组各是什么，并写明 #1223 已由 #1229 取代并入主干。**⑤ Auth 旅程转正（尾巴清单 F3，`FRONT-A2`）**：注册与重置此前只有服务端测试（`lib/__tests__/signup-door.test.ts` 打 Better Auth 端点），商家真正按得到的那几屏零旅程覆盖。新增 `e2e/journeys/21-register-and-return.spec.ts`（注册 → 读产品真的寄出去的那条链接 → 验证并落回 `?from=/create` → 退出后用密码从 `/login?from=/create` 再进来落回 `/create` → 密码错时「有账号」与「无账号」两个地址读到逐字相同的一句）与 `e2e/journeys/22-reset-password.spec.ts`（忘记密码 → 走真链接 → 设新密码 → 新密码能登录且重置本身不发会话 → 无账号地址得到**逐字相同**的一句而背后一条 `reset-password:` 记录都没铸）。投递走的是 #1229 那条明写的假通道（`AUTH_EMAIL_TRANSPORT=stub`，`OFF_MACHINE_CREDENTIAL_NAMES` 一个字未动）。**为什么这两封信从 outbox 读而不是从库读**：注册验证令牌是签名 JWT，Better Auth 根本不落库；重置令牌虽落库，但商家点的是它包出来的那条 URL，在测试里重建那条 URL 等于自己伪造要读的邮件——所以读的是 stub 通道写下的那一份（产品自己的发件箱，一字未改，值不回显；套件 `workers: 1` 串行，读前先清槽）。`apps/web/lib/__tests__/front-baseline-acceptance.test.ts` 的 `FRONT-A2` 从 `it.todo` 转成指向真身的断言（两个旅程文件在、有会跑的用例、无 `.skip`、验收点名的每一环各有锚点）。**这一条能证明与不能证明什么（判官 #1237 P2-3，口径登记，不改代码）**：它读的是旅程文件的源码文本，证明的是「文件在、没被跳过、点名的环节各有锚点」，不证明旅程真跑绿；**这两条旅程真跑绿的唯一证据是 CI 的 e2e job**。**诚实口径**：规格 §2 写的「收到真实验证码邮件」，在没有邮件商钥匙的测试机上只能到「产品真的把信交给了投递通道、旅程读的是那条通道的产物」，最后一公里（Resend 真发信）留给 S5 在真环境走一次。**⑥ 删账号诚实文案（尾巴清单 F4）**：`apps/web/app/profile/DeleteAccountCard.tsx` 的文案缺「多久」这一格；现补齐四要素——① 这是给客服的请求（`This opens an email to support`，且明写 `Nothing is deleted automatically`）、② 删什么（`What goes:` 工作区与里面的作品）、③ 留什么（`What stays:` 账单与积分历史、审计记录）、④ 多久（没有自动删除、由人处理、处理完发邮件；备份里还会留一段时间）。**「留多久」不写第二个数字**：天数的唯一作者是 `/legal/data-deletion`（`about 30 days`），这一屏**链过去**而不是再抄一遍（§7.3）。**行为一字未改**：仍是输入自己的登录邮箱二次确认 → 打开一封发给 support 的邮件，组件不碰数据库。新围栏 `apps/web/app/profile/__tests__/delete-account-honesty.test.tsx` 4 条（真渲染、真按 Delete、读真 DOM，不用源码断言），钉四要素、钉「这一屏不许出现任何保留天数」、钉行为未变；**变异实证**：删掉「How long」那一句 ⇒ 当场红。**两件待 Founder**：㈠「客服多久处理完」今天没有权威来源（无已定 SLA），本轮写成不带数字的实话，请 Founder 给一个天数或确认维持；㈡ 自助删除按拍板原话「另立规格」，尚未开。**写集溢出四处**（本组清单点名的写集是 `apps/web/app/login/**`、`apps/web/lib/email/**`、env-contract 相关测试、本表）：`packages/core/src/env-contract.ts`（③ 那句不实注释的**产地**，只改注释与该条目的 `summary` 一行文案、零行为）、`.env.example`（③ 那句话面向运维的那一处，只改注释、零行为）、`apps/web/app/profile/DeleteAccountCard.tsx` 与其新测试（F4 的唯一落点）、`e2e/`（F3 的旅程与 `support/auth.ts` 的收件箱读法，旅程只能落在这里）。 | 2026-09-04（编排者代记 Founder 2026-09-04 20:45「删账号 → beta 先改诚实文案。写清这是给客服的请求、删什么、留什么、多久处理；自助删除另立规格」） |
| 2026-09-05 | **⑨ 段收口 · 起步页参考契约落地（清单 I1）**。**改前**：Create 起步页的「+ Add context」按裁决九「无契约的控件不出现」整颗不画——`createCanvasConversation` 只收 `{prompt, requestId}`，handoff 行只落 `{prompt, threadId}`，商家在这一页挑的任何东西 navigation 之后都会无声消失。**本刀建的契约**：① handoff payload 加**类型化引用**（`apps/web/lib/canvas-entry-actions.ts`，沿用 §7.3③ 引用选择器的 `{type, id}` 形状，不裸存字符串、不存图片 URL；形状不对整笔拒绝，不写半条 handoff）；② 读回那一刻**按 ownerId 重查归属**（`getCanvasConversationHandoff` 解成 `entityIds`／`sourceGenerationIds`／`referenceVideoGenerationIds` 三份，不是这个租户的或已删的一件都不挂——客户端自报的 id 只是定位参数）；③ 画布首轮消费**复用既有通道**：`ImmersiveCanvasEntry` 把三份交给已经存在的 `pendingFirst`，`OttoChatStream` 用手动送出用的**同一个** `composerReferencePayload` 映射进 body（引用在 body 里长什么样，全仓仍只有一个作者）。**三条路各自接的**：Upload image → 既有上传权威 `finalizeCandidateUploads`；Choose from Library → 画布 composer 那一个 `CanvasLibraryPicker`（读全店生成史，不需要 projectId）；`@` 引用 → 两个 Otto composer 都在用的 `useReferencePicker`（并沿用它自己的规矩：`@名字` 删掉引用就下车）。**Add URL 仍不接**，理由不变（全仓唯一按网址导入是 Otto 自己一轮里调的 `ctx.mediaImport.fromUrl`，没有 composer 能调的 server action）。**规格 §7.3⑨ 点名未解的 projectId 问题，本刀取「先建 project 再上传」**：新增 `ensureCanvasDraft`（同一个 requestId，所以随后 `createCanvasConversation` 收编的是**同一块**画布，改名而非再建）——**代价说清楚**：商家点了 Upload image、传完又不发送，画布史上会多一块名为 `New canvas` 的画布（里面真放着他传的那张图）。**钱引擎「披露先于扣费」那条验收的连带**（编号不在本表写，避免 M2 前缀撞车）：这一页从此能触发自动理解（一笔真钱），所以 `<UnderstandingCostHint />` 随之挂上，与另外几处上传口**同一个组件、同一句话**，且在文件选择器打开之前就在屏幕上；这一页仍然一个钱数都不写。**裁决五零触碰**：可见标题行「Create with Otto」与「Nothing paid starts…」整句仍然不在，两侧围栏照钉。**围栏翻向**：`create-design-parity.test.tsx` 的「Add context 在起步页不渲染」翻成「渲染，且 Add URL 仍不渲染」；新围栏 `apps/web/lib/__tests__/front-a14-create-start-references.test.tsx`（判官 #1242 P2-3 改名对齐编号——里面每一条判据都是 FRONT-A14；落修后 7 条：三条路各一条＋上传失败不留假芯片＋只收图片＋画布首轮 body 两条）与 `canvas-entry-actions.test.ts` 新增 7 条（payload 形状／形状不对整笔拒／草稿收编／草稿幂等／归属重查）。**写集溢出四处**（都是最小那一处）：`components/canvas/ImmersiveCanvasEntry.tsx`、`components/canvas/CanvasOttoOverlay.tsx`、`components/otto/OttoChatStream.tsx`（首轮消费落点，规格 §7.3⑨ 原文即写「落点在画布落地代码」）；`apps/web/lib/library-types.ts`＋`library-actions.ts`＋`library-subjects.ts`（「哪个扩展名算影片」此前抄了两份，handoff 读回要判同一件事，规则收进 `library-types.ts` 一处，那两处改成引用它，行为一字不变）；`understanding-disclosure.test.ts` 的挂点表加一行（新上传口必须登记）；`apps/web/lib/__tests__/immersive-canvas-entry.test.ts`（handoff 替身的返回值形状随类型一起变，并加了一条 FRONT-A14 断言——判官 #1242 P2-1：这一处此前漏在本行之外，「四处」由此才对得上）。 | 2026-09-05 |
| 2026-09-05 | **`FRONT-A15` 的「Add context」判据从源码字串改成真渲染数控件（清单 I2）**。**为什么改**：判官 #1194 P1-2 已经证过源码判据挡不住「插一颗真按钮」——旧版 `menuItemsOf` 扫的是 `<DropdownMenuItem …>` 这一种写法，任何别的写法（例如一个带 `role="menuitem"` 的裸元素）它一律看不见。**改成什么**：三边**各自真挂起来、把菜单按开、读 `[role="menuitem"]` 的可及名**再比集合——夹具 `design-system/patterns/canvas/CreationComposer.tsx`、画布的 `OttoChatStream`、起步页的 `StartSomething`（`apps/web/lib/__tests__/front-a15-design-parity.test.ts`，测试名逐字带 FRONT-A15）。**两次变异实证**（逐次实跑并还原）：① 往起步页菜单里插一颗写法不同的真菜单项（`<div role="menuitem">Add URL</div>`）⇒ 当场红（旧的源码判据对这一颗是绿的）；② 把画布菜单里的「Choose from Library」整项删掉 ⇒ 当场红。**顺带**：`create-design-parity.test.tsx` 的起步页控件集合也从「只有一颗发送键」改成「加参考＋发送两颗」，并把 `[hidden]` 的元素排除在控件集合之外（藏起来的文件选择器不是商家点的那颗键，它在不在由 FRONT-A12 那条单独钉）。**`front-a15-canvas-selection.test.ts` 本轮零改动**：核对后它里面没有任何「Add context」判据（那一格只活在 design-parity 那一份里），清单点它是误记。 | 2026-09-05 |
| 2026-09-05 | **清单 I1＋I2 落修（判官 #1242 裁 FAIL，两条挡点＋两条顺手）**。**① 起步页挂的引用，画布首轮那一发请求里真的有——这一环从前零测试**（P1-1）：判官把 `apps/web/components/otto/OttoChatStream.tsx` 里那段 `composerReferencePayload` 映射**整段删掉**，apps/web 全量 543 文件 / 7558 条测试逐字照绿——商家把图挂上、画布照开、第一轮照送，而那张图从来没上车，屏幕上也没有一个字说它掉了，正是本刀要治的那一件原病。补的围栏在 `apps/web/lib/__tests__/front-a14-create-start-references.test.tsx` 末组两条：**真挂 `OttoChatStream`**（`useChat` 换替身），`pendingFirst` 给三份 id，断言 `sendMessage` 第二参 `body` 同时带 `entityIds` / `sourceGenerationId(s)` / `referenceVideoGenerationId(s)`；另一条钉「没挂引用就不凭空造出这些键」。同一变异复跑当场红（本轮实证）。**② 起步页的参考上限改成与画布同一个口径**（P1-2，商家可见）：`apps/web/lib/canvas-entry-actions.ts` 的 `parseHandoffReferences` 从前按**合计** 8 件卡，而媒体侧封顶 8（`upsertComposerReference`）、实体侧不计入——商家挂 3 张图再 `@` 6 件实体（两边各自都没超）合计 9，整笔被服务端拒，屏幕上只有一句通用的「Couldn't start that Canvas — please try again.」，而「再试一次」永远不会成功。现在两本分开数：媒体 `MAX_OTTO_COMPOSER_REFERENCES` = 8、实体 `MAX_GEN_ENTITIES` = 8（画布首轮送 `/api/otto/stream` 时 `entityIds` 用的就是后者），去重前另加一道总长度闸只为封住白跑的循环。围栏在 `canvas-entry-actions.test.ts` 新增两条：3 图 + 6 实体照常开画布；任何一本自己超到 9 才拒。**③ 起步页上传只收图片，挑到影片当场说人话**（P2-4）：`accept` 只是文件选择器的提示，商家切到「所有文件」照样挑得到影片、上传权威本身也收（`ACCEPT_ATTACH` 含 `video/*`），芯片再用 `<img>` 去画它就是一个破图。`StartSomething.tsx` 的 `handleFilePick` 先按 `file.type` 挡一道，文案 `Videos go in from the Canvas — this page takes images.`，一步都不往上传权威走。**④ 测试文件改名对齐编号**（P2-3）：`front-a15-create-start-references.test.tsx` → `front-a14-create-start-references.test.tsx`（里面每一条判据落的都是 FRONT-A14）。**⑤ 上一行的两处登记订正**（P2-1）：写集溢出的第四处补上、新围栏文件名换成改名后的那一个，均已改在同表上一行内。**待 Founder 走查（P2-2，不改代码）**：起步页输入框下现在是两行灰字（`UnderstandingCostHint` 在上、`ConversationCostHint` 在下），而裁决②只点名松开了「输入框下加一行价钱」这一格；S5 走查时请把这两行一起过目，确认或裁掉。 | 2026-09-05 |

## 6. 改签记录

- 无

## 7. S2 施工稿（设计阶段产出；S1 正文 §0–§6 一字未动）

> S2 状态: 已批准
> S2 批准: https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/pull/1131 Founder 评论「S2 批准 frontend-baseline.md」(2026-09-02)
> S2 修订: 2026-09-03 增补（第⑨段、FRONT-A15、§7.3 各段施工细则、§7.4 写集互斥表）——**待 Founder 再次批准**，开工闸见 §7.5

### 7.0 范围与本场拍板（Founder 2026-09-02）

- **纯合并段先行、今天做**：只解冲突、不加功能，过全套机器验收与 Founder 视觉验收；Creation 引擎批 I 与之并行（写集互斥见 creation-engine.md §8.4）。
- **合并规则**（侦察实证 9 文件 10 块）：钱的行为以 main 为准——冲突块里 main 侧的业务逻辑与披露文案一字不改、取价函数不改；页面长相以分支为准——JSX 骨架、className、组件选型、`design-system/` symlink 基座照收。需要判断的只有一件：把 main 的钱字按原相对位置挂回分支的新布局（标签之后/选择器之前、确认按钮同排、输入框正下方）。
- **机器验收**：main 带入的四个 AST 级围栏（`understanding-disclosure.test.ts` 写点计数表、`money-a10-search-disclosure`、`money-a5-credits-never-expire`、`understanding-quote-copy`）＋全量 apps/web 测试＋typecheck＋production build＋e2e。写点计数若因重构改变＝需 Founder 签字的改动，不得顺手改数字。
- **分支占用**：`codex/uiux-frontend` 被 Founder 的 4232 工作树占用，施工开新分支 `claude/frontend-baseline-merge`（从 `origin/codex/uiux-frontend` 起，merge `origin/main`）。
- **评审夹具路由**：`/product-patterns/*` 与 `/design-system/*` 今天无守卫；纯合并段即加生产构建 `notFound()`（FRONT-A12），不等后段。
- **零迁移、零新环境变量、lockfile 无冲突**（侦察实证）：合并后 `pnpm install --frozen-lockfile` 直接可用；需跑的迁移只有 main 带来的 `20260901150000_understanding_billing_snapshot_and_paused_balance`。
- **施工顺序（2026-09-03 修订，据 §5 同日「裁决一」）**：「对齐轮」与「四大块」**并行**，不排队。每一段一个独立 PR、一位独立判官（跨厂复审，同族不算）；写集互斥表见 §7.4——第⑨段的写集与 ②③④⑤⑦ 的目录**分开列**，并行才不会互相解冲突。哪些段今天就能动、哪些段要等 Founder 再批一次，见 §7.5。

### 7.1 施工切段

| 段 | 内容 | 验收行 |
|---|---|---|
| ① 纯合并 | 上述规则解 9 处冲突；`globals.css` 保留分支 symlink、把 main 的改动搬进 `design-system/foundations/globals.css`；`.claude/CLAUDE.md` 两边都留；夹具路由加守卫；四处 server 邻接改动逐条对照 main 复审并补行为测试 | A1、A12、A13 |
| ② Library | `Collection`/`CollectionItem` 迁移；Elements 用现有 Entity；收藏、搜索、Use in canvas 接真 | A5、A6、A7 |
| ③ 引用选择器 | 统一 reference search（租户内、分页、类型化 ID＋来源）；消息保存真实引用 ID；Official avatar 类别经 `catalogKey` 官方角色点亮（creation-engine.md §8.1③） | A10 |
| ④ Brand | 五分区落 `BrandRecord`/`Memory`；谁改/何时改列；`getBrandContextText` 覆盖五分区 | A8、A9 |
| ⑤ Home | 布局持久化（org 级一行，迁移）＋`Manage home` capability；多来源 aggregate 带 freshness | A3、A4 |
| ⑥ Auth | 本地接 Resend 测试 key；注册/验证码/回跳/重置旅程 | A2 |
| ⑦ Settings/Billing | 新壳上复核现有真能力；无契约控件不出现 | A11 |
| ⑧ Founder 验收 | 六面登录态走查；差异登记 §5 | A14 |
| ⑨ Canvas/Create 按设计接线（2026-09-03 新增） | **已合入**：摘掉白底覆盖层、工具条让位（PR #1144，main `52a93a4a`）。本段要做的：卡片 4:5 与「名称／版本」页脚；节点操作条收敛为设计的 5 个图标（Edit with Otto／Create variations／Animate／Download／More actions——Download 与 Share 后端已有能力，本段补齐挂上）；composer 补「+ Add context」三选（Upload image／Choose from Library／Add URL）并把计费说明改为**按需披露**（不是常驻一行）；对话历史收成 280px 紧凑列表；Create 起步页文案／几何／发送键／焦点环按设计，「+ Add context」接线（据裁决五去掉「Create with Otto」标题行与「Nothing paid starts…」句）。**不显示**：Frame select 与 撤销／重做（裁决六，无对应功能）。**不在本段**：付费确认收回 Otto 状态卡的 needs-confirmation 状态机——那条触钱路，另立小规格再动 | A14、A15 |

**§7.1 新增验收行**（S1 §2 的验收表冻结不动；本行由 2026-09-03 的 §7 修订引入，随本次 S2 修订一并呈批，S5 时与 §2 各行同等对待）：

| 编号 | 商家做 X | 看到 Y |
|---|---|---|
| FRONT-A15 | （工程演示）跑 Canvas 与 Create 两面的设计对照测试 | 两面渲染出的控件集合与设计夹具（`design-system/patterns/canvas/CanvasReference.tsx`、`CreateWorkspaceReference.tsx`、`CreationComposer.tsx`）逐控件一致：节点操作条 5 个图标、composer 的「+ Add context」三选、卡片 4:5 与「名称／版本」页脚、Create 起步页没有「Create with Otto」标题行与「Nothing paid starts…」句；裁决六点名不显示的两个控件（Frame select、撤销／重做）两边都不出现 |

### 7.2 本地起动（纯合并段收尾即跑）

新 worktree → `merge origin/main` → 解冲突 → 从主检出复制 `.env.local` 与 `apps/web/.env.local`（只复制，不改值）→ `pnpm install --frozen-lockfile` → `pnpm --filter "./packages/*" build` → `prisma migrate deploy` → `next dev`；worker 同起。Founder 用主检出已有账号登录走 Create → Canvas → Library。

### 7.3 2026-09-03 对照审计后的施工细则（②③④⑤⑦ 增补；⑨ 的细则写在 §7.1 表内）

审计法：设计夹具（`apps/web/design-system/patterns/*` 的 Reference 组件与 fixtures）对生产路由逐面并排走查；结论三级（外壳一致／结构不同／没接线）与九项裁决见 §5 的 2026-09-03 各行。下面每段写四件：**要建的后端对象与迁移 / 依赖 / 切片顺序 / 工程量**。工程量是一人日估算，含测试与判官落修，不含等 Founder 走查的时间。

#### ② Library（估 3–4 天）

**后端对象与迁移**

- `Collection` 与 `CollectionItem` 两表（§1 九问 4 已定）：都带租户列（本仓库为 `ownerId`）外键与「同一 collection 内同一对象只一行」的唯一约束；删 collection 只删 membership，成员对象留在 Library（A6 明写）。
- **收藏是跨类型的——Founder 2026-09-03 已裁（裁决十）：新建一张统一收藏表**。设计的 Favorites 是把「生成结果」与「上传」混在一起的一个列表（`patterns/library/model.ts` 的 `LIBRARY_VIEWS`），而今天只有 `Generation.favorite` 一列（§1 九问 4 记的现状），上传（`Asset`）无处可存收藏。落法：新建 `Favorite(ownerId, subjectType, subjectId, createdAt)` 一张表**不分素材类型**，记「谁收藏了哪个素材」，带租户列（本仓库为 `ownerId`）约束与 `(ownerId, subjectType, subjectId)` 唯一约束；收藏页**一次查询、按时间排**。`Generation.favorite` 施工内一次性回灌进新表，之后只作读时回落。备选「给 `Asset` 补一列、两类各存各的」**已否决**（Favorites 视图会变成两次查询＋应用层合并排序，游标还要各算一套）。这一条原是 §1 九问 4 没覆盖的缺口，已由裁决十补齐。
- **生成历史改游标分页读模型**：设计的 History 是按时间分组（Today／Yesterday／Earlier this month）的长列表；今天 `getGenerationHistory` 一次性取。改成 `(createdAt, id)` 复合游标的服务端分页，分组在读模型里算好（时区按 org 设置），前端不算日期。
- **Uploads 独立身份**：Uploads 视图读 `Asset`，不混进生成历史；两条列表各自的游标互不影响。
- **Elements 只读**：Products／Characters／Locations 读现有 `Entity`；Official avatars 由 Creation 引擎的 `Entity.catalogKey` 点亮（creation-engine.md §8.1③）；Clothes 在演员库造型 preset 交付前**整格不出现**，不摆空态占位（裁决九）。

**依赖**：Official avatars 一格依赖 Creation 批 I 的 `catalogKey` 迁移（施工时核对它是否已在 main）；其余无新依赖。

**切片顺序**：① 游标分页读模型＋History／Uploads 两视图（**属对齐轮**，只接现有资产，今天可动）→ ② 收藏表与 Favorites 视图 → ③ `Collection`／`CollectionItem` 与 Collections 视图 → ④ Use in canvas 复核（现有 `createCanvasNode` 沿用）。

**验收落点**：A5（①②）、A6（③）、A7（④）。

#### ③ 引用选择器（估 2–3 天）

**今天的事实**：`@` 有**两套实现**，而且两套都只认 `Entity`——画布用 TipTap 的 `MentionInput`（`apps/web/components/MentionInput.tsx`，建议项类型写死成 `EntityDTO["type"]`），Otto 前门与聊天用纯文本的 `activeMentionQuery` ＋ `OttoMentionPopover`（`apps/web/components/otto/OttoFrontDoor.tsx:23,144-145`，前端内存 `filter`）。设计要七种类型（product／character／official-avatar／location／clothes／generation／upload，见 `patterns/reference-picker/model.ts`）。

**要建的后端对象**

- **统一 reference search**：一个服务端动作，租户内、按类型过滤、游标分页，返回**类型化 ID**（`{type, id}`，不是裸字符串）与来源标签，覆盖 `Generation`／`Asset`／`Entity` 三个源。两套 UI 先收口到这一个数据源，再收口成一个组件。
- **消息保存真实引用 ID**：施工时核对 `ChatMessage` 的引用字段，缺则加列（迁移）；发送后可从消息回链到那个对象（A10 明写）。

**依赖**：② 的生成／上传读模型（类型化 ID 与游标从那里来）；Official avatar 一类依赖 Creation 的 `catalogKey`。

**切片顺序**：① 服务端 reference search＋类型化 ID（后端，可与 ② 并行起步，联调排在 ② 第①刀之后）→ ② 两套 `@` UI 收口成一个组件（**这一刀里的「@ 小片」外观对齐属对齐轮**）→ ③ 消息落引用 ID 与回链。

**验收落点**：A10。

#### ④ Brand（估 4–5 天）

**分节映射（六 → 五）**：生产今天是 Founder 2026-07-02 批的六节（`packages/core/src/memory-sections.ts` 的 `SECTIONS`），设计是五节（`patterns/brand/model.ts` 的 `BRAND_SECTIONS`）。

| 生产旧节 | 设计新节 | 依据 |
|---|---|---|
| About the brand（about） | Brand voice | **Founder 2026-09-03 裁决十一** |
| Your customers（customers） | Audiences | **Founder 2026-09-03 裁决十一** |
| Your products（products） | Knowledge base | **Founder 2026-09-03 裁决三点名** |
| Your offers（offers） | Knowledge base | **Founder 2026-09-03 裁决三点名** |
| Look & feel（look） | Visual guidelines | **Founder 2026-09-03 裁决十一** |
| Do & don't（rules） | Style guide | **Founder 2026-09-03 裁决十一** |

六条映射**全表已裁**（产品／优惠 → Knowledge base 出自裁决三，其余四条出自裁决十一），施工照落，没有待确认项。迁移只改「这条记录归哪一节」，**不删任何行**；`sectionForCategory` 的 LEGACY 表同批扩写，旧 category 字符串继续解析得出新节。

**要建的字段与迁移**（设计的 `ContextRecord` 要 source／sourceDetail／status／usage／history，今天一个都没有）

- `updatedById`（谁改的）：A8 要求每条显示「谁改的、何时改的」。今天 `Memory.source` 与 `BrandRecord.source` 只有 `'otto' | 'user'` 两个值——答得出「是不是 Otto 改的」，答不出「是谁」；`updatedAt` 已有。两张表各加一列。
- `origin` ＋ `originDetail`（设计的 source／sourceDetail：手写／来自某个网址／来自某份上传）：与上面的 `source` 不是一回事，**不复用同一列**。
- `contextStatus`（设计的 Ready／Draft／Processing）：今天 `BrandRecord.status` 是 `'active' | 'archived'`（归档语义），两套语义**不复用同一列**。
- 改动史：新建一张 revision 表（带 `orgId` 与租户约束）。今天只有 `deletedAt` ＋ restore，答不出「改过什么」。
- `usage`（这条记录被哪些面用到）：**由读模型算，不落列**——落列就会与真相不同步。

**草稿流动作链（裁决四）**：`addSource → extract → draft → preview → confirmSave` 五个服务端动作。确认之前只写 `contextStatus='Draft'` 的行；`getBrandContextText` 只取 `Ready`，草稿因此永远进不了 Otto 上下文。`preview` 要调模型，那笔小额成本按钱引擎自动理解的计费口径（验收 A9：披露先于扣费、按动作时刻价目、我方故障重试同一素材同一阶段只计一次）在**点预览之前**披露。

**依赖**：钱引擎自动理解计价（已在 main，商家可见面见 `apps/web/app/billing/page.tsx:207`）。

**切片顺序**：① 五节映射＋迁移（数据先落位）→ ② 谁改／何时改与 revision 表 → ③ 草稿流五动作＋预览披露 → ④ `getBrandContextText` 覆盖五节的行为测试。

**验收落点**：A8（①②③）、A9（④）。

#### ⑤ Home（估 3–4 天）

**后端对象与迁移**

- **Customize home 落库**：`OrgHomeLayout`（租户列（本仓库为 `ownerId`）上有唯一约束、有序的 `componentIds`、`updatedAt`、`updatedById`）——设计的可定制项是 8 个组件 id（`patterns/founder-home/model.ts` 的 `HOME_COMPONENTS`），落的是**顺序 ＋ 勾选**两件事，一行一 org（§1 九问 4 已定「org 级一行」）。入口由具体 capability `Manage home` 判定，不看角色名（A4）。
- **ready 读模型要真实生产者**：`MarketingHealthReadModel`（`apps/web/lib/home-marketing-health.ts`）今天有 not-configured／insufficient／partial／ready 等状态；设计的 ready 版面要 8 个组件各自的数字。**没有真实生产者的组件不出现**（裁决九），不摆空卡、不摆占位数字。
- **partial 单源版面先做**：多数商家开局只连一个渠道，partial 才是常态；先把 partial 单源版面做完整（`MarketingHomeView` 已有 partial 分支），ready 多源版面随生产者逐个点亮。

**依赖**：连接状态五态读模型已接真（§1 九问 4）；Otto 页面上下文 chip 归 Otto 引擎，本段不做（§3「不做」）。

**切片顺序**：① partial 单源版面按设计对齐（**属对齐轮**，不需新后端）→ ② `OrgHomeLayout` 迁移＋Customize home 落库＋capability → ③ ready 版面按真实生产者逐个点亮。

**验收落点**：A3（①③）、A4（②）。

#### ⑨ 下一刀 · 起步页参考契约（估 1–2 天；2026-09-03 §5 登记）

**现状与证据**：Create 起步页 composer 已按设计对齐（PR #1151），但「+ Add context」（Upload image / Choose from Library / Add URL）本轮**不显示**，按裁决九「无契约的控件不出现」处置。根因：起步页没有把参考带进画布的通道——`createCanvasConversation` 的 handoff 只落 `{prompt, threadId}`（`apps/web/lib/canvas-entry-actions.ts:87`）；夹具 `design-system/patterns/canvas/CreationComposer.tsx` 的上传回调只是 `onReferenceChange(file.name)`（:101 附近），不真上传（PR #1151 报告「设计有、生产暂不显示」表）。

**要建的接线**

- **handoff payload 加引用字段**：`canvas-entry-actions.ts` 的 `createCanvasConversation` 写入的 `payload`（今天是 `{prompt, threadId}`）扩成带引用列表的形状，类型化 ID 沿用 ③ 引用选择器的 `{type, id}` 形状，不裸存字符串。
- **画布侧消费**：新会话第一轮要把这些引用挂上去，不能只挂 prompt——落点在画布落地代码（PR #1150 合入后的节点/composer）。
- **起步页 Upload image / Choose from Library 接线**：Upload image 走现有 `finalizeCandidateUploads`，但起步页此刻还没有 `projectId`（PR #1151 报告已指出）；Choose from Library 复用 Library 页自己的挑选动作（画布侧的 Otto composer 已经在用同一动作，见 PR #1150 报告）。
- **Add URL 仍不接**：全仓唯一的按网址导入是 `ctx.mediaImport.fromUrl`（`apps/web/lib/otto-media-port.ts:207`），是 Otto 自己一轮里调的工具，没有 composer 能调的 server action；触发条件不变——出现客户端可调的按网址导入动作。

**依赖**：PR #1150（Canvas 表面按 pattern 对齐）合入 main——画布侧的消费逻辑要挂在合入后的节点/composer 代码上。

**工程量**：估 1–2 天（人日，含测试）。

**验收落点**：A14；A12（起步页不出现假按钮的围栏，PR #1151 已落）不变。

#### ⑦ Settings/Billing（估 2 天）

- **外壳级对齐**：Settings 四节（Profile／General／Connections／Billing & credits）在导航权威源里已经有（`packages/core/src/navigation.ts` 的 `SETTINGS_SECTIONS`）；生产上四节是四个独立路由页，设计是「左轨 ＋ 右侧内容」的一体内壳。本段做外壳，不动各节自己的能力。
- **去卡片化裸表单**：生产用 `Card`／`CardHeader`／`CardTitle` 包每一块（`apps/web/app/settings/page.tsx:27-30`），设计是 `<section>` ＋ 一条 `divide-y` 边框行列表、没有 Card；按设计改。
- **Connections 真实厂牌 logo**：设计的 `WorkspaceConnection` 带 `icon`（`patterns/settings/model.ts`），生产今天没有厂牌图形。补真实 logo 资产，走 `apps/web/design-system/brand/` 的资产路径，不散落进 `public/`。
- **Billing 按裁决二**：按设计两节的排版与组件重画，但**保留主干三条花钱披露**——余额卡与「Credits don't expire」（`apps/web/app/billing/page.tsx:131`）、Spend cap（`app/billing/SpendCapCard.tsx`）、Auto-understanding（`:207`）、Web search in chat（`:227`）。设计的「Plan & payment」（付款方式、发票）**整节不出现**，也不放占位（§3「不做 subscription plan / payment method / invoice」原本就这么写）。

**依赖**：无新后端对象——这正是本段能整段进对齐轮的原因。

**切片顺序**：① 外壳＋去卡片化（**属对齐轮**）→ ② Connections logo → ③ Billing 重画，并补一道「四条披露仍在位」的围栏测试。

**验收落点**：A11。

### 7.4 写集互斥表（并行开工的前提；谁后合并谁解冲突）

| 段 | 写集（目录／文件） |
|---|---|
| ⑨ Canvas/Create | `apps/web/components/canvas/`、`apps/web/components/start-something/`、`apps/web/app/create/`、`apps/web/components/otto/panel/` |
| ② Library | `apps/web/app/library/`、`apps/web/components/library/`（新建）、`apps/web/lib/data.ts` 的历史读取段、prisma（Collection／CollectionItem／收藏 迁移） |
| ③ 引用选择器 | `apps/web/components/MentionInput.tsx`、`apps/web/components/otto/OttoMentionPopover.tsx`、`apps/web/lib/otto-mentions.ts`、新建的 reference-search 服务端动作、prisma（ChatMessage 引用列） |
| ④ Brand | `apps/web/app/brand/`、`apps/web/components/brand/`、`packages/core/src/memory-sections.ts`、`apps/web/lib/brand-record-actions.ts`、`apps/web/lib/memory-actions.ts`、prisma（Brand 各列与 revision 表） |
| ⑤ Home | `apps/web/app/(home)/`、`apps/web/components/home/`、`apps/web/lib/home-marketing-health.ts`、prisma（OrgHomeLayout 迁移） |
| ⑦ Settings/Billing | `apps/web/app/settings/`、`apps/web/app/profile/`、`apps/web/app/billing/`、`packages/core/src/navigation.ts` |
| 交集与规矩 | `packages/db/prisma/migrations/` 各段目录名不同即不冲突，但 `schema.prisma` 只有一份——后合并的那段负责重贴自己的 model 并重跑迁移校验。`apps/web/design-system/` 对所有段**只读**：任何段都不得改夹具去迁就实现（裁决九） |

### 7.5 批准与开工闸（本次 §7 修订）

- 本次 §7 修订（第⑨段、FRONT-A15、§7.3、§7.4）**需 Founder 在本 PR 评论「S2 批准 frontend-baseline.md」再次批准**，各段方可开工。
- **已放行的例外**：裁决一里的「对齐轮」中**不涉及新后端对象**的项目——⑨ Canvas/Create 接线、Create 与设置换皮（⑦ 的第①②刀）、素材库接现有资产（② 的第①刀）、@ 小片外观（③ 第②刀的对齐部分）、登录破图、面包屑——Founder 2026-09-03 裁决一已放行，不等本次批准。
- **原先要回呈的两件，Founder 2026-09-03 已裁**（§5 裁决十、十一），施工照落，不再是待确认项：①Favorites 跨类型收藏 = **新建一张统一收藏表**（不分素材类型、租户约束、收藏页一次查询按时间排；「给 `Asset` 补一列」否决）；②Brand 六节→五节映射**全表落定**（about→Brand voice、customers→Audiences、look→Visual guidelines、rules→Style guide、产品／优惠→Knowledge base）。
- **不在本次修订范围**：付费确认收回 Otto 状态卡的 needs-confirmation 状态机（触钱路，另立小规格）；剪辑拼接（裁决七，不属阶段一）。
