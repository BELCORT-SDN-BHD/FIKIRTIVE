# 登录门（照 Linear 模型）规格书（S1）

> 状态: 已冻结 · v1
> 批准: https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/issues/1260 Founder 评论「S1 批准 sign-in.md」(2026-09-08)
> 规格前缀: SIGNIN（验收编号 = SIGNIN-A1、A2…，全仓不得与其他规格撞前缀）

## 0. 一句话

登录照 Linear：一个登录页、两扇门——**Continue with Google** 与 **Continue with email**（邮件里一条登录链接＋一个可手输的 6 位码）。陌生人和老用户走同一扇门，第一次来就开好账号；密码注册、密码登录、忘记密码整体下线。商家因此不用记密码，也不会再有「别人先用我的邮箱设了密码」这种事。

## 1. 九问（S1 grill 的答案）

1. **商家做什么动作、看到什么结果？**
   - 打开登录页，看到两个按钮：Continue with Google、Continue with email（下面一个邮箱输入框）。没有密码框，没有「Forgot password?」，没有第二个「注册」页。
   - 按 Google：跳到 Google 选账号 → 回来直接进产品。第一次来的人此刻账号与工作区已经建好，和老用户一样落在首页（或原本要去的深链）。
   - 按 email：输入邮箱 → 页面说「We sent a temporary login code to …」并给一个码输入框 → 邮件里有一个 **Log in** 按钮和一个 **6 位码**。点按钮：登录页打开、码已填好，按一次 Continue 就进去；或者手输 6 位码。第一次来的人此刻账号与工作区已经建好。
   - 举例：商家 Aisha 拿 Gmail 来，按 Google，十秒后已经在画布上；她的同事用公司邮箱，按 email、收码、输码，同样十秒。两个人都没有注册过任何东西。
2. **入口在哪里？（列全，含深链）**
   - `/login`（唯一的门；任何需要登录的深链都转到 `/login?from=<原地址>`，登录后回原地址；`from` 经 `apps/web/lib/safe-redirect.ts` 的 `sanitizeCallbackURL` 只接受站内相对路径）。登录墙本身是 `apps/web/proxy.ts`，由 `AUTH_ENABLED` 开关（生产默认开；非生产须显式 `AUTH_ENABLED=true`）——本地验收必须开着它。
   - 邮件里的登录链接：打开 `/login`，邮箱与码预填（码放在 URL 片段 `#` 里，不进服务器日志、不随 Referer 外泄），商家按一次 Continue。
   - `/signup` → 永久转到 `/login`（旧链接不能断）。`/forgot-password`、`/reset-password` → 下线（转到 `/login`）。
   - Google Cloud 端（外部系统，冻结前由 Founder 或执行者在控制台核一次，不当既定事实）：OAuth 用户端「FIKIRTIVE」发布状态「正式发布 · 对外」，回跳网址含 production 与 staging 两条。2026-09-08 已在 Founder 登录的控制台里看过一次，仓库无法证明。
3. **四态：空、加载、错误、成功各长什么样？**
   - 空：登录页两扇门。若「暂停新注册」开关打开，页顶多一条横幅：「New signups are paused right now. Existing accounts can still log in.」
   - 加载：按钮文案变 Sending… / Signing in… / Redirecting…（沿用现有）。
   - 错误：码错或过期 →「That code didn't work. Check the email or send a new one.」；一小时内第 6 次要码 →「Too many codes requested. Try again in an hour.」；Google 取消、失败、或 Google 报该邮箱未验证 → **回到 `/login`** 并提示「Google sign-in didn't complete. Try again or use email.」。被撤销的邮箱、暂停期间的陌生邮箱：页面反应与「码错」完全一致，不说明原因（防枚举）。**任何拒绝都不得落在 better-auth 自带的错误页或裸 JSON 上**（今天 Google 门的拒绝正是这两种，见 §1.4）。
   - 成功：进 `from` 或首页。首登与老用户看到的是同一个首页，没有新手引导页（Founder 2026-09-08 裁决：不抄 Linear 的引导页）。
4. **数据从哪来、写到哪去？**（下列行号以 main `0e1f2ab3` 为准，2026-09-08 研究员四份事实册交叉核验）
   - better-auth 自带表：`BetterAuthUser` / `BetterAuthAccount` / `BetterAuthSession` / `BetterAuthVerification`（`packages/db/prisma/schema.prisma:1304-1353`）。
   - **码门建号时机**：better-auth 的 `emailOTP` 插件在验码成功那一刻为陌生邮箱建用户，`emailVerified: true`（`better-auth/dist/plugins/email-otp/routes.mjs:404-409`；我们没设 `disableSignUp`，库本来就允许）。今天拦住陌生人的是我们自己的三处名单检查：前门中间件（`apps/web/lib/better-auth/server.ts:307-309`）、寄码钩子（`server.ts:495`）、寄信队列（`apps/web/lib/better-auth/sender.ts:481`）。本规格把这三处的判定统一收窄为「已撤销 → 拒；暂停中且从未登录过 → 拒；其余放行」。
   - **Google 门建号时机**：回调一次请求内完成建号＋建会话（`better-auth/dist/oauth2/link-account.mjs:29-40, :126`）。今天拦住陌生人的是 `databaseHooks.user.create.before` 里的 `assertAllowedEmail`（`server.ts:359`）与 `session.create.before`（`server.ts:375`）。**实现陷阱（两名研究员各自实跑证实）**：数据库钩子里读到的 `ctx.path` 是路由模板字面量 `"/callback/:id"`，不是 `"/callback/google"`；供应商名只能从 `ctx.params.id` 取，缺失时 fail closed。写成 `"/callback/google"` 的判定永远不会触发，而 grep 字符串的测试还是绿的。
   - **Google 账号合并**：`accountLinking: { enabled: true, trustedProviders: ["google"], requireLocalEmailVerified: true }`（`server.ts:86-92`）。码门建的用户 `emailVerified = true`，所以同邮箱之后按 Google 会合并到同一个用户（`link-account.mjs:17-40`），这就是 SIGNIN-A3 成立的机制。密码下线后不再存在「未验证的本地账号」，`account_not_linked` 死胡同随之消失。
   - **Google 的邮箱验证声明今天根本没人看**：`trustedProviders: ["google"]` 让库跳过 `userInfo.emailVerified` 的检查（`better-auth/dist/oauth2/link-account.mjs:20-22`），而 `server.ts:90` 的注释却说它在检查。本规格要求把 `"google"` 从 `trustedProviders` 移除（保留 `enabled` 与 `requireLocalEmailVerified`），让库真的校验该声明，并删掉那句注释。
   - **Google 报 `email_verified: false`**（透传自 `@better-auth/core/dist/social-providers/google.mjs:97`）：按现码会建出一个 `emailVerified=false`、没有租户、也收不到验证信的孤儿用户（`apps/web/lib/better-auth/converge.ts:35` 早退；`emailVerification.sendOnSignUp` 未配置）。本规格要求：**这种 Google 账号一律拒绝并回登录页提示改用 email**（SIGNIN-A13）。Google 现实中何时会给 false 仓库无法验证，按 fail closed 处理。
   - **拒绝如何回到登录页**：今天 `LoginForm.tsx:214-217` 调 `signIn.social` 不传 `errorCallbackURL`，闸 1 的拒绝落在 better-auth 自带错误页（`/api/better-auth/error?error=…`），闸 2 的拒绝是没有 Location 的 403 JSON（研究员端到端探针实测；`gate.ts:10` 抛的 `APIError` 没有 `code` 字段，`callback.mjs:153-156` 的守卫因此不转重定向）。本规格要求传 `errorCallbackURL: "/login"` 并把 `app/login/page.tsx:15-20` 的错误映射改成实际会出现的键（SIGNIN-A14）。
   - 「注册即邀请」沿用 #543：第一次成功登录（两扇门都算）把邮箱写进 `AllowedEmail`（status active，invitedBy 标明来源门；`apps/web/lib/signup-gate.ts:39-46`），`revoked` 行永不复活（`skipDuplicates`）。邮箱归一化必须与 `signup-gate.ts:40` 同款 `trim().toLowerCase()`——`AllowedEmail.email` 没有大小写不敏感的唯一约束（`schema.prisma:850-856`，长期修法挂 #578）。
   - 首登副作用沿用现有 `convergeIdentity`（`server.ts:366` user.create.after → `converge.ts`）→ `bootstrapPersonalOrg`（`apps/web/lib/auth-guard.ts:207`）：确定性 `org_<userId>`、Membership、`grantCreditsTx(SIGNUP_GRANT_CREDITS, idempotencyKey: signup:<orgId>)`、`seedActorLibrary`。赠金一人一次由数据库唯一约束保证（`schema.prisma:909`），与登录方式无关。**工作区名**：现行规则「没收集店铺名的门，工作区就没有名字」（`auth-guard.ts:249-256, :272`）；Google 会把个人姓名带进 `User.name`，若不处理工作区会以人名命名。本规格定：**两扇门首登的工作区名一律为空**，商家在设置页填店铺名（SIGNIN-A10）。
   - **事务事实**：`prismaAdapter` 未开 `transaction`（`server.ts:79`），建号中途失败会留下孤儿 `BetterAuthUser` 行。本规格要求「同一邮箱再试一次能成功、库里只有一个用户」（SIGNIN-A15），不要求原子性。
   - 密码凭据退役：`BetterAuthAccount` 中 `providerId = "credential"`（better-auth 给密码凭据的固定标记，`better-auth/dist/api/routes/password.mjs:154`）的行由一条 prisma 迁移删除（零正式用户，Founder 2026-08-01；只影响测试账号），fresh-database 验证通过。`emailAndPassword.enabled` 改 false，`HOURLY_PUBLIC_DOORS`（`apps/web/lib/public-auth-doors.ts:33-37`）里的密码门一并撤下。
5. **碰不碰钱路？** 只碰赠金的**幂等键**，不碰金额与计费。今天一人一工作区一笔 `SIGNUP_GRANT_CREDITS`（`packages/core/src/spend.ts:582`，键 `signup:<orgId>`），而码门对陌生人打开后，`me+001@gmail.com`、`me+002@gmail.com` 都落进同一个真实邮箱，每个号都能领——1000 个号约等于 25,000 点、约 875 美元的真实供应商花费（审计 [3]，P1）。本规格要求：赠金幂等键改按「归一化邮箱」算（去掉 `+tag`，gmail 去点），再叠一个全站每小时新账号上限＋告警（SIGNIN-A17）。Turnstile 之类的人机验证不在本规格，登记 DEFERRED。验收里的金额一律写常量名，不写死数字。
6. **权限与租户边界是什么？**
   - 租户身份只来自服务端会话；回跳地址只接受站内相对路径（`apps/web/lib/safe-redirect.ts`，现有）。
   - 门的判定顺序（两扇门一致，三处名单检查同一函数）：① `SIGNUPS_PAUSED` 打开且邮箱从未登录过 → 拒；② `AllowedEmail.status = revoked` → 拒；③ 其余放行并建账号。`FOUNDER_ADMIN_EMAILS` 的超管种子逻辑不变。deny-by-default 的口径从「不在名单一律拒」收窄为「未撤销＋未暂停即放行」，撤销仍然绝对。
   - 撤销：操作员把某邮箱标 `revoked` 后，两扇门都进不来，**而且他手上已登录的会话在同一事务里被删除**（照 `apps/web/lib/tenant-actions.ts:54` 的写法）。今天的撤销只删名单不删会话，旧 cookie 还能打 `/api/better-auth/*` 最长 7 天（审计 [5]）。后台的「撤销」按钮今天只认 `status = invited` 的行（`tenant-actions.ts:164`），两扇门写入的都是 `active`，所以必须放宽到 `status ≠ revoked`，否则自助进来的地址永远撤不掉（审计 [6]）。
   - `AllowedEmail` 从此只有两个含义：「已证明拥有该邮箱的人」（两扇门在验证之后才写入）与 `revoked` 黑名单；它不再是邀请名单（审计 [24]）。
   - Google 回调是 GET，不在每小时配额之内（`apps/web/app/api/better-auth/[...all]/route.ts:24` 裸转发）；它由 Google 那关把守，本规格接受。
7. **参考对照：抄哪家？**
   - Linear 登录流（同一扇门、Google／Email 登录链接＋手输码、无密码）：https://mobbin.com/flows/2b9e0315-3654-482e-b877-f8da3736939f
   - Notion（先发码、验证后才能设密码，本规格不做密码）：https://mobbin.com/flows/7a12675f-80fc-4608-9feb-7ec412cf55c5
   - Slack（Google／Apple 或 6 位码，密码只是次要路径）：https://mobbin.com/flows/73ce2adc-fbee-4d1d-a200-6997380b12b5
   - 三家共同点：先证明邮箱是你的，才允许任何凭据存在。
8. **胃口：轻／中／重挡，为什么？**
   - 重挡：碰登录边界（M1 路径地板 `scripts/ci/process-gates.sh:39` 命中 `auth`），且退役一套实现（密码门）。实现 PR 必须引用本规格，不可能走轻改。
   - 胃口两天（含测试与 E2E 旅程）。超过就先砍「邮件里的登录链接」，只留 6 位码（Linear 的「Enter code manually」本来就是等价路径），其余不砍。
   - **上游依赖**：`docs/specs/frontend-baseline.md`（已冻结 · v1）§1 把「邮箱密码、忘记/重置」列为既有 Auth 能力，FRONT-A2 的验收旅程含密码注册与重置。本规格落地会让 FRONT-A2 原旅程不复存在：实现 PR 同时在 frontend-baseline.md §5 变更登记写一行（指向本规格），FRONT-A2 在下次 S5 按本规格 A1/A4 改写；不改冻结正文。
9. **Otto 怎么协助这个功能？** 不适用（登录发生在 Otto 之前）。

## 2. 验收表（S5 只认这张表；一行一个可当场演示的判定）

| 编号 | 商家做 X | 看到 Y |
|---|---|---|
| SIGNIN-A1 | 一个从未出现过的邮箱：登录页按 Continue with email，收邮件，手输 6 位码 | 直接进产品；账号与工作区已建立；全程没有出现第二个页面叫注册 |
| SIGNIN-A2 | 一个从未出现过的 Google 账号：按 Continue with Google，选账号 | 直接进产品；账号与工作区已建立 |
| SIGNIN-A3 | 先用码登录过的邮箱，改用同邮箱的 Google 登录；再反过来 | 进的是同一个账号、同一个工作区；数据库里只有一个用户、一个工作区 |
| SIGNIN-A4 | 走访 `/signup`、`/forgot-password`、`/reset-password`，并查看登录页 | 三个地址都回到 `/login`；登录页没有密码框、没有 Forgot password；对公网请求 `/sign-up/email`、`/sign-in/email`、`/forget-password`、`/reset-password`、`/change-password`、`/set-password`、`/request-password-reset` 一律 404 |
| SIGNIN-A5 | 点邮件里的 Log in 按钮 | 登录页打开、码已填好，按一次 Continue 即登录；同一封邮件的链接第二次点无效；15 分钟后无效 |
| SIGNIN-A6 | 打开「暂停新注册」开关（`SIGNUPS_PAUSED=1`）后：陌生邮箱按 email、陌生 Google 按 Google；老用户照常登录 | 页顶横幅说明暂停；陌生人两扇门都进不来、不建账号、不寄码；老用户正常进入 |
| SIGNIN-A7 | 操作员在后台把一个**自助进来**的邮箱撤销（它此时已登录），该邮箱再分别走两扇门 | 后台能撤（不再回「No pending invite」）；他原来的登录下一次请求即失效；两扇门都进不来；页面反应与输错码一模一样，不说明原因 |
| SIGNIN-A8 | 同一邮箱一小时内要 6 次码；拿一个码连错 4 次 | 第 6 次被拒并提示一小时后再试；第 4 次要求重新发码；对陌生邮箱与老邮箱的响应时间与文案一致 |
| SIGNIN-A9 | 查数据库 | `BetterAuthAccount` 里 `providerId = "credential"` 的行数为 0；fresh database 跑完全部迁移无错 |
| SIGNIN-A10 | 用同一份测试脚本分别经 Google 与码建两个新账号 | 工作区名都为空（等商家在设置页填店铺名）、赠金都恰好一笔 `SIGNUP_GRANT_CREDITS`（幂等键 `signup:<orgId>`）、`emailVerified = true`、AllowedEmail 都是 status active，只差来源门标记；登录审计各恰好一行 |
| SIGNIN-A11 | 走查登录页与全部公网 auth 端点，尝试用任何方式设置密码 | 没有任何途径能建立密码；上线闸 GATE-A8 与 #980 的攻击顺序从此不可能 |
| SIGNIN-A12 | 端到端旅程：陌生邮箱收码登录 → 生成一张图 → 登出 → 同邮箱 Google 登录 | 看到刚才那张图；始终是同一个工作区 |
| SIGNIN-A13 | 一个 Google 报「邮箱未验证」的账号按 Google | 回到 `/login` 提示改用 email；数据库里没有为它建任何用户行 |
| SIGNIN-A14 | 让 Google 门失败一次（取消授权、撤销邮箱、暂停期陌生人各一次） | 每次都回到 `/login` 页内提示；从不落在 better-auth 自带错误页，从不出现裸 JSON |
| SIGNIN-A15 | 让首登在建号中途失败一次（例如断开数据库连接），同一邮箱再登录一次 | 第二次成功进入；库里该邮箱只有一个用户、一个工作区、一笔赠金 |
| SIGNIN-A16 | 用 `Aisha@Example.com` 与 `aisha@example.com` 各登录一次 | 同一个账号；`AllowedEmail` 只有一行小写 |
| SIGNIN-A17 | 用 `me+001@gmail.com`、`me+002@gmail.com`…连开 30 个账号（脚本） | 赠金只发给第一个（幂等键按去掉 `+tag` 与点号变体后的邮箱算）；同一小时内全站新账号超过上限（默认 50）后第 51 个进不来并触发 Sentry 告警，老用户登录不受影响 |

## 3. 不做（非目标）

- **SAML SSO、Passkey**：Linear 的另外两个按钮。企业客户的身份提供商在 beta 没人用得上；触发条件＝第一家要求 SSO 的付费商家出现。
- **新手引导页**：不抄 Linear 的 Welcome／邀请同事流程；首登直接进产品（Founder 2026-09-08）。触发条件＝Founder 另立规格。
- **密码作为次要选项**（Slack 式）：不做；触发条件＝Founder 明示。
- **操作员邀请流**：`AllowedEmail` 的 invited 状态与后台邀请页维持现状，不在本规格内改。
- **邮箱变更流**：维持关闭（`CLOSED_EMAIL_OTP_PATHS`）。
- **登录审计、会话管理页、Resend 侧发信量告警**：不在本规格（告警登记 DEFERRED）。
- **`AllowedEmail.email` 的大小写不敏感唯一约束**（#578）：不在本规格；A16 只要求写侧归一化。
- **人机验证（Turnstile）**：不在本规格；触发条件＝A17 的每小时上限被真实流量撞到，或供应商账单出现异常。登记 DEFERRED。
- **会话与撤销基本盘**（登出所有设备、会话绝对上限与过期清理、撤销后的每请求复查、限流计数器故障告警、密钥强度门、验证码门的秒级泄洪闸、陌生人请求预算与地址发信预算解耦）：审计 [4][5][12][14][15][22][23]，另立规格或登记 DEFERRED（见 `docs/DEFERRED.md` D-087 起），本规格只做 A7 那一刀。

## 4. 异议栏

- **最大风险：码门对陌生人打开＝对外寄信面打开。** 任何人都能让我们往任意邮箱寄登录码。现有防线：每邮箱每小时 5 封（`apps/web/lib/better-auth/sender.ts:21`）、每个码最多试 3 次（`apps/web/lib/better-auth/server.ts:427`）、每来源限流。仍建议上线前在 Resend 侧设每日发信量告警（登记 DEFERRED，不在本规格）。Linear／Slack 同样对外开码门，这是行业常态而非我们独有。
- **实现差异（已选）**：Linear 的邮件链接点开即登录；本规格改为「链接打开登录页、码预填、按一次 Continue」。理由：企业邮箱的安全扫描器会预先打开邮件里的链接，「点开即登录」会被扫描器消耗掉甚至替它登录；多按一次的代价换来链接不会被机器人用掉。若 Founder 坚持一模一样，改回即可，验收表 A5 相应改写。
- **去密码＝账号安全等于邮箱安全**：这是 Linear 的既有取舍；我们没有比 Linear 更强的理由保留密码。
- **残余风险（已知、未在本规格修）**：知道某商家邮箱的人，可以在一小时内替他要满 5 个码、再猜错 3 次，让他这一小时进不来（审计 [12]，发信预算按地址计、猜错次数按邮箱计）。Linear／Slack 的码门同样暴露于此；修法（陌生人预算与地址预算分开、猜错次数绑定来访者）登记 DEFERRED，触发条件＝第一个真实商家开始用码门。
- **工作区名取空而不取 Google 姓名**（§1.4）：这是我替 Founder 做的取舍，理由是店铺名≠人名，且与现行 #680 规则一致；Founder 若要「先用 Google 姓名占位」，改 A10 一行即可。

## 5. 变更登记（冻结后的中途想法只进这里，下次 S5 批量裁决；不当场执行）

| 日期 | 想法 | 裁决（留空待 S5） |
|---|---|---|
| 2026-09-10 | **PR #1336：密码退役之后 `SIGNUPS_PAUSED` 成了一个没人调的开关**。`apps/web/lib/signup-gate.ts` 的 `signupsPaused()` 与 `admitSelfSignup()` 今天全仓零调用点——它们原来只挂在密码注册那扇门上（`lib/better-auth/server.ts` 的 `isSelfSignupPath`，逐字比对 `/sign-up/email`；外加 `/signup` 页顶的暂停横幅），门退役，钩子随之删除。验收 A6 描述的行为因此暂时无处落地，登录门④（#1319）会把它重新接到两扇活门（码门与 Google 门）上。同一段时间里**没有自助注册路径**：`admitSelfSignup` 不再被调用，能建账号的只剩名单内邮箱（`AUTH_ALLOWED_EMAILS` / `FOUNDER_ADMIN_EMAILS` / `AllowedEmail` 里的 active 行），登录门②（#1318）开码门之后恢复。这两件事都是切片顺序造成的中间态，不是行为裁决，写在这里让下一个人不用重新推一遍。 |  |
| 2026-09-10 | **PR #1336：A9 那条删行迁移在本仓库是「合并即执行」**。推 `main` 自动部署，容器每次启动在 serve 之前跑一次 `prisma migrate deploy`（`apps/web/Dockerfile` → `apps/web/scripts/boot.mjs` 的 `runMigrations`），`.github/workflows/` 里没有任何 deploy workflow，也没有 environment required reviewer。链条是 merge → deploy → boot → `DELETE`，中间没有一步等人——所以**Founder 对这一次删除的确认必须发生在合并之前**，否则永远不会发生。`DESTRUCTIVE-OK` 只让 `scripts/check-destructive-migrations.sh` 放行，它过的是 CI 扫描闸，不是批准。 |  |
| 2026-09-10 | **登录门②：A17 的赠金去重键写不进 `CreditLedger`，需要它自己的唯一约束**。§1.5 写「赠金幂等键改按归一化邮箱算」，而 `CreditLedger` 的唯一约束是 `(orgId, idempotencyKey)`（`schema.prisma` `@@unique([orgId, idempotencyKey])`）——`me+001@gmail.com` 与 `me+002@gmail.com` 是两个用户、两个 org，那条约束的**另一半已经不同**，所以幂等键写成什么字符串都拦不住第二笔赠金。落地方式因此是一张新表 `SignupGrantClaim`：主键就是归一化邮箱（`@fikirtive/core` 的 `canonicalGrantEmail`，去 `+tag`、gmail 去点），在开户那笔事务里 `INSERT … ON CONFLICT DO NOTHING`，抢到的 org 发赠金。A10 那句「幂等键 `signup:<orgId>`」原样保留并仍在测试里逐字断言（换那条键会让**每一个既有 org** 下次登录再领一笔）。行为与 A17、A10 逐字一致，改的只是「这条唯一性住在哪张表」。 |  |
| 2026-09-10 | **登录门②：A8 的每小时上限从 `(来访者, 地址)` 改成 `(地址)`**。验收表说「同一**邮箱**一小时 6 次码上限」，而 `signin-code-request.ts` 原来的桶是 `(caller, email)`：换一个出口地址就买回一份新的五次，公布的数字从来不是这道闸执行的数字（真正按地址计的那道在寄信队列里，跑在背景、说不出话）。规则搬到门上、按邮箱计之后，「第 6 次被拒并提示一小时后再试」才有地方说出口。同时 `rate_limited` 从「不许存在的答案」改判为可以说：#678 禁它的理由是「码门只放行名单内的地址 ⇒ 限流 = 有账号」，码门对陌生人打开之后那个等号不成立。关于**地址本身**的两种拒绝（撤销、暂停期陌生人）仍然沉默，页面反应与「码错」一致。 |  |
| 2026-09-10 | **登录门②：`enqueueAuthEmail` 成为地址归一化的唯一上游**。emailOTP 的寄码路由按原样写 verification 的 identifier，而验码路由自己 `toLowerCase()`——一个大写地址会铸出一个永远验不掉的码。以前靠每个调用者先小写（请求路径确实做了），队列下游三处读同一个地址却各有各的归一口径。归一化因此落在进队列那一刻，一次，对所有调用者（A16）。 |  |
| 2026-09-10 | **登录门②：A10 的「工作区名为空」改为不再读 `User.name`**。#680 的实现是 `workspaceName = User.name`，对码门是空串所以一直看起来对；Google 门会把**个人姓名**写进那个字段，同一段代码给同一件事写出两种结果。§1.4 已拍板「两扇门首登的工作区名一律为空」，实现改成常量空串（`auth-guard.ts`）。落点在 `apps/web/lib/auth-guard.ts` —— 它不在 #1317 票面写集的文件清单里，但票面明写要交付 A10/A17，而这两条的唯一落点就是 `bootstrapPersonalOrg`；改动限于两处（工作区名、赠金前的 claim 判定）。 |  |
| 2026-09-10 | **登录门②：邮件验证链（`/verify-email` 与 `sendVerificationEmail`）在密码退役后仍然无人可达**（登录门① 判官 P2，本片未退役，按票面要求登记）。现状核对：`emailVerification.sendVerificationEmail` 仍配在 `lib/better-auth/server.ts`，`app/verify-email/` 页面仍在，`sender.ts` 的 `verify-email` 队列分支仍在；而**没有任何产品路径会触发它**——密码注册退役之后 `sendOnSignUp` 未配置，码门与 Google 门建号时 `emailVerified` 直接为 true。`HOURLY_PUBLIC_DOORS` 里那道 `/send-verification-email` 因此也守着一扇没人走的门。不在本片退役的理由：它跨 `app/verify-email/**` 与 `lib/email/**` 的多处，属于清理而非本片验收，且退役与否会影响 A9/A11 的围栏面。建议由登录门⑤（端到端旅程）或一张独立整理票处置：要么整条退役，要么写明它未来的用途（例如商家改邮箱）。 |  |
| 2026-09-10 | **PR #1336：Auth 设计夹具仍含密码屏，生产已退役**。`apps/web/design-system/patterns/auth/AuthAccessJourneyReference.tsx` 还画着密码屏、「Use password instead」「Forgot password?」与「Create an account」，生产侧这三样已随本 PR 退役，夹具与生产因此分歧。夹具改动归设计治理（`apps/web/design-system/governance/frontend-integration-handoff.md`），不在本切片的写集内，待下次设计侧同步；它在 A11 源码围栏里是具名白名单（那两处 `setPassword(...)` 调用全是 React `useState` 的 setter，一行都碰不到 Better Auth）。 |  |
| 2026-09-11 | **登录门④：后台那颗「撤销」按钮还没换到新动作上**。本片把撤销的领域动作落在 `apps/web/lib/signup-gate.ts` 的 `revokeEmailAccess`（谓词 `status ≠ revoked`，名单翻面与切断会话同一笔事务），操作员入口是 `apps/web/app/admin/access-actions.ts` 的 `revokeMerchantAccess`（`requireRole("tenants","mutate")` ＋ 审计行）。**后台界面上那颗按钮仍然调旧的 `revokeTenantInvite`**，所以在界面上撤一个自助进来的地址仍会回「No pending invite for that address.」。差的是两处：`components/admin/AdminDashboardV2.tsx` 换一个 import，`lib/tenant-actions.ts` 让 `revokeTenantInvite` 转调新动作（它多守的那条「地址已属于某工作区就别用邀请工具管他」的前置条件，对本动作恰恰是要撤的对象，需一并去掉）。两个文件都不在 #1319 的写集内，故登记而不动手。A7 的服务端半边（撤得掉、会话当场失效、两扇门都拒、双租户）已有真库测试。 |  |
| 2026-09-11 | **登录门④：A6 的「页顶横幅」未落地**。§1.3 要求开关打开时登录页顶挂一条「New signups are paused right now. Existing accounts can still log in.」，落点是 `apps/web/app/login/page.tsx`（文案常量 `SIGNUPS_PAUSED_MESSAGE` 已在 `lib/signup-gate.ts`）。登录页不在 #1319 的写集内（票面明写「Google 那扇的 UI 提示交给③的映射表」），A6 的门那一半（两扇门都拦、不建账号、不寄码、老用户照进）已有真库测试。建议由登录门③（它本来就要改 `app/login/page.tsx` 的错误映射表）顺手接上。 |  |
| 2026-09-11 | **登录门④：`FOUNDER_ADMIN_EMAILS` 仍然先于数据库，与「撤销仍然绝对」有张力**。本片把 `AUTH_ALLOWED_EMAILS` 的短路拆掉了（判官在登录门② 抓到的那一刀：环境名单命中就不查撤销，于是写在那个变量里的地址撤了等于没撤；`lib/signup-gate.ts` 与 `lib/allowlist.ts` 两处同改，负例测试逐字钉住）。**founder 那一条没动**：它是破窗锤，一行数据库记录不该能把部署者锁在自己的产品外面（这条例外自 #543 起就在）。§1.6 写的是「撤销仍然绝对」，字面上不留例外，所以这条张力放在这里等 S5 裁一句：要么承认 founder 例外并写进 §1.6，要么连 founder 也可撤（那就必须同时给一条不经数据库的恢复路径）。 |  |
| 2026-09-11 | **登录门④ 第 2 轮：上面两条「未落地」的登记已就地补齐，登记作废**。判官指出 #1319 票面从来没有文件写集，写的是行为（「后台撤销自助进来的邮箱后会话立即失效、两扇门都拒」），据以停手的「不在写集内」是自定边界。本轮因此把两半都做完：① A7 —— 操作员入口从 `app/admin/access-actions.ts` 搬进 `apps/web/lib/tenant-actions.ts`（与 `inviteTenant` / `revokeTenantInvite` 同一个模块，组件一律从 `@/lib/*` 取动作），后台「Invite a merchant」面板的地址输入框旁新增一颗 **Revoke access** 按钮＋确认弹窗，调的就是它；邀请那条路（`revokeTenantInvite`，谓词 `status = "invited"`）按 §3 非目标原样保留，两颗按钮文案分开（Revoke invite ／ Revoke access）。② A6 —— 页顶横幅落在 `apps/web/app/login/page.tsx`，文案取 `SIGNUPS_PAUSED_MESSAGE`，该常量补齐成 §1.3 的两句原文。设计侧缺口一并登记：`design-system/patterns/auth/` 没有暂停横幅的样张，本次复用既有 `components/ui/alert` 的 default 变体（不新建组件），待设计侧补样张时核对。 |  |
| 2026-09-11 | **登录门④ 第 2 轮：确认弹窗里「环境名单不被这里盖过」那一句已改**。本片把 `AUTH_ALLOWED_EMAILS` 的短路拆掉之后，后台撤销弹窗原本那句「Configured founder or environment allowlists are not overridden here.」的后半句变成假话（环境名单命中现在照样查撤销）。已改成只说 founder 那把破窗锤。这不是新裁决，是让界面跟上本片已经改掉的行为。 |  |
| 2026-09-11 | **登录门④ 第 3 轮：`FOUNDER_ADMIN_EMAILS` 与「撤销仍然绝对」的张力已就地解掉，上一条登记（同日「founder 仍然先于数据库」）作废**。判官第 2 轮把它判成 P0：§1.6 字面上不留例外，而 founder 那条捷径让撤销对整整一个环境变量失效（`lib/signup-gate.ts` 的 `lookupAddress` 与 `lib/allowlist.ts` 的 `isAllowedEmail` 各一处，都在读库之前 `return`）。本轮**拆掉门上的例外**（两处都改：撤销由 `AllowedEmail` 那一行对每个地址一视同仁地拍板，库读不到就 fail closed，founder 也不例外），破窗锤搬到**写侧** —— `revokeEmailAccess` 对还挂在 `FOUNDER_ADMIN_EMAILS` 上的地址答 `protected`、一行都不写，后台那颗按钮回一句「That address is named in the founder allowlist. Remove it from FOUNDER_ADMIN_EMAILS before revoking.」。于是「一行数据库记录锁死部署者」发生不了，恢复路径也不经数据库（先改环境变量，再撤），正是上一条登记要求的那个条件。§1.6 正文不改（它本来就没有 founder 例外），这里只记落点。 |  |
| 2026-09-11 | **登录门④ 第 3 轮：「从未登录过」的判据换成 `ba_user` 里有没有他那一行**。判官第 2 轮的另一条 P0：`known` 原本是「环境名单点过名 ‖ `AllowedEmail` 里有任何一行」，而 `inviteTenant` 在任何人登录之前就写得下一行 `invited`、`AUTH_ALLOWED_EMAILS` 里加一个地址更是一次请求都不用发 —— 暂停期间「先邀请（或先写进变量），再让他进来」因此是一条绕过开关的现成的路。改判之后门只认「有账号」（两扇门都只在身份证明完成之后才建那一行），成本从一次库读变成恒定两次。**两个可见后果登记在此**：① 暂停期间一张还没被用掉的邀请不再让人进来，操作员要么等暂停结束、要么先关开关；② 从没登录过的 founder 地址在暂停期间同样进不来（他手上就有那个开关）。两条都符合 §1.6 ① 的字面，但都是新的可观察行为，留给 S5 看一眼。 |  |
| 2026-09-11 | **登录门④ 第 3 轮：撤销的审计行写失败不再无声**（判官 P1）。`revokeMerchantAccess` 原本 `.catch(() => {})` 之后照样答 `{ ok: true }`：撤销本身已经落库、会话已经切断，把整个动作报成失败会是反过来的那个谎，但一次没有痕迹的撤销在事后对账时是一个查不出来的洞。现在两件事一起说出口 —— 答案多一面 `auditFailed` 旗（后台横幅加一句「The audit entry could not be written — tell the team.」），外加一条固定分类的 Sentry 告警（`area: admin` / `gate: tenant-revoke-audit`，文本里不带邮箱）。**未做、登记**：同一模块的 `inviteTenant` 与 `revokeTenantInvite` 两处审计写仍然是 `.catch(() => {})`；邀请流是 §3 非目标，不在本片动它。 |  |
| 2026-09-11 | **登录门④ 第 4 轮：`revokeTenantInvite` 对 founder 地址整条拒绝 —— 一处邀请流的行为变化，按 §3「邀请流不改」登记**。§3 把操作员邀请流列为非目标，而第 3 轮把破窗锤搬到写侧时，`revokeTenantInvite`（谓词 `status = invited`）也跟着长出一条新的拒绝：还挂在 `FOUNDER_ADMIN_EMAILS` 上的地址撤不动，答「That address is named in the founder allowlist. Remove it from FOUNDER_ADMIN_EMAILS before revoking.」，那一行仍是 `invited`。理由是**能写下 `revoked` 的动作都能把部署者锁在产品外面**，只守住 Revoke access 那一颗按钮等于留着另一扇后门（一个还没登录过的 founder 地址正好落在 invited 谓词手里）。这是 A7 的破窗锤在邀请流上的投影，不是邀请流的新产品能力；落点 `apps/web/lib/tenant-actions.ts`，测试 `lib/__tests__/admin-revoke-access-action.test.ts` 最后一条。S5 若判「邀请流一个字都不许动」，替代做法是把 founder 判定只留在 `revokeMerchantAccess`，并接受 invited 那条路仍可锁死部署者。 |  |
| 2026-09-11 | **登录门④ 第 4 轮：门的「从未登录过」判据仍然是「`ba_user` 有行」，边界登记，本轮不改**（判官 Codex 第 4 轮第 ③ 条）。§1.6 ① 说的是「从未登录过」，实现读的是 `ba_user` 行存在（`lib/signup-gate.ts` 的 `lookupAddress`）。两者有一道缝：用户行与会话由**两个钩子**先后建，首登若失败在两者之间（收敛抛错、浏览器中途关掉），会留下一行没有会话的用户行；暂停开关打开之后，这个地址会被当成「老商家」放进来。**本轮不收紧，理由是现成的两种收紧法都更坏**：① 用「`ba_session` 有过记录」——会话行在登出与过期时会被删掉，于是一个登出过的真商家在暂停期会被锁在门外，正好违背 §1.3 横幅承诺的那句「Existing accounts can still log in」，用一个更重的失败换一个更轻的；② 用 `auth.signin` 审计行——它是耐久的，但 `ActionEvent` 只有 `(ownerId, createdAt)` 与 `(projectId, type)` 两个索引，按 `payload->>'email'` 找要全表扫，而门这条路刻意是「恰好两次读、无分支」的固定成本（#678 那一族缺陷的根）。真正的修法是在 `ba_user` 上加一列耐久的「最近登录时间」，由 `session.create.after` 写——那是 schema 变更（重挡，要新冻结规格），不在本片。风险面：需要「暂停开关打开**之前**曾经开始过一次登录并失败在中途」，这类地址才进得来；他拿到的是一个新账号与开户赠金。S5 裁：加列另立规格，还是承认这条边界并把 §1.6 ① 的措辞改成「有账号即视为老商家」。 |  |
| 2026-09-11 | **登录门④ 第 4 轮：撤销与建会话并发的那一张漏网会话已就地修掉（落点登记）**（判官 Codex 第 4 轮第 ② 条）。`session.create.before` 那道闸只**读**，撤销在自己的事务里翻名单并删会话，于是「闸读过 → 撤销整笔提交（待建的那张还不存在，删不到） → 待建的那张才 INSERT」这道序会留下一张属于已撤销地址的活会话。闸放在 `before` 里怎么读都修不掉：它与那条 INSERT 不在同一笔事务。本轮加的是**会话落库之后**的二次确认 `assertSessionSurvivesRevoke`（`lib/better-auth/gate.ts`，挂在 `session.create.after`、跑在收敛之前），那一次读是 `SELECT … FOR SHARE`：撤销还在飞就等它提交，已经提交就直接读到 revoked，两种情形都把刚建的那张收回去再按门的统一话术拒绝。撤销那一侧一行未改。行为与 A7 逐字一致（「他原来的登录下一次请求即失效」），改的只是「这道判定在哪一刻再问一次」；真库复现测试在 `lib/__tests__/signin-pause-and-revoke.test.ts`。 |  |
| 2026-09-11 | **登录门④ 第 4 轮：撤销审计失败的告警不再带原始错误消息**（判官 Codex 第 4 轮第 ① 条）。`lib/tenant-actions.ts` 的 `extra.reason` 原样放 `e.message`，而 Prisma 的错误消息会把**调用参数渲染进去** —— 一次唯一键冲突就会把商家邮箱送出我们的机器，#575 日志纪律只守住了标题那一行。改成只上报 `errorName` 与 `errorCode`（如 `PrismaClientKnownRequestError` / `P2002`），测试断言整条 Sentry payload 里不含邮箱。纯日志纪律，无商家可见行为变化。 |  |
| 2026-09-11 | **登录门②（PR #1344）判官第 1 轮 P1 落修四处，全部在冻结正文的口径之内**。①「一个真实收件箱只领一次」原来漏了域：`googlemail.com` 与 `gmail.com` 是 Google 给同一个收件箱的两个域名，`canonicalGrantEmail` 只做了去点、没折域，同一个收件箱因此能领两笔（`packages/core/src/spend.ts` 折成 `gmail.com`）。②同一句话对**存量**数据也不成立：`signup_grant_claim` 是新表，升级那一刻是空的，已经领过赠金的收件箱换个变体地址在另一个工作区还能再领一笔——迁移 `20260910190000_signup_grant_claim` 因此补一段只读既有表的 INSERT 回填（事实来源是 `CreditLedger` 里那行 `signup:<orgId>`，同一收件箱多个存量 org 记最早那次）。③`AUTH_ALLOWED_EMAILS` 命中即短路放行、一次库都不读，于是被撤销的地址只要还留在那份环境名单里就照样进得来，与 §1.6「撤销 → 拒」和 A7「两扇门都进不来」相违；环境名单改为只回答「来过吗」，撤销一律以库为准（`FOUNDER_ADMIN_EMAILS` 仍是唯一短路，那是刻意的破窗）。④A5 那条登录链接把邮箱与一次性码放在 URL 片段里，读完不清，浏览器端 Sentry 的默认集成会把带片段的 `location.href` 写进 `event.request.url`——登录页读完即 `replaceState` 抹掉片段，上报通道再加一道 `scrubUrlFragments` 兜底。四条都不改验收表文字，改的是「实现是否真的做到了那句话」。 |  |
| 2026-09-11 | **PR #1318（登录门③）：拒绝的 `?error=` 键分得出「暂停」与「撤销」，页面文案分不出**。规格 §1.3 要求这两种拒绝「页面反应与码错完全一致，不说明原因」。落地时**文案**做到了（`app/login/page.tsx` 对任何键只说同一句 "Google sign-in didn't complete. Try again or use email."），但**地址栏里的键**是两个（`sign_in_paused` / `sign_in_revoked`，`lib/better-auth/signin-refusal.ts`）。为什么不能压成一个键：Better Auth 在 OAuth 回调的建号那条路上把 APIError 的 `message` 当键用（`oauth2/link-account.mjs:107-111` → `api/routes/callback.mjs:156-158`），在建会话那条路上读 `body.code`（`callback.mjs:152-155`），两条路必须给出同一个不含空格的常量；而服务端日志与告警要分得清「暂停误伤老商家」和「撤销生效了」（`lib/signup-gate.ts` 那个 enum 的原话），登录门④（#1319）也要复用同样两个键。若 Founder 认为连地址栏也不许分辨，改成一个常量即可（一行）。 |  |
| 2026-09-11 | **PR #1318（登录门③）：A13 落地成一条比票面更宽的不变量**。验收 A13 说的是「Google 报邮箱未验证 → 拒绝且不建任何用户行」，实现写的是「`databaseHooks.user.create.before` 里 `emailVerified !== true` 一律拒」——不分供应商。理由：库自己**只**在合并到既有用户时看那个声明（`oauth2/link-account.mjs:20-22`），新建那一支（:80-96）一个字都不问，所以判定只能在我们这一侧；而本产品今天已经没有「未验证的新用户」的合法产地（码门验码成功那一刻写 `emailVerified: true`，密码注册已退役）。写成供应商无关的不变量是为了将来加第二个 OAuth 供应商时这道闸已经在。 |  |
| 2026-09-11 | **PR #1318（登录门③）：被撤销的邮箱走 Google 门时，库会先写下一行 `ba_account` 才被拒**。`handleOAuthUserInfo` 的合并分支先 `linkAccount` 再 `createSession`（`oauth2/link-account.mjs:29-40` → `:126`），而我们的拒绝落在 `session.create.before`，所以顺序上凭据行已经写下。会话没有、商家进不来（A14 用例逐条断言），但库里会留一行指向被撤销用户的 google 凭据。规格未就此表态；清理它需要一个 account 钩子或库侧改动，不在本切片写集。 |  |
| 2026-09-11 | **PR #1318（登录门③）判官第 2 轮 P1：A14 那句「从不落在 better-auth 自带错误页」只靠 `errorCallbackURL` 做不到**。那个值存在 state 内部（`better-auth@1.6.20 dist/oauth2/state.mjs:14`），而有一整族失败发生在 state 解开**之前**——商家在回调页刷新或后退（state 行第一趟已被消费，`dist/state.mjs:124`）、state 过了十分钟（`:126`）、有人直接打回调地址（`state_not_found`）。这些路上库退回 `options.onAPIError?.errorURL \|\| ${baseURL}/error`（`dist/oauth2/state.mjs:33`），也就是那张无品牌的 `<title>Error</title>` 页；`state_not_found` / `state_mismatch` / `state_invalid` / `state_generation_error` / `internal_server_error` 五个键因此原本一个都到不了登录页。修法是在 `betterAuth({...})` 顶层补 `onAPIError: { errorURL: "/login" }` 当地板（同一行还让自带错误页那个端点本身 302 回 `/login`，`dist/api/routes/error.mjs:371-375`）。验收表文字不变，改的是「实现是否真的做到了那句话」；围栏是 `apps/web/lib/__tests__/signin-google-door.test.ts` 新增的四条 state 用例，旧的那条反向围栏（「不传 `errorCallbackURL` 就落在自带错误页」）随之改写成「不传也仍然回 `/login`」。 |  |
| 2026-09-11 | **登录门④ 第 5 轮：合主干时把「环境名单仍让暂停期老地址进得来」那条测试按 §1.6 ① 改写**（合并裁定，本轮与 `origin/main` 合并）。主干 #1347（登录门② r2）带进一条 `it("SIGNIN-A7 —— 环境名单仍然让暂停期的老地址进得来")`，配的是主干那版 `lookupAddress`（`known = listed \|\| !!row`，外加 `FOUNDER_ADMIN_EMAILS` 短路）。它与 §1.6 ① 「`SIGNUPS_PAUSED` 打开且邮箱**从未登录过** → 拒」相悖：写进一个环境变量一次请求都不用发，把它算成「登录过」等于给暂停开关留一条现成的绕过路（第 2 轮判官 P0 的同一刀）。裁定是**门上的环境名单既不豁免撤销、也不等于登录过**，`known` 只认 `ba_user` 有行；founder 被一行数据库记录锁死的风险由**写侧** `revokeEmailAccess` 的 `protected` 承担（恢复路径不经数据库）。那条测试因此改写成本分支口径（只写在 `AUTH_ALLOWED_EMAILS`、从未登录过的地址在暂停期被拒；登录过的照常进）而不是删除，主干其余测试逐条保留。落点 `apps/web/lib/signup-gate.ts`、`apps/web/lib/__tests__/better-auth-gate.test.ts`。 |  |
| 2026-09-11 | **登录门④ 第 5 轮：二次确认拒绝时商家读到的是一次 500，不是登录页那句话 —— 边界登记，本轮不改**（第 5 轮为判官 r4 P1 ① 补接线测试时实测发现）。`session.create.after` 的钩子由 better-auth 排在端点**处理完之后**才跑（`@better-auth/core/dist/context/transaction.mjs` 的 `runWithAdapter`：先 `als.run(fn)` 拿到结果，再 `for (const hook of pendingHooks) await hook()`，然后才 return），所以 `assertSessionSurvivesRevoke` 抛的 APIError 落在端点自己那层错误映射**外面**：整个 `auth.handler` reject，`app/api/better-auth/[...all]/route.ts` 那句 `await forward.POST(request)` 跟着抛，Next 回一份 500。对比之下 `session.create.before` 的拒绝是一份正常的 403（`better-auth-oauth-session-gate.test.ts` 逐条断言）。**fail closed 仍然成立**：会话行已被收回，响应连同 `Set-Cookie` 一起作废，商家进不来；代价是 §1.3 那句「页面反应与码错完全一致」在这条竞态上不成立（它要求的是登录页那一句，不是一张 500）。触发条件很窄：操作员的撤销必须恰好落在「闸读过」与「会话 INSERT 落库」之间的那几毫秒里。不在本轮改的理由：改它要么在路由里把 `/sign-in/email-otp` 的 throw 也归一成 `invalidSignInCode()`、要么在 Google 回调那条路上另接一次映射，两处都在本片写集之外，且会动到登录门③ 刚冻下来的那张错误映射表。S5 裁：接一层映射，还是承认这条竞态上的500。围栏 `lib/__tests__/signin-pause-and-revoke.test.ts` 那条「接线」用例照实钉住了现状（断言的是 thrown APIError，不是 403）。 |  |
| 2026-09-11 | **登录门④ 第 5 轮：判官 r4 的两条 P1 与三条 P2 的落点登记**。P1 ①「接线无测试钉住」与 ②「`FOR SHARE` 无测试钉住」各补一条**第二条连接握着行锁不提交**的并发用例（`lib/__tests__/signin-pause-and-revoke.test.ts` 末尾那个 describe，`holdRevokeUncommitted` 用 Prisma 交互式事务握锁，8 秒封顶、两条用例都在 1 秒内放手）：前者驱动真码门走完 `session.create.after`，断言会话被收回、拒绝带 `sign_in_revoked`、且这一次登录在 `auth.signin` 审计流里一行都不留（跑在收敛之前）；后者断言二次确认在锁持有者提交之前不返回、提交之后才拒。变异验证四条真跑并记在 PR 描述里（C 注释掉那一行、C2 挪到收敛之后、D 去掉 `FOR SHARE`、E 把删会话的 `.catch(() => {})` 装回去），四条都红。P2 三条：③ 二次确认的拒绝改用 `signin-refusal.ts` 的 `SIGN_IN_REFUSED_REVOKED`（Google 回调那条路才映射得成登录页文案）；④ 删会话失败不再无声 —— 拒绝照抛，另发一条 `area: auth` / `gate: session-survives-revoke` 的告警，extra 只放 `errorName`/`errorCode`，测试断言整条 payload 不含邮箱、会话 id 与 Prisma 原始消息；⑤ §5 那条 7 根竖线的畸形行已拆开去重，全节逐行核对每行恰好 3 列。无商家可见行为变化。 |  |
| 2026-09-11 | **登录门④ 第 6 轮：判官 r5 三条 P2 与一条 P3 的落点登记（无商家可见行为变化）**。① 上一轮写在测试注释与 PR 描述里的变异 C／C2 结果是错的（写成「抛 `RevokedDuringProvisioning`，收敛有后手兜着」）。本轮 C 复跑 4 次、C2 复跑 2 次，读数一致：会话数 1→2 —— **删掉 `assertSessionSurvivesRevoke` 没有后手**，`RevokedDuringProvisioning` 从不删那张已落库的会话、也给不出门的统一话术，它会不会响还取决于谁先赢 `AllowedEmail` 那一行上的等待（收敛第 0 步 `admitSelfSignup` 是 `INSERT … ON CONFLICT DO NOTHING`）。注释与 PR 描述按复跑事实重写。② `gate.ts` 上「为什么用 `SIGN_IN_REFUSED_REVOKED`」的理由失实（写 Google 回调 `callback.mjs:152-155` 会把 `body.code` 映射成 `?error=`）：after 钩子的抛错由 `queueAfterTransactionHook` 排在 router handler 之外，那段映射与 `onAPIError.errorURL` 都够不着它。改成真话 —— 沿用 `signin-refusal` 的不变量（`message` 与 `code` 同值、无空格）以便日志／审计与将来接上映射时一致；这条竞态今天商家读到 500，见上一条 §5 登记。③ 二次确认两条 fail-closed 分支（`FOR SHARE` 那次读失败、读不到 `ba_user` 邮箱行）此前零测试，补两条 SIGNIN-A7 用例（`lib/__tests__/better-auth-gate.test.ts`），两条变异都真跑并红。④ `holdRevokeUncommitted` 的事务 promise 改成创建时即挂 `.catch`，放手做成幂等并由用例 `finally` 兜底 —— 握锁与放手之间断言先红时不再留下一条 unhandled rejection、也不再多握 8 秒锁。 |  |
| 2026-09-11 | **登录门④ 第 6 轮：二次确认那一次 `FOR SHARE` 加了 3 秒等待上限（落点登记，判官 r5 P3 ⑤）**。`FOR SHARE` 会等握着这一行写锁的人提交，而那个等原本没有尽头：一笔挂住的运维事务、一笔卡住的撤销，就能让这次登录的 `session.create.after` 永远回不来 —— 一个请求与一条数据库连接一起悬着。现在那一次读跑在一笔带 `SET LOCAL lock_timeout = '3s'` 的事务里（`lib/better-auth/gate.ts` 的 `FOR_SHARE_LOCK_TIMEOUT`），超时按「读库失败」处理：收回会话、拒绝（fail closed，等不到答案 ≠ 可以放行）。3 秒的依据是它要等的写者只有两个，`revokeEmailAccess` 那一笔与 `bootstrapPersonalOrg` 里 invited→active 那一次，都以毫秒计。行为与 A7 逐字一致，改的只是「等不到时多久放弃」；围栏是 `lib/__tests__/signin-pause-and-revoke.test.ts` 那条全程握锁不放的用例（删掉 `SET LOCAL` 那一句即红：实测 `expected 8020 to be less than 6000`）。S5 可裁这个刻度是否合适。 |  |
| 2026-09-11 | **登录门④ 第 7 轮：首登时「撤销地址不得留下会话行」不再依赖钩子顺序（落点登记，判官 r6 P1）**。首登那一次登录有两个 after 钩子：码门验码成功先 `createUser` 再 `createSession`，两个钩子被 `queueAfterTransactionHook` 排进同一条 `pendingHooks` 队列，handler 返回后按序执行（`@better-auth/core/dist/context/transaction.mjs` 的 `runWithAdapter`：`for (const hook of pendingHooks) await hook()`）。排在前面的 `user.create.after` 跑收敛，撤销恰好在这一刻追上时它抛 `RevokedDuringProvisioning`（#538 的 carve-out），**整条队列就此中断** —— 排在后面的二次确认 `assertSessionSurvivesRevoke` 一个字都跑不到，而会话行早已落库：响应是非 2xx、cookie 没送出去，可 `ba_session` 里留着一张属于已撤销地址的活会话，最长 7 天。修法不是给钩子重排（那个顺序是库的），而是 `user.create.after` 自己兜住：收敛抛任何错先按 userId 把会话删光再原样重抛（`lib/better-auth/gate.ts` 的 `discardSessionsOfFailedProvisioning`，删不掉照第 6 轮那套不含邮箱的告警）。真库首登并发用例钉住（`signin-pause-and-revoke.test.ts`「首登时撤销追上 user.create.after」），变异去掉那一句即红。行为口径与 A7 逐字一致，没有新的商家可见行为。 |  |
| 2026-09-11 | **登录门④ 第 7 轮：那一次 500 的触发面比第 5 轮登记的更宽，并把三个超时数一起交 S5 裁（判官 r6 P2 ①）**。第 5 轮登记「二次确认拒绝时商家读到的是一次 500」时，触发条件写的是「撤销恰好撞上那个毫秒窗口」。第 6 轮把那次 `FOR SHARE` 读放进一笔带 `lock_timeout` 的事务之后，这句话不再完整：**任何一次拿锁超时、读库失败、或连接池打满**都会让这道确认 fail closed 并抛错，而它抛在端点错误映射之外，商家读到的同样是一次 500 —— 不再是一个毫秒级的罕见窗口。相关的三个数现在都显式写在 `lib/better-auth/gate.ts` 里而不是吃默认值：`lock_timeout = 3s`（等行锁）、事务 `timeout = 5s`（必须大于前者，否则 55P03 那条路永远走不到）、事务 `maxWait = 2s`（从连接池拿连接，`DB_POOL_MAX` 默认 10）。三个数与「这条路上要不要把 500 换成登录页那句话」是同一个裁决，一起等 S5。 |  |
| 2026-09-11 | **登录门④ 第 7 轮：更正第 6 轮那行对 `FOR_SHARE_LOCK_TIMEOUT` 的措辞（判官 r6 P3 ④；§5 只追加，旧行原样留着）**。第 6 轮那行写「它要等的写者只有两个、都以毫秒计」「绝不会挂住一个请求」，两句都过强。握同一把 `AllowedEmail` 行锁的写者至少还有：`inviteTenant` 的重新邀请、`revokeTenantInvite` 的撤销邀请，以及 `bootstrapPersonalOrg` 那笔把组织／成员／赠金写在一起的事务（`auth-guard.ts`）—— 最后这一笔在同一笔事务里做的事远不止一次 UPDATE，说它「以毫秒计」没有依据。「绝不会挂住一个请求」同样说过头了：3 秒上限保证的是**这次登录最多被挂 3 秒**，不是零。以本行为准。 |  |
| 2026-09-11 | **登录门④ 第 7 轮：第 5 轮那行括号里「（Google 回调那条路才映射得成登录页文案）」是假的，以第 6 轮那行为准（判官 r6 P3 ⑤；§5 只追加，旧行原样留着）**。那句话当时的依据是 `api/routes/callback.mjs:152-155` 会把 `body.code` 读成 `?error=`；第 6 轮已在 `gate.ts` 注释里逐字更正：这道确认挂在 `session.create.after`，而 after 钩子被排到 router handler **之外**才跑，它抛的错落在端点那层错误映射的外面 —— callback.mjs 那段映射与 `onAPIError.errorURL` **一个都够不着它**，Google 那条路也不例外。两行里以第 6 轮那一行（与 `gate.ts` 的注释）为准。 |  |
| 2026-09-11 | **登录门④ 第 8 轮：更正第 7 轮那行「这次登录最多被挂 3 秒」的措辞（判官 r7 P2 ①；§5 只追加，旧行原样留着）**。`lock_timeout` 只管**等行锁**这一段，管不住这道确认的总时长。同一次 `assertSessionSurvivesRevoke` 在它之外还有几段各自会花时间：按 userId 读 `ba_user` 那一次查询（无上限，跟着连接与库走）、这笔交互式事务从连接池拿连接的 `maxWait` 2 秒、事务自身 `timeout` 5 秒（`lib/better-auth/gate.ts` 的 `FOR_SHARE_TX_MAX_WAIT_MS` / `FOR_SHARE_TX_TIMEOUT_MS`），拒绝之后还有最多两次 `deleteMany`。所以准确的说法是：**等行锁这一步最多 3 秒**，而这道确认的最坏总时长由那几个上限相加决定（今天约 7 秒量级，外加两次读写自身的耗时），不是 3 秒封顶。三个数与这句话一起仍在等 S5 裁。以本行为准。 |  |
| 2026-09-11 | **登录门④ 第 8 轮：删会话失败那条路改成「重试一次 ＋ 告警包 try/catch」，并实测残留会话行是惰性的（落点登记，判官 r7 P1 ①／P3 ①，无商家可见行为变化）**。① 告警自己会抛（transport 未初始化、DSN 配错、序列化炸）：上一版 `Sentry.captureMessage` 直接写在 `.catch()` 回调里，告警的错会**顶替掉调用者正在抛的那个拒绝** —— 首登路上盖掉 `RevokedDuringProvisioning`、二次确认路上把 `sign_in_revoked` 换成一句「sentry transport down」。现在告警整个包在 try/catch 里（`lib/better-auth/gate.ts` 的 `deleteSessionsOrAlert`），响不响都不改变这条路抛什么。② 删会话失败先原地重试一次（`DELETE_SESSIONS_ATTEMPTS = 2`）再告警：瞬时失败（P2034 死锁、连接被掐）代价不对称，多一条 DELETE ＜ 留一张属于已撤销地址的会话行；两次都失败只发一条告警。③ 判官的「零残留才安全」经真库实测更正为「**残留惰性 ＋ 必定告警**」：把那张删不掉的行原样造回库里之后，better-auth 那一层确实还读得出它，但每个受保护动作的第一句 `requireSession → allowed → isAllowedEmail` 按库重查 `AllowedEmail`，`revoked` 一律拒（用例「删失败留下的那张会话行是惰性的」；变异注释掉 `allowlist.ts` 那句撤销判定即红）。残留仍要告警、要人处理，但它不是能进产品的活口。 |  |
| 2026-09-11 | **登录门④ 第 8 轮：首登竞态用例补三条「窗口真的被击中」的断言，并各加一条反证时序（落点登记，判官 r7 P1 ②）**。上一版那条用例只断言结果面（会话零行、审计零行），而**撤销早在登录之前就提交**这种没击中窗口的时序同样满足它 —— 假阳性。现在用例先自证击中：`ba_user` 有且只有一行（门放行过、钩子队列真的跑了）、收敛真的抛了 `RevokedDuringProvisioning`（探针包住 `convergeIdentity` 的原实现只记名字）、首登兜底真的以该 userId 被调用（探针包住 `discardSessionsOfFailedProvisioning`）。两条反证用例钉住这三条断言在另外两种时序下读数不同：撤销提前到登录之前 → 用户行 0、两个探针都空；撤销推迟到登录之后 → 首登成功、两个探针都空。变异 E（把主用例的 `hold.commit()` 挪到 `submitCode` 之前）实跑红在 `expected +0 to be 1`。 |  |
| 2026-09-11 | **登录门④ 第 9 轮：撤销改由「前门」统一执行 —— 任何带会话的 better-auth 请求都重查名单（落点登记，判官 r8 P0，无商家可见行为变化）**。判官用同一张真登录 cookie 打 better-auth **自带**的 `/api/better-auth/list-sessions` 与 `/update-user`，两个都 200，`/update-user` 还真的把 `name` 写进了库 —— 那一排路由只认 `ba_session` 那一行，从来不问我们的名单，所以第 8 轮那句「残留会话行是惰性的」只对**我们自己写的那一层**（`requireSession → allowed → isAllowedEmail`）成立。修根按 §1.6「三处名单检查同一函数」「撤销仍然绝对」：判定搬进 §1.4 点名的前门中间件（`apps/web/lib/better-auth/server.ts` 的 `hooks.before` → `gate.ts` 的 `assertRequestSessionNotRevoked`），对每一个**已经解析到会话**的请求跑同一个 `signInDoorDecision`；判成 `revoked` 就按 userId 删光会话（`deleteSessionsOrAlert`）并抛 `SIGN_IN_REFUSED_REVOKED`，端点根本不跑所以不发新 cookie。登录门自身（`/sign-in` `/sign-up` `/callback` `/oauth2/callback`）与 `/sign-out` 不走这道闸（前三者各有自己的三步判定且拒绝要映射成 `?error=`，后者只会**收回**权限）；`paused` 照旧放行（§1.3「老商家照常进」）。产品这一层由 `lib/better-auth/compat.ts` 把这一种拒绝翻译成「没有会话」，所以 `requireSession` 照旧答 `Not authorized.` 而不是一次 500。代价：每个带会话的 better-auth 请求多两次唯一键点查；读库失败按 `signInDoorDecision` 自己的口径 fail closed（＝拒绝＋请回登录页）。用例「撤销后那张 cookie 打 better-auth 自带端点」（真 better-auth、真库）；变异注释掉 `server.ts` 那一行 → `expected { listStatus: 200, …(4) } to deeply equal { listStatus: 403, …(4) }`，三个端点同时 200、`nameAfter` 变成被改过的名字、`rowsLeft: [1, 1, 1]`。 |  |
| 2026-09-11 | **登录门④ 第 9 轮：撤销动作里的告警不再能把一次成功的撤销变成失败（落点登记，判官 r8 P2，无商家可见行为变化）**。`apps/web/lib/tenant-actions.ts` 的 `revokeMerchantAccess`：撤销已经落库、会话已经切断之后，若审计写入失败**且** `Sentry.captureMessage` 自己抛（transport 未初始化、DSN 配错、序列化炸），告警的错会顺着 `.catch()` 冒出去让整个 server action reject，后台于是误报「撤销失败」。现在告警包在 try/catch 里（与 `gate.ts` 第 8 轮同一条口径），仍返回 `{ ok: true, result, auditFailed: true }`。用例「告警通道自己炸掉时：撤销照样算数」（真库，注入 `captureMessage` 抛错）；变异去掉 try/catch → `Error: sentry transport down`。 |  |
| 2026-09-11 | **登录门④ 第 9 轮：更正第 8 轮那行「必定告警」的措辞（判官 r8 P2；§5 只追加，旧行原样留着）**。第 8 轮那行把删会话失败的口径写成「残留惰性 ＋ **必定**告警」，可同一轮的实现自己就写着 `Sentry.captureMessage` 会抛、抛了被吞掉 —— 告警是**尽力**的，不是必定的。正确口径：「残留惰性 ＋ 尽力告警（captureMessage 自身失败被吞、不影响这条路照样拒）」。代码注释（`lib/better-auth/gate.ts`、`lib/tenant-actions.ts`）、测试注释与 PR 描述同步改写；以本行为准。 |  |
| 2026-09-11 | **登录门④ 第 9 轮：更正第 8 轮变异表条目 I 的行号（判官 r8 P3；§5 只追加，旧行原样留着）**。那一行写的 `signin-pause-and-revoke.test.ts:396` 指到的是注释不是断言；本轮起变异表一律以**提交后 head 的行号**为准，条目 I 的真断言在 `apps/web/lib/__tests__/signin-pause-and-revoke.test.ts` 的「删失败留下的那张会话行是惰性的」用例里（本轮该用例的断言 ① 已按 P0 改写）。以本行为准。 |  |

## 6. 改签记录

- 2026-09-08 起草前：Founder 先裁四条（只开 Google／Google 登入并作废未验证密码／一个开关关所有门／首登直进产品），同日改令「直接和 Linear 一模一样，不用那么麻烦」，并选「两扇门，去掉密码」。本稿按后者；「一个开关关所有门」与「首登直进产品」两条保留，其余两条因密码下线而作废。#1260 票面同步改题。
- 2026-09-08 研究回填：四份事实册（better-auth 语义／现行闸与测试／历史与规格／首登副作用）经独立核验后并入 §1.4，新增 A13–A16。#980 与 GATE-A8 的处置方向由「作废密码」改为「密码整体下线」，#980 待本规格交付后关闭；`docs/DEFERRED.md` 此前并未登记 #980，实现 PR 一并补登记或关闭。
- 2026-09-08 审计回填：登录逻辑审计（6 个角度、99 名 agent、25 条证实／8 条驳回）并入：A7 加撤销删会话与后台可撤 active 行、新增 A17 反刷号、§1.4 加 Google 邮箱验证声明的真检查、§3/§4 登记残余项。25 条中 9 条因密码或注册页下线自然消失、9 条由本规格覆盖、6 条登记 DEFERRED（D-087 起）；开放跳转一条（审计 [11]）作轻改单独修（PR #1262），不进本规格。
