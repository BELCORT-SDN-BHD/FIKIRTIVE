# Creation 引擎 规格书（S1）

> 状态: 已冻结 · v2
> 批准: https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/issues/1101 Founder 评论「S1 批准 creation-engine.md」(2026-08-29 v1;2026-08-30 v2 改签重签)
> 规格前缀: CREATE（验收编号 = CREATE-A1、A2…）

## 0. 一句话

商家说人话、丢几张素材，就能拿到可直接提交投放流程的图片与短视频广告——提示词手艺由引擎代劳，花钱前看得见、改得了。（成片观感不入 S5 验收，由评测基线另场标定。）

## 1. 九问（S1 grill 的答案，2026-08-29 Founder 逐项拍板）

1. **商家做什么动作、看到什么结果？**
   五个直接变化：
   ① 商家在生成提交面输入人话，花钱前先看到引擎替他写好的专业提示词（**可改可直接用**，Founder 拍板「花钱前可见可改」）。
   ② 丢素材说人话，引擎**自动指派参考角色**（这张当产品参考、那段学镜头、这条配节奏——始终落在官方合法组合内），商家不学任何引用语法（拍板「全角色自动指派」）。指派结果以增强稿里的指派句呈现——商家改文字即改指派；点选式结构化改指派本版不做（见「不做」）。
   ③ 视频有**声音开关**（落点＝视频规格选择器；默认开）。**开关不影响我们的报价**（售价只看档位与时长，可实证），界面文案照此说，不让商家误以为关掉能省钱；供应商侧声音是否另计费列入 §7 实测轮核查。
   ④ 需要 1080p 高清时引擎自动升档，**报价照实前置显示**——商家只见能力与价格，不见型号名。新档位在钱引擎线定价落地前**不可售**（fail closed，见九问 5）。
   ⑤ **AI 形象出镜（演员库）**：平台自建虚拟代言人演员库——每个角色＝一张 Seedream **纯文生**多视角角色表（beta 首发 50 名，Arcads 演员库形态的虚构版），商家挑角色连续出片，同一角色跨场景同脸（2026-08-30 三场景实证），零认证零摩擦。上传真实人脸照片＝诚实拦截＋人话出路（提示口径 English sentence case：「Real human faces aren't supported yet」＋引导选演员库角色；Founder 2026-08-30 拍板）。真人出镜后续唯一正门＝字节 ACR 实名素材库（Entry 档已开通，见「不做」节触发条件）。
   片型**全部铺开**（Founder 拍板），不设片型白名单。片型表以手艺文件为**单一来源**；初始底稿＝社区指南场景节**全收**（中文版 12 节：角色一致、镜头复刻、特效模板复刻、视频延长、视频编辑、卡点音乐、对白配音、一镜到底、电商产品展示、科普教育、AI 短剧/漫改、视频融合/续写）——此处仅为快照，以手艺文件为准。

2. **入口在哪里？（列全，含深链）**
   不新增入口。生成提交面现有：画布 composer（直接生成）、画布卡片 animate、资产详情面板的三个付费动作（Regenerate／Animate／Generate edit）、Otto 对话（卡片确认）。本轮增强稿口径逐面写死：**画布 composer 与 Generate edit 必落**（两处都是商家自写提示词的直接花钱面）；两处 animate 本轮均不增强——事实口径按 2026-08-31 严口径重查纠正：资产详情 Animate 为模板化动作、无自由提示词；画布卡片 Animate 的 **Custom 选项存在一条自由文本运镜输入、原样进付费生成**（`FlowCanvas.tsx` Animate 弹窗），该通道要不要纳入增强稿与 A12 覆盖＝中途想法，登记变更登记待 S5 裁；**Regenerate 本轮改为重发该资产上次的 `sentPromptText`**（与商家批准的一致——否则增强出的片按一次 Regenerate 就变回生话直出）；Otto 路径随已冻结 otto-engine.md（ENGINE-A3）施工后在确认卡片上同源生效——「花钱前可见可改」是能力级口径，两种施工先后都成立，不与 ENGINE 冲突。参考音频的商家入口＝画布 composer 素材区（与图片/视频参考同一入口）。

3. **四态：空、加载、错误、成功各长什么样？**
   生成四态沿用现有画布卡片状态代数（`canvas-card-status`，不明即 unknown 绝不永转）。新增**增强预览**一态：输入 → 增强中 → 可改预览＋报价 → 提交。**增强失败不挡生成**：回退商家原文，预览处诚实标注「未增强」，照常可生成（错误不外溢到钱路）。

4. **数据从哪来、写到哪去？**
   - **手艺文件（单一来源）**：`seedance.md` 与 `seedream.md` 两份内容由本规格负责；**目录形状与路径以已冻结 otto-engine.md 的文件柜（S2 落地版）为准，路径随 ENGINE 走**，本规格不另定路径。内容四源合成，冲突时**以官方口径为最高准绳**：① 官方提示词指南（URL 见九问 7）；② 社区版 Seedance 2.0 指南（逐条证实/证伪裁定表随手艺文件交付）；③ arkcli 实查的参数契约；④ 现有 `seedance-prompt.ts`/`seedream-prompt.ts` 手艺代码沉淀（分镜≤4、情绪外化、声音记号、禁词清单）。官方口径的承重校正（手艺文件必须遵守）：**2.0 不认时间戳，只认 Shot 编号分镜**；**单镜头只写一种运镜**；素材指代＝「类型名＋序号」（官方示例 `@Image1` 等效可用）；官方建议素材 4–5 件效果最佳、不建议顶格；提示词 ≤500 中文字／1000 英文词。官方自家的 Seedance 2.0 提示词改写技能（sd2-pe，官方 TOS 桶分发）施工时取来对照。引擎增强与 Otto 对话**同源取用**这两份文件，不抄第二份；取用机制施工稿（S2）定。**CREATE 施工写入 craft/ 文件时，须重跑 ENGINE-A1 评测基线且不低于基线**——两份规格共用一套评测骨架（`packages/otto/evals/`），不另起炉灶。
   - **模型槽位注册（能力路由，两槽）**：视频＝mini（默认）／2.0（1080p 高清档）；图片＝5.0-lite（默认；组图）／5.0-pro（透明底、人物精修路由；标准图与大图两个 SKU，图层分离本版不卖）。**各槽位能力差异以逐槽 `supported_params` 实查回执为准（S2 开工首件事出回执）**；参考音频是 2.0 全系官方能力（走默认档 mini），不作升档理由。商家永远只见能力，不见型号（蓝图「供应商是内部实现」）。
   - **围栏现状与改造（设计不变量）**：视频侧今天有 fail-closed 毛利地板围栏（`FLAT_PRICED_VIDEO_MODELS`＋`assertSpendableModel`）；**图片侧今天没有等价围栏**；且现有闸每种类型只放行唯一在售型号，与能力路由结构不兼容。本规格施工范围包含把这道闸改造为：**「路由结果必须落在已定价、已过毛利地板的槽位白名单内；白名单外＝拒绝生成、$0，不是降级」**，并在图片侧建同形闸。fail-closed 语义一格不放宽。**「已定价」的机械判据**：该 SKU（槽位×分辨率/图种×时长档）在显式价目表里有自己的条目才算已定价；**兜底护栏格（如 `VIDEO_CREDITS_BY_RESOLUTION` 的 1080p=16cr）与图片侧对型号无感的「1 credit/张」一律不算已定价**——新槽位进白名单之前，这两处必须先改成对新 SKU 返回「无价」，属本规格施工范围。毛利表与 CI 闸（`margin-truth.ts`／`check-margin-floor.mjs`）里的图片 SKU 今天是手写死的两行、新增槽位不会自动进名单——pro 上架前须改成与 `sellableVideoSkus()` 同形的按 SKU 枚举，同属本规格施工范围（价格数字仍归钱引擎线）。另注：配置错误的**默认档**沿用今天机制（降级回白名单＋留日志）；商家请求的**路由结果**一律拒绝、不降级。
   - **输入上限按官方契约执行**（2026-08-29 arkcli 实查＋官方 API 参考）：Seedance 2.0 系全模态场景最多 **9 张参考图、3 个参考视频、3 段参考音频**；参考视频/音频**总时长 ≤15 秒**；**音频不可单独输入**；**首尾帧与全模态参考官方互斥**（不能混用）。图片侧**输入＋输出合计 ≤15 张**（官方），参考图上限＝15 − 本次出图张数。引擎在花钱前按此校验，超限诚实拒绝、$0——**此段为目标态**（2026-08-31 严口径重查澄清）：现网主生成路径对元素参考是「取到上限截断＋卡面披露实用张数＋照常收费」，图片侧上限为定值 10（`MAX_CONDITIONING_IMAGES`）；refgen 侧已实现 min(10, 15−出图数)。S2 施工须把主路径改为公式化上限＋花钱前拒绝 $0，处理与既有卡面披露机制的替换，并补图片侧上限的验收行。
   - **血统信任（原「信任回灌通道」，2026-08-29/30 实测改判）**：Seedance 视频端只信任 **Seedream 纯文生产物**作含人像参考（跳过输入审核；4 投 4 中实证）；Seedream 图生图产物、外部模型产物（GPT 等）、真人照片一律创建阶段免费拒收「may contain real person」（13 次实测全档；分销商侧一手佐证＝过滤在模型层、不可配置）。「30 天」时效口径未实证、不再承重——引擎实现为「演员库角色图一律 Seedream 文生产出、产物直引，不落任何外部改写路径」。行业同款机制佐证＝Artlist 帮助文档「Trust ecosystem」节（文生自动信任；图生图在账号实名后信任——该升级在我方直连是否成立＝待验，见变更登记）。
   - **provenance 沿用并扩一格**：`promptText`（商家原文）／`sentPromptText`（实发引擎的字符串，＝商家批准的增强稿逐字）／`finalPromptText`（供应商侧改写记录）三列语义不变；新增**路由理由**落盘，商家可见口径＝能力名词（「你要了 1080p，所以走高清档」）；**引擎不得为商家不可见的理由路由到更贵槽位**——那等于替商家花钱却给不出理由。参考角色指派结果随生成落盘（沿用 entitySnapshot＋sentPrompt 编号行机制）。
   - **配额口径**（2026-08-31 严口径重查回填纠正）：concurrent_requests=10、create_task_rpm=600 是**整个账户的共享总量**，不是各模型各自可加总——仓内 2026-08-08 已实测并落码（`apps/worker/src/plan.ts` 明注「这是整个账户的额度」；gen 与 refgen 共用同一闸门实例，实际可用并发＝10−2＝8）。原「是否另有账户级总闸未查，S2 前补一次」**取消**——答案早已在仓内。

5. **碰不碰钱路（credits / 计费）？碰则幂等键是什么？**
   碰。人话版：老三样（预扣→交付→结算，失败退款）一格不动；本规格新增的只有「没定价的档不许卖」这道闸，和增强这笔平台自付的小成本。
   - **沿用 reserve→settle→refund 单一通道与既有幂等键（`refId`=GenJob.id、`canvas:`/`cowork:`/`asset:`/`batch:` 四族请求键），不新增键。** 成立前提（S2 必须守住）：增强稿不得做成按次计费或带独立付费副作用的东西。
   - **新档位定价不在本规格**：Founder 2026-08-29 裁定——毛利与定价的事**归钱引擎线统一处理**，creation 引擎只管 creation。本规格只立规矩：①未定价槽位不可售（fail closed，见九问 4 围栏不变量）；②任何新档定价必须过仓库法毛利地板。1080p 档与 pro 图两 SKU 的 credit 数字由钱引擎线算出后回填变更登记、Founder 追认。
   - **增强成本**：平台吸收，不入商家账、不动现有单价（Founder 拍板）。上界与口径：单次增强成本设计上界 **$0.01**（按注入上限换算的封顶值，施工时以实测校准）；每个生成动作增强调用 **≤6 次**（1 次自动＋手动重写至多 5 次）；成本计量为**独立指标**（照 `founder_absorbed` 计量先例），不悄悄摊进档位 COGS；是否计入毛利口径由钱引擎线统一裁，登记变更登记。
   - 供应商实价（2026-08-29 arkcli 实查，USD 牌价）：视频按 K tokens——mini NV2V **$0.0035**／2.0 **$0.007**（1080p $0.0077）；图片按张——lite **$0.035**／pro **$0.045**（大图 $0.09、图层 $0.0225 计价单位未实证）。**「账户 25% 折扣」经实查不存在**（推翻 2026-08-08 档案）；mini 60% off 与 fast 25% off 两档公开促销均 **2026-09-07 到期**。定价与毛利不受影响（一直按牌价算，`spend.ts` 现行做法，列为不变量）；但给 Founder 的白话——**9 月 7 日之后我们付给供应商的视频账单约涨到今天的 2.5 倍**。账户并发与限速见九问 4 配额口径。
   - 已知钱路暗礁随规格记录：图片模型供应商侧 `watermark` 默认 true（我们已显式关）。2.5 相关未验证项不入本规格。

6. **权限与租户边界是什么？**
   人话版：谁能花钱、花谁的钱、要不要审批，一律不变；增强层只碰提示词文字，碰不到钱、身份和审批。
   不变量清单：身份只来自 `requireOwner()`；手艺文件不携带、不接触身份；信任 WeakMap（服务端铸键）、幂等键形状-出处双校验、fail-closed 模型开关（`ModelRegistryOverlay`）全部沿用。

7. **参考对照：抄哪家？（Mobbin 截图或链接，稿上注明）**
   - 官方提示词口径（2026-08-29 网页实查，最高准绳）：Seedance 2.0 系 prompt guide https://docs.byteplus.com/en/docs/ModelArk/2222480 ＋ tutorial https://docs.byteplus.com/en/docs/ModelArk/2291680 ＋ 视频 API 参考 https://docs.byteplus.com/en/docs/ModelArk/1520757 ＋ https://docs.byteplus.com/en/docs/ModelArk/2607689（该页是 2.5 指南，但本规格三处承重取用——九问 4 的「2.0 不认时间戳」「单镜头单运镜」与 A8 的「官方镜头术语表」——的官方原文出自此页的 2.0 对照节，取用范围随手艺文件逐条留档）；Seedream prompt guide https://docs.byteplus.com/en/docs/ModelArk/1829186（官方将 5.0 也指向此页）＋ 图片 API https://docs.byteplus.com/en/docs/ModelArk/1541523 ＋ 5.0-pro tutorial https://docs.byteplus.com/en/docs/ModelArk/2582774。
   - 社区版 Seedance 2.0 指南 https://github.com/dexhunter/seedance2-skill（即梦 App 形态；对 API 的逐条证实/证伪裁定表随手艺文件交付）。
   - 增强稿「先出稿再让你改」形态：Canva Magic Write（凭印象，未取证）。
   - 画布对话形态：Grok Imagine（已在 otto-engine.md，不重复）。

8. **胃口：轻／中／重挡，为什么？**
   重挡：碰钱路（围栏改造）＋ 新模型槽位与新输入类型（音频参考，落在现有素材区、不新增入口）＋ 生成提交面行为变化（增强预览）。

9. **Otto 怎么协助这个功能？或明写「不适用」。**
   同源协助：Otto 的 `seedance-prompt`/`seedream-prompt` 动作与引擎增强取用同一份手艺文件（正名与改造随 otto-engine.md 施工，不在本规格重复）。Otto 路径上「增强稿可见可改」的落点与时序同九问 2：ENGINE-A3 施工后在确认卡片上生效。

## 2. 验收表（S5 只认这张表；一行一个可当场演示的判定）

| 编号 | 商家做 X | 看到 Y |
|---|---|---|
| CREATE-A1 | 商家在画布 composer 或资产详情 Generate edit 输入人话、按生成 | 花钱前先见增强稿预览，可编辑可直接用；同一条人话走增强预览提交与直接提交，前置报价数字相同（otto-engine.md 的画布对话验收落地后，画布路径的判定落在 Otto 确认卡片上） |
| CREATE-A2 | 商家丢产品图＋一段参考视频，说「照这个感觉拍我的产品」 | 引擎自动指派角色（图＝产品参考、视频＝镜头参考），指派句在增强稿中可见、改文字即改指派；无需任何引用语法。商家改出的指派若越出该素材的合法角色枚举、或撞上官方互斥组合（首尾帧×全模态参考），花钱前诚实拒绝并说明原因、ledger 零新增行 |
| CREATE-A3 | 商家在视频规格选择器关掉声音开关后生成 | 交付视频无 AI 配音配乐（`generate_audio=false` 实发可查）；界面明示声音开关不影响报价 |
| CREATE-A4 | （工程演示）为 1080p 档配置一条过毛利地板的测试价后，商家要求 1080p | 路由到高清档；前置报价数字＝ledger 中 `reserve:<refId>` 绝对值＝`settle:<refId>` 绝对值；全程界面与消费历史不出现型号名；该次路由理由（能力名词）可查。测试价仅存于演示配置，演示后撤回并重跑 A5 同形判定（1080p 回到拒绝、$0）；生产价目以钱引擎线回填、Founder 追认的数字为准 |
| CREATE-A5 | （工程演示）把默认视频模型环境变量指向未定价/未过地板槽位；另直接请求一个未定价槽位的能力 | 前者被降级回白名单并留日志；后者拒绝生成、ledger 零新增行（不是降级） |
| CREATE-A6 | （工程演示）图片侧同形：请求未定价的 pro SKU | 拒绝生成、$0——图片围栏与视频同形生效；演示同时出示该 SKU 在显式价目表无条目（对型号无感的兜底价不算已定价） |
| CREATE-A7 | 增强服务不可用时商家照常提交 | 用商家原文生成，预览处诚实标注未增强；生成与计费不受影响 |
| CREATE-A8 | （工程演示）跑增强评测：≥10 个人话任务，覆盖手艺文件片型表逐项各≥1 题 | 输出全部通过机械检查：角色指派完整、分镜为 Shot 编号结构（零时间戳）、禁词零命中、镜头词全部命中手艺文件 seedance.md 的镜头术语表（该表来源逐条注明）；检查项与判分落 `packages/otto/evals/`；且改动 craft/ 文件后重跑 otto-engine.md 的评测基线（其验收表第一行），总分不低于基线 |
| CREATE-A9 | 商家上传含真人脸（或任何非平台血统的写实人像）的参考图生成 | 创建阶段拦截＋人话提示（「Real human faces aren't supported yet」口径）＋出路指向演员库角色；余额净变化为 0（ledger 上 `reserve:`/`refund:` 成对、无 SETTLE），绝不静默失败 |
| CREATE-A10 | 商家选演员库角色（平台 Seedream 文生角色表）在不少于两个不同场景连续出片 | 正常生成，不触发人脸拦截（血统信任，2026-08-30 三场景 3/3 实证）；跨场景同脸由 Founder 验收样片判定；生成记录可查到所引角色（引用落盘） |
| CREATE-A11 | 商家丢一段音频说「照这个节奏剪」；另丢 4 段音频（或总长超 15 秒） | 前者自动指派为节奏参考、报价前置；后者花钱前诚实拒绝、给出人话原因、ledger 零新增行 |
| CREATE-A12 | （工程演示）任取一次走增强路径的生成（含对该资产按一次 Regenerate），查记录 | `sentPromptText` 与商家批准的增强稿逐字一致（Regenerate 重发同一串）；路由理由字段有值可读 |

## 3. 不做（非目标；写明为什么和触发条件，防「遗漏」误会）

- **不接 Seedance 2.5**：牌价 $10.70/M＝mini 三倍，破毛利地板（2026-08-08 已裁，`gen.ts:40-44`）；且 2.5 换了提示词底座（sd25 模板），2.0 手艺不能直迁。触发条件：价格降到地板之上，或评测证明 2.0 家族卡住质量上限。
- **不接 Seedance 1.5-pro**：上一代模型，参考角色与手艺规则与 2.0 家族不对齐，接它＝维护第二套手艺（顺带记录事实：1.x 侧尚有约 195 万 tokens 免费额度与批量计费项）。触发条件：2.0 家族被证实卡住某类片型时，再评估其免费额度价值。
- **不接 2.0-fast 槽位**（Founder 2026-08-29 拍板改两槽）：「参考音频独有」被官方口径证伪（全 2.0 系都收），独有升档理由不存在；按现行 720p 档价格出片毛利 44.7%（按牌价推算）破 45% 地板。触发条件：评测证明 mini 质感卡住，且钱引擎线为它单独定价过地板。
- **不做 seed 复现（「一模一样再来一张」）**：2026-08-29 实查，官方目录已将 seed 标为 Seedance 2.x 全系 `support: false`。触发条件：官方目录重新声明支持后实测再议。
- **不做视频多条同出**：今天强制 1 条；多条＝花费翻倍＋报价 UI 另案。触发条件：商家真实需求出现。
- **不卖图层分离（pro 独有能力）**：官方计价单位语义未实证（预扣 17 IPM 模式）。触发条件：商家需求出现＋单位实测＋钱引擎线定价。
- **不做点选式结构化改指派**：本版指派随增强稿文字可改；触发条件：商家真实反馈要求点选改。
- **真人出镜广告——beta 不做，正门已定**：v1 的「身份层前置」（商家照片→图像模型出 AI 肖像→进 Seedance）已被 2026-08-29/30 实测**证伪**——Seedream 图生图血统与外部模型（GPT）血统的写实人像，视频端一律拒收（连纯文字生成的外部产物、带 AI generated 标记版也拒；13 拒零过；证据 preserved/creation-probe-2026-08-29/）。分销商换道亦**证伪**（2026-08-30 七家调查＋判官复核：过滤在模型层、分销商不可配置，且无一家便宜过直连；结案表见变更登记）。beta 口径＝Founder 2026-08-30 裁决：真人照片诚实拦截（A9）。真人出镜后续唯一正门＝ACR 实名素材库（见下条）；Kling／MiniMax 海螺／Google Veo 二引擎降为 ACR 被拒后的备选（Q17 裁决沿用）；「Seedance/Seedream 独家且保密」原则因不换供应商而无需修订。
- **ACR 真人素材库——beta 不做，门已开着**：Entry 免费档已由 Founder 2026-08-30 在控制台开通（4 份条款已签；`liveness_writable:true` 实查回执在案），全账号 50 个真人名额、逐人扫码活体＋平台代劳同意机制；付费档 $14,000/年起。v1「对多商家 SaaS 走不通」修订为：beta 期 50 名额够用、正门保留暂不接。触发条件：商家真人出镜需求出现＋Founder 点名——先跑 Founder 本人活体实测（顺带 $0 验「实名后 Seedream 图生图是否获信任」彩蛋）。注：虚拟形象上传写权限实查仍关（`aigc_writable:false`），演员库以角色表参考图交付，不依赖素材库。
- **新档位定价不在本规格**：Founder 2026-08-29 裁定归钱引擎线统一处理；定价落地前新槽位不可售（fail closed）。触发条件：钱引擎线开场。
- **不动画布输入框→Otto 对话**：那是 ENGINE-A3 的验收，本规格不重复、不抢跑。触发条件：ENGINE 施工。
- **不做 MCP 化、外部 agent 接入**：沿用既有 deferred 与 otto-engine.md 裁决。

## 4. 异议栏（AI 必填）

- 最大风险：**增强层是 LLM 改写，可能歪曲商家意图**——商家批准的增强稿与他心里想的片子有偏差时，钱已花出。对策三条全部入验收：预览可改（A1）＋增强质量机械检查（A8）＋批准稿与实发串逐字一致、路由理由可查（A12）。另记一实：能力路由替商家挑档＝替商家花钱，对策＝报价前置（A4）＋路由白名单 fail closed（A5/A6）＋路由理由只认商家可见的能力名词（九问 4）。

## 5. 变更登记（两类条目：① 冻结后的中途想法——不当场执行，下次 S5 批量裁决；② 已批事实与他线裁决的回填（如钱引擎线定价）——Founder 追认即生效，裁决列填追认日期）

| 日期 | 想法 | 裁决（留空待 S5） |
|---|---|---|
| 2026-08-30 | 实测轮结案回填（实验 1/2/5）：①真人脸直传＝创建阶段免费拦截（与预期一致）；②「30 天信任回灌」机制被证伪，改判**血统信任**（Seedream 纯文生 4/4 过门；图生图与外部血统 13/13 拒，含分胜负实验与跨厂商对照）；③Seedream 图片端不拦真人脸＝证实，但其图生图产物进视频被拒→「身份层前置」证伪。证据：preserved/creation-probe-2026-08-29/（team1/2/3 回执＋sheet-test-20260830/RESULT.md） | 随 v2 重签追认（2026-08-30） |
| 2026-08-30 | Founder 裁决：beta＝自建演员库（50 名 Seedream 文生虚拟代言人，Arcads 形态）＋真人照片诚实拦截；真人出镜后续走 ACR 正门 | 2026-08-30 |
| 2026-08-30 | 分销商调查结案（Higgsfield/Artlist/fal/Freepik/Krea/Replicate/聚合商）：无一家解锁真人参考（一手引文＝「blocked at the model level…not configurable」），无一家低于直连 $1.89/条（5s·1080p 实价 $1.97–$3.58；Artlist 无视频 API 且条款禁 automation/resale；Higgsfield 服务端 API 无 2.0）——维持直连 | 随 v2 重签追认（2026-08-30） |
| 2026-08-30 | 待验彩蛋登记：账号实名/活体完成后，Seedream 图生图产物是否获视频端信任（Artlist 文档口径「Image-to-Image after account identity verification (KYC)」；我方直连未验）——Founder 活体日顺带 $0 实测 | 登记待验 |
| 2026-08-30 | Founder 裁决：beta 演员库规模由「首发 50 名」改为**创始 5 名即全量**（A1–A5 五人组已定妆：Aisyah/Weijie/Arjun/Rahman/Xinyi，组图法产出特写＋全身对、五脸两两互认 QC 过；无损原件＋人物卡归档 preserved/actor-library-v1-2026-08-30/）。扩产与否 beta 后再裁；九问1⑤「beta 首发 50 名」按本条口径读 | 2026-08-30 |
| 2026-08-30 | **像素完整性铁律**（九问4「产物直引」的工程收紧，Founder 当日追问确认）：血统信任的标记在**像素**里，演员图必须**端到端保留 Seedream 原始产物字节**。实证两笔：①原字节重新上传（base64 直传）过门出片（task `cgt-20260830161827-p28pq` succeeded）；②对已过门文生图做**裁剪**后提交＝拒收「may contain real person」（request id `021788096448297c…`，任务未创建 $0）。工程规则：提交视频端一律用无损原件；显示用缩略图/裁剪图只能另存副本、永不回流生成路径；缩放/滤镜/加字/转格式/再压缩等一切像素级再处理**未实测＝未验先禁**。证据 preserved/creation-probe-2026-08-29/pixel-integrity-20260830/ | 2026-08-30 |
| 2026-08-30 | Founder 两想法＋当场实证三笔：①「参考图不加钱」**证实**——成片账单 `prompt_tokens: 0`，输入侧（提示词＋参考图）零计费，只按输出视频计费（单参考与多参考同价 245,025 tokens/5s·1080p）；组图上限实证 `max_images` 1–15/次（API 校验回执）。②职业/类型 preset 判定＝**prompt 层换装成立**：素装定妆对＋一句厨师服 prompt → 同脸换装出片（task `cgt-20260830213513-zb8zt`，样片 preserved/actor-library-v1-2026-08-30/wardrobe-test-chef.mp4）。落地形状＝人物卡加 wardrobe preset 块（厨师/门店/诊所/健身/办公等，S2 定清单），零新图零重铸；职业造型**展示**图走显示层（可 i2i，按像素完整性铁律永不回流生成路径）。③character sheet **免重设计**判定：视频端角色图 1–2 张＝官方配方上限（多则同脸漂移），免费参考图真正解锁的是同请求免费加挂商品/场景图（官方甜点 4–5 件总素材）；多图请求需逐图 `role`（`reference_image`，实证）。 | 2026-08-30 |
| 2026-08-30 | Founder 拍板:演员库**两轴模型+九套造型 preset+新增演员铸造法**——后续每次加 avatar 一律遵循本行。**两轴模型**:演员(定妆对+人物卡)× 造型 preset(prompt 层 wardrobe 块)正交组合,任何演员可穿任何 preset;preset 定的是「意图」,细节按人物卡适配(如 Aisyah 一律 hijab 友好 modest 版、厨师装=围裙版不动头巾)。**九套 preset(Founder 2026-08-30「就先这九套」)**:①素装(默认定妆原样)②Street wear(UGC/年轻向)③厨师后厨白制服(已实测过片)④门店服务(polo/围裙前场,咖啡/零售/便利店)⑤商务(blazer/smart casual,房产/金融/B2B)⑥医护(白袍/刷手服,诊所/牙科/药房)⑦美容沙龙(黑制服围裙)⑧健身(运动装)⑨节庆传统装(Raya baju kurung/melayu、CNY 旗袍、Deepavali kurta)。每套上架前 mini 480p 各验一条同脸(约 $0.05/套,帽饰头饰类重点盯同脸稳定性),验过才挂给商家。**新增演员铸造法**:①人物卡先行(ID/FACE/HAIR/身高/BUILD/WARDROBE+防撞脸独有特征各不相同),与库内全部现役演员**两两互认** QC 过才收编;②seedream-5-0 组图(sequential auto,max_images=2)一次调用出「特写+全身」同身份对(官方配方 ModelArk/2222480:竖幅正面、特写无表情脸占 2/3、全身正面;2.0 禁多视角拼表);③素装统一制服+#8a8a8a 棚灰背景+photorealism 拉满;④`.bin` 无损原件=生成层唯一资产(像素完整性铁律见上,端到端直传永不再处理),`.jpg` 仅展示;⑤preset 适配块写进人物卡,Otto 与 UI 同源取用(一卡三用)。 | 2026-08-30 |
| 2026-08-31 | 严口径重查落账（Founder 裁决「严口径，重查 Creation」；125 条主张逐条对码＋Opus 复核＋Codex 跨厂判官）：①九问2 animate 口径为事实错误，已按改签记录机械纠错；②九问4 配额口径纠正（账户级总量）；③九问4 输入上限段标注为目标态；④中途想法：**画布 Animate Custom 通道要不要纳入增强稿＋A12 覆盖** | ①②③ Founder 2026-08-31 拍板「机械纠错，本轮不增强」；④留空待 S5 |
| 2026-09-01 | 钱引擎线定价回填(类型②,money-engine.md 九问4 已冻数字+S2 施工稿 7.2 落地):**1080p 视频=11cr/秒**(5 秒=55cr,毛利 65.7%)、**pro 图=2cr/张**(毛利 77.5%)——公式=成本×1/(1−0.65) 向上取整到收费格;上架仍随本规格施工(围栏改完才可售),数字待 Founder 追认 | |
| 2026-09-02 | **Founder 现场令**：「今天直接做完一个部分——像在 beta 那样用 Creation 的所有东西，要真实的、完全可用的版本、真的 product」。本稿据此把 12 条切三批（§8）；**「一 session 一阶段」本场例外**：S2 呈批与批 I 施工同日进行，范围只限本场（Founder 现场裁决，写回于此） | 2026-09-02（随 S2 批准生效） |
| 2026-09-02 | §7 实验 4 结案（零成本只读，`arkcli models get … --transform supported_params`）：mini 与 2.0 的 `supported_params` **唯一差异是 resolution 枚举**（mini=[480p,720p]，2.0=[480p,720p,1080p,4k]），其余 20 个参数逐字相同；价目表 ChargeItems 无任何音频计价项＝`generate_audio` 在价目层面不另计费；图片 lite `optimize_prompt_options.mode` support:false、pro 有 `optimize_prompt`(boolean, default true)。与规格预期一致，回填。原始 JSON 归档 preserved/creation-probe-2026-09-02/ | 回填（随 S2 批准追认） |
| 2026-09-02 | §7 实验 3 **问题形状被实查改变**：在产图片型号 seedream-5-0（lite）没有可关的优化开关；只有 pro 有 `optimize_prompt`（默认 true）；我方请求体从不发该字段＝一直跑服务端默认。承重口径「`sentPromptText`＝商家批准稿逐字」**未被推翻**（它管我们发出的串；供应商改写记录在 `finalPromptText`）。剩余 A/B（pro 开/关各一张，$0.16–0.48，§7 已批额度内）在批 I 执行，结果决定 pro 请求体默认值 | 待批 I 回执 |
| 2026-09-02 | 演员库归属模型（§8.0 拍板建议）：**每租户播种**——org 引导时把 5 名创始演员各建一份 CHARACTER 实体＋2 张参考图，字节经 `storage.put` 原样落各 owner；`Entity` 新增可空列 `catalogKey` 标记官方角色；**不引入跨租户共享实体**，租户边界零变化 | 随 S2 批准生效 |
| 2026-09-02 | **Founder 裁决（分两阶段验收）**：§8 批 I（今晚：现有生成全链在新前端、1080p/pro 上架与报价前置、未定价拒绝、声音开关、演员库五人可经 `@` 引用、真人脸口径）作为**阶段一**单独出 S5 证据表由 Founder 勾选（只勾批 I 覆盖的验收行：A3/A4/A5/A6/A9/A10 后端半/A12 路由理由半）；批 II＋批 III 合为**阶段二**＝完整 Creation，另出 S5。Founder 原话：「这个当一个阶段，我验收；完整的 creation 当第二阶段」；并令「全速前进，不浪费 attention 与资源，做好 checkpoint 就报告然后继续」 | 2026-09-02 |
| 2026-09-02 | **§7 实验 3 第二轮结案**（Founder 清欠费后重跑，pro 图开/关各一张，估算 ≈$0.19，T+1 账单待核）：①API 响应不暴露任何改写字段（`prompt` 与发出串逐字相同，无 revised_prompt 类字段）→「sentPromptText＝批准稿逐字」不受影响，供应商侧改写无从记录进 `finalPromptText`；②质量：`optimize_prompt=false` 那张凭空出现提示词未要求的乱码标签，`true` 那张构图更贴主体，且同一 size 下画幅不同（true 竖版/false 正方）。**拍板四落定**：按 §8.0 规则「增益明显→保留默认」，pro 请求体**不显式关闭**优化（保留供应商默认 true）；证据强度＝单样本、未固定种子，标注弱证据。触发再验：固定种子多组配对（预算另计）。证据 preserved/creation-probe-2026-09-02/experiment-3/RESULT.md | 2026-09-02（依 §8.0 已批规则落定） |
| 2026-09-02 | **Founder 裁决（阶段一收尾与阶段二衔接）**：阶段一全部 PR 合入并本机起动后，**先由 agent 跑一轮真实端到端验证**（真供应商出图出片、真扣费、真账本、演员引用、真人脸拦截、高清档报价、声音开关），证据表齐了再交 Founder 验收；Founder 验收期间本机服务（localhost）保持开着所需页面；**若无 blocker，agent 不等验收结果直接开阶段二**（§8.2 批 II）。Founder 原话：「在这个阶段完毕的时候，先帮我走一轮真实的 e2e，验证所有东西。然后等我的审核的时候（with local host 开着我需要的页面）你就继续 phase 2（如果没有 blocker）」 | 2026-09-02 |
| 2026-09-02 | CREATE-A3 阶段一只在资产详情 Animate 路径提供声音开关（PR #1133，判官裁定不展示死开关）；画布两条视频路的开关在阶段二（批 II）接线；阶段一验收 A3 按此口径勾。触发＝Codex 第二轮复审指出。证据：`apps/web/components/asset/DetailPanel.tsx` 传 `audioToggle`、`apps/web/components/canvas/FlowCanvas.tsx` 不传；围栏与行为测试 `apps/web/lib/__tests__/video-audio-toggle.test.ts`、`apps/web/lib/__tests__/canvas-video-spec-ui.test.ts`、`packages/generation/src/byteplus-audio.test.ts`；#1133 合并于 main `add6999e`（落修 commit `c3d36d7b`） | 待 Founder 追认 |
| 2026-09-03 | 上传素材 Regenerate：因 regen 路不携带源图（`handleRegen` 无 `sourceGenerationId`），兜底句会让商家花钱得到无关结果，暂维持拒绝；待 i2i 请求形状接上后再开放。触发＝PR #1145 修复者指出。证据：`apps/web/components/asset/DetailPanel.tsx` 的 `handleRegen` 请求体无 `sourceGenerationId`；兜底表 `ASSET_ACTION_FALLBACK_PROMPTS`（`packages/core/src/gen.ts`）只留 `animate`；行为测试 `apps/web/lib/__tests__/animate-uploaded-source.test.ts`「上传的图按 Regenerate ⇒ 原地拒收、$0、连 GenJob 都不建」 | **2026-09-03 Founder 裁决**：维持拒收，但**先把拒绝提示改成人话**——如 "Uploads can't be regenerated yet. Try Animate or Edit instead."（今天商家撞到的是 schema 层的整单拒绝，不是一句读得懂的话）；**图生图（i2i）归 Creation 增强层，排进 §8.2 批 II**，请求形状接上后再开放 Regenerate。**落地（人话拒绝句）**：PR #1148 → main `93b72293`。同条已登记 frontend-baseline.md §5（2026-09-03「裁决八」） |
| 2026-09-03 | **staging 真商家走查 S2／S3（浏览器直传）**：①商家看到的是上传库自己的原话「Unknown error」（`OttoChatStream` 的附件报错框 + 两处 `err.message` 直出）——不是我们写的话,也没给出路;②直传的字节走「浏览器 → 存储桶」,服务器不在路上,失败时 web 日志一行都没有,我们零感知。触发＝2026-09-03 staging 走查（桶的 CORS 把直传挡在门外） | **2026-09-03 Founder 令「全速修」**：诚实文案 + 服务端留痕,无钱路变化。落地：失败分两类各一句、单一来源 `UPLOAD_FAILURE_COPY`（`packages/core/src/upload.ts`,上限引用 `UPLOAD_MAX_BYTES`,底层原文只进日志）;直传失败一律回报服务端 `reportDirectUploadFailure`（org 取自 `requireOwner()`,报告体只有枚举与数字,不带文件内容与凭据),服务端打一行可 grep 的 `[upload] DIRECT-UPLOAD-FAILED`。行为测试 `apps/web/lib/__tests__/upload-failure-honest-copy.test.ts`、`apps/web/lib/__tests__/upload-failure-report-log.test.ts` |
| 2026-09-03 | **staging 走查 S4（演员库/素材库删除不真删资产）**：商家删掉一个演员（Entity）只软删 Entity 那一行，底下的 `ReferenceImage` 与存储桶里的字节一直没人删——「商家的 data 商家的权利」不成立，一张真人照片删了之后字节还留在桶里。触发＝2026-09-03 staging 走查（Founder 本人一张真人定妆照）。数据删除类改动，回滚说明：Asset/ReferenceImage 的软删可逆（`deletedAt` 置回 null），**存储对象的物理删除不可逆**——`packages/storage` 的 `deleteObject` 落在两个驱动上都是硬删，没有回收站 | **2026-09-03 Founder 裁「现在就修」**：`softDeleteEntity` / `softDeleteReferenceImage`（人工 UI 与 Otto 共用同一层，`apps/web/lib/actions.ts`）级联把该实体/该参考图**独占**的 Asset 标记 `deletedAt` 并调用 `storage.deleteObject` 真删对象；判据（`apps/web/lib/asset-purge.ts`）＝独占当且仅当没有任何活的 `ReferenceImage`（不分实体/变体）也没有任何 `Generation`（不分 `deletedAt`——生成历史「不可变，永不物理删」）还指着它，共享引用只解引用、不动对象；租户约束沿用既有 `ownerId` 闸。存量清理脚本 `scripts/tools/purge-deleted-entity-assets.ts`（默认 dry-run 只打印计数，`--apply` 才真删，幂等可重跑）补齐修复之前已软删实体漏下的资产。行为测试：`apps/web/lib/__tests__/entity-delete-purges-asset.test.ts`（独占真删／共享只解引用／Generation 永不可删／双租户隔离）、`apps/web/lib/__tests__/purge-deleted-entity-assets-script.test.ts`（脚本 dry-run／--apply／幂等）。**已知未接的口子**：`deleteVariant`（`apps/web/lib/refgen-actions.ts:545`）删变体时只软删该变体的 `ReferenceImage`，从未调用 `purgeOrphanedReferenceAssets`/`purgeAssetStorage`——变体产出的资产不建 `Generation` 行，按同一判据本可真删，但字节至今没人删；本票未接，判官第二轮复审 P1-B 指出、留给另一票单独带测试做（那条事务已经握着 EntityVariant 行的写锁做在飞作业闸，本票不建议顺手接进去，混进另一个更敏感的锁语义）；同时登记 issue #359 |
| 2026-09-04 | **staging 真商家走查 P0-1／P0-3／P0-4＋P1 四条（Otto↔画布接缝）**：①付费结果永远到不了画布——Otto 卡走到「✓ Done · it used 1 credit」、余额 73→72，画布**全程空白**，按 F5 图才出现（病根：`NorthstarCanvasWorkspace` 只接了余额，没把「这条对话有付费生成在跑」接上 `FlowCanvas` 已有的 `activity` 通道）；②Otto 说「你会在上面看到两张卡」而确认卡藏在默认折起的 Conversation 抽屉里，始终可见那张 Otto 卡没有确认位；③49 秒一句写死的忙碌文案 + 卡住的「Sending…」；④始终可见卡里露出原始 Markdown 与内部占位串「🖼 result」；⑤Otto 口头宣称「两张卡都改成 1080p 了」而卡上仍是 720p／11cr；⑥新做好的视频节点黑屏到刷新（`poster` 为 null）；⑦Otto 面板在 `/create` 自开成第二个入口、并在 `/library` 压掉筛选行最后一格。触发＝2026-09-04 走查（证据 `scratchpad/creation-friction-audit.html`，含逐条截图），Founder 12 小时令「像原型一样顺」 | **2026-09-04 修法（钱路语义零变化，全部落在 CREATE-A1「画布路径的判定落在 Otto 确认卡片上」）**：①接上 `activity`（翻 true 重读画板→服务端 `syncOttoCanvasNodes` 放下在飞占位卡；翻 false 再读一次换成产出），并在 Otto 生成期间沿用已有的 5 秒画板轮询，不赌批准那一瞬间的时序；②新 `apps/web/components/otto/OttoTurnCard.tsx` 把确认位（件数／规格行／价格／参考行／Change＋Generate）放进那张 280px 可见卡（设计源 `CanvasReference.tsx:270`），两个按钮共用 `apps/web/components/otto/plan-approval.ts` 一份动作——同一条 `ottoApprove`／`coworkGenerate` 分支、同一个 cardId+threadId 身份、同一条先披露后扣、同一个 `planCardGate` 门（报不出价两处都不出按钮）；③状态与进度句由 `apps/web/lib/otto-canvas-turn.ts` 从**已有**信号派生（`data-step` 工具标签／`data-status` 三句叙述／`CardState`），30 秒无变化补 `STILL_WORKING_NOTE`（`lib/progress-format.ts` 同一处作者），发送键按 `submitted`／`streaming` 分「Sending…」「Working…」；④正文走 `OttoMarkdown` 且只认真的 TEXT（`latestAssistantSayable`）；⑥`apps/web/lib/video-first-frame.ts` 用媒体片段 `#t=0.001` 让浏览器在元数据阶段解出首帧（片段不发服务器，签名 URL 无影响）；⑦`/create` 收进 `panel-surface.ts` 的「这一面自己已经有一个 Otto」名单、素材库筛选行补 `min-w-0`。行为测试 `apps/web/lib/__tests__/creation-otto-canvas-seam.test.tsx`、`creation-otto-canvas-activity.test.ts`、`creation-otto-canvas-result-lands.test.ts`（真库）、`e2e/journeys/16-otto-canvas-seam.spec.ts`。**两条留给 Founder 裁**：见下一行 | 落地待合并 |
| 2026-09-04 | **①（走查 P1-2 的另一半）`propose` 工具根本没有 resolution 这个字段**——`packages/otto/src/skills/propose.helpers.ts` 只收 `desiredAspect`／`desiredDuration`／`desiredAudio`，分辨率由系统按档位表决定，所以模型**改不了**它；Otto 说「两张卡都改成 1080p 了」是一句它做不到的话。本票只加了一条禁止宣称卡面没有的规格的指令（`packages/otto/src/instructions.ts`），并让确认位逐字显示卡自己的 `specChips` 与价格。真要让商家改档，得给 `propose` 加 `desiredResolution` 并接上按分辨率的价目——**那是改价的路，属于钱路**，不在接缝票里做。**②（走查 P1-8 的另一半）Otto 面板「首开默认开」**是 Founder 2026-08-18 Q3-A 的裁决（落在 `panel-state.ts` 的 `defaultOttoPanelState`）；走查建议改成「只在有活动对话／用户上次打开时展开」。本票**没有动这条默认**，只把 `/create` 收进既有的「这一面自己已经有一个 Otto」名单（副作用：`/create` 上不再有面板与 launcher，与画布同形）。 | 留空待 S5 |
| 2026-09-04 | **staging 顺滑度走查 P0-2／P0-5（下载与画布加载失败）**：①商家按素材详情的「Download」或画布选中工具条的「Download N」，浏览器不是存文件而是**导航去 R2 的裸地址**（`…r2.cloudflarestorage.com/….mp4`）——人出了应用、片子也没存下；根因＝`/files/…` 在 r2 模式下 302 跨源，`download` 属性跨源被浏览器忽略。②`/create` 与 `/create/canvas` 只有 Suspense 骨架、没有自己的 error boundary，服务器一次 502 之后骨架**永远转下去**，商家既看不到出错也没有重试或回头的路。触发＝2026-09-04 staging 顺滑度走查（Founder「12 小时内 Creation 完全可用」令）。本行属类型①中途想法，验收编号沿用现有 CREATE-A 表（下载与页面加载失败不在其中任何一行，不发明新编号；测试标题逐字带「登记 2026-09-04 P0-2／P0-5」） | **落地（本 PR）**：①下载改**同源附件流**——`/files/<key>?download=1&name=<人话文件名>` 由服务端校验租户（key 命名空间比对＋资产须属当前 org 且未软删）后流式转发字节并加 `Content-Disposition: attachment`，R2 地址不再交给浏览器；改写规则单一来源 `apps/web/lib/download-url.ts`，文件名沿用 `canvas-selection.ts` 的 `canvasDownloadFileName`（详情与画布同名）。②新增 `apps/web/app/create/canvas/error.tsx` 与 `apps/web/app/create/error.tsx`：人话文案＋「Try again」（`reset()`）＋回 Create／Otto 的路，原始报错不印给商家但照旧上报 Sentry。行为测试：`apps/web/app/files/__tests__/route-download.test.ts`（真库：本租户 200＋attachment／他租户 404／软删 404）、`apps/web/lib/__tests__/asset-download-same-origin.test.ts`、`apps/web/lib/__tests__/canvas-download-same-origin.test.ts`、`apps/web/lib/__tests__/create-error-boundary.test.ts` |
| 2026-09-04 | **staging 走查 P1（参考附件类型标注）**：Otto 从素材库附上一张商家产品图（Pandan kaya jar photo）时被标成「(person)」/「Character」；同账号此前正因这种误标触发过真人脸硬拒绝（"Real human faces aren't supported yet."）——商家的产品图被当成真人拦下，直接冲击「Otto 协助生成」主线。触发＝staging 走查报告（scratchpad/creation-friction-audit.html，P1-6）。根因非默认写死在 Otto 侧或供应商侧：`Add to Library` 上传表单（`AddAssetDialog.tsx`）的 Type 下拉曾**默认预选** `REFERENCE_FORMATS[0]`（"Avatar / Cast" → `CHARACTER`），商家未碰下拉即可提交 Add，任何素材（含产品图）静默存成 `CHARACTER`。该标签不是装饰——它原样写进送给引擎的提示词（`packages/core/src/reference-budget.ts` 的 `SLOT_NOUN.CHARACTER = "person"`，构成 "Define the person in <Image_N>"），正是这句话让引擎的真人脸检测在一张果酱罐照片上误判。**真人脸硬拒绝本身是供应商实判**（`apps/worker/src/jobs/gen.ts` 的 adapter `permanent` 标志，引擎自己检视图像后拒绝）——这条路径诚实，未改动 | 已修（PR #1164）：表单不再有默认类型；Add 按钮在商家明确选一个类型之前保持禁用（新纯函数 `apps/web/lib/add-asset-form.ts` 的 `canSubmitNewLibraryAsset` + `NO_TYPE_SELECTED`），与 `createEntity` 服务端动作本就有的 `ENTITY_TYPES.has(type)` 校验同一条口径，只是提前到 UI 层不许静默提交猜测值；`submit()` 增加同款防御性校验，Select 加占位提示「Choose a type…」。行为测试：`apps/web/lib/__tests__/add-asset-form.test.ts`（产品图无类型不可提交／显式 `PRODUCT` 可提交／显式 `CHARACTER`——商家或演员库明确标记人物时——仍可提交／名字与文件仍是必需／表单锁定时禁用）；既有 `add-asset-dialog-feedback-ui.test.ts`、`library-guardrails-934.test.ts` 同步补上「先选类型」步骤，两份原有断言保持绿。范围内不改：`propose.helpers.ts` 的 `approvedEntities` 快照（已如实读 DB `Entity.type`）、`SLOT_NOUN` 的措辞映射（对明确类型如实翻译，不是缺陷）、供应商真人脸判定本身 | 待 Founder 追认 |
| 2026-09-04 | **16:9 视频输出的宽高容差**（Codex 只读 E2E QA-CRE-001 附带、回归门 2）：Seedance 480p 标称 16:9 实出 864×496（比例 1.742，标称 1.778，差 2%），是供应商像素网格取整，不是产品或引擎的错。口径：UI 与确认卡照标批准的比例名（16:9 等）；入库以标称比例 ±3% 为合格容差，超出即视为供应商输出不符。本轮只做验证（编排者用 ffprobe 量真实出片，在 3310 验收记录里留数）；入库时自动校验（超差＝判失败并退款）登记为下一轮施工项，不占本轮 4 小时。 | 2026-09-04 Founder 裁决（AskUserQuestion 三选一，选「容差 ±3%，登记规格，本轮只验证」）；本轮不改代码 |
| 2026-09-04 | **Codex 只读走查 QA-CRE-001(画布两条视频路没有声音开关)**：商家在画布出片框(t2v)与画布卡片 Animate 弹窗里只看得到时长／清晰度／形状三格,声音那一格根本不存在;`useCanvasGen` 从不发声音意图,服务端把缺席的 `audio` 解析成这台引擎的默认值(true),于是一句「completely silent」的提示词照样拿回一条带 AAC 音轨的 MP4(更早一条还有可听见的声音,平均 -30.9 dB)。触发＝Codex 在 main `e622bec6` 上的真浏览器只读走查(job `01M1MBH5W162TQZ745K4PSN6VP`)。根因是阶段一(PR #1133)按判官裁定**故意**没在画布上展示这个开关:那时 `clampVideoSpec` 只重建 seconds/resolution/aspectRatio,开关拨了也走不到付费请求体,展示出来就是一个能拨、收钱、无效的假控件 —— 缺口本身已由 2026-09-02 那行如实登记「画布两条视频路的开关在阶段二(批 II)接线」 | **落地(PR #1166),CREATE-A3 阶段二**：钱路语义零变化(价目表的键是「清晰度:秒数」,声音不在键里),只把那条缝补上再开开关。三段接线缺一不可:①`apps/web/lib/video-spec.ts` 的 `clampVideoSpec` 保留布尔 `audio`(未设仍不落 ⇒ 请求体不出现该字段 ⇒ 服务端按默认档交付,与接线前逐字一样);②`apps/web/components/canvas/useCanvasGen.ts` 的 `animate` 与 `generateVideoFromText` 两处付费请求体带上 `audio`,回执新增 `videoAudio`(刷新后重放的仍是商家批准的那一条静音片);③`apps/web/components/canvas/FlowCanvas.tsx` 两个视频弹窗自报 `audioToggle`,复用既有 `VideoSpecPicker` 的同一份开关与同一句文案「Sound doesn't change the price」,默认开。幂等键口径:画布的键＝`canvasActionKey(actionId)`,而 actionId 由 `FlowCanvas` 的 material JSON 决定、spec 整个进材料 —— 所以**开与关是两个意图、两个键**(材料没变则仍是同一个身份的重试)。**CREATE-A3 至此覆盖三条视频路**:资产详情 Animate(阶段一 PR #1133)＋画布 t2v ＋画布卡片 Animate。行为测试(验收编号逐字):新增 `apps/web/lib/__tests__/canvas-video-audio-submit.test.ts`(真 `useCanvasGen`:请求体 `audio:false`／未拨则该字段缺席／开关不改 `expectedCredits`／回执 `videoAudio`／开关不同两个幂等键);`canvas-video-spec-ui.test.ts` 的 CREATE-A3 段由「不许有开关」翻成「必须有、拨了算数、报价一格不动、只拨声音就换动作身份」;`video-audio-toggle.test.ts` 的源码围栏改成「显示开关 ⟺ clamp 保住 audio ＋ 两处请求体带 audio ＋ 两处弹窗自报」;供应商那一截沿用既有 `packages/generation/src/byteplus-audio.test.ts`(`audio:false` ⇒ 请求体 `generate_audio:false`),三段接起来即「实发可查」 |
| 2026-09-04 | **Founder 现场裁决「Otto 改档＝要，现在做」**（2026-09-04 约 03:30 马来西亚时间，决策问答）。触发＝接缝票 #1165 那条登记行：商家在对话里说「1080p」或「先来 480p 省点 credits」，提案层接不住 —— 卡上照旧默认档、按默认档报价，而 Otto 嘴上说改好了（走查 P1-2）。病根三处逐字：①`packages/otto/src/skills/propose.helpers.ts` 的 `proposeInput` 只收 `desiredAspect`／`desiredDuration`／`desiredAudio`，模型**无处安放**画质；②`packages/core/src/cowork-route.ts` 的 `suggestModel` 把槽位锁死在 `activeVideoModel()`、分辨率写死成该槽位的默认档（`resolution: d.resolution`），就算有字段也读不到；③`packages/otto/src/instructions.ts` 反过来明令「resolution … `propose` has no field for it, so you cannot change it」。本行属类型①中途想法，验收编号沿用现有 CREATE-A 表（CREATE-A4 高清档路由与报价、CREATE-A1 花钱前可见，不发明新编号） | **落地（本 PR #1167）—— 钱路语义零变化，只是把商家的档位意愿接上既有的那条路**：①`propose` 新增 `desiredResolution`（视频专用；沿用既有 `params.resolution`，没有第二个字段）；②`suggestModel` 新增同名入参，**档位挑槽位**走的是人工路那一个 `routeVideoModel`（1080p → 高清槽位，其余 → 默认槽位），所以对话路与人工路不可能为同一句话挑出两台引擎；没点名则一格不动（仍是 `activeVideoModel`，CREATE-A5 前半条「默认档配错＝降级留日志」不变），点了名就吸附到该槽位能力表、吸不上标 `downgraded`；③铸卡侧新增 `VideoTierUnavailableError`（`ProposeRefusal` 家族，入口按基类接住）：商家点名的档位若没有**原样**落到卡上、或不是可售 SKU（`isSellableVideoSku`，与付费闸 `assertSpendableModel` 同一个函数），**一张卡都不铸 —— 拒绝、$0、GEN_CARD 与 ledger 各零新增行，绝不静默换档**；**这条规矩覆盖每一条会走到视频的路**——普通视频路、参考视频路（那条路把分辨率硬写回该槽位的默认档，所以它只铸得出那一档）、以及两步计划里那张图片卡上的片段预估（确认卡把那个数渲染成商家正要批准的总价，所以它同样要么按点名的档报、要么整张卡不铸）：**点了这条路做不到的档，一律如实拒绝、$0**。那句拒绝里能给的档位由「**这条路真正铸得出**的那几档」现算（`mintableVideoTiers` ＝ `SELLABLE_VIDEO_RESOLUTIONS` ∩ 这条路够得到的档），只有能力名词、零型号名，绝不列出这条路其实给不了的档（判官 2026-09-04 P1-1：列了就会说出「480p 拿不到 —— 我能做 480p 或 720p」，商家照它再说一次还是同一句，走进死循环）。三条钱路不变量一格没动：价仍由 `pricedGenCredits` 按**卡上那一档**算（1080p 5 秒 = 55cr，来自 §5 2026-09-01 回填行），身份仍是 `cowork:<cardId>`（改档＝重铸一张卡＝新 cardId ＝新幂等键，老卡照旧只按它自己那一档扣），披露仍先于扣费（`startCoworkGen` 的卡面价 vs 现算价对签一行未改）。说明书里可点的那几档由 `SELLABLE_VIDEO_RESOLUTIONS` 插值（golden 快照同 PR 有意更新）。行为测试：`packages/core/src/cowork-route.test.ts`（CREATE-A4 档位挑槽位、每个可售档原样落地、4k 标降级）、`packages/otto/src/skills/propose.test.ts`（CREATE-A4／CREATE-A1 卡上带这一档、按档报价取自单一价目源、规格行逐字带档、未定价即拒绝不降级、改档新卡新键、高清槽位被关＝只说这一档拿不到、参考视频路只说它真铸得出的那一档、两步计划点不可售档＝整张卡不铸而不是悄悄按默认档报价、`proposeInput` 运行时逐字收下 `desiredResolution`）、`apps/web/lib/__tests__/otto-resolution-tier-ledger.test.ts`（真库：卡面报价 == `reserve:<jobId>` 绝对值 == `settle:<jobId>` 绝对值、任务行冻的分辨率就是卡上那一档、改档两笔各自授权、4k 零卡零任务零账本行） |
| 2026-09-04 | **Library「Needs attention」把后端诊断原文直给商家**（Codex QA-CRE-007，回归门 7）：`reference video unreachable — refusing to spend`、`conditioning refs unreachable (0/2)` 等内部诊断句从 `GenJob.error`/`RefGenJob.error` 直接渲染到 Library 失败卡与演员库变体问题行；失败卡标题是未截断的完整 prompt。落地口径归 **CREATE-A2「花钱前诚实拒绝并说明原因」**：`packages/core/src/gen-failure.ts` 新增 `REFERENCE_ASSET_UNREACHABLE` 白名单句与 `referenceAssetUnreachable` 原因码（单一映射表，`merchantGenFailureCopy` 为总函数，未命中原因回退到诚实的通用句）；`apps/worker/src/jobs/gen.ts` 五处、`apps/worker/src/jobs/refgen.ts` 三处发出点改抛该句，原始诊断只进 `console.error`；`apps/web/lib/data.ts`（`getMyAdJobs`）与 `apps/web/lib/refgen-actions.ts`（`getRefGenJobs`）改读该映射表，不再有第二份文案表；`apps/web/components/otto/OttoStuff.tsx` 的 `AdJobCard` 加 `libraryCardTitle`（60 字符截断）与卡片自身对 `merchantGenFailureCopy` 的二次防线（独立于 `data.ts` 已映射，防一条写在修复前的存量行）。行为测试：`packages/core/src/gen-failure.test.ts`（含 `CREATE-A2` 命名的用例与 grep-guard）、`apps/web/lib/__tests__/library-failure-human-copy.test.ts`（真 React 挂载 `AdJobCard`）。**未做**：`ElementVariantsDialog.tsx`（演员库变体问题行的展示端）本票未触碰——`refgen-actions.ts` 已在数据层把 `RefGenJob.error` 映射成人话句，该组件按既定分工留给另一票；Try again/Replace reference 类专属恢复动作未新增，沿用卡片既有的「Retry with Otto」按钮 | 待 Founder 追认 |
| 2026-09-04 | **Codex 只读 E2E 第二轮 QA-CRE-FE9-014／FE9-004（花钱前的两句诚实话）**：①**做不到的画幅被悄悄换掉**——商家两次明确说 4:5，确认卡却写着 `2048 × 2048 · 1:1`，外加一句 `You asked for 4:5 — this will be a square…`，GenJob 冻的是 `{"aspectRatio":"1:1"}`；他的硬规格被系统改掉，却仍然请他批准，而卡上没有任何地方能改回来或明确接受（说了不等于问了）。②**同一个画面上两条批准指令**——Otto 叙述说 `Just say yes and I'll submit it — then you'll confirm on the card to start`，而卡上已经摆着 `Generate · 1 credit`；商家不知道「yes」会不会花钱、卡是不是还要再确认一次。病根两处逐字：画幅那条走的是 `propose.helpers.ts` Step 4.7 的「降级披露」路（换一格 + 一句说明），而画质档同类问题早已裁成拒绝；话术那条出在 `packages/otto/src/instructions.ts` 自己 —— 上一版最后一句把「说 yes → 我送上去 → 你再在卡上确认」写成了标准流程，模型只是忠实复述它。本行属类型①中途想法，验收编号沿用现有 CREATE-A 表（CREATE-A4 规格原样落卡、CREATE-A1 花钱前可见与卡即批准点，不发明新编号） | **落地（本 PR）—— 钱路语义零变化，只是把「做不到」从悄悄降级改成花钱前先问**：①`propose.helpers.ts` 新增 Step 3.7 与 `ImageAspectUnavailableError`（`ProposeRefusal` 家族，入口按基类接住，与画质档的 `VideoTierUnavailableError` 同一句式、同一位置）：商家点名的画幅若没有**原样**落到卡上（这一趟的适配器不兑现画幅，或那一格不在这台引擎的能力表上），**一张卡都不铸 —— 拒绝、$0、GEN_CARD 与 ledger 各零新增行，绝不静默换一格**；两步计划（`forVideo`）的图片步走的就是这一条，不另设第二支守卫。那句拒绝里能给的画幅由「**这条路真做得到**的那几格」现算（`mintableImageAspects` ＝ 该槽位 `GEN_IMAGE_MODEL_OPTIONS.aspectRatios` ∩ `imageAspectHonoured()`），按对数距离取最接近的三格（4:5 → 3:4、2:3、1:1），只有比例、零型号名，并且留一句「name another shape and I'll check it」不假装穷举。同时删掉画幅那条降级披露路（`Step 4.7` 的 `imageAspectDropped`）——它是本票要挡的那条路本身；声音开关那半条原样保留。②`instructions.ts` 把「说 yes → 我送上去 → 你再确认」改成**一张卡一个批准点**：话永远不提交、不送出、不排队、不递交（`Words also never submit, send, queue, put through or hand over a card`），商家在聊天里说 yes 一律按**兴趣**而不是批准处理，回给他的只有那一条指令（review the card and approve it there to start）；既有「不许点名按钮标签」那条围栏一字未动（Otto 看不见按钮，`instructions.test.ts` 的 UI 词表白名单是它的机器闸），所以叙述里不出现按钮字面量。服务端本来就只有一条付费路（`startCoworkGen` 只收一张已落库的卡装出来的请求），所以那第二条指令是纯粹的假承诺；`say-to-start` 禁语族补上 submit／send／queue／put it through／hand it over 五个动词，生产原句从此逐字被机器闸逮住。行为测试：`packages/otto/src/skills/propose.test.ts`（CREATE-A4 4:5 拒绝不降级、拒绝句列 3:4 与 2:3 且不含 4:5、只列菜单内的格、CREATE-A1 3:4 原样进卡与请求体、没点画幅一格不动、portrait 照旧兑现、两步计划同守、读不懂的形状一律拒绝、菜单八格全可铸）、`packages/otto/src/instructions.test.ts`（CREATE-A1 一张卡一条批准指令＋禁语族生产原句）、`apps/web/lib/__tests__/otto-aspect-honesty-ledger.test.ts`（真库：3:4 卡上＝GenJob `imageOptions.aspectRatio`＝付费请求体，4:5 零 GEN_CARD 零 GenJob 零账本行余额一分不动，拒绝后改说 3:4 照常收费，聊天文本 yes 装不成付费请求） |
| 2026-09-04 | **Codex 只读走查 QA-CRE-003(官方演员在产品里可改可删)**：Founder 2026-08-30 已裁「Official avatars 由 Fikirtive 提供、read-only;可以 browse / search / preview / favorite / use,但不能修改 identity」(`apps/web/design-system/information-architecture/README.md` Elements 行、`core-flows.md` §11),但这条只写在设计文档里 —— 主干产品 Library → Cast → Aisyah 详情里,`Use as base`、`Add a variant`、`Variant name`、`What changes` 一应俱全,两项填完 `Make variant · 1 credit` 就 enabled(Codex 未点)。根因是**没有单一判据**:`EntityDTO` 不带来源语义,`Entity.catalogKey` 只是播种脚本的幂等键、从没变成权限,`ElementVariantsDialog` 无条件画控件,五个 refgen 动作只查 owner 不查来源。触发＝Codex 2026-09-04 只读 E2E `docs/audits/creation-e2e-2026-09-04.md` §4.7(回归门 5) | **落地(本 PR),CREATE-A10 只读半**:一个源头、四层落位,钱路语义零变化。①**域层**新增 `packages/core/src/entity-policy.ts`——`entityOrigin` 按 `catalogKey` 非空判 `OFFICIAL_CATALOG`/`USER`(**绝不按名字**:元素名商家可改,商家也可自建同名角色),`entityCapabilities` 给出七格能力表(mutateBase / createVariant / regenerateVariant / renameVariant / deleteVariant / editIdentity / deleteEntity),官方全 false、商家全 true;②**DTO 层**`EntityDTO` 加 `origin` 与 `capabilities`,只在 `apps/web/lib/dto.ts` 的 `toEntityDTO` 算一次,UI 与 Otto 读同一份,不另起判断;③**UI 层**`ElementVariantsDialog.tsx` 与 `StuffLibrary.tsx` 按能力表决定**画不画**(不是画成禁用的假控件),官方那一面只剩 View / Use / @ 引用;④**server action 层**`refgen-actions.ts` 五个动作 ＋ `actions.ts` 的 updateEntity／addEntityAlias／removeEntityAlias／softDeleteReferenceImage／softDeleteEntity 逐一 fail closed,拒绝落在**任何 reserve／RefGenJob 之前**,判据回数据库现读 `catalogKey`(不信客户端可编的 DTO);租户约束不变(他店的 id 仍回「找不到」,不回只读提示)。**假设待 Founder 追认**:裁决原话只写「不能修改 identity」,没有逐字说「不能删」,这里把 `softDeleteEntity` 也按只读拒绝(它会连同定妆照字节一起真删,而商家无法自己放回)。行为测试(验收编号逐字):`packages/core/src/entity-policy.test.ts`、`apps/web/lib/__tests__/official-avatar-readonly-actions.test.ts`(真库:五＋六条动作全拒、ledger／RefGenJob／GenJob 零新增行、余额与预留不动、商家自建同名元素照旧可改、双租户两向)、`apps/web/lib/__tests__/official-avatar-readonly-ui.test.ts`(真挂载:官方 DTO 不出 Codex 点名的四段文字,商家 DTO 全在) |
| 2026-09-04 | **Codex 只读走查 QA-CRE-FE9-001（P0，画布变体一击即预留）**：商家在画布卡上点一次「Create variations」，就直接建付费 job 并 reserve —— 界面上没有件数／比例／材料／准确 credits，也没有一颗要按的确认键，价钱只在 hover title 里出现过一次（证据：`ImageNode.tsx:154-166` 工具条直调 `onVariant`；`FlowCanvas.tsx` 的 `handleVariant` 直调 `runImageEvolve`；`runImageEvolve` 建 action id 即调 `generateImage`；Codex 截图 `creation-e2e-fe9c70bd/11-variation-no-confirmation.png`，报告 `docs/audits/creation-e2e-fe9c70bd-2026-09-04.md` Stage 8）。这条路此前走的是**宪法例外①「余额即闸」**（图片直出、不弹确认；Founder 2026-07-06 拍板，`docs/superpowers/plans/2026-07-06-otto-stateful-canvas.md` 明写「防被当 bug 改回」），所以它不是回归，是一条旧裁决与设计权威的冲突。本行属类型①中途想法，验收编号沿用现有 CREATE-A 表（CREATE-A1「花钱前先见确认／画布路径的判定落在确认卡片上」，不发明新编号） | **Founder 2026-09-04 07:05 裁决：变体照设计权威走确认卡** —— 依据 `apps/web/design-system/patterns/canvas/stitch-image-video-parity-spec.md:149` 的 variation journey（`Select image → Variations → count/range/aspects → confirmation → side-by-side variants`）与该文件 §5「Paid generation confirmation」。**裁决范围只到变体这一条路**：创作输入条那条图片直出路仍走例外①，一格未动。**落地（本 PR）—— 钱路语义零变化，只改「谁按了才算数」**：①`FlowCanvas.tsx` 新增 `pendingVariant` 这个 staged intent，`handleVariant` 第一下只把材料记进去，**不建 action id、不 reserve、不建 job**；②确认卡承载在 Animate／出片框同一家的 `Dialog`（不另发明第二套确认 UI），显示来源缩略图、件数、形状、要送去的 prompt 与**准确 credits**（`costQuote.imageCredits`，服务端单一价目源，组件内零价格字面量），primary CTA `Generate · N credits`、secondary `Cancel`；报不出价就不出这颗键；③按下 `Generate` 才调**原样未改**的 `runImageEvolve`（同一条 `generateImage` 花钱路、同一个 material→actionId 身份、同一道 `evolveBusyRef` 防双击闸），提交后按钮变状态、不留可重复付款的键。**核过不动的两处**：「Edit with Otto」走 `onSendToOtto`，只是把卡交给 Otto 当引用，本来就不花钱；Animate／视频变体早已有确认框（PR #1172）。行为测试（验收编号逐字）：`apps/web/lib/__tests__/canvas-variation-confirm.test.ts`（真 FlowCanvas＋真 ImageNode：第一下零调用／Cancel 零调用／按 Generate 恰好一次且材料正确／双击一次／CTA 逐字 `Generate · N credits`／报价换数卡与 CTA 一起换／报不出价不给按）、`apps/web/lib/__tests__/canvas-variation-confirm-ledger.test.ts`（真库：一次确认＝一组 reserve→settle 且报价==reserve==settle／失败＝一组 reserve→refund 余额净变化 0／连按两下与并发双击各只有一组账本且键==`canvasActionKey(actionId)`／换材料才是另一次授权）。变异检查：把「第一下只开确认」的守卫去掉（改回直调 `runImageEvolve`），首击零账本那条当场变红，还原后绿 |
| 2026-09-04 | **画布当前轮的状态漂移**（Codex 只读 E2E `docs/audits/creation-e2e-2026-09-04.md` §4.2 QA-CRE-004）：同一画布里先失败一次、再成功一次生成之后，始终可见的 Otto 卡仍写着上一轮那句「That generation didn't go through」，圆点却是绿灯「Ready」；强制刷新也不变。根因＝那张卡有两个状态源——圆点读在飞的任务，正文读「整条对话最后一条 assistant TEXT」而**不比时间**，于是一条落库的 TEXT 永远是「最后一句」；同时 `failed` 态的唯一触发是 `ChatThreadDTO.status`，而画布这条路的 thread 由 `toChatThreadDTO` 建、**它根本不写 status**，所以画布卡的 failed 从来没有可能出现过。验收编号沿用 CREATE-A1（画布路径的判定落在 Otto 确认卡片上），不发明新编号 | **落地（本 PR）**：当前轮改为**单一权威的按序投影**——新纯函数 `latestTurnTerminal` 取这一轮（`currentTurnStartIndex` 之后）最新的终局任务事件（GEN_RESULT／TURN_ERROR，与卡片自己的 `deriveCardState` 同一对消息），`canvasTurnText` 在「Otto 后来说的话」与「那个终局」之间取更新的那个，状态词新增 `done` 并由同一个终局给（`threadStatus` 入参连同那条死接线一起删）。成功态说得出产物与收费（「Made 1 video · 11 credits.」，两个数字都取自 GEN_RESULT payload 自己）。测试：`apps/web/lib/__tests__/creation-otto-canvas-seam.test.tsx` 新增 8 条（含五态互斥、取消不算失败、上一轮终局不挂这一轮）、真浏览器旅程 `e2e/journeys/18-otto-turn-state.spec.ts` 两条。S3（两步计划批图后 video 确认卡不出现）在当前 main 复现不出来——#1165 已修，本 PR 只补回归钉 | 留空待 S5 |
| 2026-09-04 | **Codex 只读 E2E QA-CRE-FE9-013（P0）：跨 Canvas 的 Library 参考被静默丢掉**。商家在画布 B 用「Choose from Library」选了画布 A 生成的蓝杯子，composer 显示 `Image ref`，发送后 Otto 回答没看到杯子；两条 USER 消息的 DB payload 都是 `sourceGenerationIds: []`，GEN_CARD 与 GenJob 也没有杯子的 generation ID，确认卡只列 `Aisyah (person)` —— 商家可能批准并支付一张**没有用上他指定产品**的商业素材。病根：「这件素材能不能当参考」这条规矩被抄成六份 where 子句（composer 校验器 `otto-generation-validate.ts`、Otto 视觉 `otto-ref-images.ts`、付费前守卫 `cowork-guardian.ts`、worker `gen.ts` 的首帧／末帧／参考片／编辑底图），六份都多写了一格 `projectId`；而 Library 选单读的是 owner 全局历史（Founder 2026-08-30 裁定 Library 是全店级、`@` 与 Library 可跨 Canvas 引用，`apps/web/design-system/information-architecture/README.md:95`）。触发＝Codex 在 main `fe9c70bd` 上的第二轮只读 E2E（报告 Stage 13–15 与 QA-CRE-FE9-013）。本行属类型①中途想法，验收编号沿用现有 CREATE-A 表（**CREATE-A2** 素材指派与「花钱前诚实拒绝、ledger 零新增行」、**CREATE-A1** 花钱前看得见；不发明新编号） | **落地（本 PR）—— 钱路语义零变化，引用范围＝同一 owner 的任意画布，画布是出处不是权限边界**：①判据收敛成一处 `packages/core/src/generation-reference.ts` 的 `generationReferenceScope`（同一 owner、活着、扩展名对得上），六个读者共用，租户那一格一点没松（`ownerId` 仍只来自已认证 principal）；②**不再静默丢**：`validateOttoTurnReferences` 返回 `unavailable` 及原因（`notFound`／`fileMissing`，存储对象缺失在 reserve 之前就查），`ottoTurn` 与 `/api/otto/stream` 在建对话、落 USER 消息、开流、铸卡、预扣**之前**整轮拒绝，措辞来自 `packages/core/src/gen-failure.ts` 的单一表（无第二份映射），composer 收到 400 后按白名单认句、把草稿与附件原样放回；③**引用回执贯穿**：USER 消息 → `ctx.mediaReferences` → GEN_CARD 冻结 → 两张确认卡逐项列出（缩略图＋真实名字＋来源画布，不再是 `Image ref`）→ 批准材料 → GenJob `sourceGenerationId`；卡上有参考却没有回执 ⇒ `planCardGate.approvable` 为假、**不给 Generate 按钮**并说出缺哪一件；④芯片改用素材自己的名字（`composerReferenceLabel`）。行为测试（验收编号逐字）：`apps/web/lib/__tests__/creation-cross-canvas-reference.test.ts`（真库两租户：甲店跨画布引用一路到 GenJob 同一个 id／乙店 id 被引用＝可见拒绝、零 GenJob、ledger 零新增行／已软删／存储缺失／一轮里有好有坏＝整轮拒绝）、`creation-reference-receipt-card.test.tsx`（确认卡逐项列出、缺回执不给 Generate）、`otto-stream-route.test.ts`（400 + 不建对话不落消息不开流）、`otto-generation-validate.test.ts`／`otto-ref-images.test.ts`（where 里一格 `projectId` 都不许有）。变异检查：判据改回 `projectId` 过滤 ⇒ 6 条红；去掉可见错误与回执要求 ⇒ 3 条红 |
| 2026-09-04 | **Creation 4 小时冲刺收尾 · 本场判官与 Codex 第二轮报告（`docs/audits/creation-e2e-fe9c70bd-2026-09-04.md`，测 main fe9c70bd）留下的登记项，本场不做**：① Otto 拒绝句里「我能做」半句按本次路由落到的槽位现算（`sm.model`/`vm.model`），商家说「4k」时只答「480p or 720p」，同一入口其实拿得到 1080p（判官 #1167 P2，评论 5532854155）——口径题：按槽位算还是按入口可达的全部槽位算；② 视频**画幅**那一轴仍是「照铸＋降级说明」（要 4:5 的视频 → 卡铸 16:9 并写 You asked for 4:5），图片画幅与画质档已改「花钱前先问」（#1176／#1167），留着不一致商家要学两套规矩，下一票同收 `buildDowngradeNote` 的 image 死分支；#1176 验收挂 CREATE-A4，§2 字面更贴 CREATE-A5，备查；③ 官方演员只读（#1175）里 `softDeleteEntity` 按只读拒绝是落修者假设（Founder 08-30 原话只说「不能修改 identity」）；两处过时注释 `packages/db/prisma/schema.prisma:103-104`、`apps/web/lib/actor-library-seed.ts:32-33` 待 docs-only 清理；`StuffLibrary.tsx:380-383` 能力读法 fail-open（`!== false`）建议改 `=== true`；§2 CREATE-A10 正文只写「两场景连续出片」，只读那一半只活在 §5 行，S5 同读；④ 声音开关（#1166）判官 P2：关→开是新的付费身份、源码围栏钉出现次数；⑤ FE9-006 后半：/create 历史分页（首屏 20 ＋ Load more，server cursor）与标题搜索，本场只做「最新在前」（#1174）；⑥ FE9-010：预留额与已结算额在余额／回执上可区分（「1 credit reserved · Finalized when complete」），账本行为已正确，只是 UI 语义；⑦ FE9-011：资产详情「What the engine ran」保存供应商最终 prompt／规范化请求，拿不到就写 `Not provided by the engine` 并继续显示原 prompt、回执、批准规格；⑧ FE9-015：参考图在 reserve 前预检能否形成供应商可读输入（本机 local 存储对供应商不可达属环境题，staging 用 R2）；⑨ FE9-005 另一半：日后要多对话须带切换列表／标题／排序／深链，本场按 Founder 07:05 裁决 beta 收掉 New conversation（#1178）；⑩ FE9-012：下载与 Shift 点选的人工复测记录在最终验收矩阵；`FlowCanvas` `getOnDownload` 缺依赖 lint 警告待清；⑪ FE9-007／009 归 #1158（统一 `@` 选择器）／#1159；⑫ 跨画布参考（#1177）：回执之前铸的、带 `sourceGenerationId` 的旧 GEN_CARD 现不可批准、卡面请重铸（零正式用户）；⑬ 判官 #1177 P2（评论 https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/pull/1177#issuecomment-5533948618）：画布常显卡缺回执时整块消失不说原因（`OttoTurnCard.tsx:144` `return null`），抽屉卡会出 Ask again，#1168 抽屉默认折起后商家看到确认位凭空不见；worker 层无自己的租户行为测试（`gen.ts` 四处 owner 判据靠单源单测＋变异＋`runAsTenant` 间接覆盖）；多张引用的沉默半格（Otto 视觉最多看 `maxImages` 张，付费请求与回执只带第一张，卡没说其余不上车）；加分发现：`runAsUser`/`runAsTenant` 会话级守卫独立于 where 子句，租户边界双层。⑭ 判官 #1178 P2（评论 https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/pull/1178#issuecomment-5533977403）：设计权威 `stitch-image-video-parity-spec.md` §5 的 secondary actions 是 `Edit details / Cancel`，变体确认卡只有 Cancel（Founder 09-03 裁决①已拿掉每卡形状选择器）；新承诺 `No charge until you confirm`（`ImageNode.tsx:168-169`）无围栏，视频那句在 `spend-visibility-seams.test.ts:244` 钉着而图片这句删掉不会红；账本件（手写请求体直调 `startCanvasGen`）与界面件靠约定拼接；报价未回时描述行 `Cost: checking exact cost. No charge until you confirm.` 英文略拗口（此刻 CTA 不给按）。⑮ #1177 落修者观察（未动）：`status === "error"` 时 #949 A2 的通用横幅（`Otto couldn't finish this turn`）与那句具体的引用拒绝句同时上屏，商家一次看到两条错误——既有 #949 形状，非 #1177 引入。 | 登记；逐项待 Founder 排期或 S5 同读 |
| 2026-09-04 | **Codex 只读 E2E E2E-CRE-PAV-005／006**（`docs/audits/creation-product-avatar-video-062aae86-2026-09-04.md`）：①失败句「Try again」不是可执行恢复——`REFERENCE_ASSET_UNREACHABLE` 与 `notFound`／`fileMissing` 三句结尾都写着「…and try again」，而这三类失败**只**靠重试永远解不开（参考仍是那张不可达/不存在的图），句子把「换掉参考」这个真正的修法说成了可选；②`CanvasLibraryPicker.tsx`（「Choose from Library」半屏）每个素材项在 AX tree 里是无名称的 `button`＋无 `alt` 的 `image`，键盘与 screen reader 用户分不出选中的是哪一件。验收编号沿用 CREATE-A2（花钱前诚实拒绝并说明原因）；②的画布无障碍口径另落 frontend-baseline.md 自己的六面走查验收行（该文件自有编号，不在本表引用，前缀全仓唯一）；不发明新编号 | **落地（本 PR）—— 文案与无障碍名两处，不新增交互**：①`packages/core/src/gen-failure.ts` 三句结尾「try again」改「ask again」（`REFERENCE_ASSET_UNREACHABLE`＋`REFERENCE_UNAVAILABLE_SENTENCES.notFound`／`.fileMissing`），单一源头，Library 失败卡／画布失败卡／Otto 当前轮同源同句；②`CanvasLibraryPicker.tsx` 新增 `libraryItemAccessibleName`，每个 item 按钮 `aria-label`＝素材名字＋媒体类型，缩略图 `<img alt="">`／`<video aria-hidden>`（装饰，名字已在按钮上）；「来源」（generated in this Canvas／uploaded）分量因 `LibraryItem`（`lib/library-actions.ts`）当前不携带画布名与上传/生成区分而省略，未编造。**登记待下一轮**：专属 Retry／Replace-reference 按钮（新交互，接续 #1171 那行同一条「本票未新增」的登记）。 |
| 2026-09-04 | **Codex 只读 E2E E2E-CRE-PAV-004（P1 UX）：两阶段任务要商家自己「把图带回来」**：商家在画布 B 用 Choose from Library 选了刚生成的产品图、加 `@Xinyi`（官方演员），说要「Xinyi 在暖调咖啡馆举起 tumbler 喝一口后对镜头微笑，5 秒、9:16、Sound off」。Otto 正确地拆成两步（先做 9:16 starting image，再用它生成 5 秒视频），却只铸了 starting image 的确认卡，然后说 `Once you approve and generate it, bring that image back here` —— 内部 orchestration seam 直接漏到商家面前，他要重新找图、重新附加、重新把同一件事再讲一遍。病根不在措辞：两步计划在这之前**只是第一张卡上的一行价格披露**（`packages/otto/src/skills/propose.helpers.ts` 的 `CardPayload.videoStep.estimatedCredits`，注释逐字写着 DISPLAY ONLY；全仓唯二读者是 `apps/web/components/otto/OttoPlanCard.tsx:131` 的渲染与 `plan-card-contract.ts` 的解析），系统里没有任何一处会在图出来之后接着走第二步，也没有任何字段存得住第二步的规格 —— 所以 Otto 唯一诚实的下一句就只剩「你自己把图带回来」。本行属类型①中途想法，验收编号沿用现有 CREATE-A 表（CREATE-A1 花钱前先见确认卡、CREATE-A2 素材指派回执与「花钱前诚实拒绝、ledger 零新增行」，不发明新编号） | **落地（本 PR）—— 两步接力＝自动出第二张确认卡，不自动扣费**：钱路语义零变化，每一笔付费照旧各自一次确认、一次预留、一次结算，第二步是**新卡新键**（`cowork:<新 cardId>`），卡出现 ≠ 收钱。①**冻结计划**：`proposeInput` 新增 `videoPrompt`（第二步那条片子的 seedancePrompt 文本），`videoStep` 从 `{estimatedCredits}` 长成 `{estimatedCredits, next}`，`next` 就是第二步的整份规格（话／画幅／时长／画质／声音），与那行预估**共用同一份输入**，所以卡上写的价与第二张卡真正的价不可能分家；没给 `videoPrompt` ⇒ 不冻结，老卡形状逐字保留、没有接力。新增 Step 4.6b：第二步的话若撑不起「带首帧」这个形状（`decideVideoAction` 同一个函数），**一张卡都不铸 —— 拒绝、$0**，绝不让商家为第一步付了钱之后才发现第二步永远不会出现。②**接力**：新模块 `packages/otto/src/video-step-handoff.ts`（`videoStepPlanOf` 读冻结计划、`buildVideoStepCardPayload` 整张卡走 `buildProposeCard` 铸出、`planVideoStepHandoff` 只读且永不抛），触发点在**服务端事实**上 —— `apps/worker/src/jobs/gen.ts` 的 `appendCoworkResult`：Step 1 的 GenJob 交付时，第二张卡与该作业的 GEN_RESULT 写在**同一个事务**里，于是「至多一张」直接骑在 `ChatMessage(genJobId)` 那个部分唯一索引上（重投再跑撞 P2002、整个事务回滚），顺带拿到原子可见性（轮询看得见结果就一定同时看得见这张卡，`OttoChatStream` 的 `appendMissingCards` 原样接住，轮询逻辑一行未改）。Step 1 失败／取消／零产出 ⇒ 零第二张卡。③**血缘与回执**：第二张卡 payload 带 `videoStepOf`（指着 Step 1 那张卡，可查），`sourceGenerationId` 就是 Step 1 真正交付的那张图，并带上那张图的媒体参考回执 —— 回执的构造口径收敛成唯一一份 `packages/otto/src/media-reference.ts` 的 `mediaReferenceReceipt`（`validateOttoTurnReferences` 改调它），少了它 `planCardGate` 会判这张卡读不全、不给 Generate 键。④**话术**：`instructions.ts` 明令两步是一条任务、第二张确认卡会自己出现，**永远不许**再叫商家把图带回来／重新附加／重来一次；`propose` 的 skill description 同步。报价永远来自服务端单源（`buildProposeCard` → `pricedGenCredits`），本 PR 零价格字面量。行为测试（验收编号逐字）：`packages/otto/src/skills/propose.test.ts`（CREATE-A1 冻结整份第二步规格、CREATE-A2 产品图与 @Xinyi 两份回执都在、CREATE-A1 预估与计划同源、CREATE-A1 没给片子的话就不冻结、CREATE-A2 撑不起的形状整张卡不铸）、`packages/otto/src/video-step-handoff.test.ts`（15 条：冻结计划读不出来一律不接力、第二张卡的首帧／回执／血缘／报价单源、六种不该接力的情形）、`apps/worker/src/jobs/gen-video-step-handoff-db.test.ts`（真库跑真 handleGen：CREATE-A1 DONE ⇒ 恰一张视频确认卡且首帧＝刚出的那张图、CREATE-A2 账本只有 RESERVE＋SETTLE 零新增行余额一分不多扣、CREATE-A1 重投再跑还是恰一张、CREATE-A2 引擎失败 ⇒ 零卡且 RESERVE＋REFUND、CREATE-A2 老卡零接力）。变异检查：去掉接力铸卡 ⇒ 3 条红；接力时误加一次 reserve ⇒ 账本那条当场红；还原后全绿。裁决留空待 S5 |
| 2026-09-04 | **#1177 合入后的现存回归：素材面板 ClipActions 那条路铸出来的 Otto 卡，一张都批不下去**。#1177 给确认卡装了 `missingReferenceReceipts`（`apps/web/components/otto/plan-card-contract.ts:212-224`）：卡上有 `referenceVideoGenerationId` 就必须有那一件的 video 回执，否则 `planCardGate.approvable` 为假、卡上不出 `Generate · N credits`，只剩 Ask again。而商家在素材详情面板按「改这条片子」走的 `proposeClipActionCard`（`apps/web/lib/clip-actions.ts`）从来只写那个 id、不写 `mediaReferences`，于是这条入口铸出来的**每一张**卡都落进那道门 —— 商家点得出卡、却按不到生成键。判官 #1177 只登记了「回执之前铸的旧卡」（本表上一批第⑫项），没看到还有一条**在铸**的路。触发＝2026-09-04 在主干 61d48384 上的复核。验收编号沿用现有 CREATE-A 表（CREATE-A1 花钱前可见、判定落在确认卡上），不发明新编号 | **落地（本 PR）—— 钱路语义零变化，只补回执那一格**：`clip-actions.ts` 铸卡时把已验过归属的那条片子（`validateOwnedGenerationExt` 的返回值；客户端只送得进一个 id）装成一条 video 回执放进 `ctx.mediaReferences`，`buildProposeCard` 的 Step 4.8b 按 id 取、冻进卡面，形状与 Otto 路的 `validateOttoTurnReferences` 逐字同形（真名＝素材当初的提示词、超 60 字截断，缩略图＝同源内容寻址的 `/files/` 路由，来源画布名读不到退回 `Untitled canvas`）；画布那一行改成始终读（只读、owner 作用域），既供回执的画布名，又仍是「新开会话前确认项目属于本租户」那道确认，拒绝路径零写入。行为测试：`apps/web/lib/__tests__/clip-manual-entry.test.ts` 新增 6 条 CREATE-A1 用例（铸出来的 payload 过真的 `planCardGate` ⇒ 缺件为空、可批准、报得出价；回执的 id 就是卡上会上路的那个 id、kind＝video、名字是真名、缩略图同源；画布名读不到仍不少一行；抽掉回执同一道门立刻判不可批准；两租户既有拒绝一格未变）。变异检查：删掉 `mediaReferences` 那一行 ⇒ 3 条当场红，还原后 41 条全绿。**登记待下一轮**：①回执的构造在本 PR 是 `clip-actions.ts` 内的本地 helper，PR #1182 正在把它收进 `packages/otto/src/media-reference.ts`，那份合入后本处应改调那个单一构造点；②分镜视频子卡同病未修——`apps/web/lib/storyboard-gate1-actions.ts:1154`／`:1327` 把 `ctx.sourceGenerationId` 设成镜头首帧却不带回执，它在对话流里被跳过渲染（`OttoChatStream.tsx:1175`／`:1278`），但画布常显卡的 `confirmCards`（`OttoChatStream.tsx:967-974`）不筛分镜子卡，仍会把它送进同一道门；修它要在带锁的事务里加每镜读取，并决定复用比对（`videoChildMatches`）是否纳入回执（钱路相邻、需 Founder 排期），本 PR 不擅自扩围 |
| 2026-09-04 | **生成走到终局后，画布当前轮不刷新也收敛**（Codex 第三轮只读 E2E `docs/audits/creation-product-avatar-video-062aae86-2026-09-04.md` E2E-CRE-PAV-003，P1）：起始图作业 03:33:26 已 FAILED、1 credit 已退，Otto 当前轮仍写 `Generating · still working…`，刷新才变 `Failed`。已在对齐 commit（本分支 head）上用受控时钟复现，证实**不是**新旧 worker 版本错位——旧 worker `fe9c70bd` 同样写 TURN_ERROR。根因＝那扇看生成结果的窗只有一档：2.5s × 48（两分钟）打满就不再发问，而队列自己的重投退避最坏 180 秒（`GEN_QUEUE_POLICY`），屏幕先于服务端闭嘴。修法＝把 StoryboardCard 已判过的「到顶不等于放弃」（`nextSyncPhase`，#782 r7 判官 r6 P1-A）用在这条一直缺第二档的窗上，加慢轮 60s × 30；不新建状态机、不动钱路。验收归 **CREATE-A1**（journey 19）。残留登记：慢轮也到顶（`exhausted`）时，画布那张卡仍会说 `Generating`——抽屉里有诚实降级文案，卡上没有，本轮不做 | 2026-09-04 |
| 2026-09-04 | **staging 走查 CRE-STG-P0-001／P1-003／P2-004（三条同根：多参考、错类型、失败读不懂）**：①商家挂了产品图与人物图两张、Otto 说两张都收到，starting-image 确认卡只列得出一件——铸卡侧只认解析结果的第一个（`packages/otto/src/skills/propose.helpers.ts` 的 Step 4.8b 只收 `ctx.sourceGenerationId`），第二张连回执都不铸、也不进付费请求，商家买回来的是一张不含他指定产品的素材；②从「My Videos」挑一支片子当图片参考，拒绝句说的是「isn't available any more」——他一看素材库就知道是假的，而且指着一个从未发生过的删除；图片计划挂着整段参考片则**静默丢弃**（`isRefVideo` 只认视频卡），卡上一个字都不提它；③按 `Generate · 1 credit` 两次都回同一句「Couldn't start that」——那是客户端 catch 里的字面量，服务端一抛就把所有失败折叠成同一句，商家屏幕／服务端日志／走查报告三方没有一个共同的把手。触发＝2026-09-04 Codex staging 只读走查 `docs/audits/creation-staging-product-avatar-video-2026-09-04.md`。本行属类型①中途想法，验收编号沿用现有 CREATE-A 表 | **落地（本 PR）**：①图片计划的挂图**逐张上车**——卡上逐条回执各带角色（Base image／Reference／Starting frame／Reference clip，角色闭集在 `packages/core/src/reference-budget.ts`；刻意不猜 Character／Product，那两个词只有 @元素的 `Entity.type` 说得出口），`CardPayload.referenceGenerationIds` 冻结第一张之外的那几张，`GenJob.imageOptions.referenceGenerationIds` 带着它们进幂等材料与规格快照（**不改 prisma**：该表没有复数列，沿用既有 JSON 规格快照，与 `coherentSet` 同一条「只在有内容时出现」的纪律，既有行的幂等重放一格不变；它不是 `genRequest` 的字段，只走 `startCoworkGen` 的进程内可信通道，浏览器提交不了），worker 按次序 unshift 成 `<Image_1>`…`<Image_N>`；名额由 `attachedImageCap` 划，第 2 张起从 @元素名额扣格，引擎收到的总张数不超过修改之前的上限，超限时卡面照实说「只有前 N 张会上车」。②拒绝分出 `videoAsImage`／`imageAsVideo` 两个 reason（措辞单一表 `packages/core/src/gen-failure.ts`，结尾沿用「先点名那个真能修好它的动作、再说 ask again」的既有纪律）；图片计划挂着整段参考片改为**铸卡之前**拒绝（`ReferenceKindUnavailableError`，$0、零卡、账本零新增行）。③批准失败句来自单一措辞源 `GENERATION_START_FAILED`＋一个由卡身份算出的可复制短号 `diagnosticRef`（服务端 `console.error` 与卡面同一串；句子里不含 id／URL／路径／堆栈），`coworkGenerate` 加外壳不再抛过动作边界，短号显示在卡上那块**持久**的错误位（`apps/web/components/otto/CardApprovalRef.tsx`，两张卡共用）。行为测试：`apps/web/lib/__tests__/creation-multi-reference-card.test.ts`（真库：两张回执各带角色／GenJob 两个 id／账本恰一组／重试复用同一个 job／错类型拒绝零账本／铸卡前拒绝零卡）、`apps/worker/src/jobs/gen-multi-reference.test.ts`（真 `handleGen` 交给引擎的 `inputImageUrls` 逐张对表）。**未做**：素材库选择器不按类型过滤——同一支片子给视频计划是合法的，芯片已逐字标注 `Video reference`／`Image reference`（`apps/web/components/otto/OttoChatStream.tsx`），过滤会挡掉正当用法 待 Founder 追认。**收尾场补两处同根**:③之外还有一处「卡说一套、请求说另一套」——worker 把挂图整批标成 `baseImage`,而编号句由槽位产出,于是引擎读到的是 `<Image_2> is the image being edited.`(卡上那一条写的却是 `Reference`)。`ReferenceSlot` 因此多一支 `attachedReference`(`packages/core/src/reference-budget.ts`),第一张仍是底图、第 2 张起说 `<Image_N> is a reference image.` ——刻意不猜名词(画的是人还是杯子只有 @元素的 `Entity.type` 说得出口)。另:#1184 登记①兑现 ——`apps/web/lib/clip-actions.ts` 的本地回执构造改调 `mediaReferenceReceipt`(#1182 已入主干的单一构造点),两条铸卡入口不再各有一份口径。**未动并登记：视频计划的挂图披露与名额，下一循环**（`propose.helpers.ts` 的披露门槛是 `usesAttachedImage`，视频卡恒 false；`reference-budget.ts` 视频分支不数挂图 —— 所以视频计划挂多图仍会静默丢掉第 2 张起，与本轮修好的图片那一支同病；本 PR 不擅自扩围到视频名额）。另判官 P1-1 落修：`GENERATION_START_FAILED` 去掉「nothing was charged」断言 —— 它的两个调用点都接得住预扣已提交之后才炸的形状，那句话替账本担保了它不知道的事 |
| 2026-09-04 | **Codex 全 beta 审计 P0-001／P0-002（付费卡与商家原话对不上）**：①商家两次说 4:5、Otto 话里也说 4:5，确认卡却写 `2304 × 1728 · 4:3` 且 `Generate` 按得下去，商家指出后 Otto 才改口说 4:5 不支持——根因是画幅闸（`packages/otto/src/skills/propose.helpers.ts` Step 3.7）比对的是**模型转述的** `desiredAspect`，而说明书当时还写着「Pick the closest one … anything else is delivered as 1:1 and the card says so out loud」，模型照它把 4:5 换成菜单内的 4:3，闸于是永远走不到（卡面 `2304 × 1728` 正是 `GEN_IMAGE_SIZES["4:3"]`，反证模型传的就是 4:3）；②按下 `Generate · 14 credits` 启动那条片子之后，Otto 又问「A) 现在这支 B) 更稳的两步先出首帧」——铸卡与继续说话天生同一轮（卡由 propose 在 run 中途落库，最终文本在 run 结束后另存一条 TEXT），而系统里没有任何一条规矩说过铸卡之后不许再给选择题；两步接力落地之后这道选择题本就多余 | **落地（本 PR）**：①说明书口径改成与画质档同款——`packages/otto/src/instructions.ts` 图片形状段改为「原样传商家点名的形状（含菜单外的 4:5）、绝不替他挑最接近的一格、菜单外由服务端在铸卡前拒绝且 $0」，「delivered as 1:1」那半句删除；②Step 3.7 补**第二个证人**（照视频动作那一处的范式）：从服务端写入的 `ctx.turnText`（永不来自模型入参）只认**显式** `W:H` 写法，窄化不读语义（两边都是 1–2 位整数且不大于菜单最大项、比例落在 1:4 ~ 4:1、一句话里出现两个不同比例即弃权、零信号即弃权），与这张卡真会交付的那一格对表，对不上或菜单外就在 `pricedGenCredits` 之前抛 `ImageAspectUnavailableError`——GEN_CARD 一行不落库、ledger 零新增行；模型干脆漏传 `desiredAspect`、卡落到默认方图的那一半同样归它管；③`instructions.ts` propose 段补硬规矩：铸卡之后的回复只许指向那张卡，不许给备选、不许二选一，方案选择必须发生在铸卡之前，「先出图再出片」是一次 `forVideo` + `videoPrompt` 铸成的**一个**方案。**不上文本启发式机器闸**（误报率未评估，编排者裁定）。行为测试（验收编号逐字）：`packages/otto/src/skills/propose.test.ts`（CREATE-A1 模型换档后商家原话仍拦住、CREATE-A1 拒绝句说的是商家那个形状、CREATE-A1 漏传形状照样拦、CREATE-A1 走 ProposeRefusal 家族零落库、CREATE-A1 一致就照铸且人话写法不误伤、CREATE-A1 两个比例即弃权、CREATE-A1 零信号照旧、CREATE-A1 无 `turnText` 行为一格不动、CREATE-A1 时间写法不当比例读、CREATE-A1 走通时话与卡出自同一份 payload；CREATE-A2 两参考加视频请求一次 propose 只铸一张卡且第二步已冻结）、`packages/otto/src/instructions.test.ts`（CREATE-A1 新口径三件事在、旧口径两半都不在；CREATE-A2 铸卡后不给选择题的硬规矩在）。变异检查：说明书改回「pick the closest」⇒ 第二证人那一族仍全绿（闸不靠说明书站着）；去掉第二证人 ⇒ 4:5 那三条红；去掉铸卡后硬规矩 ⇒ CREATE-A2 那条红（限度：它钉的是提示词里有这条规矩，钉不住模型每一轮的自由文本，结构那一半由「一次 propose 一张卡、第二步已冻结」钉）。**同时登记一条商家可见行为变化**（判官 2026-09-04 P2-2）：说明书删掉了旧句「a story or status post is 9:16」并明令「never infer a shape from the kind of post they mentioned」，所以商家只说「IG story」而不报比例时，卡从此落默认方图（1:1），不再由模型替他猜一格竖版——这是本轮口径收紧的必然结果（猜错的那一格同样是一张他没要过的付费卡），要竖版请让商家或 Otto 明确说出比例；`instructions.test.ts` 钉住这句明令的存在与旧句的消失。**判官 2026-09-04 复审落修四件**：①菜单内形状与卡对不上时改抛新的 `ImageAspectMismatchError`（只说事实差异），不再无条件走「做不到 + 最接近几格」那句——否则「nak 9:16」会换来「9:16 isn't a shape I can make here — the closest I can do are 9:16…」，一句对旗舰竖版格式的假能力声明；②拒绝句回述商家**原文**写法（`9 : 16` 不再被重排成 `9:16`），右侧带前导零（`11:05`、`4:05`）一律弃权，并把仍会误伤的两种时间写法（`9:15`、`10:15`）作为已知缺口显式钉进测试而不是回避；③两步计划那条验收改钉它真正量到的事（批准价只含第一步，第二步预估另占一格）；④铸卡后硬规矩补 `proposePack` 豁免（选项在卡里面不是卡旁边的问题）。裁决留空待 S5 |
| 2026-09-05 | **登记轮 · 判官 #1182 P2 五条（接力卡那一票，评论 https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/pull/1182#issuecomment-5535816552），本场不做**：① 第二张卡的 `planCardGate.approvable` 没有测试钉（判官临时写用例才证出 `approvable=true`）——建议补一条把 `buildVideoStepCardPayload` 的产物直接喂真 `planCardGate` 的用例；② 「接力卡成为当前轮待确认」同样没有测试钉（判官靠读码追链 `pollAndInjectResults` → `mergeDurableIntoLive` → `appendMissingCards`）；口径记在这里：Step 1 出图那一刻常显卡从 Generating 直翻 Needs confirmation，#1173 的那句「Made 1 image」不在常显卡上停留；③ `videoStep.next` 只有 `structuredPrompt` 是必填，画幅／时长／画质／声音缺席时一律落默认档——商家说「5 秒」而 Otto 漏传时会静默变成默认时长，唯一的兜底是商家在第二张卡上仍看得见时长与价钱再按；④ `workerDisabledModels()` 每笔 GEN_RESULT 都白跑一次（`apps/worker/src/jobs/gen.ts` 里在实参位求值），挪到判定之后即可，纯浪费、不影响正确性；⑤ 接力的窄窗口：Step 1 批准到出图之间视频型号被关掉 ⇒ `planVideoStepHandoff` 返回 null，第二张卡不出现且不解释——建议接力失败时落一句 TEXT。验收编号沿用现有 CREATE-A 表，不发明新编号 | 登记；逐项待排期或 S5 同读 |
| 2026-09-05 | **登记轮 · 判官 #1183 P2 五条与 #1184 P2 的一条残项，本场不做**。**#1183**（慢轮收敛那一票，评论 https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/pull/1183#issuecomment-5537212572）：① 收敛的上界其实是 60 秒（慢轮 `GENERATION_WATCH_GEARS.slow.intervalMs = 60_000`，`apps/web/lib/otto-inject-helpers.ts:120`）而不是「下一次轮询」，S5 时把这个口径说清；② e2e 旅程 19 的终局两行是按 `gen.ts` 手抄的形状（那套件里没有真 worker），形状缝上没有围栏；③ 本票挂的 CREATE-A1 偏——§2 冻结表里没有「花钱之后终局要诚实」这一行，S5 时由 Founder 定要不要补一行；④ 余额同步 `onBalanceRefresh`（`apps/web/components/otto/OttoChatStream.tsx:570-573`）的 `freshResultCount` 那一支没有行为覆盖（围栏只做源码正则点名），而慢轮把它变成了退款可见性的主路径；⑤ 「Check again」的语义变了——`This looks stuck` 现在要等快轮加慢轮约 32 分钟才出现，而慢轮耗尽时画布上那张 280px 的卡仍写着 Generating（落修者已自报不做）。**#1184**（评论 https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/pull/1184#issuecomment-5537114279）残项一条：`apps/web/lib/clip-actions.ts:125-128` 的画布名改成始终读之后，`prisma.project` 抛错会被外层 catch 吞成一句通用话（Otto 那条路是 try/catch 退回 `Untitled canvas`），#1182 把回执构造收进单一构造点时应一并对齐。该票另一条「分镜视频子卡同病」（`apps/web/lib/storyboard-gate1-actions.ts:1154` ／ `:1327`）已在本表 2026-09-04 的 #1177 回归行里登记过，不重复 | 登记；逐项待排期或 S5 同读 |
| 2026-09-05 | **登记轮 · 矩阵走查（2026-09-04 09:45，本机 3310 生产构建，主干 `6fe3479e`，账号 e2e-founder-0903）留下的 Creation 侧 P2 五条与一道规格题，本场不做**：① Otto 流式文本出现四次句子粘连（例如 `…make it the star.Here's your product photo idea!`），句与句之间缺空格或换行；② Otto 把一个**尚未确认**的 1080p 视频提案当成可选的起始帧列了出来；③ 刷新之后 Otto 面板把上一轮那句「That generation didn't go through」压在一张无关的、待确认的 `Generate · 55 credits` 卡上方，容易被读成这张卡失败了（与本表 2026-09-04「画布当前轮的状态漂移」同族，但不是同一个现场）；④ 占位卡出现用时约 3 秒（本轮的标准是 1–2 秒），出图 24 秒；⑤ 门 8 的 `@` 只列得出 Character——要挂产品图得先去 Brand memory 建产品，而 Brand memory 又没有「带图创建产品」的入口。**规格题（要 Founder 定，不是缺陷）**：**服装类在领域模型里根本不存在**——`EntityType` 只有 CHARACTER ／ LOCATION ／ PRODUCT ／ BRANDMARK 四种（2026-09-05 复核 `packages/db/prisma/schema.prisma:22-27`）；地点类型有模型但没有商家入口。要不要把「服装」并进模型，属于 S1 层的问题，本 worker 不判。**场地三条留档（不是产品缺陷）**：R1 web 与 worker 的存储双脑（web 按 cwd 找 `.data/storage`、worker 在主检出）已用符号链接修好；R2 本机 i2v ／ i2i 必然失败（供应商取不到 localhost 上的文件，闸门正确退款）；R3 本机登录必须走 `http://127.0.0.1:3310` | 登记；服装是否入模型一题待 Founder |
| 2026-09-05 | **登记轮 · Codex 第三轮（产品→Avatar→视频，测主干 `062aae86`）的两条环境项与 Founder 2026-09-04 11:50 的当场裁决**：① **PAV-001（验收环境）**：本机 `LocalDiskStorage.presignedGet()` 直接返回 null（`packages/storage/src/index.ts:157-159`，注释写「local mode streams through the /files route」），于是 worker 在花钱之前就拒绝——**这是环境题，不是产品缺陷**。**Founder 11:50 裁决**：带参考的真出一律在 staging（R2）重测，本机 3310 不做带参考的真出；beta 之前统一建一个「验收 release profile」——web 与 worker 同一个 commit、同一个 R2 test bucket、官方演员种进同一个桶。② **PAV-002（场地）**：主检出的 worker 曾经落后于 web（worker 在 `fe9c70bd`、web 在 `062aae86`），11:50 已 `pull --ff-only` 到同一 commit 并重启；本机启动脚本现在开机就打印 sha ／ provider ／ storage ／ db 横幅，另一支本机构建脚本应当同样打印并与之比对——**待加**（这两支脚本不在本仓库里，取证止于派工书转述，不假造路径）。本行属类型①中途想法，验收编号沿用现有 CREATE-A 表 | 登记；验收 release profile 待 beta 前落地 |
| 2026-09-05 | **§8.0 拍板三占位路径改定**：手艺文件的占位路径 `packages/otto/craft/*.md` 由 `docs/specs/otto-engine.md` §7.0 拍板一改为 **`packages/otto/knowledge/craft/`**（`seedance.md`／`seedream.md` 已随 Otto ③段落地）——照拍板三原文「otto-engine.md S2 若另定路径，本稿随之改路径，不算改签」。批 III 的镜头术语表从 `packages/otto/knowledge/craft/seedance.md` 的「镜头术语表」一节解析取词（解析器 `packages/otto/evals/checks/glossary.ts` 已可用），不在 `checks/` 里抄第二份 | 随 §7.0 拍板一生效（2026-09-04 S2 批准） |
| 2026-09-05 | **画布节点级付费出图出片进对话历史（请求＋确认＋结果）＋ 结果消息写失败的补投**。Founder 原话（编排者代记 2026-09-04 20:45）：「画布即对话 → 落成规则。画布上任何付费出图出片都写进这张画布的对话历史（请求、确认、结果），刷新与换浏览器都在。」落地形状：① **请求＋确认**由 `apps/web/lib/gen-actions.ts` 的 `startGen` 在**钱事务里**写（create 之后、`reserveCredits` 之前），共用写点 `apps/web/lib/canvas-thread-log.ts`（USER 一条＝服务端闭集判出的动作原话 `Make another like this`／`Make an image`／`Animate this`／`Make a video` ＋商家那句话；AGENT GEN_CARD 一张＝规格由 `buildSpecChips` 唯一一次派生、金额直接取这一趟的 `displayCredits(pricedGenCredits(...))`，不新算）。卡上写它自己的 `genJobId` ⇒ `deriveCardState` 永不为 idle ⇒ 界面不再出 Generate；服务端在 `startCoworkGen` 对带 `canvasAction` 的卡 fail closed（$0、零任务行）。幂等沿用画布动作键（同一 actionId 重放在 `canvasHistoryVerdict` 就 reused 返回，历史不重复落）。② **结果**一直由 worker 的 `appendCoworkResult` 写（判据是 `job.threadId`，节点级路本来就带），本轮只把它的失败从 `console.warn` 吞掉改成 `console.error` ＋新增补投扫描 `apps/worker/src/jobs/gen-thread-redelivery.ts`（挂在既有 reaper tick，形状照抄 `canvas-backfill`：跨租户扫描、按行进租户写、money-free，exactly-once 由既有部分唯一索引 `ChatMessage(genJobId)` 保证）。③ 顺带闭合 H2 #1234 的窗口：`startCoworkGen` 事务外读卡 → 事务内建任务行之间卡被改档，任务行会按旧 payload 冻结；改成把读卡那一刻的 payload 指纹带进钱事务、在事务里重读同一行对签，不一致就在 create/reserve 之前拒（$0、账本零新增行）。钱路铁律未动：预扣／结算 refId 不变，只加落库。证据：`apps/web/lib/__tests__/canvas-paid-into-history.test.ts`（真库 6 条）、`apps/web/lib/__tests__/otto-card-options-ledger.test.ts` 新增 2 条（真库，改档窗口）、`apps/worker/src/jobs/gen-thread-redelivery-db.test.ts`（真库 5 条，含真实写失败注入）。**未做项**：⒜ 画布上没有活着的对话时（`activeThreadId` 为 null）这一趟仍然不落历史，也不凭空建线程——建线程会给本来没有线程标签的画布任务挂上标签，从而把它挤出 GenSpace／Assets 视图，属另一件事；⒝ 补投扫描刻意不补 `CANCELLED`（那条消息的 `cancelled` 耳记只有取消路知道）与「DONE 但零产出」（判不清），两种都只记 `console.error`；⒞ 补投扫描一轮至多看 500 行、回看 24 小时，是上界不是「一定看得完」 | 编排者代记 Founder 2026-09-04 20:45 |
| 2026-09-06 | **清单 A5（P2-013）：确认卡的「Change something」改成小表单 ＋ 供应商提示词收进 Advanced details**。落地形状：⒜「Change something」（画布那张卡上是「Change」）不再把送给供应商的那段原话整段塞回输入框，改为在卡上就地展开一张小表单（`CardChangeForm`，住在 `apps/web/components/otto/CardOptionControls.tsx`，两处确认位共用一份），表单里有一行人话输入「Tell Otto what to change」，Send 把**商家写的那句连同这张卡的原话**送回对话——走的仍是既有 `onChangeSomething` 那一条路，不新造第二条；⒝ 比例／张数／精修照旧是卡上的原生控件（改一格＝服务端 $0 重铸整张卡，界面一分钱不算），表单只指路、不抄第二份控件；⒞ **时长、声音、参考三格没有做**——服务端今天的改档口径只收图片卡那三格（视频卡整张拒绝、参考没有入口），摆一个按下去必然被拒的控件就是把 fail closed 做成陷阱，改为表单里一句「这几件我改不动，用人话告诉我，我重出一份计划」；⒟ 供应商提示词（seedream／seedance 那段原话）从卡面主视图收进**默认收起**的「Advanced details」，Copy 仍在里面。证据：`apps/web/lib/__tests__/creation-a5-card-change-form.test.tsx`（11 条，验收 CREATE-A1）。未做与触发条件：时长／声音／参考要能在卡上就地改，须先扩服务端那条改档路（`applyCardOptions` 与 `ottoUpdateGenCardOptions`），本刀写集不含它们，触发＝下一次动改档口径时一并做。**判官 #1245 复审落修三件**：① P1-1 —— 表单的渲染闸从前只问 `changeOpen`，而这张卡由消息 id 挂载、批准之后不卸载，于是商家先展开表单再按 Generate，一张关不掉的「Tell Otto what to change」会挂在刚扣过钱的卡上（切换那颗键此刻已不在）；闸改为逐字点名真正给得出它的两个状态（`waiting`／`failed`），画布那一侧本来靠「那一行会卸载」才没这个坑，现在两处确认位不再靠巧合一致；② P2-4 —— 只出得了一张的卡上不再渲染 Images 那颗死下拉（判据与指路句的 `maxCount > 1` 同一条），从前话不点它的名、卡面却摆着它；③ P2-3 —— 卡面注释里那句「服务端至今不写 `goal`」不实（`packages/otto/src/skills/propose.ts:150` 在商家给了用途时就写），改成如实说法，行为未变。**待 Founder 追认**：⒝ 的「形状（已有）」清单原文列的是表单里的一格，落地改成表单只指路、控件仍在卡上（在表单里再渲染一遍同一份控件，屏幕上就是同一张卡两份控件）——与清单原文不逐字一致 | 登记；就地改时长／声音／参考待排期；⒝ 的落地形状待 Founder 追认 |
| 2026-09-06 | **尾巴轮四组三：A4 与确认卡表单的判官残留（#1239／#1241／#1245）**。落地形状：① `ottoUpdateGenCardOptions` 补上「已成交的卡改不动」这道服务端闸——判据两条，卡 payload 上有 `canvasAction`（画布节点级那张已扣过钱的回执卡）或卡上已挂 `genJobId`，一律拒在任何写之前（从前唯一的守卫只查 `cowork:<cardId>` 这个幂等键，画布那张卡用的是 `canvas:<actionId>`，改档路能把一张已成交的收据改写）；payload 一个字节不动、账本零新增行，真库测试两条＋变异各红各的。② 两处承重注释改成实话：补投那份说「取消的行也照实记日志」（实为扫描只收 DONE/FAILED，取消的行连候选都不进），交付路那份说「没有下游会重试」（已被补投推翻，改指向 `gen-thread-redelivery.ts`）。③ 卡上那三格重铸在飞时看得见地变淡（只加样式，不锁控件、不改行为），第一趟被拒时排队那一格被丢掉也在服务端原话后如实说出来。④ 「Change something」那张小表单不再在卡走「等确认 → 已排队 → 失败」之后自己弹开（开关改成记「在哪个状态下按开的」，卡一换状态就自己收）；带参考的视频卡那句英文不再两个 and 连读。零新服务端能力、零价格算式、界面一分钱不算。验收 CREATE-A1。 | 编排者代记（判官残留，行为无新增） |

## 6. 改签记录

- 2026-08-31 机械纠错：九问2 animate 口径——原文「两处 animate 是模板化动作、无自由提示词」与现网不符（画布卡片 Animate 的 Custom 选项是自由文本付费通道，资产详情 Animate 才是模板化；同族复核链与 Codex 跨厂判官双 CONFIRM）。纠正为如实描述，**冻结范围不变**（两处本轮均不增强）；Founder 2026-08-31 收签场 AskUserQuestion 逐字拍板「机械纠错，本轮不增强（推荐）」。同批回填九问4 两处事实纠正（配额账户级总量、输入上限段标目标态），语义零方向变化；签名 #1101 沿用。
- 2026-08-29 冻结落地时机械修订：验收表 A1/A8 两处跨规格编号引用改为不落表格行的写法（M2 前缀唯一闸把表内 `ENGINE-A*` 字样误认作本规格前缀声明），语义零变化；签名 #1101 沿用。
- 2026-08-30 改签 v2：九问1⑤（演员库口径）、九问4「信任回灌→血统信任」、A9/A10 重写、「不做」节真人出镜与 ACR 两条改判、§7 实验 1/2/5 结案——「30 天信任回灌」与「身份层前置」双双被实测证伪；beta＝演员库＋诚实拦截（Founder 2026-08-30 裁决）。签名＝#1101 Founder 重新评论「S1 批准 creation-engine.md」（2026-08-30）。

## 7. 冻结后、施工前的实测轮（Founder 2026-08-29 已批，预算 ≤$20）

三个实验＋一份回执，结果处置规则：**与规格预期一致＝回填变更登记；推翻九问任一承重口径＝走改签记录**（不预判）。——**2026-08-30 状态：实验 1、2、5 已结案**（结果见变更登记与改签记录；实测轮累计花费 ≈$9.4/$20），实验 3、4 仍开放、为 S2 开工前置。

1. 【已结案 2026-08-30：拦截，如预期】**直传真人脸参考**：预期仍被创建阶段免费拦截（官方口径如此）；若竟通过＝官方文档滞后，即刻报 Founder。用谁的脸另行报备、经 Founder 点头后再跑。顺带在控制台核读 ACR 实际档位（只读）。
2. 【已结案 2026-08-30：机制改判＝血统信任，走改签】**信任回灌链路**：文生 AI 人物 → 30 天内回灌出片，预期通过（对应 A10）。加一组对照：**非本平台产物的 AI 写实人脸**是否同样可过——区分「信任通道放行」与「AI 脸本身可过检测」两种机制（行业侧证：Higgsfield 规模化走「真人照片→自研身份模型→AI 肖像→Seedance」，暗示后者成立，未实证）。
3. **`optimize_prompt_options` 是否改写我们的提示词**：图片侧 A/B 两张便宜验证。**若会改写，动的是「`sentPromptText`＝商家批准稿逐字」这条承重口径，按改签处理。**
4. **逐槽 `supported_params` 实查回执**：mini 与 2.0 的能力差异表（零成本只读查询），S2 开工的第一份输入；含 `generate_audio` 打开时供应商侧是否另计费的口径核查。
5. 【已结案 2026-08-30：图片端不拦＝证实；但其图生图产物进视频被拒，身份层前置证伪】**Seedream 图片端收不收真人脸**（身份层前置的门题）：商家真人照片走 Seedream i2i，拦或不拦？——我们的人脸拒收实测（2026-08-08）只发生在**视频**提交端，图片端历史上从未返回过该错误、但也从未实测。若图片端不拦：身份层可全用现役 Seedream 做（真人照→AI 肖像→存为形象→进 Seedance），**零新供应商**；若也拦：身份层需外部图像模型或 ACR 素材库承担「照片→肖像」一步。真人照片用谁的、如何报备，照实验 1 同一规矩。

已结案的取证（不再验）：参考图＋首尾帧混用＝官方明文互斥，免实测；mini「API coming soon」雷已排除（`enabled:false`＋8 月 17 次真实成功调用）。

## 8. S2 施工稿（设计阶段产出；S1 正文 §0–§7 一字未动，§5 只追加登记行）

> S2 状态: 已批准
> S2 批准: https://github.com/BELCORT-SDN-BHD/FIKIRTIVE/pull/1131 Founder 评论「S2 批准 creation-engine.md」(2026-09-02)；该评论同时追认 §5 2026-09-01 定价回填行（1080p 视频 11cr/秒、pro 图 2cr/张）与本节 §8.0 四项拍板建议

### 8.0 范围、量尺与本场拍板（Founder 2026-09-02）

- **目标**：Founder 现场令「今天做完 Creation，像真产品一样完全可用」。本稿的诚实量尺：12 条验收按依赖切三批——**批 I 今天**（后端、围栏、资产；不碰 PR #1117 重写的商家面文件）、**批 II 新前端纯合并落主干后**（商家面：增强预览、指派句、音频素材、角色选择器）、**批 III 随 Otto 引擎**（A8 评测基线、A1 画布路径的 Otto 卡片）。今天结束时 Founder 能真实用到：现有生成全链＋1080p 高清档与 pro 图上架（报价前置、型号不外露）＋声音开关＋演员库五人可在 `@` 引用出片＋真人脸口径。批 II 估 2–3 天，批 III 随 Otto S2。
- **拍板一（追认）**：1080p＝11cr/秒、pro 图＝2cr/张（§5 2026-09-01 行）随 S2 批准追认；上架随批 I。
- **拍板二（演员库归属）**：每租户播种（§5 2026-09-02 行）。备选「跨租户共享/官方实体」触碰租户边界＝schema 与隔离语义变更，不做。
- **拍板三（手艺文件路径占位）**：`packages/otto/craft/seedance.md`、`seedream.md`；otto-engine.md S2 若另定路径，本稿随之改路径，不算改签。
- **拍板四（pro 图优化开关默认值）**：由批 I 的实验 3 A/B 决定：若开启优化会改写提示词且质量无明显增益，请求体显式 `optimize_prompt=false`（保「实发＝批准稿」的最短链）；若增益明显，保留默认并把改写记进 `finalPromptText`。
- **机器闸**：每段 PR 带 `Spec: docs/specs/creation-engine.md`；验收编号逐字入测试（M3）；两条迁移守形状（M5）；新开关无 `BETA_*`。

### 8.1 批 I（今天；写集见 8.4）

① **能力路由＋SKU 白名单（A4/A5/A6）**：`packages/core/src/gen.ts` 视频菜单加第二槽位 `seedance-2-0`（能力：480p/720p/1080p；4k 不卖）、图片菜单加 `seedream-pro`；`packages/generation/src/byteplus.ts` 型号映射加 `dreamina-seedance-2-0` 与 `dola-seedream-5-0-pro`；`model-config.ts` 的 `assertSpendableModel` 判据从「等于唯一在产型号」改为「SKU 级已定价白名单」（消费钱引擎已落地的 `sellableVideoSkus()/sellableImageSkus()`），白名单外＝拒绝、$0、不降级；默认档配错仍降级留日志（现行）。新增路由器：视频按分辨率（1080p→2.0，其余→mini），图片按能力（透明底/人物精修→pro）；`Generation` 新增可空列 `routeReason`（迁移），商家可见口径只写能力名词。`spend.ts` 图片分支按型号取价（lite 1cr / pro 2cr）；`seedanceDisplayCredits` 认 2.0 档。测试：CREATE-A4、A5、A6 逐字。
② **声音开关（A3）**：`VideoSpecPicker` 的 `VideoSpec` 加 `audio`，开关文案「Sound doesn't change the price」；`DetailPanel.tsx:402` 一行改为传商家选择（该文件是 #1117 冲突文件，只改这一行，合并方保留）。测试：CREATE-A3 断言实发 `generate_audio=false` 且报价不变。
③ **演员库入库（A10 后端）＋真人脸口径（A9）**：迁移加 `Entity.catalogKey String?`；脚本 `scripts/ops/seed-actor-library.ts` 读 `preserved/actor-library-v1-2026-08-30/` 的 `.bin` 与 `card.json`，走 `storage.put(原字节,'jpg')`→`assetUpsert`→`Entity(CHARACTER, catalogKey)`＋2 张 `ReferenceImage`(closeup/fullbody)；org 引导（`requireOwner` bootstrap）对新租户播种；九套造型 preset 写入 `Entity.descriptionJson.presets`（一卡三用）。**像素完整性防回归测试**：断言送供应商的参考图字节 sha256＝入库字节 sha256，生成路径零再处理。A9：`gen-failure.ts` 口径改「Real human faces aren't supported yet」＋出路句指向演员库（按钮 UI 归批 II）。测试：CREATE-A9、A10（后端半：演员实体经 `@` 引用出片、引用落盘）。
④ **实验 3 A/B**（pro 开/关各一张，$0.16–0.48，§7 额度内）＋回执归档 `preserved/creation-probe-2026-09-02/`；结论回填 §5，决定拍板四。
⑤ **A12 前半**：`routeReason` 落盘随①；Regenerate 改发 `sentPromptText`（`DetailPanel.tsx:337`）归批 II。
⑥ **复审**：每段 Codex 跨厂复审；全量 apps/web 测试＋typecheck＋production build＋e2e。

### 8.2 批 II（新前端纯合并落主干后）

- **增强层（A1/A7/A12）**：新建 `packages/core/src/enhance.ts`（读手艺文件、单次上界 $0.01、每动作 ≤6 次、`founder_absorbed` 计量、失败回退原文＋「Not enhanced」标注）；画布 composer 与 Generate edit 新增「输入→增强中→可改预览＋报价→提交」一态；增强稿即付费请求携带的 `sentPromptText`；Regenerate 重发上次 `sentPromptText`。
- **自动指派＋拒绝闸（A2）**：素材→合法角色枚举指派器；指派句入增强稿；越界指派与首尾帧×全模态互斥＝花钱前拒绝、$0（替换现行「截断＋披露＋照收」）；输入上限公式化（九问 4 目标态）。
- **参考音频（A11）**：composer 素材区收音频；适配器发 audio 部件；三条上限（≤3 段、视频＋音频总长 ≤15 秒、音频不可单独）花钱前拒绝。
- **演员库商家面（A10 UI、A9 出路）**：Library Elements 与 `@` 显示官方角色（`catalogKey` 标记）；真人脸拦截卡的出路按钮。

### 8.3 批 III（随 Otto 引擎 S2）

- **A8**：依赖 ENGINE-A1 评测基线与 `packages/otto/evals/` 骨架（今天不存在）；Creation 评测集 ≥10 题与四项机械检查建在同一骨架上。
- **A1 画布路径**：ENGINE-A3 落地后判定在 Otto 确认卡片上生效。

### 8.4 写集互斥表

| 施工线 | 写集 |
|---|---|
| Creation 批 I | `packages/core/src/{gen,model-config,spend,margin-truth,gen-failure}.ts`、`packages/generation/src/byteplus.ts`、`apps/worker/src/jobs/gen.ts`、`packages/db/prisma`（两条迁移）、`scripts/ops/seed-actor-library.ts`、`apps/web/lib/auth-guard.ts`（播种钩子）、`apps/web/components/gen/VideoSpecPicker.tsx`、`apps/web/components/asset/DetailPanel.tsx` 第 402 行一行 |
| 前端基线纯合并 | 9 个冲突文件＋`apps/web/design-system/`＋`apps/web/components/ui` symlink（frontend-baseline.md §7） |
| 交集 | `DetailPanel.tsx`：批 I 只改一行，合并方保留该行 |

### 8.5 环境前置（Founder 钥匙）

- 本地真出片：worker 需 `GENERATION_PROVIDER=byteplus`＋`BYTEPLUS_API_KEY`（主检出 `.env.local` 已有部分变量；缺则 mock 出纯色假图）。
- 实验 3 与实查：`arkcli` SSO 已到期，需 Founder 重新登录。
