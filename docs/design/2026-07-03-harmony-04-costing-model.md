# Harmony 04 —— 定价 Costing 模型

> **性质**:harmony 交付物 4/6。输入数据全集见同目录 `harmony-04-costing-inputs.md`(每个常数带 file:line 出处)。
> 用法:founder 补齐 §四 的三个数字 → §三 的决定自动落地。**在此之前,任何档位定价不得写死**(宪法第 5 条 + costing 先行令)。

## 一、收入公式(双轨,G 区已拍)

```
月收入(每 org)= 创作席数 × P_creator + 审批席数 × P_approver     ← 席位轨
              + max(0, credits 消耗 − 订阅内含额度) × credit 单价   ← 用量轨
月毛利       = 月收入 − Σ(各花费点 COGS) − 通道费(直传,零毛利) − 固定成本分摊
```

## 二、单位经济学现状表(✅=已知 ❓=等发票)

| 花费点 | 我们收 | COGS | 毛利 | 状态 |
|---|---|---|---|---|
| 图(Seedream) | 1cr = $0.10 | $0.04 是 **fal 旧基数**;BytePlus 实际 = 16,384 tokens/图 × ❓单价 | 名义 2.5x | ❓ 待 Ark 发票 |
| 视频 5s/720p(Seedance) | 7cr = $0.70 | 常规 t2v/i2v tokens 未记录 ×❓ | 名义 ≈ 平价 | ❓ |
| **参考视频 5s/720p** | 7cr = $0.70 | **324,900 tokens ×❓;按 $3.30/M 市场估 ≈$1.07** | **⚠️ 可能 −53%** | 🔴 优先核 |
| Otto 每轮对话 | 实耗 × **1.5 margin**(最坏预扣 ≈9 显示 cr,实结算通常远低) | sonnet $3/$15 每 M(代码硬编码)×❓发票差异 | 名义 +50% | ❓ cache-write/overhead 未验证 |
| 新用户赠送 | −100cr(名义 −$10) | 实际 COGS ≈ $2-4(按图视频混合) | 获客成本 | ✅ 可控 |

**⚠️ 参考视频警报的预案**(你在验证脚本里已定过原则:"调时长不调价"):若发票确认亏,首选 **REF_VIDEO_MAX_SECONDS 10→6** + 参考视频单列 charge(如 10cr);两个都是配置层改动,不碰引擎。

## 三、三个悬而未决的定价决定(数据到位即落)

| 决定 | 条件公式 | 预备判 |
|---|---|---|
| **credits 滚存** | 滚存上限 = 月配额 × 1(敞口有界:最坏月成本 ×2);若毛利 <35% 则降为 0.5× | 滚存上限制(G-03 已拍方向) |
| **免费档** | 能力全开 + 100cr;若 CAC 数据显示不痛不痒,升 150-200cr(获客成本仍 <RM20) | 维持 100cr 起步 |
| **席位价** | 竞品走廊:创作席 RM 68-128/月(对标 respond.io $79≈RM370 的 1/3-1/5,Canva Pro RM250/年之上 —— 我们是"员工"不是"工具");审批席 RM 0-18(便宜到全员进来) | 区间进 G 档位 spec,精确值等毛利模型闭合 |

分市场差异定价(G-06):MYR 基准价 × 市场系数表(config 层,如 SG 1.6 / ID 0.7),**系数永不硬编码**。

## 四、只有 founder 能补的三个数(当前全部 blocking)

1. **Ark 发票单价**(F39,现在升级为 🔴 blocking):图的 $/M tokens + 视频的 $/M tokens(或每任务价)。动作:BytePlus 控制台看 7 月 2 日两笔验证(1 图 + 1 参考视频)的实扣。
2. **Anthropic 发票**:上月实际 $ ÷ 代码公式算出的名义 $ = 校正系数(验证 1.5 margin 是否真覆盖)。
3. **固定成本月账**:Railway + R2 + Stripe 手续费率(MYR 通道) + 汇率缓冲(peg 4.7 vs 实时)。

## 给审查员的钉子
- [ ] 任何档位数字入代码前,本模型 §四 三项必须已闭合
- [ ] 参考视频毛利警报在 Ark 发票核实前,不推参考视频的营销
- [ ] 市场系数/滚存上限/免费额度全部落 config 层(宪法第 5 条:永不硬编码)
