# E4 · 排期发布 + 渠道 — Capability truth matrix

> 基线:`main@b5a48d0f`(HEAD 已核对 = b5a48d0f)。只读取证,不评分/不排序。
> 部署事实(工单给定):prod web = `7ed7ac22`(**不在本 worktree 历史里,无法定日期**,故 prod 档多为 Unknown);
> prod worker SHA = Unknown;staging = `54c1de0b`(immersive 分支,非 main)。
> L1a(#219)/L1b(#227)均于 **2026-07-11** 合并,是 main 最新几个 commit;prod web 落后 main ⇒ 按工单规则
> 晚于 prod 合并的一律 prod ≤ integrated(实际=未部署)。全链在 main 里也是 **fail-closed**(`canPublish=false`)。

## Matrix

| id | zone | capability | promise_source | stage_main | stage_prod | gate | evidence | provenance | gaps |
|---|---|---|---|---|---|---|---|---|---|
| E4-01 | 排期 | 排期区 3 视图(Plan/Calendar/Queue)+ Composer(账号选择/媒体复用/时区/first comment) | UI 组件 + #123 spec §四B | integrated | Unknown(#123 于 07-04 合并,prod SHA 无法定日期→未知) | 无门(纯 UI + $0 composer,只复用已生成媒体) | `apps/web/components/otto/OttoSchedule.tsx:65`(ViewKey plan/calendar/queue)、`:151-163`(view/composer/defaultTz/autoPublish state)、`:44-48`(三视图+composer 注释)、入口 `apps/web/components/otto/OttoView.tsx:201`(view==='schedule'→OttoSchedule) | Verified current source | Composer 只复用已生成媒体(不触发 spend);auto-publish 被 App Review 硬闸,UI 有 banner 说明 |
| E4-02 | 排期/管网 | ScheduledPost / PublishAttempt / ScheduledPostMedia 数据模型 | schema.prisma + #123 迁移 | integrated(模型+迁移齐全) | Unknown | 无 | `packages/db/prisma/schema.prisma:1122-1153`(ScheduledPost)、`:1176-1194`(PublishAttempt)、迁移 `packages/db/prisma/migrations/20260703030000_schedule_data_model/migration.sql:8-84` | Verified current source | status 为 code-validated String(非 PG enum);状态机是纯 helper |
| E4-03 | 排期 | 发布防双发 / exactly-once(三重幂等) | #227 spec §四D lock 1-3 | integrated(索引+代码齐;canPublish 前不实际触发) | ≤integrated(未部署) | DB 部分唯一索引(fail-closed by P2002 skip) | 幂等索引名 **`PublishAttempt_one_applying_per_post`**:`migrations/20260703030000_schedule_data_model/migration.sql:82-84`(`ON PublishAttempt(scheduledPostId) WHERE state='APPLYING'`);三重锁 `apps/worker/src/jobs/publish.ts` handlePublish:①metaPostId 短路 ②APPLYING 唯一声明 P2002→skip ③creationId 发 media_publish 前落库;creationId 迁移 `migrations/20260710130000_publish_attempt_creation_id/migration.sql` | Verified current source | 三重幂等在代码存在;稳态下 canPublish=false→根本不触发,索引为 no-op |
| E4-04 | 管网 | L1a 连接能力层(#219:typed capability handle / $0 / fail-closed) | #219 + schema | integrated | ≤integrated(07-11 合并,未部署) | DB flag `MetaConnection.canPublish`(DEFAULT false,**fail-closed**);仅当 Meta 实授 `instagram_content_publish`+`pages_manage_posts` 才置 true | `schema.prisma:1079`(canPublish DEFAULT false)、`:1076`(organicPublishPaused kill-switch DEFAULT false);迁移 `migrations/20260710120000_meta_connection_publish_caps/`;`completeMetaConnect` 从 debug_token grantedScopes 推导(#219 commit body) | Verified current source | gate = `canPublish` flag,来源是 Meta 实授 scope 而非请求 scope;App Review 未过→恒 false |
| E4-05 | 管网 | L1b 发布链:PUBLISH_QUEUE / scheduler / adapters / signed media proxy / idempotency / reaper-reconcile | #227(四片) spec §四A/§四C/§四D/§四F | integrated(六件齐;逐件见下) | ≤integrated(未部署) | 多重 fail-closed(见各件) | queue 契约 `packages/core/src`(PUBLISH_QUEUE/PUBLISH_DLQ/PUBLISH_QUEUE_POLICY,#227 commit);scheduler `apps/worker/src/jobs/publish.ts:562 scanDuePublishPosts`(where canPublish:true,`:564`);reaper/reconcile `reapStalePublishAttempts`+`reconcileAttempt`(#227 §四F);index.ts 注册 60s tick + 5-min reaper | Verified current source | 四片合一 PR;每片注释均声明 zero-behavior-change / fail-closed;第四闸修了 4 缺陷(proxy 认证墙/Dockerfile 漏依赖/impersonation 闸/env 契约) |
| E4-06 | 管网 | 签名媒体代理(Plan B:HMAC token + `/api/media/pub/[token]`,不开桶) | #227-2 spec §四C | integrated | ≤integrated(未部署) | env `MEDIA_PROXY_SECRET`(未设→verify 返回 null→全部 404,**fail-closed**);proxy.ts matcher 精确排除该路由 | route `apps/web/app/api/media/pub/[token]/route.ts`;`signMediaToken`/`verifyMediaToken` in `@fikirtive/token-crypto`;`buildMediaUrls` 签发 `apps/worker/src/jobs/publish.ts:220`;第四闸 H1 proxy.ts matcher 修复(#227 commit) | Verified current source | Meta 抓图服务器无 session,故该路由必须绕过全局认证墙;secret 未设→整链停摆 |
| E4-07 | 排期 | 单一发布动作层(IG create-container→poll→media_publish + carousel + first comment;FB /photos\|/feed) | #227-3 spec §五 单一动作层 | integrated(notImpl→真实现) | ≤integrated(未部署) | adapter 层 fail-closed 闸(见 E4-04) | `packages/core/src/meta-publish.ts`(纯+可注入 MetaGraphPort);`apps/web/lib/channels/meta-publish-adapter.ts`(owner-scoped fail-closed 闸);`instagram.ts:publish→publishViaMeta`、`facebook.ts` 同;worker `publish.ts:265` 调 buildMediaUrls | Verified current source | web adapter 与 publish worker 共用同一 core 实现(不可分叉);carousel abort fail-closed(⑤a 无半条) |
| E4-08 | 排期 | 媒体契约:排期允许视频但 IG adapter 对非 JPEG 只抽单帧 | 代码现状(**疑点,红测在途,别处 FIKIRTIVE-1 正红测此链**) | — 只记录代码现状 | — | 无(是行为语义问题) | `apps/worker/src/jobs/publish.ts:173`(`IMG_JPEG=Set(['jpg','jpeg'])`)、`:177 transcodeToJpeg` 用 `ffmpeg -frames:v 1 -vcodec mjpeg`(**抽一帧**)、`:217` IG 非 JPEG 一律走 transcodeToJpeg;IG capabilities 含 reel/story/carousel `channels/instagram.ts:9`;`autoPublishable` reel/story→'reminder'(`instagram.ts`) | Observed(原始观察) | **断层**:若 IG 排期帖挂视频(如 mp4)且走 auto 路径,`buildMediaUrls` 把它 transcode 成单张静帧 JPEG——视频会被当静图发。无 video-container(IG reels/video)分支。reel/story 被 autoPublishable 标 'reminder'(不 auto),但 feed/carousel 挂视频仍走此路径。标注为疑点,判断留上级 |
| E4-09 | 管网 | Meta OAuth 连接 / 断开 / data-deletion | UI + 路由 | integrated | Unknown(旧能力,prod 无法定) | OAuth(env client id/secret);disconnect server action | 路由 `apps/web/app/api/meta/authorize/route.ts`、`callback/route.ts`、`data-deletion/route.ts`;`disconnectMeta` `apps/web/lib/meta-actions.ts:98` | Verified current source | data-deletion 路由存在(Meta 平台合规要求) |
| E4-10 | 广告/管网 | Ads 自治开关(Ask/Auto)+ ads kill-switch(断电) | schema + 写动作 | integrated | Unknown | DB flag `adsAutonomy`(ASK 默认,断连即 reset)+ `adsWritesPaused`(kill-switch);`runApprovedPlan` throw KILL_SWITCH 拒写 | `schema.prisma:1074-1075`;`setAdsWritesPaused` `apps/web/lib/meta-write-actions.ts:21`;`:205 throw KILL_SWITCH` | Verified current source | ads 断电闸完整可用户操作;对比 organic kill-switch 见 E4-16 |
| E4-11 | 分析 | Meta 读(insights / 逐 ad 表现 / 诊断卡)$0 | #128 运营诊断闭环 | integrated | Unknown | 无(只读) | `apps/web/lib/meta-insights.ts`、`meta-performance.ts`、`meta-performance-actions.ts`、`meta-graph.ts`;测试 `meta-performance.test.ts`、`meta-performance-actions.test.ts` | Verified current source | 频道 insights adapter 层仍 notImpl(`instagram.ts:fetchAccountInsights: notImpl`)——ads 读走 meta-performance 而非 channel adapter |
| E4-12 | 广告 | Meta 写 v1/v2(pause / resume / reschedule ad)+ App Review | #128 + meta-write-actions | integrated(代码) | Unknown | kill-switch + impersonation 闸;pause/resume/reschedule 独立于 live 数值态 | `apps/web/lib/meta-write-actions.ts:97-165`(pause/resume/reschedule)、`:434` 拒 kill-switch | Verified current source | 组织内广告改状态(断电=pause)可用;需 `ads_management` scope(App Review 门) |
| E4-13 | 管网 | OAuth 请求的 4 个 organic-publish scope(App Review 待批) | #219 spec §六 | integrated(请求了,授予与否由 Meta) | ≤integrated(未部署) | Meta App Review(**外部钥匙**);未过→scope 不授→canPublish=false | `apps/web/lib/meta-oauth.ts:59` scope 串含 `instagram_content_publish,pages_manage_posts,instagram_basic,pages_read_engagement`(与 ads_read/ads_management/pages_show_list/business_management 并列) | Verified current source | organic 发布等的钥匙 = App Review 授予 `instagram_content_publish`+`pages_manage_posts`(两者都要);断电/ads 写等 `ads_management` |
| E4-14 | 排期 | X/Twitter 发布(判决「要」+ 定价 1cr/4cr) | 判决条目(promise_source only)——**代码里无任何 X 实现** | schema 都没有(无) | 无 | 无 | 搜 `twitter\|tiktok\|shopee` 全仓:X/Twitter 零命中(仅注释性 e-commerce 提及);`getChannel('tiktok')` 断言 undefined `channels/__tests__/registry.test.ts:36`;无 pricing config 文件(`config` 目录不存在,仅 `packages/core/src/spend.ts`)、无 `1cr/4cr/creditCost/publishCost` 命中 | Verified current source(缺失) | **断层**:X/Twitter 只在判决文档里「曾决定过」,main 无 schema/无 adapter/无定价 config。organic 发布是 $0(#227),发布无 credit 计价代码 |
| E4-15 | 排期/分析 | TikTok / Shopee / Lazada | 判决/蓝图(promise_source)——代码=规划态占位 | UI shell(仅 analytics「soon」占位)/ 发布无代码 | 无 | 无 | `apps/web/lib/analytics-platforms.ts:17-18`(tiktok/shopee status:"soon");schedule 渠道校验拒 tiktok:`schedule-actions.test.ts:169`、`schedule-draft.test.ts:26`(err /channel/) | Verified current source | 断层:仅分析区「即将上线」标签;排期只认 instagram/facebook,tiktok 被 draft 校验拒;Lazada 仅出现在 ingest 注释 |
| E4-16 | 管网 | 平台可插拔(adapter 缝:加新平台改不改核心?) | 蓝图扩展缝 | integrated(缝存在) | Unknown | 无 | `apps/web/lib/channels/registry.ts`(self-register:import 新 adapter + `registerChannel(x)`,末尾 `registerChannel(instagram)`/`registerChannel(facebook)`);`getChannel/listChannels` 泛型;类型 `channels/types.ts`;client-safe 镜像 `channel-meta.ts` | Verified current source | 加新平台 = 新 adapter 文件 + 一行 registerChannel,核心 registry/types 不改;但发布 orchestration(meta-publish.ts)是 Meta 专用,非 Meta 平台需自带动作层 |

## 断层观察(原始观察,不排序不评分)

1. **organic kill-switch 只读不可写(用户操作缺口)**:`MetaConnection.organicPublishPaused`(`schema.prisma:1076`)被 publish worker/adapter 读取并强制 fail-closed,但全仓在 `apps/web` 里**没有任何写它的 server action / UI**(`grep organicPublishPaused apps/web` 仅命中 adapter + 测试)。对比 `adsWritesPaused` 有 `setAdsWritesPaused`(`meta-write-actions.ts:21`)可用户切换。organic 断电闸=enforced 但 founder/用户目前无法主动拉闸。
2. **IG 视频→单帧静图(媒体契约疑点,红测在途)**:见 E4-08。`transcodeToJpeg` 用 `ffmpeg -frames:v 1 -vcodec mjpeg` 抽单帧;IG 非 JPEG 资产一律走它,无 video/reel container 分支。reel/story 被 `autoPublishable` 降级为 'reminder' 挡住 auto,但 feed/carousel 挂视频仍会被静图化。此为别处 FIKIRTIVE-1 正红测的同一疑点。
3. **X/Twitter:判决要,代码零**:E4-14。1cr/4cr 定价与 X adapter 在 main 完全不存在;发布链是 $0 organic,无任何 credit 计价代码,故「定价」目前只是文档承诺。
4. **channel adapter 的 insights 仍全 notImpl**:`instagram.ts`/`facebook.ts` 的 `fetchAccountInsights/listPublishedPosts/fetchPostInsights` 均 notImpl;Meta 读能力(E4-11)走独立的 `meta-performance*.ts`(ads 侧),没接到 channel adapter 缝上——排期区发的帖没有经 adapter 回读表现的闭环。
5. **整条 L1 发布链在 main 也是 fail-closed 惰性**:canPublish DEFAULT false + App Review 未过 + `MEDIA_PROXY_SECRET`/`PUBLIC_BASE_URL` 未设(go-live 前配置项),四层任一未满足即 refuse/404。代码 integrated,但用户价值不成立需(a)App Review 过 (b)env 配齐 (c)部署。
6. **prod 真相无法从本 worktree 判定**:prod web `7ed7ac22` 不在 b5a48d0f 的历史里(`git show 7ed7ac22`→NOT IN HISTORY),无法定其日期/内容。除「L1 于 07-11 合并晚于 prod⇒未部署」这一工单规则外,老能力(排期 UI #123 07-04、Meta 读写)的 prod 档均 Unknown。

## Unknowns

- prod web `7ed7ac22` 的确切内容/日期:不在本 worktree 历史,无法核对;E4-01/02/09/10/11/12/16 的 stage_prod 一律 Unknown。
- prod worker 服务 SHA = Unknown(工单给定)→ 发布 worker 是否已部署/运行未知。
- staging `54c1de0b` 是 immersive 设计分支(非 main),未核对其是否含 L1 链(超出 main 判定范围,未查)。
- Meta App Review 实际是否通过(外部事实):代码只请求 scope;grantedScopes 运行时才知,repo 无法证。
- `PUBLISH_QUEUE_POLICY` 的具体 retryLimit/retryDelay/expireInSeconds 数值:commit 声明存在于 `packages/core/src`,未逐字段读取核对(预算)。
- FB /feed 恢复路径(creationId 为 null 时的 recent-posts 查询)幂等性:commit 声明,未逐行核对 reconcileAttempt 的 FB 分支。
