# E5 · 分析区 + L0 证据脊柱 + 认证 + 可观测性

基线确认:`git rev-parse --short=8 HEAD` = `b5a48d0f`(与工单一致)。

部署事实(工单给定,直接引用):production web = `7ed7ac22`(落后于 main);production worker SHA = Unknown;staging = `54c1de0b`(immersive 分支,非 main)。

> `stage_prod` 列:凡 evidence 指向的合并提交晚于 `7ed7ac22`(即本文件列出的绝大多数 E5 能力,见下方 git log)一律标 `≤ integrated`(prod 未部署);worker 侧因服务 SHA Unknown,一律标 `Unknown`。本分片没有拿到任何独立的 `7ed7ac22` 祖先关系核实手段(只读、禁网络),故 `stage_prod` 一律采用工单给定的保守规则,不做自行判断。

## 矩阵行表

| id | zone | capability | promise_source | stage_main | stage_prod | gate | evidence | provenance | gaps |
|---|---|---|---|---|---|---|---|---|---|
| E5-01 | 分析 | Analytics Phase A：真实 ad-account KPI + reach 图 + OTTO insight | PR #116 `e918fe02`；`docs/design-refs/analytics-ui-kit.html` | integrated | ≤integrated (Unknown 部署证据) | 无显式 flag；受 proxy 认证墙间接保护 | `apps/web/components/otto/OttoAnalytics.tsx:1-71`；`apps/web/lib/analytics-actions.ts:1-71`（`requireOwner()` 会话解析 + `fetchOwnerInsights`/`fetchOwnerInsightsSeries` 并行，真连 Meta Graph）；测试 `apps/web/lib/__tests__/analytics-actions.test.ts`、`apps/web/lib/__tests__/analytics-view.test.ts` | Observed | 无（代码+测试链完整） |
| E5-02 | 分析 | 平台切换器（Meta 实连 + TikTok/Shopee/Google/WhatsApp 占位"(soon)"） | PR #117 `caaff627` | UI shell（非 meta 平台）/ integrated（meta） | ≤integrated | 纯前端 state，无 flag | `apps/web/lib/analytics-platforms.ts:1-25`；`apps/web/components/otto/OttoAnalytics.tsx:27-58`（切平台不触发 server action，"soon" 后缀） | Observed | 断层：4/5 平台是死占位，只有 label + "(soon)"，无后端可切 |
| E5-03 | 分析 | Per-ad performance 面板（逐条广告真实创意+指标） | PR #128 `87fa76d7` | integrated | ≤integrated | 无 flag；`requireOwner`/`getAdPerformance` 走 session | `apps/web/components/otto/PerAdPerformance.tsx:1-85`；`apps/web/lib/meta-performance-actions.ts`；`apps/web/lib/per-ad-view.ts` + 测试 `per-ad-view.test.ts`、`meta-performance-actions.test.ts`、`meta-performance.test.ts` | Observed | 无 |
| E5-04 | 分析 | Meta 专家诊断卡（P0-P2：KB + 逐条表现 + 诊断） | PR #128 `87fa76d7`；`docs/superpowers/specs`（未逐一核对具体文件名，时间预算未覆盖） | implemented（经 Otto 对话卡片路径，非 Analytics 页面直接可见组件） | Unknown | 无 flag | `apps/web/lib/performance-card.ts:1-10`（"单一真相源 = @fikirtive/otto 的 PerformanceCardPayload"）；`apps/web/lib/dto.ts:123`；`packages/otto/src/knowledge/meta-expertise*.ts` | Observed（浅） | 断层观察：诊断卡不是 Analytics 页面内的独立组件，而是挂在 Otto 对话卡片渲染分支（PERFORMANCE_CARD）——用户要通过对话触发，不是页面自带按钮；本次未深入验证该分支在 streaming UI 是否可渲染（CODEBASE-MAP 2026-07-02 §F23 提到过"WRITE 卡片双重不可渲染"，但这是 P4 写卡片，非本 P0-P2 诊断卡，未复核是否同样受影响——标 Unknown） |
| E5-05 | 分析 | Otto 绩效周报（O-07，判决"要，简版"） | `docs/research/GRILL-VERDICTS-2026-07-03.md:22`；`docs/BLUEPRINT.md:99`（原文："待建"） | 不存在（无代码） | 不存在 | N/A | 全仓 `grep -rln "weeklyReport\|WeeklyReport\|周报" apps packages` 零命中 | Observed（否定性证据：查无） | 蓝图自己标注"待建"，与 promise_source 不冲突——只是确认判决尚未落地为代码，非争议点 |
| E5-06 | L0 量测脊柱 | 六张量测原语表（TrackedLink/QrAsset/QrPlacement/VoucherToken/SourceTag/AttributionEvent） | `docs/superpowers/specs/2026-07-10-l0-measurement-primitives.md`；PR-L0a | schema | Unknown（迁移晚于 `7ed7ac22` 几乎确定，未部署） | 无 gate（纯 additive 迁移，无读写代码挂载） | 迁移文件 `packages/db/prisma/migrations/20260710111648_l0_measurement_primitives/migration.sql`（六张表 + 索引 + 两条 hand-appended partial-unique 索引）；schema 摘录见 `packages/db/prisma/schema.prisma` | Observed | 六表全仓搜索仅出现在：①`packages/db/src/tenant-guard.ts:22`（租户守卫模型清单，只是把表名加入清单，非业务读写）②`packages/db/src/l0-measurement-uniqueness.test.ts`（134 行，只测三条 DB 级唯一性约束，不测业务逻辑）③规格文档。**零** app 层 action/route/组件引用这六张表——工单描述属实：只有 model + migration，无 redirect handler、无事件写入、无 UI |
| E5-07 | L0 量测脊柱 | 短链 redirect（TrackedLink 的 (domain,slug)→跳转，D4-2 计划件） | 工单点名"D4-2 计划件" | 不存在（无代码） | 不存在 | N/A | 全仓 `find apps/web/app -type d` 未见任何 redirect/短域捕获路由；`grep -rn "D4-2\|D4\.2" docs` 零命中 | Observed（否定性证据） | D4-2 这个编号本身在仓库文档中查无引用——无法核实这个计划件出自哪份文档（可能在工单发出方持有的、本仓库未同步的规划文档里）；标 Unknown：不确定"D4-2"是否指别的东西 |
| E5-08 | 认证 | Better Auth 核心（邮箱+密码/magic link/Google OAuth 关联） | `apps/web/lib/better-auth/server.ts` | integrated | ≤integrated | `requireEmailVerification:true`（硬编码 true，不可关）；OAuth account linking 仅信任 google + `requireLocalEmailVerified:true` | `apps/web/lib/better-auth/server.ts:1-50`；测试 `better-auth-server.test.ts`、`better-auth-client.test.ts`、`better-auth-route.test.ts`、`better-auth-compat.test.ts` | Observed | 无 |
| E5-09 | 认证 | Allowlist 三层（sign-in 前 hook / session.create.before / reset-password 邮件前） | `apps/web/lib/better-auth/gate.ts` | integrated | ≤integrated | fail-closed：`assertAllowedEmail` 对 null/undefined/未知邮箱一律抛 `APIError FORBIDDEN` | `apps/web/lib/better-auth/gate.ts:1-19`（`assertAllowedEmail` + `assertAllowedForUserId`）；测试 `better-auth-gate.test.ts`、`allowlist.test.ts`（未逐行读，按 CODEBASE-MAP 引用） | Observed | 未在本分片内逐一核实"三层"具体挂载点（hooks.before 覆盖 sign-in/sign-up、session.create.before 覆盖重复登录、sendResetPassword 内联再挂一次）——CODEBASE-MAP §F17 有描述，未逐行复核是否仍是 main 现状（应无漂移，因为 gate.ts 内容与地图描述一致） |
| E5-10 | 认证 | converge 身份收敛 + auth.signin 审计事件 | `apps/web/lib/better-auth/converge.ts` | integrated | ≤integrated | 无 flag | `apps/web/lib/better-auth/converge.ts:44`（`prisma.actionEvent.create` type `"auth.signin"`，`.catch(()=>{})` 吞错误——审计失败不阻断登录，与 auth-guard.ts 里 `rbac.deny` 审计失败模式一致） | Observed | 观察：审计写入用 `.catch(()=>{})` 静默吞错——如果 ActionEvent 写库失败，登录不受影响但审计链会悄悄断，且无任何告警（非 Sentry 上报）；这是"审计失败不阻断业务"的既定设计（CODEBASE-MAP 已载明同类模式），非新发现的 bug，列为原始观察 |
| E5-11 | 认证 | Proxy 认证墙（Next 16 proxy 后继中间件） | `apps/web/proxy.ts` | integrated | production（工单给定：web=`7ed7ac22`部署事实里proxy机制本身早于此更成熟；但本次未核实`7ed7ac22`是否已含当前 proxy.ts 全部内容——按规则仍标≤integrated，不擅自升级） | prod fail-closed：`AUTH_ENABLED !== "false"`；dev opt-in：`=== "true"` | `apps/web/proxy.ts:1-63`（matcher 排除 login/terms/privacy/legal/skin-preview/api/better-auth/api/stripe/api/health/api/meta/data-deletion/api/media/pub/statics） | Observed | 无（与 CODEBASE-MAP §7 描述一致，无漂移） |
| E5-12 | 认证 | onboarding（登录到第一个成果的引导） | `docs/design/2026-07-03-harmony-06-uiux-gamification.md`（GM-05"开店完成度"判决"要"）；`docs/BLUEPRINT.md` 第 11 条"零学习曲线"三查 | UI shell（main 上只有一个可关闭欢迎浮层，不是完成度环 checklist） | ≤UI shell | 纯前端，localStorage 键 `otto:onboarded` 控制一次性展示 | `apps/web/components/otto/OttoOnboarding.tsx:1-40`（`LS_KEY="otto:onboarded"`；欢迎 tile + 两个跳转按钮，dismiss 后永不重现，无完成度百分比/环形进度/多步骤 checklist 逻辑） | Observed | **断层观察**：`docs/northstar/WHATPASS-V2-CANDIDATES.md:552,600` 声称"GM-05 开店完成度 onboarding checklist 已在 `misc/onboarding-checklist.tsx` 全量建成（渠道连接/建产品/发首帖/看数据四步）"——但全仓 `find . -iname "onboarding-checklist*"` 与 `find . -path "*northstar*immersive*"` 在本 worktree（main@b5a48d0f）**零命中**；该 northstar immersive 组件树在 main 完全不存在，只有 `docs/northstar/` 文档存在。结合工单已知事实（staging=immersive 设计分支非 main），这份"已建成"的声称极可能仅存在于 staging(`54c1de0b`) 或另一 codex worktree，main 上的 GM-05 真实状态只是一个简单欢迎浮层（UI shell），远未达到文档所称的四步完成度环。这是本分片信心度最高的一条纵深证据候选（vertical-proof candidate）：文档声称 vs main 代码实况严重不符 |
| E5-13 | 认证 | GM-02/03/04 里程碑/进度条/周报语气 | `docs/design/2026-07-03-harmony-06-uiux-gamification.md` | 未在本分片时间预算内核实（GM-03 落点在 Campaign 区，非本分片路径；GM-04 落点在 O-07 周报，已确认 O-07 不存在故 GM-04 无宿主；GM-02 落点"各楼里程碑事件"，未逐一搜索） | Unknown | N/A | `docs/design/2026-07-03-harmony-06-uiux-gamification.md:33-36` | Observed（浅，promise_source only） | Dropped：GM-02/03 判断需要跨到 Campaign/排期分片核实，本分片仅确认 GM-04 的宿主 O-07 不存在 |
| E5-14 | 可观测性 | Sentry 接线（web + worker，各自 DSN） | 工单点名 | integrated | ≤integrated（DSN 值本身未知，只能证代码路径存在） | `SENTRY_DSN` env var；未设置则 no-op（两端都是） | web: `apps/web/instrumentation.ts:1-8`；worker: `apps/worker/src/index.ts:69-80`（`captureError` helper + fatal-on-uncaught 时 `process.exit(1)` 保留崩溃重启语义）+ `apps/worker/src/db-backup.ts:174` | Observed | 无 |
| E5-15 | 可观测性 | 结构化日志/心跳 | 工单点名 | integrated | ≤integrated | 无 flag，心跳写入无条件执行 | `apps/worker/src/index.ts:212-220`（写 `prisma.workerHeartbeat` upsert，失败仅 `console.warn` 不影响主循环；同时 `console.log` 打时间戳）；读侧 `apps/web/app/api/health/route.ts:1-22`（`workerStatus(hb?.at, now)` 判 up/stale/unknown，200/503 语义） | Observed | 观察：心跳写失败只 warn 不告警（与 ActionEvent 审计失败模式同款"静默降级"哲学）——如果 worker DB 连接持续失败，heartbeat 表不再更新，`/api/health` 会正确报 stale，所以外部监控链路本身是闭环的，此处不算断层 |
| E5-16 | 可观测性 | ActionEvent 审计事件覆盖面 | 工单点名 | integrated（覆盖面广：认证/钱路/租户管理/生成/上传/ingest 均有埋点） | ≤integrated | 多数写入 `.catch(()=>{})` 吞错误（审计失败不阻断业务，是既定模式非 bug） | 42 处 `actionEvent.create` 调用，覆盖：`auth.signin`(converge.ts:44)、`credits.purchase`/`credits.purchase.bad`(stripe/webhook/route.ts:31,41)、`tenant.*`(tenant-actions.ts 多处)、`config.edit`/`model.toggle`(admin-actions.ts)、`gen.guardian-block`(gen-actions.ts:81)、`variant.create`/`variant.delete`(refgen-actions.ts)、`asset.hash_mismatch`(worker ingest.ts:118)等 | Observed | 无新断层；覆盖面本身证实工单假设"ActionEvent 审计事件覆盖面"是真实存在的机制，非占位 |
| E5-17 | 分析 | History 全量（Otto Stuff 库存/历史面板） | UI 导航项"library"/"History" | implemented/integrated（分页加载，非一次性全量） | ≤integrated | 无 flag | `apps/web/lib/library-actions.ts:28`（`getGenerationHistory({cursor, take:80})`，cursor 分页）；`apps/web/components/otto/OttoStuff.tsx:11,137`（消费方） | Observed | 观察：CODEBASE-MAP(2026-07-02) 描述的独立 `OttoLibrary.tsx` 组件在当前 main 已不存在——`library` 视图与 `stuff` 视图已合并渲染同一个 `OttoStuff` 组件（`OttoView.tsx:207-210`），是地图基线之后的重构，非缺失；"全量"体现为 cursor 分页可翻到底，非一次性拉全表 |

## 断层观察（原始观察，不排序不评分）

1. **northstar immersive "GM-05 已建成" 文档声称与 main 代码实况不符**：`docs/northstar/WHATPASS-V2-CANDIDATES.md` 两处（552、600 行）明确写"misc/onboarding-checklist.tsx 已完整实现"，但该文件在 main@b5a48d0f 全仓不存在；main 上 GM-05 的唯一实现是一个 localStorage 控制的一次性欢迎浮层（`OttoOnboarding.tsx`），无完成度环、无四步骤 checklist。这类文档极可能描述的是 staging(`54c1de0b`)/immersive 分支或另一 codex worktree 的状态,不是 main 状态——值得上级核实"该文档的代码基线到底指哪个分支"。
2. **L0 量测原语（6 表)完全悬空**：迁移已合并（`20260710111648`），但零 app 层代码读写——无 redirect handler、无 QR/voucher 生成入口、无归因事件写入点、无任何 UI。这是纯粹的"schema"档，与工单描述完全吻合，且是本次审计里"代码 vs 承诺"落差最大、最干净的一条（TrackedLink/QrAsset/QrPlacement/VoucherToken/SourceTag/AttributionEvent 六张表，spec 见 `docs/superpowers/specs/2026-07-10-l0-measurement-primitives.md`，本分片未读该 spec 全文，只读了迁移文件本身)。
3. **短链 redirect（D4-2）在仓库内查无引用**——工单点名的"D4-2 计划件"这个编号，在本仓库全文搜索零命中,无法定位其规格文档,也没有对应路由代码。可能是工单发出方持有的外部/未合并规划,本分片无法证实或证伪其存在,只能证实"main 里没有短链跳转的实现代码"。
4. **分析区 Phase A 与 CODEBASE-MAP(2026-07-02) 基线严重脱节**：地图称 analytics/schedule 均为"ComingSoon 占位(DORMANT)",但 `git log --since=2026-07-02` 显示 PR #116/#117/#128/#131 已把 Analytics 从占位升级为：真实 KPI + reach 图 + OTTO insight + 平台切换器 + per-ad 面板 + Meta 专家诊断 KB,全部有对应测试文件。这不是断层,而是提醒:引用 CODEBASE-MAP 时必须先用 `git log --since` 复核对应路径,本分片已按工单要求这样做。
5. **诊断卡的挂载点在 Otto 对话卡片,不在 Analytics 页面本身**——用户要通过与 Otto 对话触发 PERFORMANCE_CARD 才能看到诊断卡,Analytics 页面自身没有"帮我诊断"按钮直连它(本分片未逐行核实该按钮是否存在于 OttoAnalytics.tsx 291 行的后半段,只读了前 80 行,时间预算内未覆盖)。
6. **审计/心跳双双采用"失败静默降级,不阻断业务"设计**(ActionEvent 写入普遍 `.catch(()=>{})`;heartbeat 写失败仅 `console.warn`)——这是贯穿全仓的一致设计哲学,不是本分片独有的 bug,但列为观察供上级知悉:若 DB 抖动,审计链和心跳都会悄悄断而无独立告警(health 端点本身能正确反映 heartbeat stale,所以心跳侧有兜底;ActionEvent 侧没有对应的"审计完整性"监控)。

## Unknowns

- Better Auth allowlist 三层挂载点的逐行核实(hooks.before / session.create.before / sendResetPassword 内联)未在本分片内重新读 `access.ts`/`server.ts` 全文逐行验证是否与 CODEBASE-MAP 描述完全一致(只读了 gate.ts 本体,server.ts 前 50 行)。
- 诊断卡(PERFORMANCE_CARD)在当前 streaming UI 下是否可渲染,未复核(CODEBASE-MAP 提到 P4 WRITE 卡片"双重不可渲染"的问题是否也影响这条 P0-P2 诊断卡,未验证)。
- production web SHA `7ed7ac22` 与本文件列出的各 PR 合并提交之间的祖先关系,本分片没有网络/checkout 权限核实,只能按工单给定规则统一标注 `≤integrated`。
- worker 生产服务 SHA 完全未知,故所有 worker 侧能力(Sentry/heartbeat/L0 无关但心跳有关)的 `stage_prod` 一律 Unknown。
- GM-02/GM-03(里程碑事件/Campaign 目标进度条)的 main 代码实况未核实——需要 Campaign/排期分片配合。
- "D4-2"这个计划件编号具体指什么,本分片无法从仓库内证实或证伪。
- `docs/superpowers/specs/2026-07-10-l0-measurement-primitives.md` 全文未读(只读了迁移 SQL 与其内嵌注释),故 L0 的完整设计意图(spec §二/§PR 切片描述了什么后续 PR)未纳入本文件。

## Dropped(因预算放弃覆盖)

- GM-02/GM-03 在 Campaign/里程碑区的 main 代码实况深挖(建议交给对应分片)。
- L0 measurement spec 全文阅读(`docs/superpowers/specs/2026-07-10-l0-measurement-primitives.md`),只读了迁移文件本体作为一手代码证据。
- OttoAnalytics.tsx 后半段(82-291 行,含诊断卡触发入口/图表渲染细节)未逐行读完。
- Better Auth `access.ts`/`session-role.ts`/`compat.ts` 全文逐行核实(只按 CODEBASE-MAP 引用带过,未重新验证是否漂移)。
