# E2 · Otto 本体 — 取证矩阵

基线:`main@b5a48d0f`(已核对 `git rev-parse --short=8 HEAD` = b5a48d0f)。
production web = `7ed7ac22`(落后于 main;凡晚于它合并的一律 ≤`integrated`,标注在 `stage_prod`)。
production worker SHA = Unknown。staging = `54c1de0b`(immersive 分支,非 main,不覆盖本分片)。

参考文档基线:`docs/review/CODEBASE-MAP-2026-07-02.md`(2026-07-02),已用
`git log --since=2026-07-02` 对相关路径补差到 b5a48d0f。

## 矩阵

| id | zone | capability | promise_source | stage_main | stage_prod | gate | evidence | provenance | gaps |
|---|---|---|---|---|---|---|---|---|---|
| E2-01 | Otto | skill 注册表(25 个技能,agent 工具顺序=数组顺序) | `packages/otto/src/registry.ts` | integrated | Unknown(worker/web SHA 均未晚于此文件变动核实,但 `apps/web` 使用同一 `@fikirtive/otto` 包,产出于 web=7ed7ac22 之后的提交则 prod 未接线) | 无(编译期常量,无 env/DB 开关) | `packages/otto/src/registry.ts:29-56`(allSkills 数组 25 项);`packages/otto/src/registry.test.ts:8`(pin 25 个精确名字,含新增的 saveProduct/saveCustomerSegment/saveOffer/lookupProducts/ingestProduct/metaAdPerformance/metaExpert/proposeResearch/schedulePosts —— 均晚于 2026-07-02 地图记录的 16 个) | Observed | 地图基线(2026-07-02)记 16 个技能;main@b5a48d0f 已到 25 个 —— 净增 9 个(Brand memory v2、Meta 专家诊断、Research、Schedule 等模块各带技能)。`skillCatalog` 投影字段 name/cost/effect/reach/needsApproval/description/requires 逐一核对与 `packages/otto/src/registry.ts:58-73` 一致 |
| E2-02 | Otto | needsApproval 派生公式 | `packages/otto/src/skill.ts` | integrated | 同上(纯函数,随包同版本) | 无(build-time 派生,throw=build fails,fail-closed:未声明 cost/effect 默认取"最危险值" spend/write) | `packages/otto/src/skill.ts:66-68`(`deriveNeedsApproval = cost==="spend" \|\| (effect==="write" && reach==="external")`);`packages/otto/src/skill.ts:82-84`(缺省 fail-closed 到 spend/write) | Observed | catalog 显示仅 `generate` 一个技能 needsApproval=✅(spend);`propose-meta-action`/`propose-ad-build` 均 ❌(effect=write 但 reach=internal —— 只是"造卡",真正外部写发生在卡片批准后的独立 server action,该 server action 自己另有权限门,不在本公式管辖范围内,详见 E2-08) |
| E2-03 | Otto | 审批豁免(四类封闭豁免 ADMIN/VISUAL/MONEY_IN/ACCOUNT_SECURITY) | `docs/design/2026-07-03-harmony-02-parity-manifest.md`(promise_source only) | integrated(代码侧:`packages/otto/src/parity-manifest.ts` 的 `PARITY_EXEMPTIONS`) | Unknown | 无 | `packages/otto/src/parity-manifest.ts`(exemptions 校验见 `scripts/check-parity.mjs:141-144`:`"exempt" in entry` 必须属于 `exemptions` 集合,否则报错) | Observed | — |
| E2-04 | Otto | streaming 聊天链(front door → goal tiles → useChat SSE) | UI 代码 | integrated | ≤integrated(晚于 7ed7ac22 的改动一律未部署,worker/web SHA 未知) | `otto/page.tsx:70` 硬编码 `ottoStreamEnabled=true`(2026-07-01 上线,无 env 开关回退) | `apps/web/components/otto/OttoFrontDoor.tsx`(GOAL_TILES + start() → createEmptyCoworkThread → onStreamStart);`apps/web/components/otto/OttoChatStream.tsx`(useChat<OttoUiMessage>,DefaultChatTransport api="/api/otto/stream");`apps/web/app/api/otto/stream/route.ts:POST` | Observed | 唯一现役渲染路径;`OttoConversation.tsx`(classic 非流式渲染器)在 prod chat 里已死(仅 skin-preview 可达),但仍是 ACTION_CARD/BUILD_CARD 的"原生"渲染器,流式渲染器现已补齐同款分支(见 E2-06) |
| E2-05 | Otto | GEN_CARD/PackCard/StoryboardCard/GEN_RESULT 渲染 | UI 代码 | integrated | ≤integrated | 无 | `apps/web/components/otto/OttoChatStream.tsx:735-905`(GEN_CARD pack 合并逻辑 + PackCard 分组;StoryboardCard 第 959-970 行;GEN_RESULT 第 927-944 行) | Observed | — |
| E2-06 | Otto | ACTION_CARD / BUILD_CARD 在流式 UI 的可达性(地图 F23) | 地图记「NO ACTION_CARD/BUILD_CARD branch — F23」 | **integrated(已修)** | ≤integrated(修复提交 019b552c 晚于 web=7ed7ac22,若 prod 未重新部署则 prod 仍是 F23 未修状态 —— Unknown 具体 SHA,但按规则「晚于 7ed7ac22 合并的能力在 prod = 未部署」) | 无 | `apps/web/components/otto/OttoChatStream.tsx:908-925`(注释明写「Meta approval-flow cards (F23) — mirror OttoConversation's branches」,`kind === "ACTION_CARD"` → `OttoActionPlanCard`,`kind === "BUILD_CARD"` → `OttoAdBuildCard`);修复提交 `git log --oneline -- apps/web/components/otto/OttoChatStream.tsx` 命中 `019b552c fix(otto): F23 live card streaming + F37/F41 tails (2026-07-02 audit close-out)`;配套 bridge allowlist `apps/web/lib/otto-stream-bridge.ts:82`(`CARD_TOOL_NAMES` 现含 `propose-meta-action`/`propose-ad-build`/`meta-expert`/`proposeResearch`,不再是地图记录的"仅 propose/proposeStoryboard,其余被丢弃") | Observed | main 代码已修;**prod 是否已部署此修复= Unknown**(worker/web SHA 未给出,只知 web deploy 落后于 main) |
| E2-07 | Otto | Meta 写操作本身(v1 ACTION_CARD / v2 BUILD_CARD 执行) | UI 按钮 + server action | implemented(卡片可渲染,approve 按钮调用既有 gated server action,但底层需 `ads_management`/`pages_show_list` 权限重连) | Unknown | Meta OAuth scope 门(reconnect 需要) | `apps/web/components/otto/OttoActionPlanCard.tsx`、`OttoAdBuildCard.tsx`(approve 调用既有 server action,未新增) | Observed | 地图原话「doubly dead: also unrenderable(F23)」——渲染那一半已修(E2-06),但底层 OAuth scope 门槛(ads_management/pages_show_list)是否已具备未验证,故整体判 `implemented` 非 `integrated`,原始观察不升档 |
| E2-08 | Otto | ottoApprove / coworkGenerate 恢复链 | UI 代码 | integrated | ≤integrated | 无(依赖 CAS + 权限校验,非 env 开关) | `apps/web/lib/otto-actions.ts:644`(`ottoApprove`);`apps/web/lib/cowork-actions.ts:26`(`coworkGenerate`);另有 `apps/web/lib/otto-client-actions.ts:23` 一个同名 `ottoApprove`(客户端包装) | Observed | 存在两个 `ottoApprove` 定义(otto-actions.ts 服务端 + otto-client-actions.ts 客户端),原始观察未深挖两者关系是否为薄客户端转发,留 Unknown |
| E2-09 | Otto | Otto 后台 verdict(otto-resume) | worker 代码 | integrated(worker 侧代码路径完整,含测试) | Unknown(worker SHA 未知) | at-most-once claim(`genJob.updateMany({ottoVerdictAt: null}→set)`),startGen 端口故意不注入(fail-closed:防止 verdict 轮花钱生成) | `apps/worker/src/otto-resume.ts:resumeOttoAfterGen`;测试 `apps/worker/src/otto-resume.test.ts`(484 行,晚于地图记录的 473 行,行数增长但未读全部新增断言,判 provenance=Observed 仅限"文件仍存在且测试仍在") | Observed | — |
| E2-10 | Otto | needMoreInfo 刨根问底硬门 | `docs/BLUEPRINT.md` 判决 + 代码 | integrated | ≤integrated | build-time throw(声明 `requires` 字段名必须存在于 zod parameters shape,否则 build fails) | `packages/otto/src/skill.ts:70-79`(`missingRequired` 纯函数);`skill.ts:116-153`(`requires` 校验 + `needMoreInfo` 返回值);提交 `91f2794e feat(otto): requires 资讯门 — 刨根问底硬门 on defineOttoSkill` | Observed | — |
| E2-11 | Otto | 品牌记忆注入(brandContext → 每轮 system message) | 代码 | integrated | ≤integrated | 无 | `apps/web/lib/otto-actions.ts:149-154`(`buildContextSystemMessage`:`ctx.brandContext` → system message);`otto-actions.ts:226-252`(注入点,Promise.all 拉取) | Observed | — |
| E2-12 | Otto | meta 专家诊断技能(meta-expert) | Block P 判决 | integrated | ≤integrated | 无(free/read-write internal,needsApproval=❌) | `packages/otto/src/skills/meta-expert.ts`;registry 含 `metaExpertSkill`;catalog 行 `meta-expert \| free \| write \| internal \| ❌` | Observed | — |
| E2-13 | Otto | research 技能(researchWeb + proposeResearch) | Block S 判决(4f3f0980) | integrated | ≤integrated | 无 | `packages/otto/src/skills/research-web.ts`、`propose-research.ts`;registry 含两者;OttoChatStream.tsx:972-984 渲染 RESEARCH_CARD 分支 | Observed | — |
| E2-14 | Otto | prompt 技能(seedreamPrompt / seedancePrompt,唯一提示权威) | D/E 决策 6(task_84dba154) | integrated | ≤integrated | 无 | `packages/otto/src/prompt-skills.ts:PROMPT_SKILLS/PROMPT_SKILLED_FAMILIES`;`packages/otto/src/instructions.ts`("Craft the prompt with the model skill" 章节);消费方 `apps/web/lib/cowork-actions.ts:578` | Observed | — |
| E2-15 | Otto | Parity Manifest(缝 9)CI 状态 | `docs/design/2026-07-03-harmony-02-parity-manifest.md` | **integrated,CI = hard gate**(非地图记录的 warn) | ≤integrated | CI 硬门,无 env 开关;`todoSkill` 债务棘轮(只能降不能升) | `.github/workflows/ci.yml:48-49`(`pnpm lint:parity`,无 `continue-on-error`);`scripts/check-parity.mjs:160-183`(`todoCount > maxTodoSkill` → `fail()` 退出码 1) | Observed | 地图(2026-07-02)未记录此项为 hard;`e394ec6d docs+ci: truth-fixes for lying docs + parity ratchet` 把它做成硬棘轮,晚于地图基线 |
| E2-16 | Otto | 对等债基线(TODO_SKILL 计数) | `scripts/parity-debt-baseline.json` | **84,与代码实际计数一致(未涨未降)** | Unknown | 同上棘轮 | `scripts/parity-debt-baseline.json`(`maxTodoSkill: 84`);实测 `grep -c "todoSkill: true," packages/otto/src/parity-manifest.ts` = 84(注意:粗略 `grep -c "todoSkill:\s*true"` 会得 85,因为命中了第 22 行的 TS 类型联合声明 `{ todoSkill: true; reason: string }`,需用逗号变体排除类型行) | Verified current source | 债务基线自 2026-07-07(`e394ec6d`)设定后,截至 b5a48d0f 未见任何一条被清偿或新增 —— 静止状态,既非改善也非恶化 |
| E2-17 | Otto | skill catalog 生成 + CI 防腐 | `packages/otto/src/skills/CATALOG.md` | **integrated,CI = hard gate**(非地图记录的"不拦") | ≤integrated | CI 硬门 | `.github/workflows/ci.yml:45-46`(注释「a stale CATALOG.md fails here」+ `pnpm --filter @fikirtive/otto run catalog:check`);地图原话「CI 仅跑 check-skill-imports.sh,NOT catalog:check」——已不成立 | Observed | 地图记录的缺口(catalog 可漂移不被 CI 挡)已被后续提交关闭,晚于 2026-07-02 基线 |
| E2-18 | Otto | Otto 引擎:Prompt Caching(Phase 1) | `docs/superpowers/specs/2026-07-07-otto-engine-caching-scoped-loading-design.md` | **integrated(已上线)** | ≤integrated | 无(纯 middleware,provider-level) | `packages/otto/src/model.ts:95-159`(`cache_control` 注入,`EPHEMERAL_CACHE_CONTROL`,LanguageModel middleware);提交 `2de401b6 feat(otto): Phase 1 prompt caching` | Observed | — |
| E2-19 | Otto | Otto 引擎:技能分域装载(Phase 2) | 同上 spec + MASTERPLAN.md P0.5-2 | **schema/未动工(仅 spec,代码零改动)** | Unknown | 无(尚未存在) | `grep -rn "domain" packages/otto/src/registry.ts packages/otto/src/skill.ts packages/otto/src/otto.ts` → 零命中;spec 自述「顺序在 Phase 1 之后」(`docs/superpowers/specs/2026-07-07-...-design.md:80` 起「三、Phase 2」章节,`docs/MASTERPLAN.md:130` 标「可开工」但未见落地 PR) | Observed(反面证据:代码不存在) | 25 个技能仍是地图记录的"全静态挂载"状态,未见 domain 字段;MASTERPLAN 称"可开工"但截至 b5a48d0f 未见对应 registry 改动或 PR |
| E2-20 | Otto | 上下文桥(当前视图/选中项注入每轮对话,宪法 7 第四层) | `docs/BLUEPRINT.md:70`(promise_source only,宪法条文) | **schema/未落地为代码**(仅 spec 提及 `viewContext` 概念) | Unknown | 无 | `grep -rn "viewContext\|activeView\|currentView" apps/web/lib apps/web/app/api/otto packages/otto/src` → 零命中;spec 提到概念 `docs/superpowers/specs/2026-07-07-...-design.md:99`("viewContext(上下文桥,宪法7):当前视图→域") 但那是 Phase 2 分域装载设计里的**副产品概念**,不是独立已实现的上下文注入通道 | Observed(反面证据) | 宪法明文要求"『把这个改成 9:16』里的『这个』必须可解析",但代码里没有找到把"当前视图/选中项"注入 Otto 每轮对话的通道;`buildContextSystemMessage`(E2-11)只注入 brandContext/refs/activeJob,不含"当前视图/选中卡片"这类 UI 状态 |
| E2-21 | Otto | live reflection(生成完成界面刷新,吐槽头号痛点) | `docs/research/GRILL-VERDICTS-2026-07-03.md:217`(2026-07-07 定调:headless 动作层 + UI live reflection,非 computer-use) | **implemented,但仍是轮询而非推送**(聊天侧已是 SSE 实时;canvas 侧仍 4s 轮询) | Unknown | 无(轮询间隔硬编码,非开关) | 聊天侧实时:`apps/web/app/api/otto/stream/route.ts`(SSE data-status/data-tool-propose 实时推送 GEN_RESULT 到 chat);canvas 侧轮询:`apps/web/components/otto/OttoApp.tsx:245`(`setInterval(poll, 4000)`,4s 间隔驱动 `listProjectThreadActivity` → activity Set → FlowCanvas 重新拉取节点) | Observed | 「定调」文档称此为北极星地基级修复,但 canvas 节点侧截至 b5a48d0f 仍是 4 秒轮询驱动(`git log --since=2026-07-07` 对 `apps/web/components/canvas/` 只命中 `2688c313 feat(canvas): Grok-style stateful canvas Phase 1` 一条,未见推送化改造);判「决策已定但工程未完全兑现推送」,原始观察,不评分 |
| E2-22 | Otto | 生成 provider 选择(fail-safe 默认 MockProvider) | 代码 | integrated | Unknown(取决于 worker 环境变量 `GENERATION_PROVIDER`,repo 侧看不到部署环境值) | env var `GENERATION_PROVIDER`(fail-safe:未设置/其他值 → MockProvider,$0,离线) | `packages/otto/src/index.ts` 底部 `createGenerationProvider()`(`fal`→FalProvider,`byteplus`→BytePlusProvider,否则 MockProvider) | Observed(与地图记录一致,未见改动) | — |

## 断层观察(原始观察,不排序不评分)

1. **F23(ACTION_CARD/BUILD_CARD 流式渲染缺口)main 已修,但 prod 状态 Unknown。** 提交 `019b552c` 把两个卡片分支补进 `OttoChatStream.tsx`,配套 `otto-stream-bridge.ts` 的 `CARD_TOOL_NAMES` 也已扩容到含 `propose-meta-action`/`propose-ad-build`/`meta-expert`/`proposeResearch`。但该提交晚于 production web 部署点(`7ed7ac22`),按工单规则"晚于它合并的能力在 prod = 未部署",即便找不到具体 worker/web 当前 SHA,也只能标 Unknown,不能断言用户今天已能在流式聊天里看到 Meta 写卡片。
2. **技能数从地图记录的 16 个净增到 25 个**,新增 9 个(saveProduct/saveCustomerSegment/saveOffer/lookupProducts/ingestProduct/metaAdPerformance/metaExpert/proposeResearch/schedulePosts),均带独立测试 pin(`registry.test.ts:8` 精确断言 25 个名字的排序数组)。
3. **CI 把两个此前"记录为软"的检查升级为硬门**:`catalog:check`(地图明确写"CI 仅跑 check-skill-imports.sh,NOT catalog:check")和 `lint:parity`(带 todoSkill 债务棘轮,只能降不能升)。两者在 `.github/workflows/ci.yml` 里都是普通 `run:` 步骤,无 `continue-on-error`,失败即挡 PR。
4. **对等债基线(84)自 2026-07-07 设定后原地不动**——既没有清偿,也没有失控增长,截至 b5a48d0f 恰好等于基线值(需用 `todoSkill: true,`——逗号变体——排除第 22 行的 TS 类型声明本身,否则 naive grep 会多算一条得出 85 的假警报)。
5. **Otto 引擎升级 spec 的两个阶段进度不对称**:Phase 1(prompt caching)已落地(`model.ts` 的 `cache_control` middleware,提交 `2de401b6`);Phase 2(技能按视图分域装载)在 `registry.ts`/`skill.ts`/`otto.ts` 里搜索 "domain" 零命中——仍是全部 25 个技能静态挂载给每一轮对话,MASTERPLAN 文档虽标注"可开工"但未见对应实现。
6. **宪法第七条"上下文桥"(当前视图/选中项注入对话)在代码里找不到实现通道**——`viewContext`/`activeView`/`currentView` 类命名在 `apps/web/lib`、`apps/web/app/api/otto`、`packages/otto/src` 全零命中;唯一提到 `viewContext` 概念的地方是 Phase 2 分域装载设计文档里的域路由表(canvas/library→creation+brand 等),那是"用视图猜要装载哪些技能"的内部路由概念,不是"把用户当前看到的/选中的东西塞进对话给 Otto 解析"的用户可感能力。`buildContextSystemMessage` 只注入 brandContext/refs/activeJob 三类,不含 UI 选中态。
7. **"live reflection"(生成完成界面刷新)决策与工程状态不完全对齐**:聊天侧确实是 SSE 实时(GEN_RESULT 卡片流式注入),但 canvas 节点侧刷新仍由 `OttoApp.tsx:245` 的 4 秒轮询(`listProjectThreadActivity`)驱动,`2026-07-07` 之后没有找到 canvas 组件目录下的推送化改造提交——即聊天体验已解决痛点,画布体验仍是轮询延迟(最多 4 秒)。
8. **两个同名 `ottoApprove` 导出**(`apps/web/lib/otto-actions.ts:644` 与 `apps/web/lib/otto-client-actions.ts:23`)未深挖二者关系(是否为客户端薄包装转发到服务端版本),留作 Unknown,未展开验证。

## Unknowns

- production worker 服务 SHA 未知,故所有标 `stage_prod: Unknown` 的行本质是"repo 侧代码已到某档,但线上实际部署版本无法判定是否含此代码"。
- `apps/web/lib/otto-actions.ts:ottoApprove` 与 `apps/web/lib/otto-client-actions.ts:ottoApprove` 的调用关系(是否为薄客户端转发)未展开读取源码验证。
- Meta 写操作(E2-07)底层 `ads_management`/`pages_show_list` OAuth scope 当前是否已具备(即用户重连后是否真能跑通,而非仅"卡片能渲染")——repo 侧无法验证运行时 OAuth 授权状态。
- `apps/worker/src/otto-resume.test.ts` 从地图记录的 473 行长到 484 行,新增的 11 行断言内容未逐行核对,只确认文件与测试仍存在且命名一致。
- Otto 引擎升级 spec 的验收节(各域前缀 token 报表、RunState 三场景回归绿、回滚开关)因 Phase 2 未开工,无对应产物可核。
- `.github/workflows/ci.yml` 顶部注释仍写"push 到 main 自动部署 web+worker 到 Railway",与用户全局记忆记录的"2026-07-10 已验证:push main 不再自动部署"相冲突——本分片未展开核实该行注释是否已过期(不属于 Otto 本体范围,仅顺带发现,记录不展开)。

## Dropped

- 预算内已覆盖工单列出的全部 14 个子项(skill 注册表/审批公式/streaming 链/卡片渲染/F23/恢复链/otto-resume/needMoreInfo/品牌记忆/meta 诊断/research/prompt 技能/parity CI/engine 缓存分域/上下文桥/live reflection)。未深入的部分已在 Unknowns 节列出,均为"验证运行时行为"或"逐行读全部新增断言"级别的深挖,非结构性遗漏。
