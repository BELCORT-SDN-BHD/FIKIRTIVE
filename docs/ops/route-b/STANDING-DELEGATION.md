# 路线乙 · Standing Merge Delegation（founder 签署件——您合并本 PR 即授权生效）

> 2026-07-12。依据：founder 两处指令——本控制面 session「你能直接自己批准，做完全部任务后，正式全部再交接给我」+ 经 FIK-2 转达的「我不要一个一个审核，最后直接一次过处理」（转达已按规则不作授权，本 PR 即是把它变成您的一次有效签名）。
> **生效即：路线乙余程中途零审批，直到终验一次过。** 您随时可用一句话撤销或收紧本授权。

## 一、授权范围（您合并本 PR 后即刻生效）

1. **合并权全面下放**：路线乙全部 PR（含此前 founder-only 的 schema/迁移、钱路相邻、账本/治理相邻类）由控制面调度合并，**前提=机器闸全过**：
   - current-head CI 全绿（含语义冻结锁、parity 棘轮、破坏性迁移闸等全部既有围栏）；
   - 独立异族评审无未解 P0/P1（codex 额度触顶时=fallback 异族 agent 评审，全程标注）；
   - 花费路径 diff 过 `money-safety-review`；
   - **作者不自合结构性保留**（蓝图级）：worker 作品由控制面合并；控制面亲笔件由「独立评审 agent 评审 + 非作者 agent 执行合并」四权分离闭环——founder 不再参与执行，但分权不消失。
2. **中途 blocking 触点全部解除**：体量过目已按双顾问建议档圈定（D-021）；B2/B9 等共享契约的冻结改为「双顾问签核 + 异族复审 + 机器闸」放行，冻结决定全部入决策日志并在终验重现；偏离默认档的项与全部 Q6/Q7 待裁**攒进终验待裁清单**。
3. **放行清单改只读**：继续增量投递（您不需回应），终验时即审计索引。

## 二、不可转授（无论本授权多宽，以下永远回到您）

1. **真实花费超出 $300 信封的任何一笔**（信封内分项跟账、80% 提醒照旧）；
2. **生产部署**（`railway up` 到 production）与生产数据写入；
3. **真实外部动作**：Meta/WABA/EasyStore/GBP 递审递交、真实发帖、收款配置变更、凭据轮换的实际执行；
4. **`docs/BLUEPRINT.md`**（宪法，永不可由 agent 修改）；
5. **终验**：唯一一次产品总验收 = 您（交付标准三件套 + 15 步剧本）。

## 三、审计与安全带（授权越宽，留痕越严）

- 每次合并在决策日志留一行（PR 号/类别/机器闸证据链接/执行 agent）；
- 高后果类（schema/钱路/治理）额外要求：双顾问之一 complete 签核入 provenance；
- 任何机器闸红=停，不存在「授权覆盖红灯」；发现越界即回滚并立即报告您；
- 本授权 epoch 绑定（`claude-20260712-03` 及其合法继任），换届时新控制面须在状态账重申。

## 四、生效动作（您的这一坐，四次点击，之后到终验前零打扰）

按序合并：**#241 → #250（账本栈）→ #252（B0 修宪包）→ 本 PR**。完成后路线乙直建 loop 按执行合同 §四 全速推进，下次见面=终验。
