node_modules/
.next/
dist/
packages/db/generated/
build/
*.tsbuildinfo
.env
.env.local
.env*.local
.DS_Store
.gstack/
coverage/
playwright-report/
test-results/

# resident E2E run output (#799) — traces, screenshots and the HTML report of one run
e2e/.artifacts/
e2e/.report/
.turbo/

# local dev storage (LocalDiskStorage adapter)
.data/

# prod test session cookies — secret, must never be committed or shipped to a build
.prod-session.json

# Railway CLI agent artifacts
.agents/
.railway/

# codegraph local index
.codegraph/
.gbrain-source

# working drafts / scratch (underscore-prefixed) — kept on disk, out of git
_*.md

# tool-local staging (gstack / superpowers) — not project artifacts
.out-of-scope/
.superpowers/

# Stray Claude worktree dirs must never be committed (see F32 audit)
.claire/

# codex CLI local config
.codex/
