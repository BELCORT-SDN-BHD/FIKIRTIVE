-- B4 debt-70 (universal approval card chain): the durable card a non-generate
-- approval-gated skill (approveScheduledPost) parks as. Additive enum value only.
ALTER TYPE "ChatMessageKind" ADD VALUE 'APPROVAL_CARD';
