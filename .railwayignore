# Never ship to a Railway build/image:
.prod-session.json
.data/
.git/
# dev tooling (codegraph index/socket + agent config) — useless at runtime, just bloats the upload
.codegraph/
.claude/
.mcp.json
